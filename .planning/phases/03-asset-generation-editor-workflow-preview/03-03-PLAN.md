---
phase: 03-asset-generation-editor-workflow-preview
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - Packages/com.graffitientertainment.namer/Tests/Editor/AssetGeneratorTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/SourceImmutabilityTests.cs
  - Packages/com.graffitientertainment.namer/package.json
  - Packages/com.graffitientertainment.namer/README.md
autonomous: true
requirements: [GEN-02, TEST-03]
must_haves:
  truths:
    - "Automated EditMode tests prove every written asset lives under the configured destination with correct prefix/suffix, and overwrite refuses non-`NamerGenerated`-stamped targets (TEST-03, D-16)"
    - "An automated SHA-256 test proves every source file and its `.meta` is byte-identical before and after a full Process run (GEN-02, D-15)"
    - "The package is shippable: package.json metadata complete, README documents install + workflow, and the full headless EditMode + PlayMode suite is green at HEAD (D-17)"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/AssetGeneratorTests.cs"
      provides: "path/naming/overwrite-gating/import-stamping coverage"
      contains: "AssetGeneratorTests"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/SourceImmutabilityTests.cs"
      provides: "SHA-256 source + .meta immutability coverage"
      contains: "SHA256"
    - path: "Packages/com.graffitientertainment.namer/package.json"
      provides: "complete UPM metadata"
      contains: "description"
    - path: "Packages/com.graffitientertainment.namer/README.md"
      provides: "install + workflow documentation"
      contains: "## Installation"
  key_links:
    - from: "AssetGeneratorTests"
      to: "NamerProcessor.Process"
      via: "full Process flow against a temp destination"
      pattern: "NamerProcessor.Process"
    - from: "SourceImmutabilityTests"
      to: "SHA256.ComputeHash"
      via: "hash source files + .meta before/after Process"
      pattern: "SHA256"
    - from: "re-import round-trip test"
      to: "TextureImporter"
      via: "assert sRGBTexture=false / Uncompressed / Point / no-mips + packed alpha survives"
      pattern: "sRGBTexture"
---

## Phase Goal

**As a** Unity artist, **I want to** select a textured FBX and run `Process with NAMER`, **so that** I get a correctly rendering, source-compatible NAMER material without ever modifying my imported source assets.

<objective>
Prove the phase's two safety guarantees by test and close out a shippable MVP package: `AssetGeneratorTests` (generated-asset paths, prefix/suffix naming, overwrite gating, import-stamping round-trip), `SourceImmutabilityTests` (SHA-256 of every source file + `.meta` before/after the full Process flow), and completion of `package.json` + `README.md` with the full headless suite green at HEAD.

Purpose: GEN-02 and TEST-03 are the phase's non-negotiable safety requirements — they must be automated, capability-gated, and run against a temp destination so a developer's real `NAMERGenerated/` content is never at risk.

Output: `Tests/Editor/AssetGeneratorTests.cs`, `Tests/Editor/SourceImmutabilityTests.cs`, updated `package.json`, updated `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-asset-generation-editor-workflow-preview/03-CONTEXT.md
@.planning/phases/03-asset-generation-editor-workflow-preview/03-RESEARCH.md
@.planning/phases/03-asset-generation-editor-workflow-preview/03-PATTERNS.md

<interfaces>
<!-- Contracts produced by 03-01 (committed when this plan runs). -->
From Editor/NamerEditorConstants.cs: const string GeneratedLabel = "NamerGenerated"; const string DefaultDestination; const string DefaultPrefix; const string DefaultSuffix;
From Editor/Settings/NamerProcessorSettings.cs: sealed class { string Destination; string Prefix; string Suffix; bool OverwriteGenerated; }  (EditorPrefs-backed; setters write EditorPrefs)
From Editor/Pipeline/NamerProcessor.cs: static NamerProcessResult Process(UnityEngine.Object selection, NamerProcessorSettings settings);
From Editor/Pipeline/NamerProcessor.cs: sealed class NamerProcessResult { List<NamerGeneratedAsset> GeneratedAssets; List<string> Warnings; string Error; int MaterialCount; }
From Editor/Generation/AssetGenerator.cs: sealed class NamerGeneratedAsset { string MaterialPath; string BaseTexturePath; string SurfaceTexturePath; }

<!-- Test conventions to reuse. -->
From Tests/Editor/SourceInspectorTests.cs: temp folder `Assets/NAMER_Tests_Temp`, EnsureTempFolder/CreateImportedMap/Destroy helpers, `try/finally` + `AssetDatabase.DeleteAsset(TempFolder)` cleanup.
From Tests/Editor/GpuGoldenTests.cs: capability gate `SystemInfo.supportsComputeShaders && SystemInfo.supportsAsyncGPUReadback` -> `Assert.Ignore(...)`, `AsyncGPUReadback.Request(...).WaitForCompletion()`, `req.hasError` assert.

Namespaces: Tests = GraffitiEntertainment.Namer.Tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AssetGeneratorTests — paths, prefix/suffix, overwrite gating, import-stamping round-trip</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/AssetGeneratorTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/SourceInspectorTests.cs (temp folder + fixture + Destroy cleanup patterns)
    - Packages/com.graffitientertainment.namer/Tests/Editor/GpuGoldenTests.cs (capability gate + AsyncGPUReadback assertion pattern)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs and AssetGenerator.cs (the API under test)
    - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs (GeneratedLabel/DefaultSuffix constants)
  </read_first>
  <action>
    Create `Tests/Editor/AssetGeneratorTests.cs` in namespace `GraffitiEntertainment.Namer.Tests` with `using System.IO; using NUnit.Framework; using UnityEditor; using UnityEngine; using UnityEngine.TestTools; using GraffitiEntertainment.Namer.Editor;` plus a `ComputeAvailable` gate (`SystemInfo.supportsComputeShaders && SystemInfo.supportsAsyncGPUReadback`).
    - `[Test]` destination validation (no GPU): `new NamerProcessorSettings { Destination = "Assets/../Outside" }` and empty `Destination` -> `NamerProcessor.Process(material, settings)` returns a result with a non-empty `Error` and `GeneratedAssets.Count == 0`; `Assets/NAMER_Tests_Temp` passes validation.
    - `[UnityTest]` path + naming (D-16, gated): build a URP Lit material with a known name + base map inside `Assets/NAMER_Tests_Temp/`, set settings `Destination = "Assets/NAMER_Tests_Temp/Out"`, `Prefix = "P_", Suffix = "_N"`; run `NamerProcessor.Process(material, settings)`; assert `GeneratedAssets.Count == 1` and every path in `NamerGeneratedAsset` (MaterialPath/BaseTexturePath/SurfaceTexturePath) starts with `"Assets/NAMER_Tests_Temp/Out/"`, and the files exist (`AssetDatabase.LoadAssetAtPath<Material>`/`<Texture2D>` non-null), and names match `P_{name}_N_Base.png`, `P_{name}_N_Surface.png`, `P_{name}_N.mat`.
    - `[UnityTest]` overwrite gating (D-04, gated): run Process once to generate assets; then create a NON-stamped collision at the same target path (write a plain `.mat`/PNG via `File.WriteAllBytes` + `ImportAsset`, do NOT stamp), re-run Process with `OverwriteGenerated = true`, assert `result.Error` contains "not a NAMER-generated asset" (overwrite refused); then `AssetDatabase.SetLabels(existing, new[] { NamerEditorConstants.GeneratedLabel })` and re-run, assert success.
    - `[UnityTest]` import-stamping round-trip (GEN-04, Pitfall 2, gated): after Process, `TextureImporter importer = (TextureImporter)AssetImporter.GetAtPath(surfacePath)`; assert `importer.sRGBTexture == false`, `importer.textureCompression == TextureImporterCompression.Uncompressed`, `importer.filterMode == FilterMode.Point`, `importer.mipmapEnabled == false`; and assert the BASE texture importer has `sRGBTexture == true`; re-load the packed texture and assert a known packed alpha byte survives (metallic/roughness bit pattern preserved, not corrupted by compression/mips).
    - Known-value base-PNG assertion (same gated test, D-06 linear->sRGB): use a source base color with a known linear value (e.g. `Color(0.5f, 0.5f, 0.5f, 1f)`), then after Process decode the generated `_Base.png` (`ImageConversion.LoadImage` into a non-sRGB `Texture2D`, or `File.ReadAllBytes` + `LoadImage`) and assert a sampled pixel equals the expected sRGB-encoded value (linear 0.5 encodes to sRGB byte ≈ 188) — proving the linear->sRGB GPU conversion actually ran rather than passing raw linear bytes through.
    - EditorPrefs isolation (cross-plan contract with 03-01 Task 1: `NamerProcessorSettings` setters persist immediately to EditorPrefs): before each test that sets `Destination`/`Prefix`/`Suffix`/`OverwriteGenerated` via a `NamerProcessorSettings` object initializer, snapshot the four keys `NamerProcessor.Destination`, `NamerProcessor.Prefix`, `NamerProcessor.Suffix`, `NamerProcessor.OverwriteGenerated` (`EditorPrefs.GetString`/`GetBool`); in the SAME `try/finally` that deletes the temp folder, restore each key with `EditorPrefs.SetString`/`SetBool` or `EditorPrefs.DeleteKey` when no prior value existed. A test's `Assets/../Outside` or empty destination must never survive the test run as the user's persisted setting.
    - Cleanup: wrap every generating test in `try/finally` -> `AssetDatabase.DeleteAsset("Assets/NAMER_Tests_Temp")` (deletes folder + `.meta`), and `DestroyImmediate` any created material/texture. Never run against the real `Assets/NAMERGenerated/`.
  </action>
  <acceptance_criteria>
    - `AssetGeneratorTests.cs` contains `class AssetGeneratorTests`
    - `AssetGeneratorTests.cs` contains `NamerProcessor.Process(` and `ComputeAvailable` and `Assert.Ignore`
    - `AssetGeneratorTests.cs` contains `Assets/NAMER_Tests_Temp` and `AssetDatabase.DeleteAsset` (temp-destination hygiene)
    - `AssetGeneratorTests.cs` contains `GeneratedAssets.Count` and `StartsWith` (path/naming assertions)
    - `AssetGeneratorTests.cs` contains `sRGBTexture` and `TextureImporterCompression.Uncompressed` and `FilterMode.Point` and `mipmapEnabled` (import-stamping assertions)
    - `AssetGeneratorTests.cs` contains `NamerEditorConstants.GeneratedLabel` and `AssetDatabase.SetLabels` (overwrite gate)
    - `AssetGeneratorTests.cs` contains `LoadImage` and `GetPixel` (known-value base-PNG sRGB assertion)
    - `AssetGeneratorTests.cs` contains `EditorPrefs.GetString` and `EditorPrefs.SetString` and `EditorPrefs.DeleteKey` (settings snapshot + restore so a mutated destination never leaks into user prefs)
    - `AssetGeneratorTests.cs` does NOT reference `"Assets/NAMERGenerated/"` as a hard-coded test destination (uses the temp override)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "class AssetGeneratorTests\|NamerProcessor.Process(\|ComputeAvailable\|Assert.Ignore\|Assets/NAMER_Tests_Temp\|AssetDatabase.DeleteAsset\|GeneratedAssets.Count\|StartsWith\|sRGBTexture\|TextureImporterCompression.Uncompressed\|FilterMode.Point\|mipmapEnabled\|LoadImage\|GetPixel\|NamerEditorConstants.GeneratedLabel\|AssetDatabase.SetLabels\|EditorPrefs.GetString\|EditorPrefs.SetString\|EditorPrefs.DeleteKey" Packages/com.graffitientertainment.namer/Tests/Editor/AssetGeneratorTests.cs; /Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testFilter GraffitiEntertainment.Namer.Tests.AssetGeneratorTests -testResults /tmp/namer-assetgen.xml -logFile -</automated>
  </verify>
  <done>AssetGeneratorTests proves generated assets land under the configured destination with correct prefix/suffix, overwrite refuses non-stamped targets (D-04), and the packed surface re-imports linear/uncompressed/point/no-mips with the packed bits intact. All writing tests run against a temp destination that is deleted afterward, and the four EditorPrefs settings keys are snapshotted and restored so a test's mutated destination never leaks into the user's persisted settings. Note: batchmode requires the interactive editor closed (project lock); if locked, use the clone runner `/tmp/namer-gsd/run-tests-in-clone.sh EditMode`.</done>
</task>

<task type="auto">
  <name>Task 2: SourceImmutabilityTests — SHA-256 of source files + .meta across the full Process flow</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/SourceImmutabilityTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/SourceInspectorTests.cs (CreateImportedMap fixture helper + temp-folder + Destroy cleanup)
    - Packages/com.graffitientertainment.namer/Tests/Editor/GpuGoldenTests.cs (capability gate + WaitForCompletion pattern)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (full Process flow under test)
    - .planning/phases/03-asset-generation-editor-workflow-preview/03-CONTEXT.md (D-15: hash every source file AND its .meta; metadata/timestamp comparisons insufficient)
  </read_first>
  <action>
    Create `Tests/Editor/SourceImmutabilityTests.cs` in namespace `GraffitiEntertainment.Namer.Tests` with `using System.IO; using System.Security.Cryptography; using NUnit.Framework; using UnityEditor; using UnityEngine; using UnityEngine.TestTools; using GraffitiEntertainment.Namer.Editor;`.
    - `[UnityTest]` (gated by `ComputeAvailable`): inside `Assets/NAMER_Tests_Temp/`, create real source fixtures via the importer — a source base-color PNG, a source material referencing it, and (where feasible) a small model/`GameObject` — then:
      1. `string[] sourcePaths = AssetDatabase.GetAllAssetPaths()` filtered to files under `Assets/NAMER_Tests_Temp/` (the source fixtures), for each also the `.meta` (`path + ".meta"`).
      2. Compute SHA-256 over each source file's bytes and each `.meta`'s bytes into a `Dictionary<string, byte[]>` (use `File.ReadAllBytes` + `SHA256.Create().ComputeHash`).
      3. Run `NamerProcessor.Process(selection, new NamerProcessorSettings { Destination = "Assets/NAMER_Tests_Temp/Out", OverwriteGenerated = false })`.
      4. Re-read and re-hash the same source files + `.meta`; `Assert.AreEqual` byte-for-byte (compare `Convert.ToBase64String` or `CollectionAssert.AreEqual`) for every entry — byte-identical, NOT a timestamp/metadata comparison (D-15).
      5. Also assert the generated outputs exist under `Assets/NAMER_Tests_Temp/Out/` (the flow actually ran, not skipped silently).
    - EditorPrefs isolation: snapshot and restore the four `NamerProcessor.*` keys around the settings object initializer (same `try/finally` discipline as AssetGeneratorTests) so the `Destination = "Assets/NAMER_Tests_Temp/Out"` override never leaks into the user's persisted settings.
    - Cleanup `try/finally` -> `AssetDatabase.DeleteAsset("Assets/NAMER_Tests_Temp")`; `DestroyImmediate` any transient objects. Never hash or touch the real `Assets/NAMERGenerated/`.
  </action>
  <acceptance_criteria>
    - `SourceImmutabilityTests.cs` contains `class SourceImmutabilityTests`
    - `SourceImmutabilityTests.cs` contains `SHA256` and `ComputeHash` and `File.ReadAllBytes`
    - `SourceImmutabilityTests.cs` contains `".meta"` (hash the .meta too, D-15)
    - `SourceImmutabilityTests.cs` contains `NamerProcessor.Process(` and `Assert.Ignore` (capability gate)
    - `SourceImmutabilityTests.cs` contains a byte-for-byte comparison (`CollectionAssert.AreEqual` or `Convert.ToBase64String` or `SequenceEqual`)
    - `SourceImmutabilityTests.cs` contains `Assets/NAMER_Tests_Temp` and `AssetDatabase.DeleteAsset`
    - `SourceImmutabilityTests.cs` contains `EditorPrefs.GetString` and `EditorPrefs.SetString` and `EditorPrefs.DeleteKey` (settings snapshot + restore)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "class SourceImmutabilityTests\|SHA256\|ComputeHash\|File.ReadAllBytes\|\".meta\"\|NamerProcessor.Process(\|Assert.Ignore\|CollectionAssert.AreEqual\|SequenceEqual\|Assets/NAMER_Tests_Temp\|AssetDatabase.DeleteAsset\|EditorPrefs.GetString\|EditorPrefs.SetString\|EditorPrefs.DeleteKey" Packages/com.graffitientertainment.namer/Tests/Editor/SourceImmutabilityTests.cs; /Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testFilter GraffitiEntertainment.Namer.Tests.SourceImmutabilityTests -testResults /tmp/namer-immutability.xml -logFile -</automated>
  </verify>
  <done>SourceImmutabilityTests hashes every source fixture file and its .meta (SHA-256), runs the full Process flow, re-hashes, and asserts byte-identical — proving source assets (and their importer metadata) are never modified (GEN-02, D-15). It also snapshots/restores the four EditorPrefs keys so its destination override never leaks into user settings. Note the editor-lock constraint on headless runs (use the clone runner if locked).</done>
</task>

<task type="auto">
  <name>Task 3: Complete package.json + README (D-17) and confirm the full suite green</name>
  <files>Packages/com.graffitientertainment.namer/package.json, Packages/com.graffitientertainment.namer/README.md</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/package.json (current metadata — add only the missing D-17 fields)
    - Packages/com.graffitientertainment.namer/README.md (keep the existing packed-format table; append install + workflow)
    - .planning/phases/03-asset-generation-editor-workflow-preview/03-CONTEXT.md (D-17 exact scope)
  </read_first>
  <action>
    Edit `package.json` to add ONLY the missing D-17 fields, leaving `name` (`com.graffitientertainment.namer`), `version` (`0.1.0`), `displayName` (`NAMER`), `unity` (`6000.0`), `dependencies`, and `testables` unchanged:
    - `"description": "Unity-native editor plugin that converts PBR materials into compact NAMER materials — generate, preview, and stylize textures without leaving Unity."`
    - `"author": { "name": "Graffiti Entertainment" }`
    Edit `README.md` to append two sections AFTER the existing packed-format table (do not remove or alter the table):
    - `## Installation` — "Add the package by path or via UPM from the `Packages/com.graffitientertainment.namer` folder; requires Unity 6000.0+ and Universal Render Pipeline 17.x."
    - `## Workflow` — "Select a textured FBX, prefab, model, material, or folder, then run `Tools > NAMER > Processor` (or `Assets > Process with NAMER` / `GameObject > Process with NAMER`), preview before/after and debug channels, adjust AO un-multiply, then press `Process with NAMER`. Generated assets land under `Assets/NAMERGenerated/{source}/`; source assets are never modified."
    Then run the FULL EditMode + PlayMode suite headless (no `-quit`) and confirm exit 0 at HEAD: `/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer-editmode.xml -logFile -` and the equivalent `-testPlatform PlayMode`. If the interactive editor holds the lock, use the clone runner `/tmp/namer-gsd/run-tests-in-clone.sh EditMode` and `/tmp/namer-gsd/run-tests-in-clone.sh PlayMode`.
    - Phase-close traceability (requirement_coverage): in the 03-03 SUMMARY, record UI-03/UI-04/UI-05 as PHASE-SCOPED partial delivery — the Phase-3 slice (D-14 controls, D-09 before/after preview, six D-11 derivable debug channels) is delivered, while the vertex-color/residual/reconstruction-error debug views and the Phase-4/5 stylization/palette/smoothing/normal-detail/roughness-simplification controls remain open for Phases 4-5 (locked D-11/D-14/D-05). Do NOT mark UI-03/04/05 fully Complete in REQUIREMENTS.md this phase.
  </action>
  <acceptance_criteria>
    - `package.json` contains `"description"` and `"author"` and still contains `"name": "com.graffitientertainment.namer"`, `"unity": "6000.0"`, and the four `com.unity.*` dependencies unchanged
    - `README.md` contains `## Installation` and `## Workflow` and still contains `## Packed Surface Format`
    - The full EditMode suite exits 0 (all NAMER tests pass; capability-gated tests either run green or `Assert.Ignore` with an explicit report)
    - The full PlayMode suite exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -n "\"description\"\|\"author\"\|\"name\": \"com.graffitientertainment.namer\"\|\"unity\": \"6000.0\"" Packages/com.graffitientertainment.namer/package.json; grep -n "## Installation\|## Workflow\|## Packed Surface Format" Packages/com.graffitientertainment.namer/README.md; /Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer-editmode-final.xml -logFile -; /Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform PlayMode -testResults /tmp/namer-playmode-final.xml -logFile - (clone-runner fallback for either platform if locked: /tmp/namer-gsd/run-tests-in-clone.sh EditMode | PlayMode)</automated>
  </verify>
  <done>The package.json carries complete D-17 metadata, the README documents install + workflow, the full headless EditMode + PlayMode suite is green at HEAD, and the SUMMARY records UI-03/UI-04/UI-05 as phase-scoped partial delivery (remaining controls/channels map to Phases 4-5) — the phase is a shippable MVP package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> AssetDatabase | Tests create real assets under `Assets/NAMER_Tests_Temp/` and must clean them up (including `.meta`) |
| source fixtures -> hashing | Immutability test reads source file + `.meta` bytes; the assertion must be byte-exact |
| package metadata -> UPM | `package.json`/`README.md` edits are consumed by the Unity Package Manager |
| test settings -> EditorPrefs | Test `NamerProcessorSettings` object initializers write EditorPrefs; tests must restore the user's persisted values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | test cleanup leaving stray assets/`.meta` in the project | mitigate | every writing test uses `try/finally` -> `AssetDatabase.DeleteAsset("Assets/NAMER_Tests_Temp")` (folder + `.meta`); tests never touch the real `Assets/NAMERGenerated/` |
| T-03-11 | Integrity | immutability assertion weakened to timestamp/metadata checks | mitigate | SHA-256 over `File.ReadAllBytes` of every source file AND its `.meta`, compared byte-for-byte (D-15 forbids metadata/timestamp-only) |
| T-03-12 | Integrity | capability-gated test silently passing without running | mitigate | GPU tests `Assert.Ignore` with an explicit report; the immutability test also asserts generated outputs exist so a skipped flow is detectable |
| T-03-13 | Tampering | test `NamerProcessorSettings` object initializers persisting a mutated/empty `Destination` into the user's EditorPrefs | mitigate | snapshot + restore/`DeleteKey` the four `NamerProcessor.*` keys in the same `try/finally` as temp-folder cleanup (03-01 Task 1 setters persist immediately) |
| T-03-SC | Tampering | package installs | accept | zero new packages this plan (Unity Editor + .NET BCL only) |
</threat_model>

<verification>
- `grep` source assertions on both test files and package.json/README.md pass.
- `AssetGeneratorTests` and `SourceImmutabilityTests` pass headless EditMode (batchmode `-runTests`, no `-quit`; clone runner if the interactive editor holds the lock).
- Full EditMode + PlayMode suite green at HEAD (D-17).
</verification>

<success_criteria>
- [ ] AssetGeneratorTests: paths under destination, prefix/suffix honored, overwrite refuses non-stamped targets, packed texture imports linear/uncompressed/point/no-mips with bits intact
- [ ] SourceImmutabilityTests: SHA-256 of every source file + `.meta` byte-identical before/after the full Process flow
- [ ] Both test files snapshot/restore the four `NamerProcessor.*` EditorPrefs keys so no mutated/empty destination leaks into user settings
- [ ] package.json metadata complete (description + author added; dependencies unchanged)
- [ ] README documents Installation + Workflow
- [ ] Full headless EditMode + PlayMode suite green at HEAD
- [ ] SUMMARY records UI-03/UI-04/UI-05 as phase-scoped partial delivery (Phase-3 slice only)

</success_criteria>

<output>
Create `.planning/phases/03-asset-generation-editor-workflow-preview/03-03-SUMMARY.md` when done (include the UI-03/UI-04/UI-05 phase-scoped partial-delivery annotation per the Task 3 traceability bullet)
</output>
