---
phase: 03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/com.graffitientertainment.namer/Compute/NAMERAO.compute
  - Packages/com.graffitientertainment.namer/Compute/NAMERAO.hlsl
  - Packages/com.graffitientertainment.namer/Core/NamerConstants.cs
  - Packages/com.graffitientertainment.namer/Compute/NamerEncode.hlsl
  - Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerAOPipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerAOExtractionTests.cs
autonomous: true
requirements: [NORM-02, ENCD-03]
must_haves:
  truths:
    - "A Tripo-style source (no authored `_OcclusionMap`) now produces a non-white AO in the packed B channel and a functional `_AoUnmultiplyStrength` slider (D-01/D-02 — the urgent live pain is dead)"
    - "An authored `_OcclusionMap` still produces byte-identical output to today (D-07)"
    - "The un-multiply divisor for extracted AO is floored at 0.1 while the packed B channel stores the raw AO (D-09)"
    - "Round-trip `base_unmul × ao_packed ≈ base` holds at `_AoUnmultiplyStrength = 1.0` for every AO above the 0.1 floor (D-08)"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Compute/NAMERAO.compute"
      provides: "image-space AO extraction kernels (luminance, hierarchical block-average, separable blur, AO remap)"
      contains: "CSLuminance"
    - path: "Packages/com.graffitientertainment.namer/Compute/NAMERAO.hlsl"
      provides: "AO floor + remap constants mirroring Core"
      contains: "NAMER_AO_FLOOR"
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerAOPipeline.cs"
      provides: "AO stage orchestration (extraction + tweak remap + green-channel output)"
      contains: "class NamerAOPipeline"
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs"
      provides: "source gate (authored map vs extracted AO) + conditional un-multiply floor"
      contains: "_AoUnmultiplyFloor"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerAOExtractionTests.cs"
      provides: "extraction non-white + round-trip + floor + authored byte-identity tests"
      contains: "class NamerAOExtractionTests"
  key_links:
    - from: "NamerComputePipeline.Process"
      to: "NamerAOPipeline.Extract"
      via: "when inspection.OcclusionMap == null (D-07 gate)"
      pattern: "_aoPipeline.Extract"
    - from: "NamerComputePipeline.BindAndDispatch"
      to: "CSNormalize _AoUnmultiplyFloor"
      via: "0.1 extracted / 1e-6 authored"
      pattern: "_AoUnmultiplyFloor"
    - from: "CSAoRemap kernel"
      to: "_AoIn.g consumer (CSNormalize + CSOctahedralEncode)"
      via: "AO written to the GREEN channel"
      pattern: "float4(0, ao, 0, 1)"
---

<objective>
Kill the urgent live pain end-to-end: when a source material has NO authored `_OcclusionMap` (the Tripo case), auto-run a GPU image-space AO extraction so `_AoUnmultiplyStrength` stops being a no-op and the packed surface B channel carries a real, non-white AO value. Add the un-multiply divisor floor (0.1 for extracted/baked AO, `NAMER_EPSILON` for authored maps) so the un-multiply stays reference-compatible (D-09) while authored-map output stays byte-identical (D-07). The SAME extracted AO value is the `CSNormalize` divisor AND the packed B channel, so `base_unmul × ao_packed ≈ base` holds by construction (D-08).

Purpose: Make the slider functional on the source type that motivated this phase, without touching the runtime decode or the authored-map path.

Output: `Compute/NAMERAO.compute`, `Compute/NAMERAO.hlsl`, edits to `Core/NamerConstants.cs`, `Compute/NamerEncode.hlsl`, `Compute/NAMERPack.compute`, new `Editor/Pipeline/NamerAOPipeline.cs`, edits to `Editor/Pipeline/NamerComputePipeline.cs`, and `Tests/Editor/NamerAOExtractionTests.cs`.

Traceability note: ROADMAP.md lists Phase 03.1's requirements as `NORM-02, ENCD-03, UI-03` (boundary-aware IDs this phase strengthens — no dedicated IDs exist). This plan strengthens NORM-02 (baked-lighting removal) and ENCD-03 (AO preservation); its success criteria are derived from CONTEXT.md decisions D-01, D-02, D-07, D-08, D-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit/03.1-CONTEXT.md
@.planning/phases/03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit/03.1-RESEARCH.md
@.planning/phases/03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit/03.1-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor builds against (already in the codebase). -->
From Core/NamerConstants.cs (namespace GraffitiEntertainment.Namer.Core):
  public static class NamerConstants { public const float Epsilon = 1e-6f; ... }

From Compute/NamerEncode.hlsl:
  static const float NAMER_EPSILON = 1e-6;   // line 21
  float2 NamerOctahedralEncode(float3 v);     // line 28
  float4 NamerPackSurfaceFromOct(float2 oct, float ao, float metallic, float emissive, float roughness); // line 52

From Compute/NAMERPack.compute:
  uniform float _AoUnmultiplyStrength;        // line 36
  uniform uint2 _Size;                        // line 43
  CSNormalize divisor line 56: _BaseColorOut[id.xy] = float4(base / lerp(1.0, max(ao, NAMER_EPSILON), _AoUnmultiplyStrength), 1.0);
  CSOctahedralEncode line 96: _Octahedral[id.xy] = float4(oct, _AoIn[id.xy].g, 0.0);   // B = raw AO (do NOT change)
  #include "Packages/com.unity.render-pipelines.core/ShaderLibrary/Color.hlsl"  // line 16, SRGBToLinear

From Editor/Pipeline/NamerComputePipeline.cs:
  public NamerComputeResult Process(NamerMaterialInspection inspection);   // lines 74-135 (uploads base/normal/ao/metallicGloss, dispatches)
  public void ReleaseResult(NamerComputeResult result);
  public void Dispose();
  private static void Upload(Texture source, RenderTexture target, Texture2D fallback);   // line 221 — raw-copy contract
  internal static Material RawCopyMaterial();  // line 248
  private static RenderTextureDescriptor NewDescriptor(int w, int h, GraphicsFormat format);  // line 267
  private static Texture2D WhiteFill();        // line 278 — authored-map fallback

From Editor/Pipeline/NamerSourceModel.cs:
  public sealed class NamerMaterialInspection { Texture2D OcclusionMap; bool BaseMapIsSrgb; Texture2D BaseMap; float AoUnmultiplyStrength; ... }

From Editor/Pipeline/ComputeTexturePool.cs:
  RenderTexture Lease(RenderTextureDescriptor); void Release(RenderTexture); int LiveCount;

<!-- Contracts this plan CREATES for downstream plans (03.1-02, 03.1-03). -->
From Editor/Pipeline/NamerAOPipeline.cs (namespace GraffitiEntertainment.Namer.Editor):
  public sealed class NamerAOPipeline : IDisposable {
      // Extract image-space AO from the already-uploaded linear/raw base RT; returns a
      // pool-leased R8G8B8A8_UNorm linear RT with AO in .g, released via ReleaseAo.
      public RenderTexture Extract(NamerMaterialInspection inspection, RenderTexture baseColorIn, int w, int h);
      public void ReleaseAo(RenderTexture ao);
      public void Dispose();
  }

From Core/NamerConstants.cs:
  public const float AoFloor = 0.1f;   // NEW — divisor-side-only floor for extracted/baked AO
From Compute/NamerEncode.hlsl:
  static const float NAMER_AO_FLOOR = 0.1;   // NEW — mirrors NamerConstants.AoFloor
From Compute/NAMERPack.compute:
  uniform float _AoUnmultiplyFloor;          // NEW — 1e-6 (authored) | 0.1 (extracted/baked)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AO compute kernels + floor constants (GPU foundation)</name>
  <files>Packages/com.graffitientertainment.namer/Compute/NAMERAO.compute, Packages/com.graffitientertainment.namer/Compute/NAMERAO.hlsl, Packages/com.graffitientertainment.namer/Core/NamerConstants.cs, Packages/com.graffitientertainment.namer/Compute/NamerEncode.hlsl, Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute (kernel guard `uint3 id : SV_DispatchThreadID` + `if (id.x >= _Size.x) return;` + `[numthreads(8,8,1)]` shape, lines 45-75; the SRGBToLinear include line 16)
    - Packages/com.graffitientertainment.namer/Compute/NamerEncode.hlsl (constants block lines 14-21, the "mirrored manually with Core" comment)
    - Packages/com.graffitientertainment.namer/Core/NamerConstants.cs (Epsilon line 33, doc-comment style)
    - .planning/phases/03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit/03.1-RESEARCH.md (Pattern 1 separable blur lines 143-159, Pattern 2 AO estimate lines 161-169, the CSNormalize floor edit lines 245-253)
  </read_first>
  <action>
    Create `Compute/NAMERAO.hlsl` in the same include-guard style as `NamerEncode.hlsl` (guard `GRAFFITI_ENTERTAINMENT_NAMER_AO_INCLUDED`). Add: `static const float NAMER_AO_FLOOR = 0.1;` plus `static const float NAMER_LUMA_R = 0.2126;` / `NAMER_LUMA_G = 0.7152;` / `NAMER_LUMA_B = 0.0722;` (Rec.709) and the remap helper `float NamerAoRemap(float ao, float strength, float contrast)` implementing `ao = saturate(lerp(1.0, ao, strength)); ao = saturate((ao - 0.5) * contrast + 0.5); return ao;` (research Code Example lines 279-284). Keep the "mirrors Core/NamerConstants.cs — do not edit independently" header comment.

    Add to `Core/NamerConstants.cs` alongside `Epsilon` (line 33): `public const float AoFloor = 0.1f;` with a `/// <summary>Un-multiply divisor floor for extracted/baked AO (D-09); divisor-side only — the packed B stores the raw AO.</summary>` doc.

    Add to `Compute/NamerEncode.hlsl` alongside `NAMER_EPSILON` (line 21): `static const float NAMER_AO_FLOOR = 0.1;` with the mirrored-constants comment.

    Create `Compute/NAMERAO.compute` with the SAME header/guard/dispatch conventions as `NAMERPack.compute` (`#include "NAMERAO.hlsl"` + `#include "Packages/com.unity.render-pipelines.core/ShaderLibrary/Color.hlsl"`). Declare `#pragma kernel` for exactly: `CSLuminance`, `CSAverage`, `CSBlurH`, `CSBlurV`, `CSAoRemap`. Declare: `RWTexture2D<float4> _Luma;` `RWTexture2D<float4> _BlurA;` `RWTexture2D<float4> _BlurB;` `RWTexture2D<float4> _AoOut;` `RWTexture2D<float4> _AvgA;` `RWTexture2D<float4> _AvgB;` (the two ping-pong downsample-average targets), `RWTexture2D<float4> _DstAvg;` (the generic block-average write target, bound to `_AvgA`/`_AvgB` per pass), `Texture2D<float4> _BaseColorIn;` `Texture2D<float4> _SrcAvg;`, and uniforms `uint2 _Size; float _SourceIsSrgb; int _Radius; float _Sigma; float _AoStrength; float _AoContrast; float _LumaAverage; uint2 _AvgSrcSize; uint2 _AvgDstSize;`. Every kernel uses `uint3 id : SV_DispatchThreadID` + a size guard + `[numthreads(8,8,1)]`:
      - `CSLuminance`: read `_BaseColorIn[id.xy]`, apply `SRGBToLinear` only when `_SourceIsSrgb > 0.5` (mirror CSNormalize lines 53-54), write `float4(dot(base, float3(NAMER_LUMA_R, NAMER_LUMA_G, NAMER_LUMA_B)), 0, 0, 1)` to `_Luma`.
      - `CSAverage`: a HIERARCHICAL block-average, NOT a global accumulation. Each thread computes the MEAN of the 8×8 input block it covers (read `_SrcAvg` over `[id.xy * 8, id.xy * 8 + 8)` clamped to `_AvgSrcSize`; divide by the actual covered count) and writes `float4(blockMean, 0, 0, 1)` to `_DstAvg` at `id.xy` (guard `id.xy >= _AvgDstSize`). The output is 1/8 the input size. Because each output texel is a MEAN in [0,1] (not an accumulated sum), the reduction is overflow-free at any resolution (2K/4K/4096²) and deterministic (fixed threadgroup). Do NOT use `InterlockedAdd`, a `RWStructuredBuffer`, or a fixed-point accumulator.
      - `CSBlurH`/`CSBlurV`: separable gaussian exactly as RESEARCH Pattern 1 (lines 143-159) reading `_Src` and writing `_Dst` — bind `_Luma`/`_BlurA`/`_BlurB` as the ping-pong pair via the `Texture2D<float4>` `_Src` and `RWTexture2D<float4>` `_Dst` uniforms you declare locally (name them `_Src` and `_Dst` so the same kernel body serves both passes).
      - `CSAoRemap`: `ao = saturate(_Dst[id.xy].r / max(_LumaAverage, 1e-4));` then `ao = NamerAoRemap(ao, _AoStrength, _AoContrast);` then `_AoOut[id.xy] = float4(0, ao, 0, 1);` (AO in the GREEN channel — the `_AoIn.g` contract, RESEARCH Pitfall 1).

    Edit `Compute/NAMERPack.compute`: add `float _AoUnmultiplyFloor;` to the uniforms block (after `_AoUnmultiplyStrength`, line 36) and change the CSNormalize divisor line 56 from `max(ao, NAMER_EPSILON)` to `max(ao, _AoUnmultiplyFloor)`. Do NOT touch CSOctahedralEncode line 96 (B stays raw `_AoIn.g`).

    Constraint: all compute write targets are LINEAR (`R8G8B8A8_UNorm` / `R16G16B16A16_SFloat`), never sRGB. No per-pixel C# loops — everything here is HLSL.
  </action>
  <acceptance_criteria>
    - `NAMERAO.hlsl` contains `NAMER_AO_FLOOR` and `NamerAoRemap`
    - `NamerConstants.cs` contains `AoFloor = 0.1f`
    - `NamerEncode.hlsl` contains `NAMER_AO_FLOOR = 0.1`
    - `NAMERAO.compute` contains `#pragma kernel CSLuminance`, `#pragma kernel CSAverage`, `#pragma kernel CSBlurH`, `#pragma kernel CSBlurV`, `#pragma kernel CSAoRemap`, and `_AvgSrcSize`/`_AvgDstSize`
    - `NAMERAO.compute` contains `_AoOut[id.xy] = float4(0, ao, 0, 1)` (green-channel write)
    - `NAMERAO.compute` does NOT contain `InterlockedAdd` or `RWStructuredBuffer` (overflow-free hierarchical average only)
    - `NAMERPack.compute` contains `_AoUnmultiplyFloor` and its CSNormalize divisor reads `max(ao, _AoUnmultiplyFloor)`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "NAMER_AO_FLOOR\|NamerAoRemap" Packages/com.graffitientertainment.namer/Compute/NAMERAO.hlsl; grep -n "AoFloor = 0.1f" Packages/com.graffitientertainment.namer/Core/NamerConstants.cs; grep -n "NAMER_AO_FLOOR = 0.1" Packages/com.graffitientertainment.namer/Compute/NamerEncode.hlsl; grep -n "CSLuminance\|CSAverage\|CSBlurH\|CSBlurV\|CSAoRemap\|_AvgSrcSize\|_AvgDstSize" Packages/com.graffitientertainment.namer/Compute/NAMERAO.compute; grep -n "float4(0, ao, 0, 1)" Packages/com.graffitientertainment.namer/Compute/NAMERAO.compute; ! grep -v '^#' Packages/com.graffitientertainment.namer/Compute/NAMERAO.compute | grep -q "InterlockedAdd\|RWStructuredBuffer"; grep -n "_AoUnmultiplyFloor\|max(ao, _AoUnmultiplyFloor)" Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute</automated>
  </verify>
  <done>NAMERAO.compute + NAMERAO.hlsl define the five extraction kernels with AO written to the green channel and an overflow-free hierarchical average; the 0.1 floor constant is mirrored in Core and HLSL; CSNormalize's divisor is driven by the new `_AoUnmultiplyFloor` uniform. No existing kernel behavior changed except the divisor floor plumbing.</done>
</task>

<task type="auto">
  <name>Task 2: NamerAOPipeline + NamerComputePipeline source gate + floor wiring</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerAOPipeline.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs (Process lines 74-135, BindAndDispatch lines 168-209, Upload lines 221-246, NewDescriptor lines 267-276, the `_pool.Lease`/`Release` pattern)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/ComputeTexturePool.cs (Lease/Release/LiveCount)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs (NamerMaterialInspection fields)
    - .planning/phases/03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit/03.1-PATTERNS.md (NamerAOPipeline analog: compute load + FindKernel + RT pool + dispatch lines 132-202)
  </read_first>
  <action>
    Create `Editor/Pipeline/NamerAOPipeline.cs` in namespace `GraffitiEntertainment.Namer.Editor` as `public sealed class NamerAOPipeline : IDisposable`. It owns its OWN `ComputeTexturePool _pool` and loads `Compute/NAMERAO.compute` via `AssetDatabase.LoadAssetAtPath<ComputeShader>` + `FindKernel` (null-throw `InvalidOperationException` like `NamerComputePipeline`). Resolve kernels `CSLuminance`, `CSAverage`, `CSBlurH`, `CSBlurV`, `CSAoRemap` in the constructor. Constants (named, no magic numbers): `AverageDownsampleFactor = 8`, `MinLowPassRadius = 8`, `MaxLowPassRadius = 64`, `LowPassRadiusDivisor = 32`.

    `public RenderTexture Extract(NamerMaterialInspection inspection, RenderTexture baseColorIn, int w, int h)`:
      - Derive the internal low-pass: `radius = clamp(max(w, h) / 32, 8, 64)` and `sigma = radius / 3.0f`. Set `_AoStrength = 1.0f`, `_AoContrast = 1.0f` (identity for now — Wave 3 wires user params).
      - Lease `_Luma` (R16G16B16A16_SFloat), `_BlurA`/`_BlurB` (R16G16B16A16_SFloat), `_AvgA`/`_AvgB` (R16G16B16A16_SFloat), `_AoOut` (R8G8B8A8_UNorm linear) from `_pool`. No `ComputeBuffer` — the average is a texture hierarchy, not an atomic accumulation.
      - Bind `_Size`, `_SourceIsSrgb` (from `inspection.BaseMapIsSrgb`), `_Radius`, `_Sigma`, `_AoStrength`, `_AoContrast`, and the textures. Dispatch `CSLuminance` (`_BaseColorIn` -> `_Luma`), then reduce `_Luma` with `CSAverage` in a loop: `src = _Luma; srcW = w; srcH = h;` while `srcW > 1 || srcH > 1`, ping-pong `_AvgA`/`_AvgB` as `_DstAvg` with `_AvgSrcSize = (srcW, srcH)` and `_AvgDstSize = (ceil(srcW/8), ceil(srcH/8))` (a `[numthreads(8,8,1)]` block-mean), then `src = dst; srcW = ceil(srcW/8); srcH = ceil(srcH/8);`. When `src` is a small RT (<= 8×8, i.e. <= 64 texels), read it back once via `AsyncGPUReadback` and compute `_LumaAverage` as the scalar mean of those <= 64 texels in C# (a constant-bound loop over a tiny buffer, not a per-pixel loop over the texture). Then dispatch `CSBlurH`/`CSBlurV` (low-pass over `_Luma`) and `CSAoRemap` (with `_LumaAverage` uniform) -> `_AoOut`. Dispatch every kernel with the `(w + 7) / 8, (h + 7) / 8, 1` math (the average pass uses `(_AvgDstSize + 7) / 8`). Return `_AoOut` (still leased); release every intermediate (`_Luma`, `_BlurA`, `_BlurB`, `_AvgA`, `_AvgB`) in a `finally` — the caller owns only the returned `_AoOut`.

    `public void ReleaseAo(RenderTexture ao)` -> `_pool.Release(ao)`. `public void Dispose()` -> `_pool.Dispose()`.

    Edit `Editor/Pipeline/NamerComputePipeline.cs`:
      - Add `private NamerAOPipeline _aoPipeline;` (lazily constructed when first needed) and dispose it in `Dispose()`.
      - In `Process`, HOIST `bool usesExtractedAo = false;` ABOVE the `try` (so it is in scope in the `finally` and in `BindAndDispatch`). Replace the unconditional `aoIn = _pool.Lease(unorm8); ... Upload(inspection.OcclusionMap, aoIn, WhiteFill());` (lines 103/112) with a source gate: when `inspection.OcclusionMap != null`, keep the exact current behavior (`aoIn = _pool.Lease(unorm8); Upload(inspection.OcclusionMap, aoIn, WhiteFill()); usesExtractedAo = false;`). When `inspection.OcclusionMap == null`, do NOT lease/upload `aoIn` from the pack pool; instead `aoIn = EnsureAoPipeline().Extract(inspection, baseColorIn, w, h); usesExtractedAo = true;` (extraction runs AFTER `baseColorIn` is uploaded so it can read the linear/raw base). Pass `usesExtractedAo` through to `BindAndDispatch` and add `_compute.SetFloat("_AoUnmultiplyFloor", usesExtractedAo ? NamerConstants.AoFloor : NamerConstants.Epsilon);`. In the `finally`, release the extracted `aoIn` via `_aoPipeline.ReleaseAo(aoIn)` when `usesExtractedAo`, else `Release(aoIn)` — but never double-release (the extracted RT belongs to the AO pipeline's pool, not the pack pool).
      - Keep the authored-map path byte-identical: same `Upload`, same `_AoUnmultiplyFloor = Epsilon`, same `WhiteFill()`.
    Constraint: interactive preview stays memory-only; this plan adds no disk writes. No per-pixel C# loops.
  </action>
  <acceptance_criteria>
    - `NamerAOPipeline.cs` contains `class NamerAOPipeline` and `AssetDatabase.LoadAssetAtPath<ComputeShader>` and `FindKernel("CSAoRemap")` and `AverageDownsampleFactor = 8` and `ComputeTexturePool`
    - `NamerAOPipeline.cs` contains `public RenderTexture Extract(` and `public void ReleaseAo(` and `public void Dispose()` and `AsyncGPUReadback`
    - `NamerAOPipeline.cs` does NOT contain `new ComputeBuffer(` or `InterlockedAdd`
    - `NamerComputePipeline.cs` contains `_aoPipeline` and `NamerAOPipeline`
    - `NamerComputePipeline.cs` contains `_AoUnmultiplyFloor` and `NamerConstants.AoFloor` and `NamerConstants.Epsilon`
    - `NamerComputePipeline.cs` contains a branch on `inspection.OcclusionMap` (D-07 gate) with `usesExtractedAo` hoisted above the try
  </acceptance_criteria>
  <verify>
    <automated>grep -n "class NamerAOPipeline\|LoadAssetAtPath<ComputeShader>\|FindKernel(\"CSAoRemap\")\|AverageDownsampleFactor = 8\|ComputeTexturePool\|public RenderTexture Extract(\|public void ReleaseAo(\|public void Dispose()\|AsyncGPUReadback" Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerAOPipeline.cs; ! grep -v '^#' Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerAOPipeline.cs | grep -q "new ComputeBuffer(\|InterlockedAdd"; grep -n "_aoPipeline\|NamerAOPipeline\|_AoUnmultiplyFloor\|NamerConstants.AoFloor\|NamerConstants.Epsilon\|inspection.OcclusionMap\|usesExtractedAo" Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs</automated>
  </verify>
  <done>NamerComputePipeline.Process now routes to NamerAOPipeline.Extract when no authored `_OcclusionMap` exists and sets the divisor floor to 0.1 (extracted) vs 1e-6 (authored); the authored path is unchanged byte-for-byte. NamerAOPipeline owns the AO compute stage with a hierarchical (overflow-free) average and correct RT lifecycle.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end AO extraction tests (round-trip + floor + authored byte-identity + non-white)</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerAOExtractionTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/GpuGoldenTests.cs (capability gate lines 43-44/56-59, LoadShader/CreateRenderTexture/UploadPixels/ReadBackColor32 helpers lines 385-433, the `Assert.Ignore` on unsupported backends)
    - Packages/com.graffitientertainment.namer/Tests/Editor/ComputeSmokeTests.cs (pipeline-level `Process` + readback + `ReleaseResult` pattern, the SRGBToLinear helper, `FillSolid`)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs (Process/ReleaseResult contract)
  </read_first>
  <action>
    Create `Tests/Editor/NamerAOExtractionTests.cs` in namespace `GraffitiEntertainment.Namer.Tests` (namespace + `using` set from GpuGoldenTests). All GPU tests are `[UnityTest]` and `Assert.Ignore` when `!SystemInfo.supportsComputeShaders || !SystemInfo.supportsAsyncGPUReadback` (D-15 — never a silent pass). Build a helper `BuildInspection(Texture2D baseMap, bool baseIsSrgb, Texture2D occlusionMap)` returning a `NamerMaterialInspection` with `NormalMap = null, MetallicGlossMap = null, Metallic = 0, Smoothness = 0, Roughness = 1, Emissive = 0, AoUnmultiplyStrength = 1, SmoothnessTextureChannel = 0`.

    Write four tests:
      1. `Extraction_ProducesNonWhiteAo_WhenNoOcclusionMap` (kills the live pain): a `WorkingSize` (use 64) base map split into a bright half (linear ~0.9) and a dark half (linear ~0.15) with `BaseMapIsSrgb = false` and `OcclusionMap = null`. Run `NamerComputePipeline.Process`, read back `PackedSurface`. Assert the AO bytes are NOT uniformly 255 (i.e., at least one sampled texel `b < 250`) AND that a bright-region texel's `b` is >= a dark-region texel's `b`. Release the result and dispose the pipeline.
      2. `RoundTrip_BaseUnmultiplyTimesPackedAo_EqualsBase` (D-08): drive the kernels directly (like GpuGoldenTests) — upload a known linear base `0.3` and known AO `0.5`, run `CSNormalize` with `_AoUnmultiplyFloor = 0.1` and `_AoUnmultiplyStrength = 1`, then `CSOctahedralEncode` + `CSSurfacePack` with the same AO. Assert `normalizedBase.r × surface.b ≈ 0.3` within `2/255` (double quantization tolerance), proving divisor and B are the SAME ao.
      3. `Floor_IsDivisorSideOnly_NotStoredInB` (D-09): base `0.08`, AO `0.05`. Run CSNormalize with floor 0.1 -> assert `normalizedBase ≈ 0.08 / 0.1 = 0.8` (divisor floored) and `surface.b ≈ 0.05` (B stores raw, NOT floored). This pins Pitfall 3 (no double-darkening).
      4. `AuthoredOcclusionMap_IsByteIdentical_AndUsesEpsilonFloor` (D-07): an authored `_OcclusionMap` whose green channel is BELOW the 0.1 floor (e.g. `g = 0.05`) with a linear base `0.5`. Run `NamerComputePipeline.Process`. Assert `PackedSurface.b ≈ 0.05` (byte-identical to authored) AND `normalizedBase ≈ base / 0.05` (the divisor used `1e-6`, NOT the 0.1 extracted floor — this is the discriminating case: at `g = 0.05`, `base / 0.05` differs from `base / 0.1`, so a 0.1-floor regression is caught). Optionally add a second authored value `g = 0.5` as a plain byte-identity check, but the `0.05` case is the one that pins the floor.
    Use `ReleaseResult`/`Dispose` in `finally` everywhere; assert `pipeline.LiveRenderTargetCount == 0` after release where the pipeline is used (mirror ComputeSmokeTests).
  </action>
  <acceptance_criteria>
    - `NamerAOExtractionTests.cs` contains `class NamerAOExtractionTests`
    - `NamerAOExtractionTests.cs` contains all four test method names: `Extraction_ProducesNonWhiteAo_WhenNoOcclusionMap`, `RoundTrip_BaseUnmultiplyTimesPackedAo_EqualsBase`, `Floor_IsDivisorSideOnly_NotStoredInB`, `AuthoredOcclusionMap_IsByteIdentical_AndUsesEpsilonFloor`
    - `NamerAOExtractionTests.cs` contains `Assert.Ignore` (D-15 capability gate) and `supportsComputeShaders`
    - The authored-path test drives AO `0.05` (below the 0.1 floor) and asserts `base / 0.05` (not `base / 0.1`)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "class NamerAOExtractionTests\|Extraction_ProducesNonWhiteAo_WhenNoOcclusionMap\|RoundTrip_BaseUnmultiplyTimesPackedAo_EqualsBase\|Floor_IsDivisorSideOnly_NotStoredInB\|AuthoredOcclusionMap_IsByteIdentical_AndUsesEpsilonFloor\|Assert.Ignore\|supportsComputeShaders\|0.05f" Packages/com.graffitientertainment.namer/Tests/Editor/NamerAOExtractionTests.cs; /tmp/namer-gsd/run-tests-in-clone.sh EditMode 25</automated>
  </verify>
  <done>The four tests are green in the clone runner and the full EditMode suite stays green (all pre-existing tests pass). The live pain is dead: no-authored-map sources produce non-white AO; authored maps are byte-identical with the epsilon floor (discriminating case below the 0.1 floor); the floor is divisor-side-only; round-trip holds at strength 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inspection -> NamerComputePipeline | Untrusted `_OcclusionMap` presence + sRGB flag cross into the source gate |
| NamerComputePipeline -> NamerAOPipeline | The base RT + extraction params cross into the AO compute stage |
| NamerAOPipeline -> GPU | AO kernels dispatch; RT resource-safety boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Input Validation (V5) | null inspection / null base map in `Extract` | mitigate | `NamerComputePipeline.Process` already null-guards; `Extract` falls back to the base-map-derived `w/h` (no base map = DefaultResolution, mirroring Process) |
| T-03.1-02 | DoS | unbounded RT allocation per extract | mitigate | every intermediate RT released in `finally`; only `_AoOut` is returned and the caller releases it via `ReleaseAo` (leak-watchdog covered in 03.1-02 tests) |
| T-03.1-03 | Integrity | sRGB base double-decoded / wrong divisor floor | mitigate | `_SourceIsSrgb` gating mirrors CSNormalize; floor is a single uniform set from `usesExtractedAo` (authored -> Epsilon, extracted -> 0.1), never mixed |
| T-03.1-04 | Integrity | AO written to the wrong channel (breaks `_AoIn.g`) | mitigate | `CSAoRemap` writes `float4(0, ao, 0, 1)`; golden test 2/3 pin the B channel and divisor values |
| T-03.1-05 | Integrity | average reduction overflow at 2K/4K | mitigate | hierarchical block-mean average (each texel a mean in [0,1]), no fixed-point/atomic global accumulator — overflow-free by construction |
| T-03.1-SC | Tampering | package installs | accept | zero new packages this plan (Unity Editor + already-declared deps only) |
</threat_model>

<verification>
- `grep` source assertions on NAMERAO.compute/.hlsl, NamerConstants, NamerEncode.hlsl, NAMERPack.compute, NamerAOPipeline, NamerComputePipeline pass.
- `/tmp/namer-gsd/run-tests-in-clone.sh EditMode 25` is green with the four new tests and all pre-existing tests remain green.
- The urgent live pain ("AO always fully white on Tripo sources, slider a no-op") is dead: test 1 fails before this plan and passes after.
</verification>

<success_criteria>
- [ ] No-authored-map sources produce non-white AO in B and a functional `_AoUnmultiplyStrength` slider
- [ ] Authored `_OcclusionMap` path is byte-identical to today, with the epsilon (not 0.1) floor (D-07)
- [ ] Extracted-AO divisor floored at 0.1, packed B raw (D-09)
- [ ] Round-trip `base_unmul × ao_packed ≈ base` at strength 1 (D-08), test-covered
- [ ] The average reduction is overflow-free at >= 4096²
- [ ] Full EditMode suite green via the clone runner

</success_criteria>

<output>
Create `.planning/phases/03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit/03.1-01-SUMMARY.md` when done
</output>
