# Phase 03.1: AO Extraction - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-28
**Phase:** 03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit
**Areas discussed:** AO source strategy, High-res model input, Output combination, Bake tweak controls

---

## AO source strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Image now, bake later | Image-space extraction this phase; cubemap-of-light geometry bake deferred to its own phase (research recommendation — 3 of 4 advisors) | |
| Both this phase | Image-space extraction AND the geometry bake subsystem both land in this inserted phase | ✓ |
| Geometry bake only | Only the cubemap-of-light bake; no image-space estimate; slider stays a no-op for sources without usable geometry | |

**User's choice:** Both this phase
**Notes:** Explicit override of the defer recommendation. The bake is named in the phase title and the user wants it delivered now despite the larger scope. Sequencing decision added so image-space lands first (D-02).

---

## High-res model input

| Option | Description | Selected |
|--------|-------------|----------|
| Selected mesh only | The mesh being processed is the bake source (guaranteed UV/spatial coherence, zero new UI) | |
| Separate high-poly field only | User-assigned high-poly field with UV-match validation | |
| Both: default selected + optional override | Selected mesh by default; optional user-assigned high-res occluder field | ✓ |

**User's choice:** "option for both"
**Notes:** Revisited after initial selection. The high-res field is an OCCLUDER (geometry only, no UV-match requirement — rays originate on the selected mesh's texels, output UVs stay the selected mesh's). During the revisit the user asked to check the original Python code for a small offset on the higher-resolution mesh — confirmed in `/Users/Shared/SSDevelopment/Development/GraffitiEntertainment/namer_plugin/`: `cage_extrusion` "Extend Cage Out By", default 0.01 (`properties.py`), used by `bake_ao_texture` (`ao_operators.py`) along with ray distance = 10% of max bounds dimension, 64 samples, 16 px margin. These carried into D-05.

---

## Output combination

| Option | Description | Selected |
|--------|-------------|----------|
| Single source, auto | Authored `_OcclusionMap` wins when present (byte-identical to today); extraction/bake auto-runs when absent; same value divides base and packs into B (round-trip by construction) | ✓ |
| Composite with authored | Authored × extracted combined by user-chosen op (multiply/min/replace); breaks the round-trip | |

**User's choice:** Single source, auto (Recommended)
**Notes:** Reference note folded in during the revisit: `remove_ao_from_texture` clips AO to [0.1, 1.0] before dividing — adopted as the floor for extracted/baked AO (D-09) for reference compatibility.

---

## Bake tweak controls

| Option | Description | Selected |
|--------|-------------|----------|
| Blur + strength/contrast | AO blur radius + AO strength + AO contrast alongside the existing un-multiply slider | ✓ |
| Blur only | Only an AO blur radius control this phase | |
| Also gate bake section | Image tweaks plus a gated geometry-bake section now (direction count, bake blur) | |

**User's choice:** Blur + strength/contrast (Recommended)
**Notes:** Controls shape the AO output regardless of source. Plain gaussian blur (edge-preserving is Phase 5 STYL-04). Bake exposes only functionally required parameters (Phase 03 D-14).

---

## Claude's Discretion

- Geometry-bake implementation route (CPU Burst raycast vs GPU depth-projection) — research decides; Burst favored for determinism/testability
- Image-space extraction algorithm details, blur separability, halo mitigation ordering
- Kernel/file decomposition (extend NAMERPack.compute vs new NAMERAO.compute)
- UI layout of new controls within the Processing section

## Deferred Ideas

- Composite/blend ops between authored and extracted AO — Phase 5 STYL-05
- Edge-preserving smoothing — Phase 5 STYL-04
- Separate AO texture asset output — AO lives only in packed B
- Auto-detection of source conventions (Tripo/LOD)
- Direction count / ray distance as user-facing controls

---

*Phase: 03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit*
*Discussion log generated: 2026-08-28*
