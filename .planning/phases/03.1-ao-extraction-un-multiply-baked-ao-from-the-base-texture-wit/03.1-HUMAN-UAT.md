---
status: partial
phase: 03.1-ao-extraction-un-multiply-baked-ao-from-the-base-texture-wit
source: [03.1-VERIFICATION.md]
started: 2026-08-28
updated: 2026-08-29
---

## Current Test

[Test 1 — AO debug view shows no visible change; debugging]

## Tests

### 1. Tripo-source end-to-end flow

Open the Processor window on a Tripo-style FBX (no authored `_OcclusionMap`); inspect the AO debug channel and the before/after preview; drag AO Un-multiply Strength.

expected: AO view is non-white (darker in cavities/dark regions); the slider visibly changes the preview after the ~300 ms debounce.
result: fail
reported: "can't see debug AO texture changing at all, I do see the text say it's processing, but no dialog viewport change at all" (2026-08-29; console clean — no exceptions during recompute)
severity: major

### 2. Occluder fallback warning visibility

Assign an empty mesh or the selected mesh itself as the High-res Occluder.

expected: A warning HelpBox appears ("falling back to the selected mesh"); processing continues on the selected mesh without error.
result: [pending]

### 3. First-bake hitch check

Select a mesh-backed source with no authored `_OcclusionMap` and watch the first automatic bake land.

expected: Preview refreshes with baked AO once the bake completes; note any noticeable editor hitch during the first bake (it is synchronous, one-time, then cached — Warning W2).
result: [pending]

### 4. Decision on W1: bake → generated asset?

DECISION: confirm whether the geometry bake / high-res occluder should influence the GENERATED asset (Process with NAMER output), not only the preview. Today `NamerProcessor.Process` builds a fresh pipeline (empty bake cache, no occluder), so generated materials always use extraction/authored AO while the preview can show baked AO.

expected: Human decides: accept extraction-AO-only generation (current behavior) or schedule bake-to-generation wiring in NamerProcessor (see VERIFICATION.md Warning W1).
result: pass
reported: "for the AO extraction, since it is another material created, I don't see why this can't just apply to the preview, but also it means I can swap it manually in the editor once generated" — accepted preview-scope; the generated material is a separate asset the user can swap/adjust manually after generation.

## Summary

total: 4
passed: 1
issues: 1
pending: 2
skipped: 0
blocked: 0

## Gaps

<!-- YAML format for plan-phase --gaps consumption -->
- truth: "The AO debug channel visibly changes when AO controls are adjusted on a real no-occlusion-map source (non-white AO, live recompute visible in the viewport)"
  status: fix_planned
  reason: "User reported: 'can't see debug AO texture changing at all, I do see the text say it's processing, but no dialog viewport change at all' — recompute runs (status text updates, console clean, no exceptions), but the AO debug view output never visibly changes"
  severity: major
  test: 1
  root_cause: "NamerAOBvh.TraceBvh starts traversal at node index 0 (stack[sp++] = 0, NamerAOBvh.cs:284) but Build constructs the hierarchy post-order (children before parents), so the root is the LAST node; the root index BuildRecursive returns is discarded. For any occluder >4 triangles (kLeafSize), traversal only tests the deepest-left leaf's ~4 triangles, virtually every ray misses, and the geometry bake outputs pure white (real-source probe: 99.99% of texels at AO 1.0). The three-way gate then supersedes a GOOD image-space extraction (~24% of texels below 0.49) with that white bake after the first recompute, pinning AO at 1.0 — no AO slider can move the AO view and un-multiply divides by 1.0 so Base/Shaded cannot change either. Existing bake tests pass despite the bug because their occluders have ≤4 triangles (single node = root = 0). Falsified in the clone: changing only the root index to nodes.Length - 1 flipped the real bake to mean 0.876 / min 0.000 / 10.78% below 0.5 and made every AO sweep move surface.b"
  artifacts: [".planning/debug/ao-debug-view-no-change.md"]
  missing: []
  debug_session: "ao-debug-view-no-change"
  fix_commit: "aca6f1f"
  fix_verified: "75/75 EditMode green via clone runner (72 existing + 3 new: Bvh_RayCast_HitsTriangleOutsideFirstLeaf, Bake_RoofAboveSubdividedFloor_Darkens, RequestBake_Cancelled_DoesNotCachePartialBake); bake now runs in cancellable 16-row slabs with EditorUtility.DisplayCancelableProgressBar; cancel aborts without caching so the gate falls back to extraction"
