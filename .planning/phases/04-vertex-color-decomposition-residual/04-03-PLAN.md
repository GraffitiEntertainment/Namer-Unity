---
phase: 04-vertex-color-decomposition-residual
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs
  - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs
  - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerDebugChannelMaterial.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs
  - Packages/com.graffitientertainment.namer/Shaders/NamerDebugView.shader
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "Process with decomposition ON writes the full set — seam-split mesh .asset + residual EXR (when required; D-13 auto-drop omits it) + Base/Surface PNG + .mat — under NAMERGenerated/{source}/ (D-06)"
    - "The generated material binds the residual at _BaseResidualMap and the scene renderer's sharedMesh swaps to the split mesh so vertex colors render at runtime (D-07 / Pitfall 3)"
    - "The window exposes the Vertex Color Decomposition toggle (default OFF), Error Threshold slider (0.0-0.10, default 0.02), Residual Resolution ladder (Auto/2048/1024/512/256/128), the live Statistics block, and 10 debug channels (D-05/D-15/D-17/D-09/D-10)"
    - "Auto-drop below threshold omits the residual (stats 'not required') while transparent/cutout materials always keep a residual (D-13 / Pitfall 5)"
    - "Source assets are never modified — enforced by automated test (GEN-02 / TEST-02)"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs"
      provides: "WriteResidualExr + WriteMeshAsset + residual material bind + MeshPath/ResidualTexturePath"
      contains: "WriteResidualExr"
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs"
      provides: "D-05 decomposition stage + sharedMesh swap on bind"
      contains: "DecompositionEnabled"
    - path: "Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs"
      provides: "DecompositionEnabled / ErrorThreshold / ResidualResolution EditorPrefs keys"
      contains: "NamerProcessor.DecompositionEnabled"
    - path: "Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs"
      provides: "DefaultDecompositionEnabled / DefaultErrorThreshold / DefaultResidualResolution"
      contains: "DefaultDecompositionEnabled = false"
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      provides: "toggle/threshold/ladder/stats UI + 10 debug labels + decomposed preview parity"
      contains: "Vertex Color Decomposition"
    - path: "Packages/com.graffitientertainment.namer/Shaders/NamerDebugView.shader"
      provides: "COLOR semantic + channels 6/7/8"
      contains: "NAMER_DECOMP_HEATMAP"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs"
      provides: "end-to-end Process + mesh swap + EXR write + auto-drop + source immutability"
      contains: "class NamerDecompIntegrationTests"
  key_links:
    - from: "NamerProcessor decomposition stage"
      to: "NamerDecompPipeline.GenerateResidual + AssetGenerator.WriteResidualExr"
      via: "gated on settings.DecompositionEnabled (D-05)"
      pattern: "GenerateResidual"
    - from: "NamerProcessor.BindGeneratedMaterials"
      to: "renderer.sharedMesh"
      via: "MeshFilter.sharedMesh / SkinnedMeshRenderer.sharedMesh swap to generated split mesh"
      pattern: "sharedMesh = "
    - from: "AssetGenerator.WriteMaterial"
      to: "_BaseResidualMap"
      via: "residual EXR when decomposed, Base PNG otherwise (D-07 switch-back)"
      pattern: "_BaseResidualMap"
    - from: "NamerEditorWindow.RecomputePreview"
      to: "in-memory fit + residual + stats"
      via: "300 ms debounce, never writes disk (D-08/D-12)"
      pattern: "DecompositionEnabled"
---

<objective>
Ship vertex-color decomposition end-to-end through `Process with NAMER`: write the seam-split mesh + residual EXR + material, swap the scene renderer's sharedMesh so vertex colors render, and expose the opt-in toggle / threshold / resolution ladder / live stats / three new debug channels in the editor window — while keeping source assets byte-identical. This plan is the only one that touches disk and the UI, and it closes TEST-02 with headless integration tests.

Purpose: Turn the 04-01 (CPU fit/split) and 04-02 (GPU residual) stages into a user-visible, non-destructive feature with preview parity (D-08) and the no-texture endgame (D-13).

Output: edits to `AssetGenerator.cs`, `NamerProcessor.cs`, `NamerProcessorSettings.cs`, `NamerEditorConstants.cs`, `NamerEditorWindow.cs`, `NamerDebugChannelMaterial.cs`, `NamerPreviewRenderer.cs`, `NamerDebugView.shader`, and new `Tests/Editor/NamerDecompIntegrationTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vertex-color-decomposition-residual/04-CONTEXT.md
@.planning/phases/04-vertex-color-decomposition-residual/04-RESEARCH.md
@.planning/phases/04-vertex-color-decomposition-residual/04-PATTERNS.md
@.planning/phases/04-vertex-color-decomposition-residual/04-UI-SPEC.md

<interfaces>
<!-- Existing contracts the executor extends (already in the codebase). -->
From Editor/Generation/AssetGenerator.cs:
  public NamerGeneratedAsset Generate(NamerComputeResult result, NamerMaterialInspection inspection, NamerProcessorSettings settings, string destinationFolder);
  public static void PreflightTargets(NamerSourceModel model, NamerProcessorSettings settings, string destinationFolder);
  public static string ComposePath(NamerMaterialInspection inspection, NamerProcessorSettings settings, string destinationFolder, string extension);
  private static void WriteSurfaceTexture(Texture2D texture, string path, bool overwriteGenerated);  // the EXR writer's template
  private static void WriteMaterial(NamerMaterialInspection inspection, string surfacePath, string basePath, string materialPath, bool overwriteGenerated);  // binds _BaseResidualMap at line 378
  private static void EnsureWritableTarget(string path, bool overwriteGenerated);  private static void Stamp(Object asset);
  public sealed class NamerGeneratedAsset { public string MaterialPath; public string BaseTexturePath; public string SurfaceTexturePath; }

From Editor/Pipeline/NamerProcessor.cs:
  public static NamerProcessResult Process(Object selection, NamerProcessorSettings settings);
  private static void BindGeneratedMaterials(Object selection, Dictionary<Renderer,int[]> slotSources, NamerSourceModel model, NamerProcessResult result);
  public sealed class NamerProcessResult { public List<NamerGeneratedAsset> GeneratedAssets; public List<string> Warnings; public string Error; }

From Editor/Pipeline/NamerComputePipeline.cs:
  public NamerComputeResult Process(NamerMaterialInspection inspection);  public void ReleaseResult(NamerComputeResult result);
  public static AsyncGPUReadbackRequest RequestReadback(RenderTexture source, int mip = 0, TextureFormat format = TextureFormat.RGBA32);
  public sealed class NamerComputeResult { public RenderTexture NormalizedBaseColor; public RenderTexture PackedSurface; public int Width; public int Height; }

From Editor/UI/NamerEditorWindow.cs:
  private static readonly string[] DebugChannelLabels = { "Shaded", "Base Color", "AO", "Normal", "Roughness", "Metallic", "Emissive" };  // 7 entries -> 10
  private void DrawProcessingSection(NamerMaterialInspection inspection);  // AO sliders + disabled group + status line
  private void RecomputePreview();  private void MarkDirty();  private void Tick();
  private void ResolveGeneratedPreview();  // loads generated .mat/_Surface.png/_Base.png
  private static readonly int BaseResidualMapId = Shader.PropertyToID("_BaseResidualMap");

From Editor/UI/NamerPreviewRenderer.cs:
  public Texture Render(Mesh mesh, Material before, Material after, Rect rect);  // single mesh drawn twice — needs an after-mesh overload for D-08

From Editor/UI/NamerDebugChannelMaterial.cs:
  public Material Create();  public void SetTextures(Material material, Texture surface, Texture baseResidual);  public void SetChannel(Material material, int channel);

From Editor/Settings/NamerProcessorSettings.cs:
  private const string AoStrengthKey = "NamerProcessor.AoStrength";  // EditorPrefs pattern to copy

<!-- Contracts this plan CONSUMES from prior plans (already verified; no SUMMARY read needed). -->
From Editor/Decompose/MeshVertexSplitter.cs (04-01):
  public static NamerSplitResult Split(Mesh sourceMesh);  public sealed class NamerSplitResult { Vector3[] Positions; Vector3[] Normals; Vector4[] Tangents; Vector2[] Uvs; int[][] SubMeshTriangles; BoneWeight[] BoneWeights; Matrix4x4[] Bindposes; int VertexCount; }
From Editor/Decompose/VertexColorFitter.cs (04-01):
  public static VertexColorFitResult Fit(NamerSplitResult split, NativeArray<Color32> baseTexels, int baseWidth, int baseHeight);  public sealed class VertexColorFitResult : IDisposable { public Color32[] ToColor32Array(); public void Dispose(); }
From Editor/Decompose/NamerDecompPipeline.cs (04-02):
  public sealed class NamerDecompPipeline : IDisposable { public static readonly int[] ResolutionLadder = {2048,1024,512,256,128}; public NamerDecompOutput GenerateResidual(NamerSplitResult split, Color32[] quantizedColors, RenderTexture baseLinear, int w, int h, float errorThreshold, int manualResolution); public void Dispose(); }
  public sealed class NamerDecompOutput : IDisposable { public RenderTexture Residual; public NamerDecompErrorStats Stats; public void Dispose(); }
  public sealed class NamerDecompErrorStats { public float Coverage; public float AvgError; public float MaxError; public bool ResidualRequired; public int ChosenResolution; }
From Core/NamerConstants.cs (04-02): public const float VcFloor = 1e-3f;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AssetGenerator — WriteResidualExr + WriteMeshAsset + residual material bind</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs (Generate lines 36-155, WriteSurfaceTexture lines 306-330, WriteBaseTexture lines 332-352, WriteMaterial lines 358-419, ComposePath lines 188-198, EnsureWritableTarget/Stamp lines 474-506, NamerGeneratedAsset lines 512-517)
    - .planning/phases/04-vertex-color-decomposition-residual/04-RESEARCH.md (Pitfall 4 "stale EXR path" lines 298-303, "Don't Hand-Roll" EXR/mesh rows lines 268-270, Pattern 3 mesh write lines 242-254)
    - .planning/phases/04-vertex-color-decomposition-residual/04-PATTERNS.md (AssetGenerator extension section lines 266-297)
  </read_first>
  <action>
    Extend `Editor/Generation/AssetGenerator.cs` (the sole disk writer — do NOT introduce any other writer type):

    (1) New `private static void WriteResidualExr(Texture2D texture, string path, bool overwriteGenerated)` mirroring `WriteSurfaceTexture` (lines 306-330) but for EXR + linear: `EnsureWritableTarget` first, then `byte[] exr = ImageConversion.EncodeToEXR(texture);` `File.WriteAllBytes(path, exr);` `AssetDatabase.ImportAsset(path);` then set the `TextureImporter` `sRGBTexture = false`, `textureCompression = TextureImporterCompression.Uncompressed`, `filterMode = FilterMode.Point`, `mipmapEnabled = false`, `wrapMode = TextureWrapMode.Repeat`, `textureType = TextureImporterType.Default`, `SaveAndReimport()`, and `Stamp(AssetDatabase.LoadAssetAtPath<Texture2D>(path))`. The input texture must be an RGBAHalf `Texture2D` (`isReadable = true`) so `EncodeToEXR` emits a 16-bit-half EXR (D-02: residual is ALWAYS HDR EXR, never PNG).

    (2) New `private static void WriteMeshAsset(Mesh outMesh, string path, bool overwriteGenerated)`: `EnsureWritableTarget(path, overwriteGenerated);` then `AssetDatabase.CreateAsset(outMesh, path);` then `Stamp(AssetDatabase.LoadAssetAtPath<Mesh>(path));`. The mesh object itself is BUILT by a helper `private static Mesh BuildSplitMesh(NamerSplitResult split, Color32[] colors)` in this class (the splitter returns arrays; persistence stays here): `new Mesh { name = Path.GetFileNameWithoutExtension(path) }`, `if (split.VertexCount > ushort.MaxValue) outMesh.indexFormat = UnityEngine.Rendering.IndexFormat.UInt32;`, `outMesh.SetVertices(split.Positions); outMesh.SetNormals(split.Normals); outMesh.SetTangents(split.Tangents); outMesh.SetUVs(0, split.Uvs); outMesh.colors32 = colors;` `if (split.BoneWeights != null) outMesh.boneWeights = split.BoneWeights;` `outMesh.subMeshCount = split.SubMeshTriangles.Length;` `for (int i = 0; i < split.SubMeshTriangles.Length; i++) outMesh.SetTriangles(split.SubMeshTriangles[i], i);` `if (split.Bindposes != null) outMesh.bindposes = split.Bindposes;` `outMesh.RecalculateBounds();` (use the HIGH-LEVEL `SetVertices`/`SetTriangles` path — NOT `SetIndexBufferData`, RESEARCH Pitfall 6).

    (3) `Generate` gains an optional decomposition parameter: change the signature to `public NamerGeneratedAsset Generate(NamerComputeResult result, NamerMaterialInspection inspection, NamerProcessorSettings settings, string destinationFolder, NamerDecompData decomp = null)` where `NamerDecompData` (new class in this file) holds `NamerSplitResult Split; Color32[] Colors; RenderTexture Residual; NamerDecompErrorStats Stats;`. Compose two extra paths `string residualPath = ComposePath(inspection, settings, destinationFolder, "_Residual.exr");` and `string meshPath = ComposePath(inspection, settings, destinationFolder, ".asset");`, `ValidateComposedPath` + `EnsureWritableTarget` both when `decomp != null`. When `decomp != null`: ALWAYS `BuildSplitMesh` + `WriteMeshAsset` and populate `MeshPath` (the split mesh is written even when the residual is auto-dropped — D-06's full set, and success criterion 1 needs the output mesh's vertex colors). Then branch: when `decomp.Stats.ResidualRequired`, read back `decomp.Residual` (the chosen R×R residual, R = `decomp.Stats.ChosenResolution`) as `TextureFormat.RGBAHalf` via `NamerComputePipeline.RequestReadback`, build the RGBAHalf `Texture2D`, `WriteResidualExr`, and pass `residualPath` (not `basePath`) to `WriteMaterial` so it binds the residual at `_BaseResidualMap` (D-07). When `!decomp.Stats.ResidualRequired`, skip `WriteResidualExr` entirely and pass `null` (unbound) to `WriteMaterial` so `_BaseResidualMap` keeps its `white {}` shader default — albedo = vertex color only (D-13 no-texture endgame); `ResidualTexturePath` stays empty. When `decomp == null`, keep the existing Phase-3 shape (bind `basePath`; NO mesh/residual written). Return `NamerGeneratedAsset` with `MeshPath` and `ResidualTexturePath` populated (`ResidualTexturePath` empty when not written).

    (4) `WriteMaterial` gains a parameter so it can bind base, residual, or neither: add an overload/parameter `string baseResidualPath` (callers pass `basePath` for non-decomposed, `residualPath` for decomposed, `null` for the auto-drop endgame). When `baseResidualPath != null`, the `_BaseResidualMap` bind at line 378 uses `baseResidualPath`; when `baseResidualPath == null`, do NOT call `material.SetTexture("_BaseResidualMap", ...)` (leave the shader's `white {}` default so albedo = vertex color).

    (5) `NamerGeneratedAsset` (lines 512-517) gains `public string MeshPath;` and `public string ResidualTexturePath;`.

    (6) `PreflightTargets` (lines 163-182): when the model is decomposed (pass a bool or detect via a new parameter), also `EnsureWritableTarget` the `_Residual.exr` + `.asset` composed paths for every inspection so a refusal surfaces BEFORE any GPU work (T-03-02).

    Constraint: all writes flow through `File.WriteAllBytes` / `AssetDatabase.CreateAsset` ONLY in this class; source paths are never passed to any write API.
  </action>
  <acceptance_criteria>
    - `AssetGenerator.cs` contains `WriteResidualExr` and `ImageConversion.EncodeToEXR` and `WriteMeshAsset` and `BuildSplitMesh`
    - `AssetGenerator.cs` contains `SetVertices` / `SetTriangles` and `IndexFormat.UInt32` and `colors32` and `subMeshCount` (NOT `SetIndexBufferData`)
    - `AssetGenerator.cs` contains `"_Residual.exr"` and `".asset"` path compositions and `NamerDecompData`
    - `NamerGeneratedAsset` contains `MeshPath` and `ResidualTexturePath`
    - `WriteMaterial` binds `_BaseResidualMap` from a `baseResidualPath` parameter (residual when decomposed, base when non-decomposed, and SKIPS the bind when `null` so the `white {}` default yields albedo = vertex color)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "WriteResidualExr\|EncodeToEXR\|WriteMeshAsset\|BuildSplitMesh\|SetVertices\|SetTriangles\|IndexFormat.UInt32\|colors32\|subMeshCount\|_Residual.exr\|NamerDecompData\|MeshPath\|ResidualTexturePath\|baseResidualPath" Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs; ! grep -v '^#' Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs | grep -q "SetIndexBufferData"</automated>
  </verify>
  <done>AssetGenerator writes the residual as an RGBAHalf EXR (linear/uncompressed/point/no-mips) at the chosen R×R resolution, writes the seam-split mesh as a .asset (high-level mesh API, UInt32 index when >65535 verts) EVEN when the residual is auto-dropped, binds the residual at _BaseResidualMap only when decomposition is required (leaving it unbound = white default in the auto-drop case), and preflights the new _Residual.exr + .asset paths before any GPU work. No new writer types introduced.</done>
</task>

<task type="auto">
  <name>Task 2: NamerProcessor decomposition stage + sharedMesh swap + settings/constants</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs, Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs, Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (Process lines 40-149, BindGeneratedMaterials lines 157-209, CaptureSlotSources/ResolveSourceInstanceId lines 218-252, the `foreach inspection` loop lines 103-124)
    - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs (AO EditorPrefs keys lines 14-21, getter/setter shape lines 51-77)
    - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs (AO defaults lines 34-44)
    - Packages/com.graffitientertainment.namer/Editor/Decompose/VertexColorFitter.cs + MeshVertexSplitter.cs + NamerDecompPipeline.cs (the 04-01/04-02 types)
    - .planning/phases/04-vertex-color-decomposition-residual/04-RESEARCH.md (Pitfall 3 mesh-swap lines 292-297; System Architecture Diagram lines 121-168)
    - .planning/phases/04-vertex-color-decomposition-residual/04-PATTERNS.md (NamerProcessor extension lines 413-434)
  </read_first>
  <action>
    (1) Add three EditorPrefs-backed settings to `Editor/Settings/NamerProcessorSettings.cs` (same getter/setter shape as `AoStrength`):
      - `private const string DecompositionEnabledKey = "NamerProcessor.DecompositionEnabled";` -> `public bool DecompositionEnabled { get { return EditorPrefs.GetBool(DecompositionEnabledKey, NamerEditorConstants.DefaultDecompositionEnabled); } set { EditorPrefs.SetBool(DecompositionEnabledKey, value); } }`
      - `private const string ErrorThresholdKey = "NamerProcessor.ErrorThreshold";` -> `public float ErrorThreshold` via `EditorPrefs.GetFloat/SetFloat` default `NamerEditorConstants.DefaultErrorThreshold`
      - `private const string ResidualResolutionKey = "NamerProcessor.ResidualResolution";` -> `public int ResidualResolution` via `EditorPrefs.GetInt/SetInt` default `NamerEditorConstants.DefaultResidualResolution` (0 = Auto). The stored value is the POPUP INDEX (0 = Auto, else `NamerDecompPipeline.ResolutionLadder[ResidualResolution - 1]` pixels), so it passes straight through to `GenerateResidual(..., manualResolution)` with no unit conversion.

    (2) Add three defaults to `Editor/NamerEditorConstants.cs` (after the AO defaults, lines 34-44): `public const bool DefaultDecompositionEnabled = false;` (D-05 opt-in), `public const float DefaultErrorThreshold = 0.02f;` (D-15), `public const int DefaultResidualResolution = 0;` (D-17 ladder index 0 = Auto).

    (3) Extend `Editor/Pipeline/NamerProcessor.cs` `Process` — insert the decomposition stage INSIDE the `foreach (NamerMaterialInspection inspection in model.Materials)` loop, AFTER `NamerComputeResult computeResult = pipeline.Process(inspection);` (line 109) and BEFORE `generator.Generate(...)` (line 112), gated on `settings.DecompositionEnabled`:
      - When OFF: call `generator.Generate(computeResult, inspection, settings, destinationFolder, null)` (Phase-3 shape unchanged).
      - When ON: (a) read back the linear base via `NamerComputePipeline.RequestReadback(computeResult.NormalizedBaseColor, 0, TextureFormat.RGBA32)` into a `NativeArray<Color32>`; (b) `NamerSplitResult split = MeshVertexSplitter.Split(inspection.BakeSourceMesh);` (if `BakeSourceMesh == null`, fall back to a single-warning "no mesh to decompose" and continue with the Phase-3 shape — never throw); (c) `VertexColorFitResult fit = VertexColorFitter.Fit(split, baseTexels, computeResult.Width, computeResult.Height); Color32[] colors = fit.ToColor32Array();`; (d) `NamerDecompPipeline decomp = new NamerDecompPipeline(); NamerDecompOutput output = decomp.GenerateResidual(split, colors, computeResult.NormalizedBaseColor, computeResult.Width, computeResult.Height, settings.ErrorThreshold, settings.ResidualResolution);`; (e) call `generator.Generate(computeResult, inspection, settings, destinationFolder, new NamerDecompData { Split = split, Colors = colors, Residual = output.Residual, Stats = output.Stats });`. Dispose `fit`, `output`, `decomp` and the readback `NativeArray` in a `finally` (never leak; the residual RT is pool-owned and released by `output.Dispose()` after `Generate` read it back).

    (4) Mesh swap (Pitfall 3): extend `BindGeneratedMaterials` to also swap the mesh when decomposition is ON. Track the split source mesh and the generated mesh path through the process result (e.g. add `MeshPath` lookups off `NamerGeneratedAsset`). Inside the existing `foreach (Renderer renderer in gameObject.GetComponentsInChildren<Renderer>(true))` traversal, after the material swap, when a generated mesh exists: `if (renderer is MeshFilter mf && mf.sharedMesh == sourceMesh) { mf.sharedMesh = generatedMesh; } else if (renderer is SkinnedMeshRenderer smr && smr.sharedMesh == sourceMesh) { smr.sharedMesh = generatedMesh; }` where `generatedMesh = AssetDatabase.LoadAssetAtPath<Mesh>(asset.MeshPath)`. Do NOT swap when decomposition is OFF or the renderer's mesh is not the split source. The swap runs whenever decomposition is ON and a generated mesh was written — INCLUDING the auto-drop (`ResidualRequired == false`) case, where no residual is bound but the mesh still swaps so the vertex-color reconstruction renders (the swap is a hard requirement — without it vertex colors are white at runtime, RESEARCH Pitfall 3).

    Constraint: `AssetGenerator` remains the only disk writer; this task only orchestrates. No per-pixel C# loops.
  </action>
  <acceptance_criteria>
    - `NamerProcessorSettings.cs` contains `NamerProcessor.DecompositionEnabled`, `NamerProcessor.ErrorThreshold`, `NamerProcessor.ResidualResolution` with `EditorPrefs.GetBool/GetFloat/GetInt`
    - `NamerEditorConstants.cs` contains `DefaultDecompositionEnabled = false`, `DefaultErrorThreshold = 0.02f`, `DefaultResidualResolution = 0`
    - `NamerProcessor.cs` contains `settings.DecompositionEnabled`, `MeshVertexSplitter.Split`, `VertexColorFitter.Fit`, `ToColor32Array`, `new NamerDecompPipeline()`, `GenerateResidual`, and `new NamerDecompData`
    - `NamerProcessor.cs` contains a `sharedMesh` swap branch (`MeshFilter mf` / `SkinnedMeshRenderer smr`) guarded on `sharedMesh == sourceMesh`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "NamerProcessor.DecompositionEnabled\|NamerProcessor.ErrorThreshold\|NamerProcessor.ResidualResolution\|EditorPrefs.GetBool\|EditorPrefs.GetFloat\|EditorPrefs.GetInt" Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs; grep -n "DefaultDecompositionEnabled = false\|DefaultErrorThreshold = 0.02f\|DefaultResidualResolution = 0" Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs; grep -n "settings.DecompositionEnabled\|MeshVertexSplitter.Split\|VertexColorFitter.Fit\|ToColor32Array\|new NamerDecompPipeline()\|GenerateResidual\|new NamerDecompData\|sharedMesh = \|MeshFilter mf\|SkinnedMeshRenderer smr" Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs</automated>
  </verify>
  <done>NamerProcessor gates the decomposition stage on settings.DecompositionEnabled, reads back the base, splits+fit+generates the residual in-memory, passes a NamerDecompData to AssetGenerator, and swaps the scene renderer's sharedMesh to the generated split mesh. The three decomposition settings are EditorPrefs-backed with the locked defaults (OFF / 0.02 / 0).</done>
</task>

<task type="auto">
  <name>Task 3: Window UI + stats + preview parity + debug channels 6/7/8 + integration tests</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs, Packages/com.graffitientertainment.namer/Editor/UI/NamerDebugChannelMaterial.cs, Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs, Packages/com.graffitientertainment.namer/Shaders/NamerDebugView.shader, Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (DebugChannelLabels lines 22-25, DrawProcessingSection lines 581-652, RecomputePreview lines 264-328, ResolveGeneratedPreview lines 203-236, MarkDirty/Tick lines 238-262/425-429, AO slider + debounce pattern lines 587-641)
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerDebugChannelMaterial.cs (property-ID cache + SetTextures/SetChannel lines 14-55)
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs (Render single-mesh lines 56-86)
    - Packages/com.graffitientertainment.namer/Shaders/NamerDebugView.shader (Attributes/Varyings lines 52-62, DebugVert lines 64-70, DebugFrag branch lines 72-115)
    - Packages/com.graffitientertainment.namer/Shaders/NAMER.shader (COLOR semantic + Varyings vertexColor lines 113-129/213-214 — the runtime reference)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRendererBindingTests.cs (PrefsSnapshot + temp-folder + ComputeAvailable + fixture helpers — the integration-test template)
    - .planning/phases/04-vertex-color-decomposition-residual/04-UI-SPEC.md (Component Inventory #12-15, Layout lines 132-156, Interaction lines 159-186, Debug Channel Contract lines 191-210, Copywriting lines 212-234)
  </read_first>
  <action>
    (1) `NamerEditorWindow.cs` — UI controls + stats + preview parity:
      - Extend `DebugChannelLabels` (lines 22-25) to 10 entries: append `"Vertex Colors", "Residual", "Error Heatmap"` after `"Emissive"` (the existing `Mathf.Max(0, _debugChannel - 1)` mapping at line 311 now yields `_DebugChannel` 6/7/8 for those).
      - Add state fields: `_decompositionEnabled` (init from `_settings.DecompositionEnabled`), `_errorThreshold` (init `_settings.ErrorThreshold`), `_residualResolution` (init `_settings.ResidualResolution`), and a stats field holding the last `NamerDecompErrorStats` (or its five display strings) + a `NamerDecompPipeline _decompPipeline` (lazily constructed/disposed like `_pipeline`).
      - In `DrawProcessingSection` (after the AO Contrast slider, before the status line), add the decomposition group per UI-SPEC §Layout: `md` gap, then `EditorGUILayout.Toggle("Vertex Color Decomposition", _decompositionEnabled)` (tooltip per UI-SPEC Copywriting), then inside `EditorGUI.BeginDisabledGroup(!_decompositionEnabled)` an `EditorGUILayout.Slider("Error Threshold", _errorThreshold, 0f, 0.10f)` and an `EditorGUILayout.Popup("Residual Resolution", _residualResolution, new[]{"Auto","2048","1024","512","256","128"})`. Every change writes through to `_settings` (DecompositionEnabled/ErrorThreshold/ResidualResolution), calls `_afterPanelState.MarkTweaking()` and `MarkDirty()` (300 ms debounced recompute, D-08/D-12 — never writes disk). When `_decompositionEnabled`, render the stats block: a bold `"Statistics"` sub-header + five `EditorGUILayout.LabelField` rows `Coverage` / `Avg Error` / `Max Error` / `Residual` (`not required` | `required`) / `Residual Resolution` (`—` until the first fit), hidden when OFF (no dead controls).
      - `RecomputePreview` (D-08/D-12): after `_liveResult = _pipeline.Process(inspection)`, when `_decompositionEnabled`, run the in-memory fit+residual (read back base, `MeshVertexSplitter.Split` + `VertexColorFitter.Fit` + `NamerDecompPipeline.GenerateResidual`), store the stats, and bind the decomposed representation to the after pane: `_namerMaterial.SetTexture(BaseResidualMapId, output.Residual)` — when `output.Residual == null` (auto-drop) pass `null` so `_BaseResidualMap` is unbound and the preview shows albedo = vertex color (D-13 parity) — and, when a generated decomposed preview exists, `_debugMaterialFactory.SetTextures(_debugMaterial, surface, output.Residual ?? base)` + `SetDebugBaseMap(_debugMaterial, base)`. When OFF, keep the existing Phase-3 binding (base at `_BaseResidualMap`). Build an in-memory split mesh (`Mesh`) with the fitted `colors32` for the after pane.
      - `ResolveGeneratedPreview` (D-14): also load the generated mesh (`ComposePath(..., ".asset")`) and residual (`ComposePath(..., "_Residual.exr")`) so the toggle flips the after pane between decomposed (split mesh + residual) and non-decomposed (original mesh + base) instantly, without reprocessing — extend `NamerAfterPanelState` usage if needed (do not add a new state machine).

    (2) `NamerPreviewRenderer.cs` — after-mesh parity: add `public Texture Render(Mesh beforeMesh, Mesh afterMesh, Material before, Material after, Rect rect)` that draws `beforeMesh` at `beforePosition` and `afterMesh` at `afterPosition` (framing uses `beforeMesh.bounds`); keep the existing single-mesh `Render` as a delegating overload. The window calls the two-mesh overload when decomposition is ON (before = original mesh, after = split mesh with vertex colors), else the single-mesh overload.

    (3) `NamerDebugChannelMaterial.cs`: add `private static readonly int DebugBaseMapId = Shader.PropertyToID("_DebugBaseMap");` and `public void SetDebugBaseMap(Material material, Texture baseMap)` (same null-guard shape as `SetTextures`).

    (4) `NamerDebugView.shader`: add `float4 color : COLOR;` to `Attributes`, `float4 vertexColor : TEXCOORD1;` to `Varyings`, `output.vertexColor = input.color;` in `DebugVert`, and `#include "../Compute/NAMERDecomp.hlsl"` (next to the existing `#include "NamerSurface.hlsl"`). Add a `_DebugBaseMap` property (`[NoScaleOffset] _DebugBaseMap("Debug Base", 2D) = "white" {}`) + `TEXTURE2D(_DebugBaseMap); SAMPLER(sampler_DebugBaseMap);`. Extend the `DebugFrag` branch after Emissive:
      - `else if (_DebugChannel < 6.5) { channel = input.vertexColor.rgb; }` (6: Vertex Colors — white where no stream)
      - `else if (_DebugChannel < 7.5) { channel = baseResidual.rgb; }` (7: Residual — equals base when non-decomposed)
      - `else { float3 rec = baseResidual.rgb * input.vertexColor.rgb; float3 db = SAMPLE_TEXTURE2D(_DebugBaseMap, sampler_DebugBaseMap, input.uv).rgb; float err = (abs(rec.r-db.r)+abs(rec.g-db.g)+abs(rec.b-db.b))/3.0; channel = NAMER_DECOMP_HEATMAP(err / 0.25); }` (8: Error Heatmap — violet/zero when non-decomposed since rec == base and vertexColor == white).
      Keep the `NAMER_DECODE_SURFACE` call so views cannot drift from runtime decode.

    (5) `Tests/Editor/NamerDecompIntegrationTests.cs` (namespace `GraffitiEntertainment.Namer.Tests`) — 4 `[UnityTest]` cases with the `ComputeAvailable` gate, `Assets/NAMER_Tests_Temp` folder, `PrefsSnapshot` (snapshot/restore the three new decomposition keys + the 4 existing ones), and the `NamerRendererBindingTests` fixture helpers. Cases:
      - `Process_WithDecomposition_WritesMeshAndResidualAndSwapsMesh`: Process a scene GameObject with a textured material + decomposition ON -> `GeneratedAssets[0].MeshPath`/`ResidualTexturePath` non-empty; the mesh `.asset` and residual `.exr` load; `generatedMaterial.GetTexture("_BaseResidualMap")` == the residual texture; the renderer's `sharedMesh` == the generated split mesh (not the source).
      - `Process_WithDecompositionOff_KeepsPhase3Shape`: decomposition OFF -> `MeshPath`/`ResidualTexturePath` empty; `_BaseResidualMap` binds the base PNG; `sharedMesh` unchanged.
      - `Process_ConstantColorBase_AutoDropsResidual`: a constant-color base + flat quad (near-perfect fit) -> `MeshPath` non-empty and the renderer's `sharedMesh` swapped to the generated split mesh, `ResidualTexturePath` empty, `generatedMaterial.GetTexture("_BaseResidualMap") == null` (unbound white default), and stats `ResidualRequired == false` (D-13 "not required" endgame).
      - `SourceImmutability_WithDecomposition`: after Process with decomposition ON, the source mesh / material / textures still load at their original paths with unmodified bytes/hash (reuse the SHA-256 approach of `SourceImmutabilityTests`).
    Dispose all pipelines/outputs and delete the temp folder in `finally`.
  </action>
  <acceptance_criteria>
    - `NamerEditorWindow.cs` contains `"Vertex Color Decomposition"`, `"Error Threshold"`, `"Residual Resolution"`, `"Statistics"`, and a 10-entry `DebugChannelLabels` ending in `"Error Heatmap"`
    - `NamerEditorWindow.cs` contains `_settings.DecompositionEnabled` / `ErrorThreshold` / `ResidualResolution`, `_decompPipeline`, `BeginDisabledGroup(!_decompositionEnabled)`, `Popup`, and `MarkDirty()`
    - `NamerPreviewRenderer.cs` contains `Render(Mesh beforeMesh, Mesh afterMesh,`
    - `NamerDebugChannelMaterial.cs` contains `_DebugBaseMap` and `SetDebugBaseMap`
    - `NamerDebugView.shader` contains `float4 color : COLOR`, `vertexColor`, `_DebugBaseMap`, `NAMER_DECOMP_HEATMAP`, and `#include "../Compute/NAMERDecomp.hlsl"`
    - `NamerDecompIntegrationTests.cs` contains `class NamerDecompIntegrationTests` and the 4 `[UnityTest]` cases (writes mesh/residual/swaps mesh, off keeps Phase-3 shape, auto-drop, source immutability)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Vertex Color Decomposition\|Error Threshold\|Residual Resolution\|Statistics\|Error Heatmap\|_settings.DecompositionEnabled\|_settings.ErrorThreshold\|_settings.ResidualResolution\|_decompPipeline\|BeginDisabledGroup(!_decompositionEnabled)\|Popup" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs; grep -n "Render(Mesh beforeMesh, Mesh afterMesh," Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs; grep -n "_DebugBaseMap\|SetDebugBaseMap" Packages/com.graffitientertainment.namer/Editor/UI/NamerDebugChannelMaterial.cs; grep -n "float4 color : COLOR\|vertexColor\|_DebugBaseMap\|NAMER_DECOMP_HEATMAP\|NAMERDecomp.hlsl" Packages/com.graffitientertainment.namer/Shaders/NamerDebugView.shader; grep -n "class NamerDecompIntegrationTests\|Process_WithDecomposition\|Process_WithDecompositionOff\|AutoDropsResidual\|SourceImmutability_WithDecomposition\|UnityTest" Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs; /Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testFilter GraffitiEntertainment.Namer.Tests.NamerDecompIntegrationTests -testResults /tmp/namer-decomp-integration.xml -logFile -</automated>
  </verify>
  <done>The window exposes the decomposition toggle (default OFF), threshold slider (0.0-0.10), resolution ladder (Auto/2048/1024/512/256/128), and live stats block; the preview shows the decomposed reconstruction (split mesh + residual) when enabled; the debug shader renders channels 6/7/8 (Vertex Colors / Residual / Error Heatmap); and the 4 integration [UnityTest] cases pass (writes + mesh swap, OFF keeps Phase-3 shape, auto-drop, source immutability). Batchmode requires the interactive editor closed; fallback runner `/tmp/namer-gsd/run-tests-in-clone.sh EditMode`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composed output paths (prefix/suffix/destination) -> AssetGenerator | user-controlled strings cross into the disk-write path |
| generated mesh path -> renderer sharedMesh | a generated asset reference crosses into the live scene |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | `AssetGenerator.WriteResidualExr` / `WriteMeshAsset` composed paths | mitigate | Reuse `ValidateComposedPath` + `ValidateDestinationFolder` + `ValidatePathSegment` for the new `_Residual.exr` and `.asset` paths BEFORE any write; `EnsureWritableTarget` + `NamerGenerated` label stamp on the mesh + EXR (same as textures, RESEARCH Security Domain) |
| T-04-08 | Tampering | overwrite of a non-generated source asset | mitigate | `EnsureWritableTarget` refuses any target not carrying the `NamerGenerated` label; the new mesh + EXR paths run through it in `PreflightTargets` before GPU work |
| T-04-09 | Tampering | `BindGeneratedMaterials` mesh swap | mitigate | Swap `sharedMesh` ONLY when `renderer.sharedMesh == sourceMesh` and the generated mesh loaded from the stamped `.asset` path; never swap a renderer whose mesh was not the split source |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; all additions are C#/HLSL + Unity built-ins (`EncodeToEXR`, `AssetDatabase.CreateAsset`, `EditorGUILayout`) |
</threat_model>

<verification>
- `NamerDecompIntegrationTests` passes headless EditMode (batchmode `-runTests -testFilter GraffitiEntertainment.Namer.Tests.NamerDecompIntegrationTests`, no `-quit`; clone runner `/tmp/namer-gsd/run-tests-in-clone.sh EditMode` if the interactive editor holds the lock).
- Full EditMode suite still green after the `AssetGenerator`/`NamerProcessor` edits (no regressions in `AssetGeneratorTests`/`SourceImmutabilityTests`/`NamerRendererBindingTests`).
- Source immutability is enforced by `SourceImmutability_WithDecomposition` (SHA-256 of source mesh/material/textures unchanged).
</verification>

<success_criteria>
- Process with decomposition ON writes the split mesh `.asset` + Base/Surface PNG + `.mat` plus residual `.exr` when required (D-13 auto-drop omits it), binds the residual at `_BaseResidualMap` when present, and swaps the scene renderer's `sharedMesh`.
- The window exposes the opt-in toggle (OFF default), threshold slider, resolution ladder, live stats, and 10 debug channels with no dead controls.
- Auto-drop omits the residual for opaque near-perfect fits (stats "not required"); transparent/cutout always keep a residual.
- 4 integration `[UnityTest]` cases pass; source assets remain byte-identical.
</success_criteria>

<output>
Create `.planning/phases/04-vertex-color-decomposition-residual/04-03-SUMMARY.md` when done
</output>
