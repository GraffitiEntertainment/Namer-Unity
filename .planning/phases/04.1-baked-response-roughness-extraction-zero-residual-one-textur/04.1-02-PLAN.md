---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
plan: 02
type: execute
wave: 2
depends_on: [04.1-01]
files_modified:
  - Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute
  - Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.hlsl
  - Packages/com.graffitientertainment.namer/Editor/Bake/NamerRoughnessFitter.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs
  - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
autonomous: true
requirements: [NORM-01, NORM-03, VCOL-03, VCOL-04, VCOL-05, UI-03]

must_haves:
  truths:
    - "The fit-driven estimator (default) walks a deterministic strength ladder and picks the strength that minimizes the post-refit residual MaxError; the chosen strength drives both the roughness remap and the sharp-removal cleaned base."
    - "The vertex-color refit consumes the post-extraction (sharp-removal-cleaned) base, not the AO-only cleaned base (D-05)."
    - "The editor window exposes a roughness-extraction strength slider (0 = off) and a fit-driven/Sobel estimator popup, both EditorPrefs-persisted and debounced."
  artifacts:
    - "Packages/com.graffitientertainment.namer/Editor/Bake/NamerRoughnessFitter.cs"
    - "Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs (RoughnessExtractStrength / RoughnessEstimator keys)"
    - "Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs (DefaultRoughnessExtractStrength / DefaultRoughnessEstimator)"
  key_links:
    - "NamerRoughnessFitter (CPU Burst ladder) <-> NamerRoughnessPipeline (per-iteration GPU eval) <-> NamerDecompPipeline (residual MaxError objective) — the fit-driven strength search."
    - "NamerProcessor (estimator selection + D-05 cleaned-base feed) -> VertexColorFitter.Fit -> NamerDecompPipeline.GenerateResidual."

# Artifacts this phase produces (plan 02 adds the fit-driven estimator + settings/UI)
## Artifacts this phase produces
- `Compute/NAMERRoughness.compute` — new kernels `CSRoughnessBlurH`, `CSRoughnessBlurV`, `CSRoughnessSharpDetail`, `CSRoughnessSharpRemoval`, `CSRoughnessRemap` (plan 02)
- `Editor/Bake/NamerRoughnessFitter.cs` — `NamerRoughnessFitter` class + `NamerRoughnessFitResult` struct + `[BurstCompile]` search job + `StrengthLadder` fixed table (plan 02)
- `Editor/Pipeline/NamerRoughnessPipeline.cs` — fit-driven `ExtractRoughness` path + `HasCachedFit`/`RequestFit`/`ClearFitCache` (plan 02)
- `Editor/Pipeline/NamerSourceModel.cs` — `public enum NamerRoughnessEstimator { FitDriven = 0, Sobel = 1 }` (plan 02)
- `Editor/Settings/NamerProcessorSettings.cs` — `RoughnessExtractStrength`, `RoughnessEstimator` properties (plan 02)
- `Editor/NamerEditorConstants.cs` — `DefaultRoughnessExtractStrength = 0f`, `DefaultRoughnessEstimator = 0` (plan 02)
- `Editor/UI/NamerEditorWindow.cs` — `_roughnessExtractStrength`, `_roughnessEstimator` fields + Processing-section controls (plan 02)
---

<objective>
Build the fit-driven estimator and the sharp-removal (frequency-separation) stage, and wire the resulting cleaned base into the vertex-color refit so the residual collapses for baked-response assets. Add the D-02/D-03 user controls (strength slider + estimator popup) to the editor window.

Purpose: turn the Sobel proof (plan 01) into the phase's primary path — the strength search minimizes the measured reconstruction error, and the sharp-removal cleaned base is what the refit consumes (D-05), which is the precondition for the one-texture outcome in plan 03.

Output: `NamerRoughnessFitter` (deterministic Burst strength ladder), the frequency-separation kernels, the estimator-selection + D-05 wiring in `NamerProcessor`/`NamerComputePipeline`, and the persisted settings + UI controls. The one-texture material binding and D-06 escape hatch are plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-CONTEXT.md
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-RESEARCH.md
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-PATTERNS.md
@Packages/com.graffitientertainment.namer/Editor/Bake/NamerAOBaker.cs
@Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerAOPipeline.cs
@Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs
@Packages/com.graffitientertainment.namer/Editor/Decompose/VertexColorFitter.cs
@Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs
@Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs
@Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs
@Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs
@Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs
@Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
</context>

<decisions>
Plan-review assumption resolved here (reversible, no checkpoint):

- **D-03 default = fit-driven** (A1, confirmed). `NamerRoughnessEstimator.FitDriven = 0` is the default for both the inspection field and the `RoughnessEstimator` settings key; `Sobel = 1` is the parity alternative. Swapping the default is a one-line change if plan review overrides it.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Frequency-separation kernels + NamerRoughnessFitter (Burst strength ladder)</name>
  <files>Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute, Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.hlsl, Packages/com.graffitientertainment.namer/Editor/Bake/NamerRoughnessFitter.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Compute/NAMERAO.compute (CSBlurH/CSBlurV separable gaussian with clamped taps — reuse this exact body)
    - Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute (CSReduce max-reduce already present)
    - Packages/com.graffitientertainment.namer/Editor/Bake/NamerAOBaker.cs (deterministic Burst discipline: `static readonly` fixed tables, `never System.Random`, `Allocator.TempJob` intermediates, `Allocator.Persistent` caller-owned result, `IJobParallelFor`, injectable `shouldCancel`)
    - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs (ChooseResolution ladder lines 307-353)
    - Packages/com.graffitientertainment.namer/Editor/Decompose/VertexColorFitter.cs (Fit signature)
  </read_first>
  <action>
    Extend `Compute/NAMERRoughness.compute` with the frequency-separation kernels (each opens with the `_Size` bound guard):
    - `CSRoughnessBlurH` / `CSRoughnessBlurV`: copy the `CSBlurH`/`CSBlurV` separable gaussian bodies from `NAMERAO.compute` (radius `_Radius`, sigma `_Sigma`, `exp(-x*x/(2*_Sigma*_Sigma))`, `clamp` taps), reading `.rgb` of `_BaseColorOut` and writing a full-RGB blurred target (no channel-select lerp — this blur separates color, not a scalar channel).
    - `CSRoughnessSharpDetail`: `_SharpDetail[id.xy] = float4(_BaseColorOut[id.xy].rgb - _Blurred[id.xy].rgb, 0.0)`.
    - `CSRoughnessSharpRemoval`: `_CleanedBaseOut[id.xy] = float4(_BaseColorOut[id.xy].rgb - _Strength * _SharpDetail[id.xy].rgb, 1.0)` then `saturate` each RGB channel (halo mitigation: clamp removed detail — RESEARCH Pitfall 5).
    - `CSRoughnessRemap`: `_RoughnessOut[id.xy] = float4(saturate(_Strength * length(_SharpDetail[id.xy].rgb)), 0.0, 0.0, 1.0)` (fit-driven roughness = strength-scaled sharp-detail magnitude; emitted in the roughness domain, no inversion).

    Add the new RWTexture/Texture declarations and `float _Strength; int _Radius; float _Sigma;` uniforms.

    Create `Editor/Bake/NamerRoughnessFitter.cs` mirroring `NamerAOBaker`'s discipline: a `public static class NamerRoughnessFitter` with `public static readonly float[] StrengthLadder = { 0.0f, 0.15f, 0.3f, 0.5f, 0.7f, 0.9f, 1.0f };` (deterministic fixed table, never `System.Random`). Define a caller-owned `public readonly struct NamerRoughnessFitResult { public readonly float Strength; public readonly float MaxError; public readonly bool Cancelled; }`. Add a `public static NamerRoughnessFitResult Fit(...)` entry that, for each `s` in `StrengthLadder`, evaluates the post-refit residual `MaxError` (via a caller-injected `Func<float, float> evaluate` — the GPU+refit+residual evaluation the pipeline provides), polls an injectable `Func<bool> shouldCancel` between steps (`DisplayCancelableProgressBar` when null), and returns the strength whose `MaxError` is lowest. The ladder job itself is a simple deterministic loop; the `evaluate` callback owns the GPU dispatch + `VertexColorFitter.Fit` + `NamerDecompPipeline.GenerateResidual` per step, so the CPU search is a thin deterministic minimizer (no per-pixel CPU work — NORM-03).
  </action>
  <verify>
    <automated>Editor assembly compiles; `NAMERRoughness.compute` imports with zero shader errors.</automated>
  </verify>
  <done>The five frequency-separation kernels exist and `NamerRoughnessFitter` deterministically selects the strength minimizing the residual MaxError from a fixed ladder, cancellable and headless-testable.</done>
  <acceptance_criteria>
    - `NAMERRoughness.compute` contains `#pragma kernel CSRoughnessBlurH`, `CSRoughnessBlurV`, `CSRoughnessSharpDetail`, `CSRoughnessSharpRemoval`, `CSRoughnessRemap`.
    - `NamerRoughnessFitter.cs` contains `StrengthLadder` (fixed `static readonly float[]`) and `NamerRoughnessFitResult` with `Strength`/`MaxError`/`Cancelled`.
    - `CSRoughnessRemap` emits `saturate(_Strength * length(_SharpDetail[id.xy].rgb))` (no `1 - x` inversion).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fit-driven pipeline path + estimator selection + D-05 cleaned-base refit wiring</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerAOPipeline.cs (bake cache HasCachedBake/RequestBake/ClearBakeCache shape; ReduceToScalarMean)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs (Process + BindAndDispatch; result NormalizedBaseColor contract)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (settings -> inspection copy lines 131-137; decomp orchestration lines 147-197; ReadBackBase)
    - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs (GenerateResidual signature)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs (NamerMaterialInspection fields)
  </read_first>
  <action>
    In `Editor/Pipeline/NamerRoughnessPipeline.cs`, add a fit-driven path to `ExtractRoughness` (estimator dispatch on `inspection.RoughnessEstimator`): for `NamerRoughnessEstimator.Sobel`, keep the plan-01 Sobel kernels; for `NamerRoughnessEstimator.FitDriven`, run blur H/V -> sharpDetail -> (fit search via `NamerRoughnessFitter`) -> sharp-removal -> remap. The fit-driven path returns the cleaned base AND the roughness texture; add a `public void StoreCleanedBase(...)`-style contract so `NamerComputePipeline` can repoint its `NormalizedBaseColor` result at the sharp-removal-cleaned base (D-05). Add a fit cache keyed by `(int meshId, int estimator, float strength, int w, int h)` with `HasCachedFit`/`RequestFit`/`ClearFitCache` mirroring the AO bake cache, so the debounce tick never re-searches (D-04).

    In `Editor/Pipeline/NamerComputePipeline.cs`, extend the plan-01 injection: when `inspection.RoughnessEstimator == FitDriven`, run the fit search as a one-time off-debounce action (`RequestFit` with the injectable `shouldCancel`), and after extraction write the sharp-removal-cleaned base into the render target that `NamerComputeResult.NormalizedBaseColor` points at (so `ReadBackBase` in `NamerProcessor` reads the post-extraction base). Keep the D-01 map gate and the Sobel path intact.

    In `Editor/Pipeline/NamerProcessor.cs`, inside the `foreach (NamerMaterialInspection inspection in model.Materials)` loop, add `inspection.RoughnessExtractStrength = settings.RoughnessExtractStrength;` and `inspection.RoughnessEstimator = settings.RoughnessEstimator;` (next to the existing `AoUnmultiplyStrength` copies). The decomp/refit block is UNCHANGED except that `computeResult.NormalizedBaseColor` now already holds the post-extraction cleaned base (D-05), so `ReadBackBase(computeResult.NormalizedBaseColor)` + `VertexColorFitter.Fit` + `GenerateResidual` operate on the cleaned base automatically. Define the `public enum NamerRoughnessEstimator { FitDriven = 0, Sobel = 1 }` in `Editor/Pipeline/NamerSourceModel.cs`.
  </action>
  <verify>
    <automated>Editor assembly compiles; existing `NamerAOExtractionTests` + `ResidualPipelineTests` still pass (regression: the non-extraction paths are untouched when `RoughnessExtractStrength == 0`).</automated>
  </verify>
  <done>Fit-driven strength search runs off-debounce and cached; the refit consumes the post-extraction cleaned base (D-05); estimator selection and strength are wired from settings through `NamerProcessor` into `NamerComputePipeline`.</done>
  <acceptance_criteria>
    - `NamerRoughnessPipeline.cs` dispatches on `inspection.RoughnessEstimator` (Sobel vs FitDriven) and exposes `HasCachedFit`/`RequestFit`/`ClearFitCache`.
    - `NamerComputePipeline` repoints `NamerComputeResult.NormalizedBaseColor` at the sharp-removal-cleaned base in the fit-driven path.
    - `NamerProcessor.cs` copies `inspection.RoughnessExtractStrength` and `inspection.RoughnessEstimator` from settings.
    - `NamerSourceModel.cs` defines `enum NamerRoughnessEstimator { FitDriven = 0, Sobel = 1 }`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Persisted settings keys + Processing-section UI controls</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs, Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs, Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs (AoUnmultiplyStrength property shape lines 54-59)
    - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs (DefaultAoUnmultiplyStrength pattern lines 34-53)
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (RebuildInspection snapshot lines 192-217, RecomputePreview inspection copy lines 310-314, DrawProcessingSection slider lines 776-788 + popup lines 863-875)
  </read_first>
  <action>
    In `Editor/Settings/NamerProcessorSettings.cs`, add `private const string RoughnessExtractStrengthKey = "NamerProcessor.RoughnessExtractStrength";` and `private const string RoughnessEstimatorKey = "NamerProcessor.RoughnessEstimator";`, plus two properties mirroring `AoUnmultiplyStrength`: `public float RoughnessExtractStrength` (get `EditorPrefs.GetFloat(..., NamerEditorConstants.DefaultRoughnessExtractStrength)`, set `EditorPrefs.SetFloat`) and `public int RoughnessEstimator` (get `EditorPrefs.GetInt(..., NamerEditorConstants.DefaultRoughnessEstimator)`, set `EditorPrefs.SetInt`).

    In `Editor/NamerEditorConstants.cs`, add `public const float DefaultRoughnessExtractStrength = 0f;` (0 = off, D-02) and `public const int DefaultRoughnessEstimator = 0;` (0 = fit-driven default, D-03).

    In `Editor/UI/NamerEditorWindow.cs`:
    - Add private fields `_roughnessExtractStrength` and `_roughnessEstimator`; snapshot them in `RebuildInspection` (`_roughnessExtractStrength = _settings.RoughnessExtractStrength;` / `_roughnessEstimator = _settings.RoughnessEstimator;`).
    - In `RecomputePreview`, copy them onto the inspection (`inspection.RoughnessExtractStrength = _roughnessExtractStrength;` / `inspection.RoughnessEstimator = _roughnessEstimator;`) alongside the AO controls.
    - In `DrawProcessingSection`, add (after the AO controls) a strength slider `EditorGUILayout.Slider(new GUIContent("Roughness Extract Strength", "..."), _roughnessExtractStrength, 0f, 1f)` and an estimator popup `EditorGUILayout.Popup(new GUIContent("Roughness Estimator", "..."), _roughnessEstimator, new[] { "Fit-driven", "Sobel" })`, each guarded by `if (!Mathf.Approximately(...))` / `if (newX != _roughnessEstimator)` and on change assign the local field + `_settings.X = newX` + `_afterPanelState.MarkTweaking()` + `MarkDirty()`.
  </action>
  <verify>
    <automated>Editor assembly compiles; window builds with the two new controls present (verified by the existing EditMode window smoke where applicable).</automated>
  </verify>
  <done>The strength slider (0 = off) and estimator popup (Fit-driven default / Sobel) are EditorPrefs-persisted, debounced, and drive the live preview recompute.</done>
  <acceptance_criteria>
    - `NamerProcessorSettings.cs` contains `RoughnessExtractStrengthKey` and `RoughnessEstimatorKey` + matching properties.
    - `NamerEditorConstants.cs` contains `DefaultRoughnessExtractStrength = 0f` and `DefaultRoughnessEstimator = 0`.
    - `NamerEditorWindow.cs` contains the `"Roughness Extract Strength"` slider and `"Roughness Estimator"` popup with the `MarkTweaking()` + `MarkDirty()` change pattern.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strength ladder -> GPU eval / refit | Untrusted texture dimensions + mesh counts cross into the per-iteration eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-04 | DoS | `NamerRoughnessFitter` NativeArray overrun in search buffers | low | mitigate | caller-owned buffers with explicit length checks; `Allocator.TempJob` intermediates disposed in `finally` (mirror `NamerAOBaker` allocation discipline) |
| T-04.1-05 | DoS | blur/sharp-removal kernels out-of-bounds taps | low | mitigate | clamped taps + `_Size` bound guard (copied from `CSBlurH`/`CSBlurV`) |
| T-04.1-06 | Information disclosure | none — local editor image processing | low | accept | no network/auth/session; source assets remain untouched (GEN-02 path unchanged) |

No package installs in this phase, so no package-legitimacy threat row.
</threat_model>

<verification>
- `GraffitiEntertainment.Namer.Tests.NamerAOExtractionTests` and `ResidualPipelineTests` still pass (non-extraction path regression — `RoughnessExtractStrength == 0` is byte-identical to today).
- The Editor assembly compiles with the new settings keys, enum, fitter, and UI controls.
- `NAMERRoughness.compute` imports with zero shader errors.
</verification>

<success_criteria>
- Fit-driven strength search deterministically minimizes post-refit residual MaxError and is cached off-debounce.
- The refit consumes the post-extraction cleaned base (D-05).
- Strength + estimator controls persist and drive the live preview (D-02/D-03).
</success_criteria>

<output>
Create `.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-02-SUMMARY.md` when done
</output>
