---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
plan: 03
type: execute
wave: 3
depends_on: [04.1-02]
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs
  - Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs
autonomous: true
requirements: [SHDR-02, ENCD-02, VCOL-03]

must_haves:
  truths:
    - "An extraction-processed asset whose refit collapses within threshold generates a material with _BaseResidualMap UNBOUND (white default) — one surface PNG + vertex-colored mesh, no residual EXR — while the Base PNG is still written to disk (D-06/D-14 switch-back)."
    - "The D-06 _RoughnessOffsetMap input is additive to decoded roughness and neutral-when-unset, so an unset input yields byte-identical decode to the pre-04.1 shader."
    - "An asset with genuine albedo detail that survives extraction still falls back to a bound residual (D-13 extended — the honest gate, never assumed)."
  artifacts:
    - "Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs (one-texture binding + _RoughnessOffsetMap binding)"
    - "Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl (_RoughnessOffsetMap + additive offset)"
    - "Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs"
  key_links:
    - "AssetGenerator.WriteMaterial baseResidualPath = decomp != null ? residualWritePath : null (write base, do not bind) <-> NamerSurface.hlsl albedo = 1.0 x _BaseColor x vertexColor."

# Artifacts this phase produces (plan 03 adds the one-texture binding + D-06 offset)
## Artifacts this phase produces
- `Editor/Generation/AssetGenerator.cs` — one-texture `_BaseResidualMap` unbound logic + `material.SetTexture("_RoughnessOffsetMap", ...)` (plan 03)
- `Shaders/NamerSurface.hlsl` — `TEXTURE2D(_RoughnessOffsetMap)` + `SAMPLER(sampler_RoughnessOffsetMap)` + `roughness = saturate(roughness + SAMPLE_TEXTURE2D(...).r)` (plan 03)
- `Tests/Editor/NamerOneTextureTests.cs` — class `NamerOneTextureTests` (plan 03)
---

<objective>
Make the zero-residual one-texture mode real on disk: when the honest gate drops the residual for an extraction-processed asset, the generated material leaves `_BaseResidualMap` unbound (one surface PNG + vertex-colored mesh) while still writing the Base PNG for switch-back. Add the D-06 optional `_RoughnessOffsetMap` roughness-offset input to the runtime shader, neutral-when-unset.

Purpose: deliver the phase's headline outcome (Neo → one texture, no residual file) and the escape hatch for per-texel detail that 6-bit extraction underfits, without changing the format contract or the happy-path decode.

Output: the one-texture material binding, the D-06 shader input + its material binding, and the acceptance tests (one-texture Neo path, neutral-unset offset, honest-gate fallback, switch-back Base PNG).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-CONTEXT.md
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-RESEARCH.md
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-PATTERNS.md
@Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs
@Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl
@Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs
@Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs
</context>

<decisions>
Plan-review assumption resolved here (reversible, no checkpoint):

- **A2 (confirmed)**: the normalized Base PNG is still WRITTEN in one-texture mode but NOT bound to `_BaseResidualMap` (switch-back representation per Phase-4 D-06/D-14). `WriteBaseTexture` stays unconditional; only the `WriteMaterial` `baseResidualPath` argument drops to `null`.
- **A3 (confirmed)**: the D-06 `_RoughnessOffsetMap` offset is ADDITIVE to decoded roughness, neutral default 0, so an unset input yields byte-identical decode.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: One-texture material binding + D-06 roughness-offset shader input</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs, Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs (Generate lines 61-218, WriteMaterial lines 549-613, WriteBaseTexture lines 410-430)
    - Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl (CBUFFER + TEXTURE2D lines 21-34, NAMER_DECODE_SURFACE lines 63-74, InitializeNamerSurfaceData lines 78-95)
  </read_first>
  <action>
    In `Editor/Generation/AssetGenerator.cs` `Generate`, change the D-07 binding line (currently `string baseResidualPath = decomp != null ? residualWritePath : basePath;`) to `string baseResidualPath = decomp != null ? residualWritePath : null;` so a decomposed, residual-dropped asset leaves `_BaseResidualMap` unbound (white `{}` default → `albedo = 1.0 x _BaseColor x vertexColor`). Keep `WriteBaseTexture(baseTex, basePath, ...)` UNCONDITIONAL (A2 — the Base PNG is still written for switch-back). Add the D-06 binding in `WriteMaterial`: read an optional offset texture path from the inspection (a new nullable `NamerMaterialInspection` field, e.g. `public Texture2D RoughnessOffsetMap;`), and when non-null `material.SetTexture("_RoughnessOffsetMap", AssetDatabase.LoadAssetAtPath<Texture2D>(offsetPath));` — else leave it unset so the shader's white default keeps the offset at 0. (Do NOT invent a settings field for the offset path; the escape hatch is an optional material-level input wired through the inspection.)

    In `Shaders/NamerSurface.hlsl`:
    - Add `TEXTURE2D(_RoughnessOffsetMap); SAMPLER(sampler_RoughnessOffsetMap);` alongside `_SurfaceMap`/`_BaseResidualMap` (SRP-batcher-stable, no ifdef).
    - In `NAMER_DECODE_SURFACE`, immediately after `roughness = (float)(a & 0x3Fu) / 63.0;` and before `smoothness = 1.0 - roughness;`, insert `roughness = saturate(roughness + SAMPLE_TEXTURE2D(_RoughnessOffsetMap, sampler_RoughnessOffsetMap, uv).r);` — but `NAMER_DECODE_SURFACE` currently has no `uv` parameter, so extend its signature to accept `float2 uv` and update the single call site in `InitializeNamerSurfaceData` to pass `uv`. The unbound `_RoughnessOffsetMap` samples the shader's `{}` white default → `.r == 0` → `roughness` unchanged → byte-identical decode (A3).
  </action>
  <verify>
    <automated>Shader compiles (NAMER shader imports clean); Editor assembly compiles; `material.SetTexture("_RoughnessOffsetMap", ...)` guarded on a non-null inspection field.</automated>
  </verify>
  <done>One-texture assets bind no `_BaseResidualMap` while still writing the Base PNG; the D-06 `_RoughnessOffsetMap` offset is additive and neutral-when-unset.</done>
  <acceptance_criteria>
    - `AssetGenerator.cs` sets `baseResidualPath = decomp != null ? residualWritePath : null` and keeps `WriteBaseTexture` unconditional.
    - `WriteMaterial` calls `material.SetTexture("_RoughnessOffsetMap", ...)` only when a non-null offset path is present.
    - `NamerSurface.hlsl` declares `_RoughnessOffsetMap` + `sampler_RoughnessOffsetMap` and applies `roughness = saturate(roughness + SAMPLE_TEXTURE2D(...).r)` before `smoothness = 1.0 - roughness`.
    - `NAMER_DECODE_SURFACE` gains a `float2 uv` parameter and its call site passes `uv`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: One-texture + D-06 + honest-gate acceptance tests</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs (D-13 gate test shape, CreateSplitQuad/ConstantColors/CreateBase helpers)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs (integration + material-binding assertion patterns)
    - Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs (NamerGeneratedAsset / NamerDecompData shape)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (Process returns NamerProcessResult)
  </read_first>
  <action>
    Create `Tests/Editor/NamerOneTextureTests.cs` in namespace `GraffitiEntertainment.Namer.Tests`, capability-gated with `SystemInfo.supportsComputeShaders && SystemInfo.supportsAsyncGPUReadback` + `Assert.Ignore`. Add three `[UnityTest]` cases:

    1. `OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound` (Neo acceptance): drive a decomposed Process on a flat/low-frequency base whose fit collapses within threshold (`NamerDecompErrorStats.ResidualRequired == false`), then assert the generated material has no `_BaseResidualMap` bound (read `AssetDatabase.LoadAssetAtPath<Material>(asset.MaterialPath)` and assert `material.GetTexture("_BaseResidualMap") == null`) while `asset.BaseTexturePath` points at a written Base PNG and `asset.ResidualTexturePath` is empty.
    2. `RoughnessOffset_Unset_DecodesIdentical`: confirm the D-06 offset is neutral — a material with no `_RoughnessOffsetMap` bound decodes roughness identically to the pre-04.1 formula (assert via the shader's white default producing `.r == 0`; a direct shader-level assertion is acceptable: `NAMER_DECODE_SURFACE` with the offset unset yields the same `roughness` as `(a & 0x3F)/63`).
    3. `HonestGate_AlbedoDetail_StillRequiresResidual` (D-13 extended): a high-frequency base (gradient/noise that survives extraction) still reports `ResidualRequired == true` with a non-null residual, so one-texture is never assumed.

    Reuse the `ResidualPipelineTests` helper shapes (`CreateSplitQuad`, `ConstantColors`, `CreateGradientBase`/`UploadBase`, `ReadBackColor32`) and the capability-gate/`Assert.Ignore` preamble.
  </action>
  <verify>
    <automated>Unity batchmode: -runTests -testPlatform EditMode -testFilter GraffitiEntertainment.Namer.Tests.NamerOneTextureTests (results XML under /tmp). When the live editor holds the project lock, the orchestrator runs the same filter in-editor via TestRunnerApi.</automated>
  </verify>
  <done>All three one-texture/D-06/honest-gate tests pass, proving the Neo outcome, neutral-unset offset, and fallback behavior.</done>
  <acceptance_criteria>
    - `NamerOneTextureTests.cs` defines `OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound`, `RoughnessOffset_Unset_DecodesIdentical`, `HonestGate_AlbedoDetail_StillRequiresResidual`.
    - Test 1 asserts `GetTexture("_BaseResidualMap") == null`, `BaseTexturePath` non-empty, `ResidualTexturePath` empty.
    - Test 3 asserts `ResidualRequired == true` with a non-null residual for a high-frequency base.
    - All tests capability-gated with `Assert.Ignore`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated-asset paths -> disk write | Untrusted destination/offset-texture paths cross into the sole disk writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-07 | Tampering | `AssetGenerator` path escape via offset-texture or generated-asset paths | medium | mitigate | reuse `ValidateComposedPath`/`ValidateDestinationFolder` (already enforced on every write); the offset texture is read via `AssetDatabase.LoadAssetAtPath`, never a raw filesystem write |
| T-04.1-08 | DoS | none — shader offset is a single additive sample | low | accept | no new compute surface |

No package installs in this phase, so no package-legitimacy threat row.
</threat_model>

<verification>
- `GraffitiEntertainment.Namer.Tests.NamerOneTextureTests` passes (3/3).
- The NAMER shader imports clean with the `_RoughnessOffsetMap` addition.
- Existing `NamerReprocessTests`/`AssetGeneratorTests` still pass (one-texture binding does not regress the non-decomposed Phase-3 shape where `decomp == null` still binds `basePath`).
</verification>

<success_criteria>
- Extraction-processed assets with a collapsing fit produce one surface PNG + vertex-colored mesh, no residual EXR, no `_BaseResidualMap` binding, while the Base PNG is still written.
- The D-06 `_RoughnessOffsetMap` offset is additive and neutral-when-unset (byte-identical decode).
- Genuine-albedo-detail assets still fall back to a bound residual (honest gate).
</success_criteria>

<output>
Create `.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-03-SUMMARY.md` when done
</output>
