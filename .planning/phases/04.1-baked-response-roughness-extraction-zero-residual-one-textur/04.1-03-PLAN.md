---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
plan: 03
type: execute
wave: 3
depends_on: [04.1-02]
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs
  - Packages/com.graffitientertainment.namer/Shaders/NAMER.shader
  - Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs
autonomous: true
requirements: [SHDR-02, ENCD-02, VCOL-03]

must_haves:
  truths:
    - "An extraction-processed asset whose refit collapses within threshold generates a material with _BaseResidualMap UNBOUND (white default) — one surface PNG + vertex-colored mesh, no residual EXR — while the Base PNG is still written to disk (D-06/D-14 switch-back)."
    - "The D-06 _RoughnessOffsetMap input is additive to decoded roughness and neutral-when-unset (its shader Properties default is \"black\" {}, so an unbound texture samples .r == 0), so an unset input yields byte-identical decode to the pre-04.1 shader."
    - "An asset with genuine albedo detail that survives extraction still falls back to a bound residual (D-13 extended — the honest gate, never assumed)."
  artifacts:
    - "Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs (D-06 _RoughnessOffsetMap binding)"
    - "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs (RoughnessOffsetMap field)"
    - "Packages/com.graffitientertainment.namer/Shaders/NAMER.shader (_RoughnessOffsetMap \"black\" {} Properties entry)"
    - "Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl (_RoughnessOffsetMap + additive offset)"
    - "Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs"
  key_links:
    - "AssetGenerator.WriteMaterial leaves _BaseResidualMap unbound via the EXISTING D-13 drop (residualWritePath == null) — no baseResidualPath edit — while WriteMaterial sets _RoughnessOffsetMap <-> NamerSurface.hlsl albedo = 1.0 x _BaseColor x vertexColor."

# Artifacts this phase produces (plan 03 adds the D-06 offset input + binding; one-texture unbinding is already delivered by the Phase-4 D-13 drop)
## Artifacts this phase produces
- `Editor/Generation/AssetGenerator.cs` — `material.SetTexture("_RoughnessOffsetMap", inspection.RoughnessOffsetMap)` when non-null (plan 03)
- `Editor/Pipeline/NamerSourceModel.cs` — `public Texture2D RoughnessOffsetMap;` nullable field (plan 03)
- `Shaders/NAMER.shader` — `[NoScaleOffset] _RoughnessOffsetMap("Roughness Offset", 2D) = "black" {}` Properties entry (plan 03)
- `Shaders/NamerSurface.hlsl` — `TEXTURE2D(_RoughnessOffsetMap)` + `SAMPLER(sampler_RoughnessOffsetMap)` + `roughness = saturate(roughness + SAMPLE_TEXTURE2D(...).r)` in `InitializeNamerSurfaceData` (plan 03)
- `Tests/Editor/NamerOneTextureTests.cs` — class `NamerOneTextureTests` (plan 03)
---

<objective>
Ship the D-06 optional `_RoughnessOffsetMap` roughness-offset input end-to-end (NAMER.shader Properties entry, `NamerSurface.hlsl` decode offset, `NamerMaterialInspection` field, and `WriteMaterial` binding), neutral-when-unset so the happy-path decode is byte-identical, and prove with acceptance tests that the zero-residual one-texture outcome — already delivered by the Phase-4 D-13 residual drop (an extraction-processed asset whose refit collapses binds no `_BaseResidualMap`) — is real on disk.

Purpose: deliver the phase's headline outcome (Neo → one texture, no residual file) and the escape hatch for per-texel detail that 6-bit extraction underfits, without changing the format contract or the happy-path decode.

Output: the D-06 shader input + its material binding + the `NamerSourceModel` field, and the acceptance tests (one-texture Neo path, neutral-unset offset, honest-gate fallback, switch-back Base PNG). No `baseResidualPath` edit is made — the one-texture unbinding already exists via `residualWritePath == null`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-CONTEXT.md
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-RESEARCH.md
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-PATTERNS.md
@Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs
@Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl
@Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs
@Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs
</context>

<decisions>
Plan-review assumption resolved here (reversible, no checkpoint):

- **A2 (confirmed)**: the normalized Base PNG is still WRITTEN in one-texture mode but NOT bound to `_BaseResidualMap` (switch-back representation per Phase-4 D-06/D-14). This is ALREADY delivered by the existing D-13 drop: `baseResidualPath = decomp != null ? residualWritePath : basePath` yields `null` when `residualWritePath == null` (residual auto-dropped). `WriteBaseTexture` stays unconditional; **no `baseResidualPath` edit is made** — editing the `decomp == null` branch to `null` would regress non-decomposed Phase-3 assets that must still bind the Base PNG.
- **A3 (confirmed)**: the D-06 `_RoughnessOffsetMap` offset is ADDITIVE to decoded roughness, neutral default 0, so an unset input yields byte-identical decode. The shader Properties default is `"black" {}` (`.r == 0`), NOT `"white" {}` — a white default would sample `.r == 1` and saturate every unset material's roughness to 1.0 (non-neutral).
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: D-06 roughness-offset shader input + material binding</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs, Packages/com.graffitientertainment.namer/Shaders/NAMER.shader, Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs (Generate lines 158-199, WriteMaterial lines 549-613)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs (NamerMaterialInspection field block lines 34-81)
    - Packages/com.graffitientertainment.namer/Shaders/NAMER.shader (Properties block lines 3-36)
    - Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl (CBUFFER + TEXTURE2D lines 21-34, InitializeNamerSurfaceData lines 78-110)
  </read_first>
  <action>
    Add the D-06 optional roughness-offset input end-to-end. Do NOT edit the existing D-13/D-07 binding line `string baseResidualPath = decomp != null ? residualWritePath : basePath;` — the one-texture unbinding is ALREADY delivered by `residualWritePath == null` when the residual auto-drops (a decomposed, residual-dropped asset already leaves `_BaseResidualMap` unbound), and changing the `decomp == null` branch to `null` would regress non-decomposed Phase-3 assets that must still bind the Base PNG.

    - In `Shaders/NAMER.shader` Properties (after `_BaseResidualMap`), add `[NoScaleOffset] _RoughnessOffsetMap("Roughness Offset", 2D) = "black" {}`. The default MUST be `"black" {}` (`.r == 0`), NOT `"white" {}` — a white default samples `.r == 1`, which would add 1.0 and saturate every unset material's roughness to 1.0 (non-neutral).
    - In `Shaders/NamerSurface.hlsl`, add `TEXTURE2D(_RoughnessOffsetMap); SAMPLER(sampler_RoughnessOffsetMap);` alongside `_SurfaceMap`/`_BaseResidualMap` (SRP-batcher-stable, no ifdef). In `InitializeNamerSurfaceData`, immediately AFTER the `NAMER_DECODE_SURFACE(...)` call and BEFORE the `surfaceData.smoothness = smoothness;` assignment, insert `roughness = saturate(roughness + SAMPLE_TEXTURE2D(_RoughnessOffsetMap, sampler_RoughnessOffsetMap, uv).r); smoothness = 1.0 - roughness;`. Apply the offset here (not inside `NAMER_DECODE_SURFACE`) so the function's signature is unchanged and the Meta-pass call site in `NAMER.shader` (which only reads `emissive`) is untouched. The unbound `_RoughnessOffsetMap` samples the `"black" {}` default → `.r == 0` → `roughness` unchanged → byte-identical decode (A3).
    - In `Editor/Pipeline/NamerSourceModel.cs`, add a nullable field to `NamerMaterialInspection`: `public Texture2D RoughnessOffsetMap;` (null default = no offset). Do NOT invent a settings key — the escape hatch is an optional material-level input wired through the inspection.
    - In `Editor/Generation/AssetGenerator.cs` `WriteMaterial` (after the `_BaseResidualMap` binding), add `if (inspection.RoughnessOffsetMap != null) { material.SetTexture("_RoughnessOffsetMap", inspection.RoughnessOffsetMap); }`. The field is a direct `Texture2D` reference (no `AssetDatabase.LoadAssetAtPath` — the offset is a user-assigned texture, not a generated path). Intended user access path (D-06 escape hatch): manual assignment on the GENERATED material — the `_RoughnessOffsetMap` Properties entry surfaces a "Roughness Offset" texture slot in the Unity material inspector, so the artist drags an offset texture there post-generation (no settings key, no UI control — it is an optional per-material input per D-06); the `inspection.RoughnessOffsetMap` field + guarded `SetTexture` additionally rebind that slot when a SourceInspector mapping populates the field on re-process. The neutral `"black" {}` default means an unassigned slot decodes identically.
  </action>
  <verify>
    <automated>NAMER shader imports clean (the `_RoughnessOffsetMap` Properties entry + hlsl offset compile); Editor assembly compiles with the `RoughnessOffsetMap` field and the guarded `material.SetTexture("_RoughnessOffsetMap", ...)`.</automated>
  </verify>
  <done>The D-06 `_RoughnessOffsetMap` input is additive and neutral-when-unset (`"black" {}` default), bound only when `inspection.RoughnessOffsetMap != null`; the one-texture `_BaseResidualMap` unbinding needs no code change (existing D-13 drop).</done>
  <acceptance_criteria>
    - `NAMER.shader` Properties declares `[NoScaleOffset] _RoughnessOffsetMap("Roughness Offset", 2D) = "black" {}`.
    - `NamerSurface.hlsl` declares `_RoughnessOffsetMap` + `sampler_RoughnessOffsetMap` and applies `roughness = saturate(roughness + SAMPLE_TEXTURE2D(...).r); smoothness = 1.0 - roughness;` inside `InitializeNamerSurfaceData` (after `NAMER_DECODE_SURFACE`), with `NAMER_DECODE_SURFACE`'s signature unchanged.
    - `NamerSourceModel.cs` `NamerMaterialInspection` gains `public Texture2D RoughnessOffsetMap;`.
    - `WriteMaterial` calls `material.SetTexture("_RoughnessOffsetMap", inspection.RoughnessOffsetMap)` only when the field is non-null.
    - The `baseResidualPath` line in `Generate` is NOT edited (no regression to the `decomp == null` Phase-3 path).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: One-texture + D-06 + honest-gate acceptance tests</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs (D-13 gate test shape, CreateSplitQuad/ConstantColors/CreateBase helpers)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs (BuildInspection fixture with MetallicGlossMap/RoughnessExtractStrength/RoughnessEstimator fields)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs (integration + material-binding assertion patterns)
    - Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs (NamerGeneratedAsset / NamerDecompData shape)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (Process returns NamerProcessResult)
  </read_first>
  <action>
    Create `Tests/Editor/NamerOneTextureTests.cs` in namespace `GraffitiEntertainment.Namer.Tests`, capability-gated with `SystemInfo.supportsComputeShaders && SystemInfo.supportsAsyncGPUReadback` + `Assert.Ignore`. Add four `[UnityTest]` cases:

    1. `OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound` (Neo acceptance, extraction ENABLED): drive a decomposed Process on a baked-response fixture — a low-frequency base with baked gloss and NO authored `MetallicGlossMap`, `RoughnessExtractStrength = 1f`, `RoughnessEstimator = FitDriven` — whose fit-driven extraction collapses the post-refit residual within threshold (`NamerDecompErrorStats.ResidualRequired == false`), then assert the generated material has no `_BaseResidualMap` bound (read `AssetDatabase.LoadAssetAtPath<Material>(asset.MaterialPath)` and assert `material.GetTexture("_BaseResidualMap") == null`) while `asset.BaseTexturePath` points at a written Base PNG and `asset.ResidualTexturePath` is empty.
    2. `RoughnessOffset_Unset_DecodesIdentical`: confirm the D-06 offset is neutral — a material with no `_RoughnessOffsetMap` bound decodes roughness identically to the pre-04.1 formula (assert via the shader's `"black" {}` default producing `.r == 0`; a direct shader-level assertion is acceptable: the offset sampled with `.r == 0` yields the same `roughness` as `(a & 0x3F)/63`).
    3. `HonestGate_AlbedoDetail_StillRequiresResidual` (D-13 extended): a high-frequency base (gradient/noise that survives extraction) still reports `ResidualRequired == true` with a non-null residual, so one-texture is never assumed.
    4. `RoughnessOffset_Set_BindsMaterialTexture` (positive D-06 binding): set `inspection.RoughnessOffsetMap` to a generated 1x1 `Texture2D` on a baked-response fixture and drive generation; assert the generated material's `GetTexture("_RoughnessOffsetMap") != null` and equals the assigned texture (a control run with the field null leaves `GetTexture("_RoughnessOffsetMap") == null`).

    Reuse the `ResidualPipelineTests` helper shapes (`CreateSplitQuad`, `ConstantColors`, `CreateGradientBase`/`UploadBase`, `ReadBackColor32`) and the `BuildInspection`/capability-gate/`Assert.Ignore` preamble from `NamerRoughnessExtractionTests`.
  </action>
  <verify>
    <automated>Unity batchmode: -runTests -testPlatform EditMode -testFilter GraffitiEntertainment.Namer.Tests.NamerOneTextureTests (results XML under /tmp). When the live editor holds the project lock, the orchestrator runs the same filter in-editor via TestRunnerApi.</automated>
  </verify>
  <done>All four one-texture/D-06/honest-gate tests pass, proving the Neo outcome (with extraction enabled), neutral-unset offset, the positive offset-binding path, and fallback behavior.</done>
  <acceptance_criteria>
    - `NamerOneTextureTests.cs` defines `OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound`, `RoughnessOffset_Unset_DecodesIdentical`, `HonestGate_AlbedoDetail_StillRequiresResidual`, `RoughnessOffset_Set_BindsMaterialTexture`.
    - Test 1 uses a baked-response fixture with extraction enabled (`MetallicGlossMap == null`, `RoughnessExtractStrength = 1f`, `RoughnessEstimator = FitDriven`) and asserts `GetTexture("_BaseResidualMap") == null`, `BaseTexturePath` non-empty, `ResidualTexturePath` empty.
    - Test 2 asserts the `"black" {}` default yields `.r == 0` (neutral offset).
    - Test 3 asserts `ResidualRequired == true` with a non-null residual for a high-frequency base.
    - Test 4 sets `inspection.RoughnessOffsetMap` to a non-null `Texture2D` and asserts the generated material's `GetTexture("_RoughnessOffsetMap") != null` (equals the assigned texture); the null-field control run leaves `GetTexture("_RoughnessOffsetMap") == null`.
    - All tests capability-gated with `Assert.Ignore`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated-asset paths -> disk write | Untrusted destination/offset-texture paths cross into the sole disk writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-07 | Tampering | `AssetGenerator` path escape via offset-texture or generated-asset paths | medium | mitigate | reuse `ValidateComposedPath`/`ValidateDestinationFolder` (already enforced on every write); the offset texture is a direct `Texture2D` reference assigned by the user (no `AssetDatabase.LoadAssetAtPath` — the offset is a user-assigned texture, not a generated path), never a raw filesystem write |
| T-04.1-08 | DoS | none — shader offset is a single additive sample | low | accept | no new compute surface |

No package installs in this phase, so no package-legitimacy threat row.
</threat_model>

<verification>
- `GraffitiEntertainment.Namer.Tests.NamerOneTextureTests` passes (4/4).
- The NAMER shader imports clean with the `_RoughnessOffsetMap` Properties entry + hlsl offset addition.
- Existing `NamerReprocessTests`/`AssetGeneratorTests` still pass — the `baseResidualPath` line is untouched, so the non-decomposed Phase-3 shape (`decomp == null` still binds `basePath`) and the D-13 drop (`decomp != null`, residual dropped → unbound) both behave exactly as before.
</verification>

<success_criteria>
- Extraction-processed assets with a collapsing fit produce one surface PNG + vertex-colored mesh, no residual EXR, no `_BaseResidualMap` binding, while the Base PNG is still written.
- The D-06 `_RoughnessOffsetMap` offset is additive and neutral-when-unset (shader `"black" {}` default → `.r == 0` → byte-identical decode).
- Genuine-albedo-detail assets still fall back to a bound residual (honest gate).
</success_criteria>

<output>
Create `.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-03-SUMMARY.md` when done
</output>
