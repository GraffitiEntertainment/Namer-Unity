---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
plan: 05
subsystem: testing
tags: [unity, csharp, test-fixtures, roughness-fit, residual-collapse, asset-serialization]

requires:
  - phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
    provides: plans 01-04 (Sobel estimator, NamerRoughnessFitter, D-13 residual gate, one-texture binding, production gap closure)

provides:
  - minimizer fixture first-passing semantics at the shipped 0.02 threshold (0.3, not argmin 0.5)
  - collapse fixtures that genuinely collapse the residual at a fixture-local 0.04 threshold with doubled gloss amplitude
  - persisted offset-texture fixture so the D-06 _RoughnessOffsetMap binding survives .mat serialization

affects:
  - verify-phase (human-check: preview roughness + Neo scene render)

tech-stack:
  added: []
  patterns:
    - "test-local CollapseErrorThreshold gives headroom over the 64px clamped-edge/quantization floor when the shipped 0.02 is numerically unachievable"
    - "persist user-assigned texture assets before material serialization so CreateAsset does not drop the reference"

key-files:
  created: []
  modified:
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs

key-decisions:
  - "Minimizer fixture rescaled (not a local threshold) so the shipped ErrorThreshold=0.02 stays meaningful and plan 04.1-02 Task 5 item 1 holds exactly"
  - "Collapse fixtures use fixture-local CollapseErrorThreshold=0.04 + BakedResponse gloss amplitude 0.10 (test-only); production D-13 gate / NamerRoughnessFitter / MinBlurRadius untouched"
  - "Offset fixture persists the roughness-offset texture as an imported asset (mirroring CreateImportedBaseMap), matching D-06 product intent of a user-assigned project texture"

patterns-established:
  - "MaxError is threshold-independent (the threshold only drives the D-13 ResidualRequired gate / resolution search), so a fit evaluate callback can keep the shipped threshold while the fit uses a fixture-local threshold"

requirements-completed: [ENCD-02, VCOL-04, VCOL-05, UI-03]

# Metrics
duration: 2min
completed: 2026-09-14
---

# Phase 04.1 Plan 05: Test-Fixture Gap Closure Summary

**Test-side closure of UAT gap 1: rescaled the strength-search minimizer to first-pass at 0.3 under the shipped 0.02 threshold, gave the three fit-driven collapse fixtures a 0.04 fixture-local threshold plus doubled gloss amplitude, and persisted the D-06 roughness-offset texture so `_RoughnessOffsetMap` survives material serialization — no production logic touched.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-09-14T23:46:42Z
- **Completed:** 2026-09-14T23:48:35Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- The pure-CPU minimizer fixture now proves first-passing semantics at the SHIPPED `ErrorThreshold = 0.02f`: errors above threshold for strengths 0.0/0.15 and below it from 0.3 onward, so `NamerRoughnessFitter.Fit` returns `Strength == 0.3` (first-pass), NOT the global argmin 0.5 — exactly matching plan 04.1-02 Task 5 item 1.
- The three fit-driven collapse tests now genuinely collapse the residual: a fixture-local `CollapseErrorThreshold = 0.04f` supplies headroom over the 64x64 clamped-edge blur bias + quantization floor (~0.018-0.021), and the `BakedResponse` gloss amplitude is doubled to 0.10 so the first-passing strength lands non-trivial and packed roughness quantizes above 0.
- The D-06 positive-binding test now assigns a PERSISTED offset texture asset (mirroring `CreateImportedBaseMap`), so `AssetDatabase.CreateAsset` serializes the `_RoughnessOffsetMap` reference instead of dropping it to `{fileID: 0}`; the null-field control run stays unbound.

## Task Commits

Each task was committed atomically:

1. **Task 1: Rescale minimizer fixture** - `03e7b81` (test)
2. **Task 2: Collapse fixtures at fixture-local threshold** - `3658783` (test)
3. **Task 3: Persist offset texture** - `c938c02` (test)

**Plan metadata:** committed separately (docs) after SUMMARY + state updates.

## Files Created/Modified

- `Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs` - rescaled `maxErrorByStrength` dictionary; added `CollapseErrorThreshold = 0.04f`; routed the two collapse-test fit/D-13 thresholds through it; raised `BakedResponse` gloss amplitude 0.05 -> 0.10; added the MaxError-is-threshold-independent comment in `ComposeEvaluate`
- `Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs` - added `CollapseErrorThreshold = 0.04f` and the `CreateImportedOffsetMap` helper; routed the one-texture collapse threshold through it; raised `BakedResponse` gloss amplitude; replaced the in-memory offset texture with the persisted asset and dropped it from the `finally` destroy

## Decisions Made

- Minimizer fixture rescaled rather than switching to a local threshold — the rescaled dictionary keeps the shipped `0.02f` threshold meaningful and matches plan 04.1-02 Task 5 item 1 verbatim.
- Collapse fixtures keep the D-13 mechanism meaningful via a fixture-local threshold (`0.04f`) with headroom over the simulated 64px floor, plus doubled gloss amplitude (`0.10f`) so extraction genuinely causes the collapse rather than strength 0.0.
- Offset fixture persists the roughness-offset texture as a project asset, matching D-06 product intent (a user-assigned project texture on the generated material) — so production `WriteMaterial` stays as-is.

## Deviations from Plan

None - plan executed exactly as written. All three tasks applied the plan's `<action>` blocks verbatim (rescaled dictionary, `CollapseErrorThreshold = 0.04f`, gloss amplitude `0.10f`, persisted offset helper). No auto-fix deviations were required.

## Issues Encountered

Automated headless verification was not run: the user's interactive Unity editor (PID 46157) holds the project lock (`Temp/UnityLockfile` present), so a second Unity instance cannot be launched, and this executor has no unity-mcp access for an in-editor TestRunnerApi run. The two numeric knobs in Task 2 (`CollapseErrorThreshold` in [0.04, 0.06] and gloss amplitude in [0.05, 0.15]) were applied at the plan's starting values (0.04 / 0.10); the plan's tune-only-if-needed step (first-passing strength outside [0.3, 0.9] or packed roughness quantizing to 0) is deferred to the live run.

## Verification (pending — orchestrator runs in the live editor)

Static verification completed (fixture math recomputed against `NamerRoughnessFitter.Fit` semantics):

- **Task 1:** `0.0 -> 0.50`, `0.15 -> 0.20`, `0.3 -> 0.015`, `0.5 -> 0.005`, `0.7 -> 0.008`, `0.9 -> 0.012`, `1.0 -> 0.018`. Walk stops at 0.3 (`0.015 <= 0.02`), `visited == [0.0, 0.15, 0.3]` (count 3), `Strength == 0.3`, `Passed == true`. U-shape minimum remains at 0.5. The always-above-threshold second assertion is unchanged.
- **Task 2:** `NamerDecompPipeline.MaxError` reads the `_ErrorStat` max channel directly (threshold-independent); the threshold only gates `ResidualRequired` (D-13) and the resolution search. The honest-gate test keeps the shipped `0.02f`.
- **Task 3:** `WriteMaterial` binds `inspection.RoughnessOffsetMap` (verified); the new persisted asset now survives `CreateAsset` serialization.

The following remain to be run in the live editor (close the editor first, or use unity-mcp TestRunnerApi):

```
/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testFilter "GraffitiEntertainment.Namer.Tests.NamerRoughnessExtractionTests;GraffitiEntertainment.Namer.Tests.NamerRoughnessFitTests;GraffitiEntertainment.Namer.Tests.NamerEditorWindowSmokeTests;GraffitiEntertainment.Namer.Tests.NamerOneTextureTests" -testResults /tmp/uat-041-final-results.xml -logFile /tmp/uat-041-final-run.log
```

Expected: 13/13 pass (3 extraction + 3 fit + 2 smoke + 4 one-texture + the second pure-CPU assertion block inside `FitDriven_SelectsFirstPassingStrength`; SourceInspector suite is 12/12 from wave 1 and is outside this plan's four-suite gate).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- This plan closes the five test-side failures from UAT gap 1. Remaining for the verify phase: the orchestrator's live-editor run of the four suites (13/13 expected) and the human-check items (live preview roughness applied; Neo scene instance renders after Process).
- Production is untouched by this plan: `NamerRoughnessFitter`, the D-13 gate, roughness pack/decode, `MinBlurRadius`, and the D-06 `_RoughnessOffsetMap` binding all remain as wave-1 delivered them.

---
*Phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur*
*Completed: 2026-09-14*

## Self-Check: PASSED
- SUMMARY.md exists at `.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-05-SUMMARY.md`
- Task commits verified present in `git log --all`: `03e7b81`, `3658783`, `c938c02`
- Both modified test files exist on disk
- No `ProjectSettings/` or `Assets/NAMERGenerated/` churn (no headless Unity run; editor lockfile respected; user live-session artifacts left uncommitted)
