---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
plan: 06
type: execute
wave: 6
depends_on: [04.1-04]
files_modified:
  - Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs
autonomous: true
gap_closure: true
requirements: [ENCD-02, NORM-01, VCOL-04, UI-03]

must_haves:
  truths:
    - "The fit-driven roughness TEXTURE is the Blender-parity Sobel edge signal (global-max normalized, CSRoughnessNormalize) adopted by the searched strength: lerp(scalarRoughness, sobel, fit.Strength). At ladder step 0 this is exactly the scalar (identity); at 1 it fully adopts the Sobel look the user prefers. The high-pass-detail remap (saturate(strength * length(sharpDetail))) is retired from the fit path."
    - "The fit mechanism itself is UNCHANGED: the ladder search still evaluates refits on the sharp-removal cleaned base (base - strength * sharpDetail) and gates on FitOnlyMaxError, so every existing collapse / D-05 / default-on test must keep passing without modification."
    - "The fit path only runs when MetallicGlossMap == null, so the scalar the remap lerps from is inspection.Roughness — identical to CSNormalize's no-map branch (roughness = _Roughness); no smoothness-map mirroring is needed."
    - "The pack keeps full adoption of the fit-driven texture (the pack-blend fix): the adopted-strength Sobel texture is FINAL, and the Extracted Roughness debug channel shows exactly what is packed."
    - "A new test proves the signal swap directly: fit-driven extracted texels equal lerp(scalar, independently-produced standalone-Sobel texels, TryGetFitStrength) within one quantization step — RED against the old high-pass remap, GREEN after."
  artifacts:
    - "NAMERRoughness.compute: CSRoughnessRemap repurposed to the Sobel-adoption lerp (new _SobelRoughness texture + _ScalarRoughness float bindings; _SharpDetail input dropped)"
    - "NamerRoughnessPipeline.cs: RunFrequencySeparation produces roughness via ExtractSobel + the adoption kernel; scalar threaded from ExtractFitDriven (inspection.Roughness); RunSharpRemoval evaluate path passes an unused-scalar constant"
    - "NamerEditorWindow.cs: estimator tooltip states fit-driven searches strength over the Sobel edge signal"
    - "NamerRoughnessFitTests.cs: FitDriven_Roughness_UsesSobelEdgeSignal (RED->GREEN)"

context:
  why: "Live UAT third round (2026-09-15): the user rejected the fit-driven roughness LOOK (high-pass detail remap reads 'very patchy') while preferring the Sobel edge-normalized look, and separately confirmed the Sobel path can never drop the residual (no base cleaning). User decision recorded via AskUserQuestion: 'Sobel + fit search' — the strength search should drive the Sobel signal so the preferred look and the one-texture collapse come from the same path."
  design_note: "The removed-from-base detail (frequency separation) and the roughness signal (Sobel adoption) are intentionally DIFFERENT views of the same baked response: cleaning is the fit's business (make the base fittable), roughness is the render's business (Blender-parity edge response). They are coupled only through the single searched strength."

tasks:
  - id: 06-01
    title: "RED: FitDriven_Roughness_UsesSobelEdgeSignal"
    steps:
      - "Add the test to NamerRoughnessFitTests: run the fit-driven pipeline on the BakedResponse fixture; read TryGetFitStrength; produce the standalone Sobel texture via a second pipeline run (Estimator=Sobel, no evaluate); assert fit-driven extracted == lerp(scalar=1, sobel, strength) within 1/255."
      - "Run it in the live editor — must FAIL against the high-pass remap."
  - id: 06-02
    title: "Swap the fit-driven roughness signal to Sobel adoption"
    steps:
      - "NAMERRoughness.compute: repurpose CSRoughnessRemap to lerp(_ScalarRoughness, _SobelRoughness[id].r, _Strength); declare _SobelRoughness/_ScalarRoughness; update the block header comment."
      - "NamerRoughnessPipeline.RunFrequencySeparation: keep blur+sharpDetail+SharpRemoval (cleanedBase unchanged); roughness = ExtractSobel(baseColorOut) then the adoption dispatch; release the sobel lease in finally; thread scalar from ExtractFitDriven (inspection.Roughness)."
      - "RunSharpRemoval (evaluate path): pass scalar 1f with a comment that the roughness tail is unused there."
      - "Run the test — must PASS."
  - id: 06-03
    title: "Full regression + UI truth"
    steps:
      - "Estimator tooltip: fit-driven searches the strength over the Blender-parity Sobel edge signal."
      - "Full EditMode regression in the live editor — all suites green including the untouched collapse tests."
      - "Commit; update UAT.md (round 3) + ROADMAP (wave 6) + PROJECT.md current-state."
