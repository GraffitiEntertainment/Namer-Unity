---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
plan: 07
type: execute
wave: 7
depends_on: [04.1-06]
files_modified:
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs
  - Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute
  - Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - .planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-UAT.md
autonomous: true
gap_closure: true
requirements: [ENCD-02, NORM-01, VCOL-04, UI-03]

must_haves:
  truths:
    - "Quiet regions of a no-authored-map asset pack at approximately the authored scalar roughness (matte): for Neo's `_Smoothness: 0` (scalar roughness 1.0) quiet texels pack around 0.97 (63 bits), not the round-4 0.37 mirror gloss — the anchor invariant holds at every strength (D-08)."
    - "Sharp edges and baked-specular rims dip BELOW the quiet anchor toward gloss: edge texels pack strictly lower roughness than flat texels (D-08 anchored-inverted polarity)."
    - "Both estimator routes share the same anchored-inverted mapping; the estimator dropdown chooses WHO picks the strength (fit-searched vs manual slider), never polarity — the Sobel tooltip no longer claims Blender parity (D-09)."
    - "Fit-driven pack adoption stays full-strength (`isFitDriven ? 1f` at NamerComputePipeline.cs:189 unchanged); the `FitDriven_PackedRoughness_AdoptsSearchedStrengthNotSliderBlend` regression keeps pinning it (D-10)."
    - "The full EditMode regression passes with the flipped Sobel premises and the dip-depth formula; UAT round 5 records parity deviation #2 (anchored-inverted polarity) alongside #1 (p90 vs np.max)."
  artifacts:
    - "Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute — CSRoughnessRemap becomes saturate(_ScalarRoughness - _Strength * _SobelRoughness.r); CSRoughnessNormalize stays direct mag/p90"
    - "Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute — CSSurfacePack gains a _RoughnessDipApplied branch (fit-driven adopt-as-is / Sobel dip-depth)"
    - "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs — BindAndDispatchSurfacePack threads _RoughnessDipApplied; line 189 isFitDriven ? 1f UNCHANGED"
    - "Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs — three flipped Sobel premises (FlatBase ≈ scalar; SharpEdge below flat; SparseExtremeEdges smooth ≈ scalar, edge below)"
    - "Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs — FitDriven_Roughness_UsesSobelEdgeSignal expected = Clamp01(scalar - fitStrength * sobel)"
    - "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs — estimator tooltip drops Blender-parity, states dropdown = who picks the strength"
    - ".planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-UAT.md — round-5 entry recording parity deviation #2"
  key_links:
    - "CSRoughnessRemap: _SobelRoughness (.r = direct mag/p90 from CSRoughnessNormalize) → _RoughnessOut via saturate(_ScalarRoughness - _Strength * _SobelRoughness.r)"
    - "CSSurfacePack: _PackInputs.y (scalar) → _SurfaceOut via saturate(scalar - _RoughnessExtractStrength * _RoughnessTex.r) when _RoughnessDipApplied == 0 (Sobel); via _RoughnessTex.r as-is when == 1 (fit-driven)"
    - "NamerComputePipeline.BindAndDispatchSurfacePack → CSSurfacePack _RoughnessDipApplied (= isFitDriven); the caller keeps isFitDriven ? 1f : slider for _RoughnessExtractStrength (D-10)"
---

<objective>
Implement the anchored-inverted roughness mapping revision locked in CONTEXT.md **D-08, D-09, D-10** (2026-09-16): the authored roughness scalar anchors the extracted roughness map, and the Sobel edge magnitude (p90-scaled) pulls texels *down* toward gloss at the two consume sites — `CSRoughnessRemap` (fit-driven) and the Sobel-path pack blend in `CSSurfacePack`. `CSRoughnessNormalize` stays direct-polarity (`mag/p90` in `.r`); inversion happens only at the consume sites. This replaces the round-3 `lerp(scalar, sobel, strength)` mapping — the code state being revised, not the target.

Purpose: eliminate the arbitrary-normalization-dependent level failure (UAT round 4 "totally glossy" / round-3 fit-driven gloss bug) by anchoring the map at the authored scalar, matching how actual Blender-the-product treats an authored material. Two documented parity deviations stand: #1 p90 vs np.max (retained), #2 anchored-inverted polarity (this plan).

Output: dip-depth remap at both consume sites, three flipped Sobel test premises (test-first RED then GREEN), a D-09 tooltip rewrite, D-10 full-adoption pin unchanged, a full EditMode regression, and a UAT round-5 entry.

**Supersedence (do not propagate superseded guidance):** CONTEXT.md D-08–D-10 are AUTHORITATIVE over the ROADMAP goal's "Sobel Blender-parity estimator" clause, RESEARCH.md Pitfall 1 ("no inversion" — the premise is now inverted by design) and Pitfall 3's global-max framing (p90 stands, deviation #1), and plan 04.1-06's `CSRoughnessRemap = lerp(scalar, Sobel-normalized, fit.Strength)` description. `namer_core.py extract_roughness` (direct `mag/np.max`) is a testing idea, NOT ground truth.

**Working-tree note:** the p90 changes (UAT round 4, `RobustSobelScale` in NamerRoughnessPipeline.cs) are present but UNCOMMITTED in the working tree; this plan's tasks build on top of them. Treat them as already-shipped, not regressions — they carry the D-08 `mag/p90` normalization, so `NamerRoughnessPipeline.cs` is in `files_modified` and its uncommitted p90 changes ride along in this plan's commits (no code change needed beyond what is already dirty). Do NOT sweep unrelated dirty files into commits: `Assets/NAMER/NamerSmoke.unity`, `ProjectSettings/ProjectSettings.asset`, `ProjectSettings/Packages/com.unity.ai.assistant/Settings.json`, `Namer-Unity.sln.DotSettings.user`, and the `Assets/NAMERGenerated/**` PNG/EXR/material outputs. Commit only the `files_modified` listed above.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-CONTEXT.md
@.planning/notes/anchored-inverted-roughness-polarity.md

The mandated sequence (CONTEXT.md "Folded Todos"): flip the three Sobel premises RED first → apply the dip-depth remap → full regression → UAT round-5 entry (deviation #2) → live Neo re-test. Task order below preserves it exactly.

### Current code state (the two consume sites to revise)

`Compute/NAMERRoughness.compute`:
- `CSRoughnessNormalize` (line 134) emits direct-polarity `saturate(_RoughnessRaw[id.xy].g / max(_GlobalMax, 1e-4))` — STAYS as-is (D-08).
- `CSRoughnessRemap` (line 234) is today `float roughness = lerp(_ScalarRoughness, _SobelRoughness[id.xy].r, _Strength);` — becomes the dip-depth consume site. `_SobelRoughness.r` is the direct `mag/p90` texture (CSRoughnessNormalize output fed by `ExtractSobel`), `_Strength` is the searched `fit.Strength`, `_ScalarRoughness` is `inspection.Roughness` (threaded at NamerRoughnessPipeline.cs:429).

`Compute/NAMERPack.compute` `CSSurfacePack` (lines 115-118) is the shared pack blend — the Sobel-path consume site:
```hlsl
if (_HasExtractedRoughness > 0.5)
{
    roughness = lerp(roughness, _RoughnessTex[id.xy].r, _RoughnessExtractStrength);
}
```
For Sobel, `_RoughnessTex.r` is direct `mag/p90` and `_RoughnessExtractStrength` is the slider. For fit-driven, `_RoughnessTex.r` is ALREADY dip-depth (CSRoughnessRemap) and `_RoughnessExtractStrength` is the sentinel `1f` (`isFitDriven ? 1f : slider` at NamerComputePipeline.cs:189, D-10 — UNCHANGED). The two paths therefore need different formulas at this single site; a new `_RoughnessDipApplied` uniform distinguishes them.

`Editor/Pipeline/NamerComputePipeline.cs` `BindAndDispatchSurfacePack` (lines 416-434) binds the pack uniforms; the caller (lines 186-189) passes `isFitDriven ? 1f : inspection.RoughnessExtractStrength` and knows `isFitDriven` (line 116).

`Tests/Editor/NamerRoughnessFitTests.cs` `FitDriven_Roughness_UsesSobelEdgeSignal` (lines 224-301) pins the old formula at line 274: `float expected = Mathf.Lerp(scalar, sobelTex[i].r / 255f, fitStrength);` — must become `Mathf.Clamp01(scalar - fitStrength * (sobelTex[i].r / 255f))` in the same GREEN step (it encodes CSRoughnessRemap, not one of the three Sobel premises).

`Tests/Editor/NamerRoughnessExtractionTests.cs` — the three Sobel premises to flip (lines: 30 FlatBase, 86 SharpEdge, 156 SparseExtremeEdges), plus the `BuildInspection` helper (lines 323-348, `roughness: 1f` = the scalar anchor).

`Editor/UI/NamerEditorWindow.cs` line 864 — the estimator tooltip carrying the "Blender-parity" claim to drop (D-09). Line 769's "after-mesh parity" comment is unrelated — leave it.

### Test-fixture constraint (memory)
The SparseExtremeEdges hash-grain smooth population survives the AO un-multiply at ±0.022 (nonzero Sobel), so under the anchored-inverted map it dips BELOW the scalar and cannot satisfy the flipped "smooth median ≈ scalar" premise. Its smooth population must become a genuinely QUIET fill (zero Sobel → stays at the scalar). A flat mid-gray fill is the correct quiet population; a pure luminance ramp is equally quiet but the flat fill is simpler and deterministic.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-first premise flips (RED) — flip the three Sobel premises so they fail against the current lerp/direct-polarity code</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs (the file being modified — see current assertion lines 62, 132, 223, 225 and the fixture bodies at 44, 100-112, 170-193)
    - .planning/notes/anchored-inverted-roughness-polarity.md (the flipped-premise list, section "Open implementation questions")
    - Memory note: namer-test-fixtures-ao-unmultiply.md (grain survives un-multiply → nonzero Sobel; a quiet fill is required for "≈ scalar")
  </read_first>
  <action>
    Flip the three Sobel test premises FIRST, before any production change, so they fail (RED) against the current code. All three use scalar roughness 1.0 (`BuildInspection(..., smoothness: 0f, roughness: 1f, ...)`), so the scalar anchor = 63 bits.

    1. Rename `Sobel_FlatBase_YieldsNearZeroRoughness` (line 30) → `Sobel_FlatBase_YieldsAuthoredScalarRoughness`. Change the assertion at line 62 from `Assert.LessOrEqual(maxBits, 1, ...)` to `Assert.GreaterOrEqual(maxBits, 62, "flat base must yield approximately the authored scalar roughness (1.0 → 63 bits): zero Sobel → no dip → scalar anchor (D-08)")`. The solid 0.5 base has zero Sobel magnitude (constant), so the dip `saturate(1 - 1 * 0)` = 1.0 → 63 bits.

    2. Rename `Sobel_SharpEdge_YieldsHighRoughness` (line 86) → `Sobel_SharpEdge_DipsBelowFlatRoughness`. Change the assertion at line 132 from `Assert.Greater(edgeBits, flatBits, ...)` to `Assert.Less(edgeBits, flatBits, "edge texel must dip BELOW flat texels (anchored-inverted D-08: sharp edge = gloss dip)")`. The flat texel (x=8, bright half) has zero Sobel → scalar 63; the edge texel (x=32) dips below.

    3. Rename `Sobel_SparseExtremeEdges_SmoothRegionsStayRough` (line 156) → `Sobel_SparseExtremeEdges_SmoothMedianStaysAtScalar`. Two edits:
       a. Replace the hash-grain smooth body (lines 170-193) with a QUIET fill: set every pixel to `0.5f`, EXCEPT the sparse extreme dark line `if (x == WorkingSize / 2) value = 0.02f;` (keep that line). Rationale: the grain has nonzero Sobel signal that dips the smooth median below the scalar; a quiet fill has zero Sobel → stays at the scalar, which is what "smooth median ≈ scalar" means (D-08 anchor invariant). The strip (x 8..24) is well left of the line (x=32), so it is fully quiet.
       b. Change line 223 from `Assert.GreaterOrEqual(medianBits, 19, ...)` to `Assert.GreaterOrEqual(medianBits, 62, "smooth median must stay approximately at the authored scalar (quiet regions are anchored, not collapsed)")`. Change line 225 from `Assert.GreaterOrEqual(edgeBits, medianBits, ...)` to `Assert.Less(edgeBits, medianBits, "the sparse extreme edge must dip strictly BELOW the smooth anchor (gloss dip)")`.

    Do NOT touch `AuthoredMetallicGlossMap_IsByteIdentical_NoExtraction` (line 249) — the authored-map-wins path is byte-identical and must keep passing.
  </action>
  <verify>
    <automated>Run NamerRoughnessExtractionTests in the live editor via the live-editor-editmode-test-run recipe (memory note: TestRunnerApi Execute EditMode, write results to Temp/namer-07-red-results.txt, watch with an `until grep -q FINISHED` loop). EXPECTED: the three flipped tests FAIL (RED) against the current code — FlatBase got maxBits 0 (&lt; 62), SharpEdge got edgeBits &gt; flatBits (not &lt;), SparseExtremeEdges got medianBits ~23 (&lt; 62) and edgeBits &gt;= medianBits (not &lt;). AuthoredMetallicGlossMap still passes. Fallback if the relay is dead: close the editor and run headless WITH graphics (`-batchmode -projectPath &lt;repo&gt; -runTests -testPlatform EditMode -testResults /tmp/namer-07-red.xml -logFile /tmp/namer-07-red.log`, NO `-nographics`).</automated>
  </verify>
  <acceptance_criteria>
    - Source assertions: `grep -n "GreaterOrEqual(maxBits, 62" NamerRoughnessExtractionTests.cs` returns the FlatBase line; `grep -n "Less(edgeBits, flatBits"` returns the SharpEdge line; `grep -n "GreaterOrEqual(medianBits, 62"` and `grep -n "Less(edgeBits, medianBits"` return the SparseExtremeEdges lines.
    - Source assertion: `grep -n "Mathf.Sin(x \* 12.9898f" NamerRoughnessExtractionTests.cs` returns NOTHING (the hash-grain body is gone from the SparseExtremeEdges fixture; the sparse line `value = 0.02f` at `x == WorkingSize / 2` remains).
    - Source assertion: the three renamed test methods exist (`grep -n "Sobel_FlatBase_YieldsAuthoredScalarRoughness\|Sobel_SharpEdge_DipsBelowFlatRoughness\|Sobel_SparseExtremeEdges_SmoothMedianStaysAtScalar"` returns all three).
    - Behavior assertion: the three flipped tests FAIL (RED) against the current un-remapped code (RED evidence recorded to Temp/namer-07-red-results.txt or the NUnit XML).
  </acceptance_criteria>
  <done>Three Sobel premises flipped (directions + quiet-fill fixture), renamed to match, and confirmed RED against the current code.</done>
</task>

<task type="auto">
  <name>Task 2: Dip-depth remap at both consume sites (GREEN) + formula-pinning test + D-09 estimator tooltip rewrite</name>
  <files>Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute, Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs, Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute (CSRoughnessRemap lines 219-236; CSRoughnessNormalize line 134 which STAYS direct)
    - Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute (uniform block lines 44-47; CSSurfacePack lines 103-122)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs (BindAndDispatchSurfacePack lines 416-434; caller lines 186-189 — line 189 must NOT change)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs (RunFrequencySeparation lines 395-451 — confirms `_ScalarRoughness` = inspection.Roughness, `_SobelRoughness` = direct CSRoughnessNormalize output; NO change needed here, the p90 RobustSobelScale stays)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs (FitDriven_Roughness_UsesSobelEdgeSignal lines 224-301, the lerp formula at 274 and the message at 283-286)
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (estimator tooltip line 864)
  </read_first>
  <action>
    1. `Compute/NAMERRoughness.compute` — CSRoughnessRemap (line 234): replace `float roughness = lerp(_ScalarRoughness, _SobelRoughness[id.xy].r, _Strength);` with `float roughness = saturate(_ScalarRoughness - _Strength * _SobelRoughness[id.xy].r);` and keep `_RoughnessOut[id.xy] = float4(roughness, 0.0, 0.0, 1.0);`. Update the header comment (lines 219-225) to describe D-08 dip-depth: the authored scalar anchors; the direct-polarity `mag/p90` in `_SobelRoughness.r` dips texels toward gloss; the round-3 lerp-to-sobel is retired. Do NOT touch `CSRoughnessNormalize` (line 134) — it stays `saturate(_RoughnessRaw[id.xy].g / max(_GlobalMax, 1e-4))` (direct polarity, D-08).

    2. `Compute/NAMERPack.compute` — add uniform `float _RoughnessDipApplied;` after `_RoughnessExtractStrength;` (line 46) with comment `// 1 = _RoughnessTex already carries the anchored-inverted dip (fit-driven, CSRoughnessRemap) -> adopt as-is; 0 = direct mag/p90 (Sobel) -> apply the dip now`. Replace the CSSurfacePack branch (lines 115-118) with a two-way branch:
       - when `_RoughnessDipApplied > 0.5`: `roughness = _RoughnessTex[id.xy].r;` (fit-driven texture is already dip-depth — adopt as-is; this is full adoption, unchanged from the old `lerp(..., 1f)`).
       - else (Sobel): `roughness = saturate(roughness - _RoughnessExtractStrength * _RoughnessTex[id.xy].r);` (dip the scalar `_PackInputs.y` by the slider × direct `mag/p90`).

    3. `Editor/Pipeline/NamerComputePipeline.cs` — add a `bool roughnessDipApplied` parameter to `BindAndDispatchSurfacePack` (line 416) and inside its body set `_compute.SetFloat("_RoughnessDipApplied", roughnessDipApplied ? 1f : 0f);` (near lines 429-430). At the caller (lines 186-189) pass `isFitDriven` as the new argument. DO NOT change line 189's `isFitDriven ? 1f : inspection.RoughnessExtractStrength` (D-10 — full adoption stays).

    4. `Tests/Editor/NamerRoughnessFitTests.cs` — `FitDriven_Roughness_UsesSobelEdgeSignal`: change line 274 from `float expected = Mathf.Lerp(scalar, sobelTex[i].r / 255f, fitStrength);` to `float expected = Mathf.Clamp01(scalar - fitStrength * (sobelTex[i].r / 255f));`. Update the doc comment (lines 232-235) and the assertion message (lines 283-286) to state the fit-driven texture is `saturate(scalar - fitStrength * standaloneSobel)` (anchored-inverted D-08). Leave `FitDriven_PackedRoughness_AdoptsSearchedStrengthNotSliderBlend` (line 156) UNTOUCHED — it pins D-10 and must keep passing (the fit-driven texture is still adopted as-is, now via the `_RoughnessDipApplied` branch).

    5. `Editor/UI/NamerEditorWindow.cs` — rewrite the estimator tooltip (line 864), dropping the "Blender-parity" claim (D-09). New tooltip text: `"How roughness is extracted: both routes share the anchored-inverted mapping — the authored roughness scalar anchors the map and the Sobel edge magnitude dips texels toward gloss. This dropdown only chooses who picks the strength: Fit-driven auto-searches the strength that collapses the residual (default); Sobel uses the manual strength slider. Sobel mode does NOT sharp-remove the base, so Sobel-mode assets do not reach the one-texture outcome."` Leave the strength-slider tooltip (line 848) and the line-769 "after-mesh parity" comment unchanged.
  </action>
  <verify>
    <automated>Run NamerRoughnessExtractionTests (3 flipped + 1 authored-map) AND NamerRoughnessFitTests (incl. FitDriven_Roughness_UsesSobelEdgeSignal and FitDriven_PackedRoughness_AdoptsSearchedStrengthNotSliderBlend) in the live editor via the live-editor-editmode-test-run recipe (Temp/namer-07-green-results.txt). EXPECTED: all GREEN — the three RED tests from Task 1 now pass; the formula test passes with the dip formula; the D-10 pack-adoption regression stays green. Fallback: headless WITH graphics as in Task 1.</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^#' NAMERRoughness.compute | grep -c "lerp(_ScalarRoughness"` is 0 and `grep -n "saturate(_ScalarRoughness" NAMERRoughness.compute` returns the CSRoughnessRemap line.
    - Source assertion: `grep -n "saturate(_RoughnessRaw\[id.xy\].g / max(_GlobalMax" NAMERRoughness.compute` still returns line 134 (CSRoughnessNormalize unchanged, direct polarity).
    - Source assertion: `grep -n "_RoughnessDipApplied" NAMERPack.compute NamerComputePipeline.cs` returns the uniform declaration, the two-way branch, the `SetFloat`, and the new `BindAndDispatchSurfacePack` parameter.
    - Source assertion: `grep -n "isFitDriven ? 1f : inspection.RoughnessExtractStrength" NamerComputePipeline.cs` still returns line 189 (D-10 unchanged).
    - Source assertion: `grep -n "Clamp01(scalar - fitStrength" NamerRoughnessFitTests.cs` returns the new expected line; `grep -n "Mathf.Lerp(scalar, sobelTex" NamerRoughnessFitTests.cs` returns NOTHING.
    - Source assertion: `grep -n "Blender-parity" NamerEditorWindow.cs` returns NOTHING (the claim is gone from the tooltip).
    - Behavior assertion: the four-suite gate (NamerRoughnessExtractionTests, NamerRoughnessFitTests, NamerEditorWindowSmokeTests, NamerOneTextureTests) passes in the live editor (or headless-with-graphics).
  </acceptance_criteria>
  <done>Dip-depth remap live at both consume sites; CSRoughnessNormalize still direct; the formula-pinning test updated; D-09 tooltip rewritten; D-10 `isFitDriven ? 1f` unchanged; the flipped tests and fit suites GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Full EditMode regression + UAT round-5 entry (parity deviation #2) + issue the live Neo re-test</name>
  <files>.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-UAT.md</files>
  <read_first>
    - .planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-UAT.md (rounds 1-4 history and the existing Gaps/Summary structure; round 5 must record deviation #2 alongside #1)
    - Memory note: live-editor-editmode-test-run.md (full-regression recipe + headless-with-graphics fallback + ProjectSettings.asset churn note)
    - .planning/notes/anchored-inverted-roughness-polarity.md (deviation #1 p90, deviation #2 polarity)
  </read_first>
  <action>
    1. Run the FULL EditMode regression (all suites, not just the four) via the live-editor TestRunnerApi recipe or the headless-with-graphics fallback. Snapshot `cp ProjectSettings/ProjectSettings.asset /tmp/ps-before.asset` before a headless run and restore after (the run strips `SENTIS_ANALYTICS_ENABLED`; never `git checkout` that file). Record results to Temp/namer-07-regression-results.txt (live) or /tmp/namer-07-regression.xml (headless). Expect all pass (prior full regression was 123/123; the count may differ by the renamed/flipped tests only).

    2. Append the UAT round-5 entry to `04.1-UAT.md`: bump `updated:` to 2026-09-16; add a round-5 history line under `## Summary` (or a new `### 5.` test entry in the same style as the existing entries) recording the anchored-inverted polarity revision — it MUST state parity deviation #2 (anchored-inverted polarity: Sobel dips toward gloss from the authored-scalar anchor) alongside the existing deviation #1 (p90 vs np.max), and reference D-08/D-09/D-10. Record the regression result (count/totals) in the entry.

    3. Issue the round-5 human check (same pattern as the existing "HUMAN CHECK" lines): re-run Process on the Neo scene instance in the live editor and confirm quiet regions ≈ scalar matte (≈ 0.97 for `_Smoothness: 0`) and edges/baked-specular rims dip glossy. This is the user's visual confirmation; record it as the round-5 pending human check, not a blocking checkpoint.
  </action>
  <verify>
    <automated>Full EditMode regression result: for a headless run, `grep -o 'result="[^"]*"' /tmp/namer-07-regression.xml | head -1` shows `result="Passed"` with `failed="0"` (or the Temp results file shows all green with 0 failures).</automated>
    <human-check>Neo re-test (issued, user-confirmed): Process the Neo scene instance twice in the live editor — quiet regions render ≈ scalar matte, edges/baked-specular rims dip glossy, model stays visible.</human-check>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: full EditMode regression green — 0 failures, the three flipped Sobel tests, the dip-formula fit test, and the D-10 pack-adoption regression all pass.
    - Source assertion: `grep -n "deviation #2\|anchored-inverted\|polarity" 04.1-UAT.md` returns the new round-5 lines; `grep -n "updated:" 04.1-UAT.md` shows `2026-09-16`.
    - Source assertion: the round-5 entry references D-08, D-09, and D-10.
    - Behavior assertion: the Neo re-test human check is recorded as pending in the round-5 entry (quiet ≈ scalar matte, edges dip glossy).
  </acceptance_criteria>
  <done>Full EditMode regression green; UAT round-5 entry records parity deviation #2 (anchored-inverted polarity) alongside #1 (p90); the live Neo re-test is issued as a human check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The dip-depth remap changes only how roughness values are derived inside existing compute kernels and how one uniform is bound; no new input, output, asset path, or user-facing control crosses a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-13 | Tampering | NAMERRoughness.compute CSRoughnessRemap / NAMERPack.compute CSSurfacePack | accept | compute-only arithmetic on already-validated GPU textures; the `saturate()` clamp bounds roughness to [0,1] before the 6-bit quantization in `NamerPackSurfaceFromOct` — no new untrusted input surface, and out-of-range values are structurally impossible |
| T-04.1-14 | Information Disclosure / DoS | NamerComputePipeline.BindAndDispatchSurfacePack (_RoughnessDipApplied uniform) | accept | editor-time in-memory uniform derived from the already-loaded `inspection.RoughnessEstimator`; no network, auth, or disk-write surface is added |

No package installs in this plan, so no package-legitimacy threat row.
</threat_model>

<verification>
Overall phase checks:
- The three flipped Sobel tests are RED before Task 2 and GREEN after (test-first sequence preserved).
- `CSRoughnessNormalize` remains direct-polarity (`mag/p90`); inversion exists only at `CSRoughnessRemap` (fit-driven) and `CSSurfacePack` (Sobel) — grep confirms no `lerp(_ScalarRoughness` remains and no direct-polarity inversion elsewhere.
- `NamerComputePipeline.cs:189` `isFitDriven ? 1f : inspection.RoughnessExtractStrength` is byte-identical to the shipped state (D-10).
- The estimator tooltip no longer contains "Blender-parity" (D-09).
- Full EditMode regression passes; `04.1-UAT.md` round-5 entry records parity deviation #2 alongside #1.
- Only the `files_modified` (including `NamerRoughnessPipeline.cs` carrying the D-08 p90 `RobustSobelScale` changes) are committed; the unrelated dirty working-tree files (NamerSmoke.unity, ProjectSettings.asset, AI-assistant Settings.json, DotSettings.user, NAMERGenerated outputs) are left untouched.
</verification>

<success_criteria>
- Quiet regions pack at ≈ the authored scalar (matte) and sharp edges dip below it (gloss), for both the Sobel and fit-driven routes — verified by the three flipped Sobel tests plus `FitDriven_Roughness_UsesSobelEdgeSignal`.
- The authored-map-wins path is untouched (AuthoredMetallicGlossMap_IsByteIdentical_NoExtraction still passes).
- The estimator dropdown means "who picks the strength", never polarity; the Blender-parity tooltip claim is gone (D-09).
- Fit-driven full adoption stays pinned (`FitDriven_PackedRoughness_AdoptsSearchedStrengthNotSliderBlend` green; line 189 unchanged) (D-10).
- Full EditMode regression green and UAT round-5 records parity deviation #2 (D-08/D-09/D-10 cited).
</success_criteria>

<output>
Create `.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-07-SUMMARY.md` when done
</output>
