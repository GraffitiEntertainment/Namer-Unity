# Phase 04.1: Baked-response roughness extraction + zero-residual one-texture mode - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-06
**Phase:** 04.1-baked-response-roughness-extraction-zero-residual-one-textur
**Areas discussed:** Extraction gate, Estimator route, Fallback residual basis, Escape hatch & controls

---

## Extraction gate

| Option | Description | Selected |
|--------|-------------|----------|
| Auto when no authored roughness map | 03.1 D-07 authored-wins pattern: authored map wins, extraction fills absence | ✓ |
| Explicit opt-in toggle | Phase-4 D-05 decomposition precedent: off by default, user enables | |
| Strength override | User-facing strength control (0 = off), EditorPrefs, debounced recompute | ✓ (combined with auto) |

**User's choice:** "auto when no authored roughness, yes, strength override"
**Notes:** Auto extraction on the no-authored-map path, plus a user strength override control. Authored-map path stays byte-identical.

---

## Estimator route

| Option | Description | Selected |
|--------|-------------|----------|
| Sobel-parity first | Match Blender `extract_roughness` (Sobel edge-magnitude on luminance), improve later | |
| Fit-driven from day one | Strength calculated to minimize post-refit residual (exploration-notes direction) | |
| Both as a selectable option | Ship both routes, user-selectable | ✓ |

**User's choice:** "option for either"
**Notes:** Both estimators ship as options. CONTEXT records the default as fit-driven (exploration-notes direction) — an assumption flagged for plan review. The user also @-referenced `NamerAOBaker.cs` — captured as D-04: the fit-driven pass follows the baker's pattern (deterministic Burst, cancellable, off-debounce, cached).

---

## Fallback residual basis

| Option | Description | Selected |
|--------|-------------|----------|
| Cleaned (post-extraction) base | Refit/residual computed against the extraction-processed base | ✓ |
| Original base | Residual fit against the pre-extraction source | |

**User's choice:** "residual fit against the cleaned"
**Notes:** Extraction is never undone on the fallback path; extracted roughness stays in bits 0–5.

---

## Escape hatch & controls

| Option | Description | Selected |
|--------|-------------|----------|
| Ship RGBA roughness-offset input in 04.1 | Optional shader input to offset roughness where 6-bit per-texel extraction underfits | ✓ |
| Defer it | Fallback-only feature; defer to a later phase | |

**User's choice:** "ship it in 04.1"
**Notes:** Neutral-when-unset so decode equivalence and the one-texture happy path are untouched.

---

## Claude's Discretion

- AO × roughness-extraction composition inside `CSNormalize` (order/divisor math) under the 03.1 D-08/D-09 constraints
- Scalar-gate semantics for D-01 (non-default scalar vs authored map)
- Fit-driven search internals, blur kernel, halo mitigation
- `_BaseMap` neutral binding in one-texture mode; whether Base PNG stays written (unbound) for D-14 switch-back (assumption: yes)
- Escape-hatch input details; window layout of new controls

## Deferred Ideas

- Metallic extraction from base (Blender `extract_metallic`) — natural follow-up, not requested
- Stylization interplay incl. roughness simplification (STYL-06) — Phase 5
- Literal Blender fixture parity — v2 (PIPE-02)
- Batch processing — v2 (BATCH-01)

---

## Plan-Phase Addendum (2026-09-06)

Decisions taken during the plan-phase revision loop (max-iterations escalation, user chose "provide guidance and retry"):

1. **Fit-driven fallback when decomposition unavailable** → skip extraction, identity legacy path (Sobel remains the explicit standalone-extraction choice)
2. **Fixture disposition** → verify/pin existing processor-routed oracles + add one default-on end-to-end D-01 acceptance test
3. **Evaluate-callback owner** → `NamerProcessor` composes; `NamerRoughnessPipeline` mirrors the AO three-way (`HasCachedFit`/sync-fit-in-`Process`/`RequestFit`)
4. **Extracted-roughness debug channel** → ships in this phase (small window task)
5. **D-07** → processor window collapsible sections + scrolling (dialog too tall for screen)
