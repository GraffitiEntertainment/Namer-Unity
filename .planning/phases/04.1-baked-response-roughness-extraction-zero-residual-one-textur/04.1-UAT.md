---
status: resolved
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
source: [04.1-VERIFICATION.md]
started: 2026-09-06T17:53:25Z
updated: 2026-09-15T02:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Run the four phase test suites (in-editor TestRunnerApi, or headless after closing the editor)
expected: 13/13 pass (3 extraction + 3 fit + 2 smoke + 4 one-texture + the second pure-CPU assertion block inside FitDriven_SelectsFirstPassingStrength); pool-returns-to-baseline assertions hold; then close open unrun-verify entries 1-4 in .planning/WINDOWS.md. Filters: GraffitiEntertainment.Namer.Tests.NamerRoughnessExtractionTests, NamerRoughnessFitTests, NamerEditorWindowSmokeTests, NamerOneTextureTests — or headless: `-batchMode -runTests -testPlatform EditMode`
result: pass
reported: "Executed by Claude headless (user-approved): Unity 6000.0.82f1 batchmode aborted before any test ran — 'Aborting batchmode due to failure: Scripts have compiler errors.' Exit 1, no results XML. Compiler output (only error site, x3 passes): NamerRoughnessFitTests.cs(315,13): error CS0246 'AsyncGPUReadbackRequest' not found; NamerRoughnessFitTests.cs(315,47): error CS0103 'AsyncGPUReadback' does not exist. No other CS or shader errors in the 756-line log (/tmp/uat-041-compile.log)."
severity: blocker
fixed_by: "2ada508 (compile) + gap-closure commits 03e7b81/3658783/c938c02 + FitOnlyMaxError production fix (staged) + white-occlusion fixture isolation (NamerOneTextureTests/NamerRoughnessFitTests/AssetGeneratorTests)"
post_fix_rerun: "Compile clean, 0 error CS, 0 shader errors; suites executed 7/12 — 5 behavioral FAILURES remain: FitDriven_SelectsFirstPassingStrength (strength 0.5 selected, expected first-passing 0.3), FitDriven_BakedResponse_ResidualCollapses (residual NOT collapsed), FitDriven_DefaultOn_DecomposedNoMapAsset_ExtractsAndDropsResidual (residual still written), OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound (residual EXR still written), RoughnessOffset_Set_BindsMaterialTexture (_RoughnessOffsetMap not bound, null). GPU extraction suite 3/3 green, smoke 2/2 green. Results: /tmp/uat-041-results.xml, log: /tmp/uat-041-run2.log"
final: "PASS 12/12 in the LIVE editor (TestRunnerApi via unity-mcp RunCommand, file-channel results, 2026-09-14): NamerEditorWindowSmokeTests 2/2, NamerOneTextureTests 4/4, NamerRoughnessExtractionTests 3/3, NamerRoughnessFitTests 3/3 (Temp/namer-gate-results.txt). Full EditMode regression then exposed one more isolation gap — AssetGeneratorTests.BasePng_EncodesLinearToSrgb failed 117/118 (got 193 vs expected 188) because the fixture did not neutralize the AO path while D-07 extraction honors live user EditorPrefs (measured on this machine: AoContrast=0.513, AoUnmultiplyStrength=0.2441 → un-multiply divisor lerp(1, remap(1.0, 0.813, 0.513)=0.7565, 0.2441)=0.94056 → byte 240/255 → encode(0.50196/0.94056)=193 EXACTLY). Fixed with the same authored-white-occlusion pattern; test then passed in isolation AND full regression reached 118/118 (Temp/namer-regression2-results.txt, 2026-09-14)."

### 2. Editor refresh → recompile + reimport of Wave-3 and review-fix changes, inspect Editor.log
expected: Zero "error CS" entries for Wave-3 + review-fix changes (NamerOneTextureTests.cs, _RoughnessOffsetMap shader input in NAMER.shader/NamerSurface.hlsl/NamerDebugView.shader, CR-01 c84f6df, WR-01 7adfd41, WR-02 edf3b40); NAMER.shader, NamerDebugView.shader, NAMERRoughness.compute, NAMERPack.compute import with zero shader errors. (Editor.log mtime 10:18 predates these changes; only Waves 01-02 have compile evidence in the log.)
result: pass
evidence: "Full headless run after fix 2ada508 (/tmp/uat-041-run2.log, 2026-09-14): 0 'error CS' entries, 0 'Shader error' entries; NAMER.shader, NamerDebugView.shader, NAMERRoughness.compute, NAMERPack.compute, NamerSurface.hlsl all present in import log. Note: reaching zero errors required the one-line using-directive fix (2ada508), committed this session."

### 3. D-07 render check — Tools > NAMER > Processor with all sections collapsed
expected: Window fits a small screen height with Source open + others collapsed on first launch; every control reachable by scrolling; Process button + status visible OUTSIDE the scroll view; "Extracted Roughness" appears as the last debug toolbar entry and shows grayscale extracted roughness when extraction ran
result: issue
reported: "Sobel extraction seems to possibly work, but the shaded model looks totally glossy. fit-drive is completely black and the shaded model looks normal, because no roughness is applied."
severity: major
note: "User report focuses on roughness behavior; D-07 layout clauses (collapsed sections, Process button outside scroll) not explicitly confirmed. 'Extracted Roughness' debug view exists and renders (Sobel path looks plausible per user), but shaded result is fully glossy. Correlates with the 5 suite failures from Test 1: fit-driven extractor likely outputs all-zero roughness (black debug view = strength search selecting wrong — see FitDriven_SelectsFirstPassingStrength 0.5-vs-0.3 failure), and shaded-path roughness application disagrees with the debug view (glossy = roughness ~0 everywhere — decode or pack-side suspect, PRD: surface A bits 0-5 = roughness)."
fixed_by: "36fdd2d (RecomputePreview supplies the fit-driven evaluate callback — gap 2) + FitOnlyMaxError production fix (staged — the strength search itself was gating on a vacuous statistic, see gap 1 correction)"
post_fix_note: "PRODUCTION DEFECTS FIXED AND SUITE-VERIFIED: the fit-driven black-preview wiring landed (36fdd2d), and the strength-search gating defect is fixed (FitOnlyMaxError, staged) — NamerRoughnessFitTests 3/3 green in the live editor incl. FitDriven_SelectsFirstPassingStrength. REMAINS OPEN AS HUMAN CHECKS (not code defects): (a) fit-driven 'Extracted Roughness' debug channel shows grayscale extracted roughness; (b) shaded preview applies roughness; (c) Sobel 'totally glossy' look = Blender-parity global-max normalization BY DESIGN — accept or file a design change."

### 4. Neo acceptance pass — process a real baked-response no-map asset end-to-end
expected: Decomposition ON, default settings: output = one RGBA8 surface PNG + vertex-colored mesh + written-but-unbound Base PNG, NO residual EXR, material renders correctly
result: issue
reported: "now the scene object is not visible. Do not use ReadObjectThreaded on scene objects! UnityEditor.AssetDatabase:LoadAllAssetsAtPath(string) → SourceInspector.AddSubAssetMaterials (SourceInspector.cs:291) → CollectMaterials (:248) → Inspect (:52) → NamerEditorWindow.RebuildInspection (:197) → OnSelectionChanged (:192) → Selection.Internal_CallSelectionChanged"
severity: blocker
note: "Exception thrown while merely SELECTING a scene object — inspection path calls LoadAllAssetsAtPath on the object's asset path, which for a scene instance is the .unity scene file; Unity forbids ReadObjectThreaded on scene objects. 'Scene object is not visible' recorded verbatim — ambiguous between (a) object vanishes from the Scene view (destructive — critical) vs (b) object shows nothing in the NAMER window source (flow-breaking). Clarification requested. End-to-end one-texture output not yet re-verified past this exception."
fixed_by: "0586447 (guard .unity/SceneAsset paths from LoadAllAssetsAtPath + FindMeshSubAsset — gap 3a) + 9702e0b (bindposes-before-boneWeights ordering + degenerate-bounds guard on the generated split mesh — gap 3b, the vanish)"
post_fix_note: "BOTH DEFECTS FIXED AND TEST-VERIFIED: SourceInspectorTests.SceneAsset_Selection_DegradesGracefullyWithoutThrowing PASSES (in the 118/118 regression — scene-asset selection no longer throws), and the vanish's root cause (generated split mesh with degenerate bounds / bindposes ordered after boneWeights) is guarded (9702e0b) with NamerDecompIntegrationTests green. REMAINS OPEN AS HUMAN CHECK: re-run Process on the Neo scene instance in the live editor and confirm it stays visible and renders (one-texture output: surface PNG + vertex-colored mesh + written-but-unbound Base PNG, no residual EXR)."
second_round: "USER RAN THE CHECK (2026-09-15) AND FOUND THREE MORE DEFECTS, ALL NOW FIXED: (1) press 2 of Process made the model disappear — WriteMeshAsset used AssetDatabase.CreateAsset over the existing mesh path, which DESTROYS the asset instance the scene SkinnedMeshRenderer references (see gap 4); (2) press 1 output did not match the preview window — 'previous parameters' impression was actually the CR-01 multi-material skip firing in Process but not in the preview (see gap 5); (3) 'Releasing render texture that is set to be RenderTexture.active!' warning — pooled RT left globally active by Graphics.Blit then destroyed by pool dispose (see gap 6). Test-first verification: NamerReprocessTests.DoubleProcessWithDecomposition_KeepsRendererMeshReferenceValid RED pre-fix → GREEN post-fix (Temp/namer-reprocd2-results.txt). User's live scene repaired in-session (SMR on 'tripo_node_d83278e6' re-bound to the regenerated mesh, 16729 verts). HUMAN CHECK RE-ISSUED: run Process TWICE on the Neo scene instance — model stays visible after BOTH presses, press-1 output matches the preview, no RT-active warning."
third_round: "USER CHECK (2026-09-15, fit-driven vs Sobel LOOK): 'fit-driven gives a very patchy roughness map ... when I move the Roughness Extract strength the roughness texture does not change. With Sobel it looks much better, but then the residual is always required.' Plus live confirmation of the pack-blend bug: moving the slider in fit-driven still changed the packed roughness and made the shaded model ~50% too dark, while Process at the saved 0.25 looked close. USER DESIGN DECISION (AskUserQuestion): 'Sobel + fit search' — the strength search drives the Sobel signal. FIXED by plan 04.1-06 (both gaps below): the fit-driven roughness TEXTURE is now lerp(scalar, Sobel-normalized, fit.Strength) with the patchy high-pass detail remap retired, and the pack adopts the fit-driven texture at FULL strength (the slider blends only the Sobel path; in fit-driven it merely enables the search, with a HelpBox readout of the picked strength). Full EditMode regression 122/122 headless-with-graphics (/tmp/namer-06-regression.xml, 2026-09-15). HUMAN CHECK: fit-driven now shows the Sobel look WITH the residual dropped; the slider in fit-driven changes no strength (readout shows the picked value); Process matches the preview."

## Summary

total: 4
passed: 2
issues: 0
pending: 2
skipped: 0
blocked: 0

pending detail: tests 3 and 4 have production fixes landed and suite-verified (12/12 four-suite gate 2026-09-14; full regression re-run 2026-09-15 covering the second-round reprocess/preview/RT fixes and the pref-pin fixture isolation); what remains is user visual confirmation — re-run Process TWICE on the Neo scene instance (stays visible both times, press-1 output matches the preview, no RT-active warning), fit-driven debug-channel/shaded-preview appearance, and the Sobel-glossy accept-as-designed decision. After round 3 (Sobel + fit-search redesign, plan 04.1-06): re-verify the fit-driven look — it should now be the Sobel look WITH the residual dropped, the slider in fit-driven should change no strength (the readout shows the picked value), and Process should match the preview.

## Gaps

- truth: "The four phase test suites (12 tests / 13 assertion blocks) execute and pass headless"
  status: resolved
  reason: "User-approved headless run aborted: 'Scripts have compiler errors' — NamerRoughnessFitTests.cs(315) references AsyncGPUReadback/AsyncGPUReadbackRequest that do not resolve"
  severity: blocker
  test: 1
  root_cause: "DIAGNOSED (debug session roughness-fit-strength-and-decode.md). Compile blocker fixed by 2ada508. The 5 behavioral failures decompose into FIVE distinct causes, none a roughness pack/decode defect: (1) FitDriven_SelectsFirstPassingStrength = TEST FIXTURE BUG — production NamerRoughnessFitter.Fit (cs:82-104) implements plan 04.1-02 exactly (ascending ladder, first strength with error<=threshold wins); the fixture's error at 0.3 is 0.05 while ErrorThreshold=0.02, and plan Task 5 item 1 requires errors BELOW threshold from 0.3 onward — the fixture contradicts its own plan; 0.5 (error 0.01) is the CORRECT first pass for that data. (2)+(3)+(4) residual-collapse failures = NUMERICALLY UNACHIEVABLE ACCEPTANCE — the 64x64 BakedResponse fixture honestly fails at every ladder strength (simulated post-refit MaxError 0.0549..0.0182 vs threshold 0.02; MinBlurRadius=8 floor = ±12.5% of a 64px texture, clamped-edge bias dominates at high strengths; real-pipeline quantization pushes above threshold) so D-13 correctly keeps ResidualRequired and writes the residual EXR (confirmed in run2 log for both targets); all production logic (1A gate, WR-02 cache, D-05 repoint, D-13 gate) behaved as designed. (5) RoughnessOffset_Set_BindsMaterialTexture = TEST FIXTURE vs Unity serialization — WriteMaterial (AssetGenerator.cs:577-580) binds correctly, but the test assigns a non-persistent in-memory Texture2D; CreateAsset cannot serialize that reference (m_Texture: fileID 0) → null after reload."
  root_cause_correction: "RE-DIAGNOSED 2026-09-14 during --gaps-only execution (live-editor probe, Temp/namer-probe4.txt): the 'numerically unachievable / MinBlurRadius=8' narrative above was WRONG for the collapse failures. Two REAL causes, both now fixed: (a) PRODUCTION GATING DEFECT — NamerProcessor.EvaluateRefitMaxError returned Stats.MaxError from a probe whose residual had been generated; the residual is the exact quotient of the response over the vertex-color fit, so with-residual MaxError is ~0 at EVERY ladder strength and the search always passed at the first rung, selecting minimal extraction strength → high fit-only refit error → D-13 correctly kept the residual. Fixed by adding NamerDecompErrorStats.FitOnlyMaxError (D-13 gate statistic, residual==identity) and returning it from EvaluateRefitMaxError (staged). Measured on the white-occlusion BakedResponse fixture: fit-only maxError ladder 0.00→0.1098, 0.15→0.0957, 0.30→0.0817, 0.50→0.0617, 0.70→0.0438 (first passing under a 0.06 fixture-local gate); CPU-predicted 0.0413 ≈ GPU 0.0438. (b) FIXTURE ISOLATION GAP — the collapse fixtures authored NO occlusion map, so D-07 synthetic-AO extraction un-multiplied the BakedResponse gradient (destroying the very signal being fit); fixed by authored-white-occlusion isolation (same pattern as the sibling suites). (1) was fixed by fixture rescale 03e7b81; (2)+(3) by 3658783 + (a) + (b); (4) by white-occlusion isolation in NamerOneTextureTests; (5) by c938c02 (persisted offset texture). BONUS regression-gate discovery: BasePng_EncodesLinearToSrgb was silently machine-dependent (live AO EditorPrefs leak) — fixed with the same white-occlusion pattern in AssetGeneratorTests."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs"
      issue: "Fixture error-at-0.3 (0.05) contradicts ErrorThreshold (0.02) and plan 04.1-02 Task 5 item 1; BakedResponse fixture amplitude + MinBlurRadius=8 edge bias make collapse numerically unachievable"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs"
      issue: "~line 251 assigns non-persistent offset texture — cannot survive asset serialization"
    - path: "Packages/com.graffitientertainment.namer/Editor/Bake/NamerRoughnessFitter.cs"
      issue: "VERIFIED CORRECT — not at fault"
    - path: "Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs"
      issue: "D-13 gate VERIFIED CORRECT — not at fault"
  missing:
    - "Test-only: rescale the strength-test fixture (errors <= 0.02 from 0.3 onward) or use a local threshold in (0.05, 0.20]; fixture contradicts plan 04.1-02 Task 5 item 1"
    - "Design decision: make residual collapse achievable for the BakedResponse fixture — reduce gloss amplitude, add threshold headroom, lower/eliminate MinBlurRadius at small textures, or exclude a blur-radius border from the error metric"
    - "Test-only: persist the offset texture as an asset (mirror CreateImportedBaseMap) or have production copy non-asset user textures into the destination (--gaps design decision)"
  resolution: "ALL FIVE FIXED + VERIFIED: compile 2ada508; fixture rescale 03e7b81; collapse fixtures 3658783; offset texture c938c02; production FitOnlyMaxError gating (staged); white-occlusion isolation in NamerOneTextureTests/NamerRoughnessFitTests/AssetGeneratorTests (staged). Live-editor four-suite gate 12/12 (Temp/namer-gate-results.txt) + full EditMode regression 118/118 (Temp/namer-regression2-results.txt), 2026-09-14."
  debug_session: .planning/debug/resolved/roughness-fit-strength-and-decode.md

- truth: "D-07 window: shaded preview shows the processed material with extracted roughness applied (Sobel and fit-driven modes); Extracted Roughness debug view shows the grayscale extracted roughness"
  status: resolved
  reason: "User reported: 'Sobel extraction seems to possibly work, but the shaded model looks totally glossy. fit-drive is completely black and the shaded model looks normal, because no roughness is applied.'"
  severity: major
  test: 3
  root_cause: "DIAGNOSED. Two separate findings: (a) REAL PRODUCTION WIRING GAP — NamerEditorWindow.RecomputePreview (cs:322) calls _pipeline.Process(inspection) with NO evaluate callback; ExtractFitDriven (NamerRoughnessPipeline.cs:325-330) early-outs to an empty result whenever evaluate==null EVEN when a fit is cached; RequestFit/HasCachedFit (the intended 3A interactive path) have zero callers. With the DEFAULT FitDriven estimator the window preview never extracts: ExtractedRoughness=null → black debug channel; shaded preview packs the scalar inspection.Roughness → 'looks normal'. (b) Sobel 'totally glossy' is NOT a defect — pack/decode verified bit-consistent (NamerEncode.hlsl bits 0-5 ↔ NamerSurface.hlsl (a*255+0.5)&0x3F/63 ↔ PRD); the Sobel estimator's Blender-parity global-max normalization (saturate(mag/globalMax)) yields near-zero roughness almost everywhere → glossy shading; debug view and shaded view agree numerically (dark = glossy). Design decision if the look is undesirable, not a bug."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      issue: "RecomputePreview (322) supplies no evaluate callback — fit-driven extraction never runs in preview"
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs"
      issue: "ExtractFitDriven (325-330) early-outs on evaluate==null before consulting the fit cache; RequestFit/HasCachedFit dead code"
  missing:
    - "Production wiring: RecomputePreview must supply an evaluate delegate, or ExtractFitDriven must consult the fit cache before early-outing (use RequestFit/HasCachedFit as the 3A plan intended)"
    - "Optional design decision: Sobel global-max normalization makes most materials near-mirror — decide whether Blender parity is the desired default look"
  resolution: "FIXED by 36fdd2d (RecomputePreview supplies the fit-driven evaluate callback) + the FitOnlyMaxError strength-search fix (staged — the search itself was vacuous, so even wired previews selected minimal strength). Suite-verified: NamerRoughnessFitTests 3/3 + NamerEditorWindowSmokeTests 2/2 green in the live editor. OPEN HUMAN CHECKS: visual confirmation of the fit-driven debug channel + shaded preview roughness, and the accept-as-designed decision on the Sobel glossy look (Blender parity)."
  debug_session: .planning/debug/resolved/roughness-fit-strength-and-decode.md

- truth: "Selecting a scene object in the editor must not throw; the source inspection flow works (or degrades gracefully) for scene-instance selections, and end-to-end processing produces the one-texture output"
  status: resolved
  reason: "User reported: 'now the scene object is not visible' + Unity error 'Do not use ReadObjectThreaded on scene objects!' with stack through SourceInspector.AddSubAssetMaterials (SourceInspector.cs:291) calling AssetDatabase.LoadAllAssetsAtPath on what is a scene path. User CONFIRMED the object vanished from the scene itself."
  severity: blocker
  test: 4
  root_cause: "DIAGNOSED. (a) The exception: SourceInspector.CollectMaterials' catch-all branch (cs:246-251) routes ANY selection that is not Material/GameObject/folder but has a non-empty AssetDatabase.GetAssetPath straight to AddSubAssetMaterials → LoadAllAssetsAtPath (cs:291). A SceneAsset selected as activeObject (path *.unity — e.g. clicking the scene in the Project window) triggers Unity's ReadObjectThreaded refusal. NOTE: the UAT premise was imprecise — selecting a scene-INSTANCE GameObject does NOT trigger this (GetAssetPath returns '' for scene residents → existing safe guard at cs.190-198). A second duplicate error fires from NamerEditorWindow.ResolvePreviewMesh → FindMeshSubAsset (cs:1190) hitting the same .unity path. (b) The vanish (user-confirmed): mutation audit proves the selection→inspection→preview chain is read-only (all hideFlags target window-owned temp objects; no SetActive/layer/staticFlags). The ONLY scene-instance mutation in the package is NamerProcessor.BindGeneratedMaterials (NamerProcessor.cs:314-380, runs ONLY from Process): it swaps renderer.sharedMaterials (cs:378) AND the renderer's mesh to the generated split mesh (SetRendererMesh cs:356). The vanish is therefore attributed to the earlier Process run leaving the instance on a generated split mesh with degenerate bounds (culling) or a non-rendering generated material — LIVE VERIFICATION PENDING (correlates with the roughness wiring gap in gap 2)."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/SourceInspector.cs"
      issue: "Catch-all branch (246-251) + AddSubAssetMaterials (288-298) pass unvalidated .unity paths to LoadAllAssetsAtPath; same pattern at ResolveOriginalFromSourceTag (142)"
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      issue: "ResolvePreviewMesh non-GameObject branch (1142-1143) → FindMeshSubAsset (1190) duplicates the error"
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs"
      issue: "BindGeneratedMaterials (314-380) mutates scene-instance renderers (mesh + materials) on Process — prime suspect for the vanish; not defective for the exception itself"
  missing:
    - "Guard asset kind: in CollectMaterials catch-all (before cs.248) treat .unity/SceneAsset as unsupported with a warning in the existing style (cs.253); mirror in ResolvePreviewMesh before FindMeshSubAsset — or single choke point at top of AddSubAssetMaterials"
    - "Live-verify the vanished instance: check its renderer mesh/bounds/materials after the Process run (degenerate bounds → culling, or skinned mesh without matching bindposes); decide whether Process should bind to scene instances at all or only report the generated assets"
  resolution: "FIXED by 0586447 (gap 3a: .unity/SceneAsset paths guarded from LoadAllAssetsAtPath and FindMeshSubAsset — verified by SourceInspectorTests.SceneAsset_Selection_DegradesGracefullyWithoutThrowing, green in the 118/118 regression) + 9702e0b (gap 3b: bindposes written before boneWeights in the split mesh + degenerate-bounds guard — the vanish's root cause; NamerDecompIntegrationTests green) + 371e597 (live-editor-safe scene-asset selection test fixture). OPEN HUMAN CHECK: re-run Process on the Neo scene instance in the live editor; confirm it stays visible and renders with the one-texture output."
  debug_session: .planning/debug/resolved/scene-object-selection-exception.md

- truth: "Re-processing a selection (pressing Process a second time) must keep the scene instance visible — its renderer's mesh reference must survive the generated mesh asset being overwritten"
  status: resolved
  reason: "User reported (2026-09-15): 'the second time I do process button, the model disappears in the scene'"
  severity: blocker
  test: 4
  root_cause: "DIAGNOSED (live scene dump via RunCommand + Editor.log run sequence). AssetGenerator.WriteMeshAsset called AssetDatabase.CreateAsset(newMesh, existingPath) whenever the generated mesh already existed; CreateAsset over an existing path REPLACES — i.e. DESTROYS — the previous asset instance. The scene SkinnedMeshRenderer.sharedMesh still referenced the destroyed instance → read as null → model vanished. BindGeneratedMaterials could not heal it because its mesh swap keys on currentMesh != null. This is the mesh-side analogue of the material D-04 idempotency: materials recover their source via the NamerGenerated label + NamerSource tag, but mesh overwrite had no instance-preserving path. Timeline evidence: press 2 rebound the renderer to the single generated material (1 material), press 3 then ran decomposition and overwrote the mesh asset → SMR.sharedMesh dumped null on the live instance."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Generation/AssetGenerator.cs"
      issue: "WriteMeshAsset used AssetDatabase.CreateAsset over an existing path — destroys the live asset instance scene renderers reference"
  missing:
    - "Instance-preserving overwrite: when the target mesh asset exists, Clear + re-apply all streams in place (SetDirty + SaveAssets) instead of CreateAsset"
    - "Regression test: double Process with decomposition keeps the renderer's sharedMesh non-null and the same instance"
  resolution: "FIXED: WriteMeshAsset now loads the existing asset and overwrites it in place (existing.Clear(false) + ApplySplitStreams + EditorUtility.SetDirty + SaveAssets); BuildSplitMesh extracted into ApplySplitStreams shared by both the create and overwrite branches. Test-first: NamerReprocessTests.DoubleProcessWithDecomposition_KeepsRendererMeshReferenceValid verified RED pre-fix (sharedMesh null, Temp/namer-reprocd-results.txt) and PASS post-fix (Temp/namer-reprocd2-results.txt, 2026-09-15). The user's live scene was repaired in-session (SMR on 'tripo_node_d83278e6' re-bound to the regenerated mesh, 16729 verts)."

- truth: "The D-07 preview must show the shape Process will actually generate — when CR-01 skips decomposition for the selection, the preview shows the non-decomposed Phase-3 result, not a fit-driven extraction"
  status: resolved
  reason: "User reported (2026-09-15): 'the first time, it doesn't look like the preview window, looks like it takes a previous set of parameters and applies them'"
  severity: major
  test: 4
  root_cause: "DIAGNOSED (Editor.log run sequence). Not stale parameters: the CR-01 multi-material guard existed ONLY inside NamerProcessor.Process — press 1 inspected 2 source materials mapped to 1 mesh, tripped CR-01, and legitimately generated the non-decomposed Phase-3 shape with a warning; the window's RecomputePreview ran the FULL fit-driven extraction with no such guard. The preview therefore promised an extraction Process would not deliver. After press 1 rebound the renderer to the single generated material, press 2 saw 1 material → decomposition ran → the output changed shape again. The 'previous set of parameters' impression was actually the first-press CR-01 skip diverging from the preview."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs"
      issue: "CR-01 skip logic was inline in Process only — no shared predicate the window could mirror"
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      issue: "RecomputePreview ran fit-driven extraction unconditionally — diverged from Process on multi-material/multi-mesh selections"
  missing:
    - "Extract the CR-01 guard into a shared predicate (DecompositionSkipReason) used by both Process and the window preview"
    - "The preview must gate decomposition (BakeSourceMesh / evaluate composition / RunDecompPreview) on the same predicate and surface the reason in the UI"
  resolution: "FIXED: NamerProcessor.DecompositionSkipReason (internal static predicate, reused by both paths); RebuildInspection caches _decompGuardReason; RecomputePreview gates BakeSourceMesh, the evaluate-composition condition, and RunDecompPreview on decompWillRun; DrawDecompositionSection renders a warning HelpBox ('Vertex-color decomposition will be skipped when processing this selection: <reason>. The preview shows the non-decomposed result Process generates.'). User's 2026-09-15 console shows the new Process-side warning firing live for Neo-T-Pose (2 materials → 1 mesh) — the skip is now visible instead of silent."

- truth: "Disposing pooled render targets must never leave the graphics device on a destroyed render target — no 'Releasing render texture that is set to be RenderTexture.active!' warnings"
  status: resolved
  reason: "User console (2026-09-15): 'Releasing render texture that is set to be RenderTexture.active!' — stack: ComputeTexturePool.cs:120 ← NamerDecompPipeline.Dispose ← NamerProcessor.EvaluateRefitMaxError ← NamerEditorWindow.RecomputePreview (fit search per-step probe)"
  severity: minor
  test: 4
  root_cause: "DIAGNOSED (grep: zero RenderTexture.active assignments or restores anywhere in package code). Graphics.Blit leaves RenderTexture.active pointing at its destination; the fit search's per-step probe decomp pipeline destroyed its pooled RTs on dispose while one was still the global active target — Unity warns on release. Benign-looking but leaves the device pointed at a destroyed target until the next global assignment."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/ComputeTexturePool.cs"
      issue: "DestroyRenderTexture released RTs that could still be RenderTexture.active (Graphics.Blit destination leak)"
  missing:
    - "Unbind RenderTexture.active in DestroyRenderTexture before Release when it points at the RT being destroyed"
  resolution: "FIXED: DestroyRenderTexture now sets RenderTexture.active = null when it == rt before rt.Release(). No dedicated test (single guard line on the static dispose path); covered by the full regression re-run."

- truth: "The EditMode suite must be independent of the user's live editor session — byte-exact fixtures pin every NamerProcessor pref their premise depends on"
  status: resolved
  reason: "Full regression re-run 2026-09-15 after the UAT fixes: 117/119 — AssetGeneratorTests.ImportStamping_SurfaceLinearUncompressedPointNoMips_AndPackedAlphaSurvives (packed alpha expected 159, got 149) and NamerRoughnessFitTests.FitDriven_DefaultOn_DecomposedNoMapAsset_ExtractsAndDropsResidual (residual not collapsed). Both were green in the 118/118 run the day before."
  severity: minor
  test: 1
  root_cause: "DIAGNOSED (live pref dump via RunCommand). Not production defects — live EditorPrefs leakage. The user's active session since the 118/118 run left NamerProcessor.RoughnessEstimator = 1 (Sobel; default 0 = FitDriven), RoughnessExtractStrength = 0.2548 (default 1), and four non-default AO values (AoContrast moved 0.513 → 0.472). NamerComputePipeline extracts whenever RoughnessExtractStrength > 0 — independent of decomposition — so ImportStamping's scalar-alpha premise (0x80 | 31 = 159) ran through Sobel extraction (packed bits 21 → 149), and FitDriven_DefaultOn's 'DefaultOn' premise ran through the Sobel path (no fit → residual kept). Both fixtures relied on ambient prefs holding their defaults."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/AssetGeneratorTests.cs"
      issue: "ImportStamping left RoughnessExtractStrength ambient (and the file's PrefsSnapshot did not cover that key, so pinning it would have clobbered the user's live value)"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs"
      issue: "FitDriven_DefaultOn's initializer comment said Estimator/Strength 'left at DEFAULT' — i.e. dependent on ambient user prefs (snapshot/restore already covered the keys; only the pin was missing)"
  missing:
    - "ImportStamping: pin RoughnessExtractStrength = 0 (scalar pack premise) + extend the file's PrefsSnapshot/Capture/Restore to cover the key"
    - "FitDriven_DefaultOn: pin RoughnessEstimator = FitDriven and RoughnessExtractStrength = default explicitly"
  resolution: "FIXED (fixture-only, no production change): both pins applied; AssetGeneratorTests' PrefsSnapshot extended with RoughnessExtractStrength (+Had flag) through capture/restore so the user's live slider value survives the suite. Same convention as the white-occlusion isolation family: tests neutralize live user EditorPrefs. VERIFIED: full EditMode regression 119/119 (Temp/namer-regression4-results.txt, 2026-09-15)."

- truth: "Re-processing a hierarchy that mixes a source-wearing renderer with its own NAMER-generated material counts ONE material — inspection deduplicates on the resolved D-04 source, not the collected instances"
  status: resolved
  reason: "User challenged the CR-01 warning (2026-09-15): 'I don't think it has two materials, it has one. It might have two because you are adding the NAMER material?' — still seeing 'maps 2 material(s) to 1 source mesh(es)' on Neo-T-Pose reprocess."
  severity: major
  test: 4
  root_cause: "USER WAS RIGHT. Live scene dump (RunCommand): the Neo-T-Pose hierarchy has TWO renderers — the root MeshRenderer still wearing the SOURCE material (tripo_mat_d83278e6, URP/Lit) and the child SkinnedMeshRenderer wearing the GENERATED material (tripo_mat_d83278e6_Namer, NamerSource tag → the source). SourceInspector.Inspect ran D-04 recovery per collected material (generated → resolves back to source, correct) but deduplicated only the COLLECTED instances (the `seen` HashSet) — source and its generated material are different instances that both resolve to the same source, so both were added → model.Materials.Count = 2 → CR-01 tripped on every reprocess of a partially-bound hierarchy. The '2 material(s)' in the warning literally counted the NAMER material alongside its own source."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/SourceInspector.cs"
      issue: "Inspect deduped collected material instances but not RESOLVED sources — source + its generated material double-counted"
  missing:
    - "Dedup on the resolved source instance ID before adding each inspection unit"
    - "Regression test: hierarchy with one renderer on the source + one on its generated material inspects as ONE unit"
  resolution: "FIXED test-first: SourceInspectorTests.Dedupe_SourceAndItsGeneratedMaterialCountOnce mirrors the user's hierarchy (renderer A on source, renderer B on generated with label + NamerSource tag) and asserts Materials.Count == 1 — verified RED (got 2) then GREEN after the fix (Temp/namer-dedupe-results.txt / -dedupe2-results.txt). Fix: `resolvedSeen` HashSet on source.GetInstanceID() in Inspect's resolution loop, with a comment documenting the partial-bind reprocess case. Full EditMode regression 120/120 after the fix (Temp/namer-regression5-results.txt, 2026-09-15)."

- truth: "The fit-driven roughness TEXTURE is the Blender-parity Sobel edge signal (global-max normalized) adopted by the searched strength — lerp(scalarRoughness, sobel, fit.Strength) — not a strength-scaled high-pass detail remap"
  status: resolved
  reason: "User (2026-09-15): 'fit-driven gives a very patchy roughness map and makes residual not required ... With Sobel it looks much better, but then the residual is always required.'"
  severity: major
  test: 4
  root_cause: "The roughness SIGNAL SOURCE. The fit-driven path reused its frequency-separation high-pass detail as the roughness texture (saturate(strength * length(sharpDetail))), which turns noise/freckle detail into roughness patches; the Sobel estimator's hierarchical global-max normalization concentrates the response at real edges (the look the user prefers). Meanwhile Sobel-mode could never drop the residual (parity-only, no base cleaning) — the preferred look and the one-texture collapse were split across two estimators. USER DESIGN DECISION (AskUserQuestion): 'Sobel + fit search'."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute"
      issue: "CSRoughnessRemap emitted saturate(_Strength * length(_SharpDetail)) — the patchy high-pass signal"
  missing:
    - "CSRoughnessRemap = lerp(_ScalarRoughness, _SobelRoughness[id].r, _Strength) fed by the standalone Sobel run"
    - "RunFrequencySeparation to produce roughness via ExtractSobel + the adoption dispatch, scalar threaded from inspection.Roughness (the CSNormalize no-map branch), fit mechanics unchanged"
    - "A test proving fit-driven texels == lerp(scalar, standalone-Sobel texels, TryGetFitStrength) within one quantization step"
  resolution: "FIXED (plan 04.1-06): CSRoughnessRemap repurposed to the Sobel-adoption lerp (new _SobelRoughness texture + _ScalarRoughness float bindings; the detail-remap body retired); RunFrequencySeparation keeps blur + sharpDetail + SharpRemoval for the cleaned base (ladder search / FitOnlyMaxError gating untouched) and produces roughness = ExtractSobel(baseColorOut) + adoption dispatch, scalar = inspection.Roughness threaded from ExtractFitDriven. VERIFIED: NamerRoughnessFitTests.FitDriven_Roughness_UsesSobelEdgeSignal GREEN (RED evidence = the user's live patchy-map report; the live TestRunnerApi bridge was down with relay refusals, so the standing constraint accepting a live user report as red evidence applied) in the full 122/122 headless-with-graphics regression (/tmp/namer-06-regression.xml, 2026-09-15)."

- truth: "The surface pack adopts the fit-driven extracted texture at FULL strength — the Roughness Extract Strength slider blends ONLY the Sobel path; in fit-driven it merely gates extraction on (> 0)"
  status: resolved
  reason: "User (2026-09-15): 'Roughness Extract Strength still changes the Roughness texture and the shaded model is about 50% too dark in that mode if I move the slider, if I process with NAMER button then it looks close, but then as soon as I move the roughness extract strength the shaded final model is 50% + too dark.'"
  severity: major
  test: 4
  root_cause: "NAMERPack CSSurfacePack lerped roughness by _RoughnessExtractStrength unconditionally. Correct for Sobel (strength-free texture; slider = the D-02 adoption blend) but wrong for fit-driven: RunFrequencySeparation already baked the SEARCHED strength into both the cleaned base and the texture, and the fit validated that exact pairing — re-blending by the user slider under-applied the searched response (live slider 0.2548 packed ~25% of it) and made a control that cannot drive the fit look like it did."
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs"
      issue: "BindAndDispatchSurfacePack always received inspection.RoughnessExtractStrength"
  missing:
    - "Pass isFitDriven ? 1f : inspection.RoughnessExtractStrength to the pack"
    - "A test asserting fit-driven packed alpha bits 0-5 == extracted bits (within 1 quantization step), not lerp(scalar, extracted, userSlider)"
    - "UI truth: slider tooltip split per estimator + a readout of the cached searched strength"
  resolution: "FIXED: the pack blend is isFitDriven ? 1f : slider (NamerComputePipeline); UI truth added — slider tooltip split per estimator, estimator tooltip updated, and a fit-driven HelpBox readout of the cached searched strength via NamerRoughnessPipeline.TryGetFitStrength (exposed through NamerComputePipeline). VERIFIED: NamerRoughnessFitTests.FitDriven_PackedRoughness_AdoptsSearchedStrengthNotSliderBlend GREEN in the 122/122 regression (/tmp/namer-06-regression.xml, 2026-09-15)."
