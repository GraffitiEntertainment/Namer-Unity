---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
verified: 2026-09-16T20:52:24Z
status: human_needed
score: 30/30 must-haves verified (12 goal truths + 4 plan-06/07 truths + 7 gap-closure truths + 7 review-fix truths)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 19/19
  gaps_closed:
    - "All 19 previously-verified truths re-checked against the CURRENT tree (quick regression): no regressions — the plan-06/07 redesign and the round-2 review fixes were layered on top without breaking any previously-passing mechanism"
    - "Post-verification change set 1 (84af782): fit-strength HelpBox null-inspection guard verified at NamerEditorWindow.cs:893 (`&& inspection != null`)"
    - "Post-verification change set 2 (c7ffda2..12d7de7): CR-01 three-way residual bind, WR-01 cancelled-fit surfacing + FitDriven_CancelledFit_SurfacesCancelledFlag, WR-02 12-element fit-cache key (occlusionMapId + AssetContentStamp), WR-03 slider/constants copy, WR-04 SaveAssetIfDirty, public pool-ownership flag — all verified in code (see Review-Fix Truths)"
    - "Final regression evidence: Temp/namer-041-reg3-results.txt TOTAL passed=124 failed=0 skipped=0, FINISHED 2026-09-16T20:46:21Z — postdates the last source edit (NamerComputePipeline.cs mtime 13:40:05 -0700 = 20:40:05Z, commit 12d7de7); Packages/ tree is clean (zero uncommitted source changes)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "UAT round 5 (pending, 04.1-UAT.md `### 5.`): with the anchored-inverted revision live, run Process on the Neo scene instance (tripo_node_d83278e6) in the live editor"
    expected: "Quiet regions render at approximately the authored scalar matte (roughly 0.97 for _Smoothness: 0 — NOT the round-4 mirror gloss); sharp edges and baked-specular rims dip glossy; the model stays visible across a double Process; press-1 output matches the preview window; no 'Releasing render texture that is set to be RenderTexture.active!' warning"
    why_human: "Visual/acceptance outcome on the user's real scene asset. Every mechanism (dip formula, anchor invariant, mesh in-place overwrite, RT-active unbind) is code-verified and covered by the 124/124 regression; pixels on screen are not machine-checkable from the verifier."
  - test: "Fit-driven preview parity after the CR-01 binding change: select a no-map source with decomposition enabled, let the preview extract, and compare the shaded after-pane against the generated .mat in the scene"
    expected: "Preview and Process output agree — the collapsed-residual one-texture case renders _BaseColor x fitted vertex color (no base double-multiply darkening); the Extracted Roughness debug toolbar entry shows the grayscale anchored-inverted roughness (quiet = light/matte, edges = dark/glossy)"
    why_human: "The CR-01 fix is a binding-logic change whose own fix report marks it 'requires human verification'; rendering equivalence between an in-memory preview material and a serialized asset has no automated test."
  - test: "Interactive cancel UX (WR-01): start a Process that triggers the fit-driven strength search, cancel the progress bar"
    expected: "A warning 'NAMER: roughness fit cancelled for material ... — assets written WITHOUT roughness extraction.' appears in the console; assets are still written (identity roughness); slider drags in the preview never pop a cancelable progress bar"
    why_human: "The propagation chain is regression-tested end-to-end (FitDriven_CancelledFit_SurfacesCancelledFlag in the 124/124 run), but the interactive EditorUtility progress-bar branch only executes in the live editor."
---

# Phase 04.1: Baked-Response Roughness Extraction + Zero-Residual One-Texture — Verification Report

**Phase Goal:** Extract material response (gloss/shading/cavity) baked into the base texture as 6-bit roughness (surface alpha bits 0-5) when no authored roughness map exists — via a Sobel Blender-parity estimator and a fit-driven strength search — then refit the sharp-removal-cleaned base into vertex colors so the Phase-4 D-13 residual auto-drop becomes the primary one-texture path (one RGBA8 surface PNG + vertex-colored mesh, no residual), with a D-06 optional roughness-offset escape hatch.
**Verified:** 2026-09-16T20:52:24Z (post-plan-change verification against the CURRENT tree)
**Status:** human_needed
**Re-verification:** Yes — third round. Previous: 2026-09-15T19:31:24Z (human_needed, 19/19). This round covers changes landing AFTER that report: plan 04.1-07 (anchored-inverted dip-depth), commit 84af782 (HelpBox null guard), the round-2 code-review fixes (c7ffda2..12d7de7), and the final 124/124 regression.

## Verification Method

Every claim below was checked directly against the working tree (not SUMMARY/UAT/REVIEW-FIX prose): compute kernels read in full, C# wiring read at the cited hunks, test bodies read for vacuousness, commit diffs inspected (84af782 shown in full), and the timeline reconstructed from commit timestamps and file mtimes. The Unity suite cannot be re-run from this verifier (launching the editor is forbidden); execution evidence rests on `Temp/namer-041-reg3-results.txt` — `TOTAL passed=124 failed=0 skipped=0`, FINISHED 2026-09-16T20:46:21Z — which postdates the last source edit (NamerComputePipeline.cs, commit 12d7de7 at 20:40:05Z) with a clean `Packages/` tree, so the run covers the exact tree verified here. Caveat noted honestly: this results file carries only the TOTAL summary line (74 bytes), not per-test PASS lines like the earlier 120/120 artifact; corroboration is the count arithmetic (123 in reg2 after 84af782, +1 for the new cancel test = 124) and the established TestRunnerApi file-channel pattern (memory: file markers, not console logs).

**Design-evolution note (goal wording vs final design):** the goal text says "Sobel Blender-parity estimator". Two deliberate, user-sanctioned parity deviations landed through the phase's own plan process after live UAT rounds: (1) p90 robust normalization instead of np.max (round 4, UAT-resolved), and (2) anchored-inverted polarity — quiet texels stay at the authored scalar, edges dip toward gloss (round 5, CONTEXT D-08/D-09, plans 04.1-06/04.1-07, ROADMAP waves 6-7 all record it). The operative must-haves are plan 07's, which supersede plan-01's "global-max, flat~0" premise; the goal's intent (extract baked response as 6-bit roughness driving the one-texture path) is achieved. Not a gap; no override needed because no plan-07 must-have fails.

## Goal Achievement

### Observable Truths (12 goal truths — current design)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | No-map source + extraction enabled packs per-texel roughness into surface alpha bits 0-5 (not scalar `_Roughness`) | ✓ VERIFIED | Gate `NamerComputePipeline.cs:116` (`MetallicGlossMap == null && RoughnessExtractStrength > 0`); CSSurfacePack `_HasExtractedRoughness` override branch (`NAMERPack.compute:116-130`). Tests: `Sobel_FlatBase_YieldsAuthoredScalarRoughness` (asserts packed alpha bits, pool-baseline leak check) in the 124/124 run. |
| 2 | Authored `MetallicGlossMap` produces byte-identical packed output (extraction never runs — D-01) | ✓ VERIFIED | Same gate short-circuits; `AuthoredMetallicGlossMap_IsByteIdentical_NoExtraction` asserts per-texel RGBA byte equality strength-0 vs strength-1. |
| 3 | Sobel estimator: Rec.601 luminance, 3x3 Sobel, hierarchical 8x8 max-reduce, robust p90 normalize — final mapping anchored-inverted per D-08/D-09 (supersedes plan-01 global-max Blender parity) | ✓ VERIFIED | `NAMERRoughness.compute`: `CSRoughnessSobel` (Gx/Gy coefficients correct), `CSRoughnessMaxReduce` (`.g = max` body), `CSRoughnessNormalize` (direct `mag/max(_GlobalMax,1e-4)`); `NamerRoughnessPipeline.RobustSobelScale` (p90 of 256x256 subsample, clamped `[1e-4, trueMax]`); `NamerRoughness.hlsl` Rec.601 weights (0.299/0.587/0.114). Tests: three flipped premises pass (see plan-07 truths). |
| 4 | Fit-driven estimator walks deterministic ascending ladder, picks FIRST passing strength (not argmin) | ✓ VERIFIED | `NamerRoughnessFitter.StrengthLadder` fixed `{0, .15, .3, .5, .7, .9, 1}` (static readonly, no Random); `FitDriven_SelectsFirstPassingStrength` proves 0.3-first-pass vs argmin-0.5 AND the exact visited order (0.0, 0.15, 0.3, stop). |
| 5 | The search measures the fit-only D-13 statistic (FitOnlyMaxError), not the vacuous with-residual MaxError | ✓ VERIFIED | `NamerProcessor.EvaluateRefitMaxError` returns `probeOutput.Stats.FitOnlyMaxError` (cs:551); doc comment states exactly why with-residual must not feed the search; the test-harness `ComposeEvaluate` mirrors it. |
| 6 | D-04 NamerAOBaker pattern: deterministic tables, injectable shouldCancel, off-debounce `RequestFit`, caching, caller-owned buffers (documented managed-loop vehicle deviation) | ✓ VERIFIED | `RequestFit` retained (`NamerRoughnessPipeline.cs:212`), `_fitCache` keyed store, `TryGetFitStrength` readout, `shouldCancel` poll in `Fit`. Note: `RequestFit` currently has zero callers (IN-01, info) — the shipped wiring uses the synchronous cache-miss search inside `ExtractFitDriven`, which shares `MakeFitKey`. |
| 7 | Vertex-color refit consumes the post-extraction (sharp-removal-cleaned) base (D-05) | ✓ VERIFIED | `NamerComputeResult.NormalizedBaseColor = cleanedBase != null ? cleanedBase : baseColorOut` with `NormalizedBaseColorOwnedByRoughnessPool` routing the release to the roughness pool (`NamerComputePipeline.cs:196-205, 276-286`). |
| 8 | Fit-driven falls to identity legacy path when decomposition off or mesh unresolved (1A); authored/zero-strength byte-identical; preview/Process share the skip predicate | ✓ VERIFIED | 1A gate at cs:168 (`isFitDriven && BakeSourceMesh != null && evaluate != null`); shared `NamerProcessor.DecompositionSkipReason` consumed by both `Process` (cs:119-126) and the window (`_decompGuardReason` cs:215, `decompWillRun` cs:332). |
| 9 | Editor window: strength slider + estimator popup, EditorPrefs-persisted, debounced; preview actually extracts (FitDriven) | ✓ VERIFIED | `DrawRoughnessExtractionSection` (cs:849-908) writes `_settings.RoughnessExtractStrength/RoughnessEstimator`; `RecomputePreview` composes the same evaluate callback as `NamerProcessor` (cs:342-363) with `BakeSourceMesh` attached; settings keys in `NamerProcessorSettings.cs:25-32`. |
| 10 | Processor window: scroll + 6 persisted foldouts + Extracted Roughness debug channel (D-07) | ✓ VERIFIED | `BeginScrollView` (cs:674) + Source/Preview-Debug/Roughness-Extraction/AO/Decomposition/Output foldouts (cs:688-1092), all EditorPrefs-backed; `NamerDebugView.shader` channel 9 `_ExtractedRoughness` (lines 8, 60-61, 147-149); `DebugChannelLabels_AppendsExtractedRoughnessAtIndex10` + `FoldoutDefaults_*` smoke tests. |
| 11 | Extraction-processed asset with collapsed fit: `_BaseResidualMap` UNBOUND, no residual EXR, Base PNG written, vertex-colored mesh (one-texture outcome) | ✓ VERIFIED | `AssetGenerator.cs:199` (`baseResidualPath = decomp != null ? residualWritePath : basePath` — null when collapsed) + guarded bind at cs:593-596; `FitDriven_DefaultOn_DecomposedNoMapAsset_ExtractsAndDropsResidual` asserts empty ResidualTexturePath, null `_BaseResidualMap`, written MeshPath; `OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound`. |
| 12 | Genuine albedo detail surviving extraction still falls back to a bound residual (D-13 honest gate); D-06 `_RoughnessOffsetMap` additive + neutral-when-unset | ✓ VERIFIED | `HonestGate_AlbedoDetail_StillRequiresResidual`; `NAMER.shader:13` `"black" {}` default pinned by `GetPropertyTextureDefaultName == "black"` (WR-05, `NamerOneTextureTests.cs:151-158`); `NamerSurface.hlsl:98` additive `saturate(roughness + offset.r)` applied after the decode macro; guarded `SetTexture` at `AssetGenerator.cs:601-604`; `RoughnessOffset_Set_BindsMaterialTexture` (persisted asset). |

### Plan-06/07 Truths (anchored-inverted revision — full verification, post-dates previous report)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 13 | D-08 anchor invariant: quiet texels pack at approximately the authored scalar (matte); edge texels dip strictly BELOW the anchor at BOTH consume sites | ✓ VERIFIED | `CSRoughnessRemap` = `saturate(_ScalarRoughness - _Strength * _SobelRoughness[id].r)` (compute :228-237, scalar threaded from `inspection.Roughness` at pipeline cs:438); CSSurfacePack Sobel branch = `saturate(roughness - _RoughnessExtractStrength * _RoughnessTex.r)` with `_RoughnessDipApplied` selecting fit-driven adopt-as-is (`NAMERPack.compute:116-130`, uniform threaded at `NamerComputePipeline.cs:436`). Tests: `Sobel_FlatBase_YieldsAuthoredScalarRoughness` asserts `maxBits >= 62` (scalar 1.0 anchor); `Sobel_SharpEdge_DipsBelowFlatRoughness` asserts `edgeBits < flatBits`; `Sobel_SparseExtremeEdges_SmoothMedianStaysAtScalar` asserts `medianBits >= 62 && edgeBits < medianBits`. |
| 14 | D-09: estimator dropdown chooses WHO picks the strength, never polarity; tooltip drops the Blender-parity claim | ✓ VERIFIED | `NamerEditorWindow.cs:879` tooltip states the anchored-inverted mapping and "This dropdown only chooses who picks the strength"; both routes share `ExtractSobel` output. |
| 15 | D-10: fit-driven pack adoption stays full-strength (`isFitDriven ? 1f`) | ✓ VERIFIED | `NamerComputePipeline.cs:191` unchanged; `FitDriven_PackedRoughness_AdoptsSearchedStrengthNotSliderBlend` asserts packed bits == extracted bits within 1 quantization step at slider 0.3 (non-vacuous: compares against the extracted texture itself). |
| 16 | Fit-driven texture = standalone-Sobel signal transformed by the searched strength; proved against an independently produced Sobel run | ✓ VERIFIED | `FitDriven_Roughness_UsesSobelEdgeSignal` asserts per-texel `fitTex == Clamp01(scalar - fitStrength * sobelTex)` within 2/255 (0 violations), with `fitStrength > 0` guard against vacuity; `RunFrequencySeparation` runs `ExtractSobel(baseColorOut)` (NOT the cleaned base) + the remap dispatch (pipeline cs:435-441). |

### Gap-Closure Truths (previous verification — quick regression, all intact)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 17 | Double-Process keeps the renderer mesh reference (no vanish) | ✓ VERIFIED | `WriteMeshAsset` in-place branch: `existing.Clear(false)` + `ApplySplitStreams` + `SetDirty` + `SaveAssetIfDirty` (cs:475-482); bindposes-before-boneWeights in `ApplySplitStreams` (cs:548-557); `DoubleProcessWithDecomposition_KeepsRendererMeshReferenceValid` in the run. |
| 18 | Pool dispose unbinds an active RenderTexture before release | ✓ VERIFIED | `ComputeTexturePool.cs:124-127` guard present. |
| 19 | EditMode suite independent of live user EditorPrefs | ✓ VERIFIED | `PrefsSnapshot` capture/restore + pinned `RoughnessEstimator`/`RoughnessExtractStrength` in `FitDriven_DefaultOn` (fit tests cs:364, 383-389). |
| 20 | Inspection dedupes on the RESOLVED D-04 source | ✓ VERIFIED | `SourceInspector.Inspect` `resolvedSeen` HashSet on `source.GetInstanceID()` (cs:60-69). |
| 21 | Round-1 review fixes intact: 10→12-element fit key, Resample keyword disable, catch-rethrow lease releases, shader-default pin | ✓ VERIFIED | Resample forces `_REENCODE_SRGB` off around the blit (`Editor/Decompose/NamerDecompPipeline.cs:515-526`); catch-path releases in `NamerComputePipeline.Process` (cs:207-233), `ExtractSobel` (cs:333-342), `RunFrequencySeparation` (cs:445-452). |
| 22 | Scene-asset selection degrades gracefully (.unity guard, no ReadObjectThreaded) | ✓ VERIFIED | `SourceInspector.cs:260-262` (warning) + `:314` (AddSubAssetMaterials guard); `SceneAsset_Selection_DegradesGracefullyWithoutThrowing`. |
| 23 | Degenerate generated mesh never bound; skinned bindposes ordered | ✓ VERIFIED | Bindposes reorder in `ApplySplitStreams`; degenerate-bounds guard in `NamerProcessor.BindGeneratedMaterials`; `NamerDecompIntegrationTests` renderability assertions in the run. |

### Review-Fix Truths (round-2 code review, commits c7ffda2..12d7de7 + 84af782 — full verification)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 24 | CR-01: preview shows the one-texture shape the saved material has (no base x vertex-color double multiply) | ✓ VERIFIED | Three-way branch in `RecomputePreview` (`NamerEditorWindow.cs:377-395`): residual produced -> bind Residual; `_decompOutput == null` -> bind previewBaseMap (Phase-3 shape preserved — smarter than the review's literal two-way fix); decomp ran + residual collapsed -> bind null. Commit c7ffda2. |
| 25 | WR-01: cancelled fit never silent — Cancelled propagates end-to-end, warning on write, no cancelable progress bar on preview recomputes, ClearProgressBar only when interactive | ✓ VERIFIED | `NamerRoughnessExtractResult.Cancelled` (pipeline cs:29, returned cs:383, never cached) -> `NamerComputeResult.RoughnessFitCancelled` (compute cs:115, 177, 202, public) -> `Debug.LogWarning` in `NamerProcessor.RunPipeline` (cs:175-183); window passes `() => false` (cs:363); `Fit` tracks `interactive = shouldCancel == null` and clears the bar only in that branch (fitter cs:84, 113-116). Regression `FitDriven_CancelledFit_SurfacesCancelledFlag` (fit tests cs:156-201) asserts Cancelled + null ExtractedRoughness + identity base — included in the 124/124 count (123 -> 124). Commits 37a17f0 + 8a5555a. |
| 26 | WR-02: fit cache key includes occlusion-map identity and base-map content stamp | ✓ VERIFIED | `MakeFitKey` 12-tuple with `occlusionMapId` (instance ID, 0 when null) + `baseMapStamp` from `AssetContentStamp` = `File.GetLastWriteTimeUtc(path).Ticks`, 0 for non-persistent textures (pipeline cs:627-668); key/comment documents the invalidation contract. Commit a1c8fcc. |
| 27 | WR-03: tooltip/doc no longer claim strength > 0 unconditionally enables extraction | ✓ VERIFIED | Slider tooltip: "requires Vertex Color Decomposition to be enabled — with decomposition off, the scalar roughness is used unchanged" (window cs:862-866); `DefaultRoughnessExtractStrength` doc states the decomposition requirement (constants cs:49-51). Commit af3dd7f. |
| 28 | WR-04: in-place mesh persist scoped to the mesh only (no global SaveAssets flush) | ✓ VERIFIED | `EditorUtility.SetDirty(existing); AssetDatabase.SaveAssetIfDirty(existing);` with the explanatory comment (AssetGenerator cs:477-482); no `SaveAssets()` remains in the write path (grep confirms). Commit e0603dd. |
| 29 | 84af782: fit-strength HelpBox null-inspection guard (latent NRE fix) | ✓ VERIFIED | `if (_roughnessEstimator == FitDriven && inspection != null)` (window cs:893); commit diff inspected — 1-line guard, rationale documented (domain-reload NRE failed 22 tests pre-fix). |
| 30 | 12d7de7: pool-ownership flag public so the WR-01 regression test compiles | ✓ VERIFIED | `public bool NormalizedBaseColorOwnedByRoughnessPool` (`NamerComputePipeline.cs:577`); the cancel test asserts it (cs:186). |

**Score: 30/30 truths verified.**

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `Compute/NAMERRoughness.compute` | 8 kernels: Sobel, MaxReduce, Normalize, BlurH/V, SharpDetail, SharpRemoval, Remap (dip-depth) | ✓ VERIFIED | 237 lines, all 8 kernels substantive, dip-depth remap + direct-polarity normalize |
| `Compute/NAMERRoughness.hlsl` | Rec.601 luminance helper | ✓ VERIFIED | Weights 0.299/0.587/0.114 with do-not-reuse-NAMER_LUMA comment |
| `Compute/NAMERPack.compute` | CSSurfacePack roughness override + `_RoughnessDipApplied` branch | ✓ VERIFIED | Two-branch consume site verified |
| `Editor/Bake/NamerRoughnessFitter.cs` | Deterministic ladder + first-passing Fit + injectable cancel | ✓ VERIFIED | 121 lines, ladder + interactive guard |
| `Editor/Pipeline/NamerRoughnessPipeline.cs` | Sobel + fit-driven extraction, p90 scale, 12-element fit cache | ✓ VERIFIED | 692 lines, all wiring traced |
| `Editor/Pipeline/NamerComputePipeline.cs` | D-01/1A gates, D-05 repoint, D-10 pin, cancel propagation | ✓ VERIFIED | All cited lines confirmed |
| `Editor/Pipeline/NamerProcessor.cs` | Settings copy, evaluate compose, FitOnlyMaxError, skip predicate, cancel warning | ✓ VERIFIED | cs:119-183, 533-559 |
| `Editor/UI/NamerEditorWindow.cs` | Foldout/scroll window, extraction controls, preview parity, tooltips | ✓ VERIFIED | cs:325-424, 671-1092, 849-908 |
| `Shaders/NAMER.shader` + `NamerSurface.hlsl` + `NamerDebugView.shader` | D-06 offset input + channel 9 | ✓ VERIFIED | "black" default, additive offset, extracted-roughness branch |
| `Tests/Editor/NamerRoughness{Extraction,Fit}Tests.cs`, `NamerOneTextureTests.cs`, `NamerEditorWindowSmokeTests.cs` | Behavior coverage of every truth above | ✓ VERIFIED | 12 phase tests read in body — substantive, formula-level assertions |

### Key Link Verification

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| CSNormalize `_BaseColorOut` | CSSurfacePack `_PackInputs.y` override | NamerRoughnessPipeline extraction slot between normalize and pack | ✓ WIRED (compute cs:154-192) |
| NamerRoughnessFitter ladder | per-strength GPU eval + refit | `Func<float,float>` evaluate composed by NamerProcessor (3A), reentrant via `_currentBaseColorOut` | ✓ WIRED (processor cs:157-172, compute cs:339-342) |
| Fit search objective | D-13 statistic | `EvaluateRefitMaxError` -> `Stats.FitOnlyMaxError` | ✓ WIRED (processor cs:551) |
| AssetGenerator.WriteMaterial | NamerSurface decode | `_BaseResidualMap` null-on-collapse + guarded `_RoughnessOffsetMap` | ✓ WIRED (cs:199, 593-604) |
| Preview material | saved-material shape | three-way `_BaseResidualMap` bind mirroring `Generate` | ✓ WIRED (window cs:377-395) |
| Cancel state | user visibility | Cancelled -> RoughnessFitCancelled -> LogWarning | ✓ WIRED (pipeline cs:383 -> compute cs:202 -> processor cs:180) |
| Debug toolbar channel 10 | shader channel 9 | `SetChannel(Max(0, idx-1))` -> `_DebugChannel >= 8.5` branch | ✓ WIRED (window cs:423-424, debug shader cs:147-149) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Packed surface alpha bits 0-5 | `_RoughnessTex` | live GPU Sobel of the cleaned base (pool-leased RT) | Yes — per-texel, read back and asserted in tests | ✓ FLOWING |
| Fit-driven strength | `fit.Strength` | ladder search over real refit MaxError (GPU sharp-removal -> readback -> VertexColorFitter -> GenerateResidual) | Yes — `FitDriven_Roughness_UsesSobelEdgeSignal` reads it back via `TryGetFitStrength` and asserts fitStrength > 0 | ✓ FLOWING |
| Fit-cache key | `MakeFitKey(...)` | mesh/base/occlusion instance IDs + AO controls + file mtime stamp | Yes | ✓ FLOWING |
| Window fit readout | `TryGetFitStrength` | `_fitCache` populated by real searches | Yes (null-guarded when no inspection) | ✓ FLOWING |
| One-texture material | `baseResidualPath` | residual write decision from measured fit error | Yes — honest-gate test proves the negative case keeps a residual | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full EditMode regression | (live editor, TestRunnerApi file channel — verifier cannot launch Unity) | `Temp/namer-041-reg3-results.txt`: `TOTAL passed=124 failed=0 skipped=0`, FINISHED 2026-09-16T20:46:21Z | ✓ PASS (artifact; run postdates every source edit — see Method) |
| Compile cleanliness | inferred | EditMode suites cannot execute against a non-compiling tree; the 13:46 run follows the 13:40 last edit | ✓ PASS |
| Commits exist | `git log` | 84af782, c7ffda2, 37a17f0, 8a5555a, a1c8fcc, af3dd7f, e0603dd, 12d7de7, fb33879 all present on the phase branch | ✓ PASS |

Step 7b note: no runnable entry point exists outside the Unity editor; all spot-checkable behavior routes through the regression artifact above.

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| (none) | — | No probes declared in any PLAN/SUMMARY; no `scripts/*/tests/probe-*.sh` in the repo | N/A |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| ----------- | ------------ | ----------- | ------ | -------- |
| ENCD-02 | 01, 03, 05, 06, 07 | Packed surface A bits 0-5 = 6-bit roughness | ✓ SATISFIED (strengthened) | Extraction override at pack; scalar/anchored paths byte-stable; authored-map byte-identity test |
| NORM-01 | 01, 02, 04, 06, 07 | Cleaned base with baked lighting removed where feasible | ✓ SATISFIED (strengthened) | Sharp-removal cleaned base (D-05) feeds the refit |
| NORM-03 | 01, 02 | Per-pixel processing via compute, not C# loops | ✓ SATISFIED | All extraction/pack math in HLSL kernels; CPU reads only tiny reduce/subsample buffers (documented one-shot scalar reductions) |
| VCOL-03 | 01, 02, 03, 04 | Residual from fitted-vs-source difference | ✓ SATISFIED (strengthened — D-13 auto-drop now primary) | One-texture tests + honest gate |
| VCOL-04 | 02, 05, 06, 07 | Reconstruction-error stats + debug visualization | ✓ SATISFIED | FitOnlyMaxError statistic drives the search; Error Heatmap channel |
| VCOL-05 | 02, 05 | Adaptive residual resolution | ✓ SATISFIED | Residual-resolution search untouched by the phase; fit ladder adapts extraction strength |
| UI-03 | 02, 04, 05, 06, 07 | Processor controls incl. roughness extraction | ✓ SATISFIED (phase slice; REQUIREMENTS.md correctly still In Progress — style controls are Phase 5) | Estimator/strength controls + foldout window + fit readout |
| SHDR-02 | 03 | Vertex-color reconstruction, base/residual | ✓ SATISFIED (strengthened — D-06 offset escape hatch) | Neutral-when-unset pinned; additive offset verified |

No orphaned requirements: all 8 phase IDs are claimed across plans; REQUIREMENTS.md maps no additional IDs to 04.1.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `NamerRoughnessPipeline.cs` | ~398-403 | Stale doc: `RunFrequencySeparation` summary still says "lerp(scalarRoughness, sobel, strength)" — the retired plan-06 formula; the kernel is dip-depth (plan 07) | ℹ️ Info | Doc-only; the code and tests assert the correct dip formula. Fix opportunistically (one comment block) |
| `NamerRoughnessPipeline.cs` | 212 | IN-01: `RequestFit` public API with zero callers (round-2 review, dispositioned out of fix scope) | ℹ️ Info | Dead code / divergence risk for the cache key; live path uses `ExtractFitDriven`'s internal key |
| `NamerRoughnessFitTests.cs` / `NamerEditorWindowSmokeTests.cs` | ~401 / ~74 | IN-05: two vacuous assertions remain (`packed.a & mask > 0` passes on scalar alone; `AreEqual(9, Max(0,10-1))` is constant arithmetic) | ℹ️ Info | Behavior covered non-vacuously by `AdoptsSearchedStrengthNotSliderBlend` (extracted-vs-packed bit equality) and `UsesSobelEdgeSignal` (formula equality); dispositioned out of scope |
| `NamerDebugChannelMaterial.cs` | 77 | IN-03: `SetChannel` doc says "0..8"; channel 9 exists | ℹ️ Info | Doc-only |
| Working tree | — | Uncommitted non-source changes: `Assets/NAMER/NamerSmoke.unity`, `Assets/NAMERGenerated/Neo-T-Pose/*` (incl. a new untracked `_Namer_Residual.exr` from live UAT runs), ProjectSettings, `.sln.DotSettings.user` | ℹ️ Info | Generated/test artifacts, not source; `Packages/` is fully clean. Orchestrator handles commits — do not commit the user's generated scene output blindly |

Debt-marker gate: TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER grep across all 17 phase-modified production files — zero matches.

### Human Verification Required

Three items remain (frontmatter `human_verification`):

### 1. UAT Round 5 — anchored-inverted live acceptance

**Test:** Run Process (twice) on the Neo scene instance (tripo_node_d83278e6) with default fit-driven settings.
**Expected:** Quiet regions render ≈ authored scalar matte (~0.97 for `_Smoothness: 0`); edges/baked rims dip glossy; model visible after BOTH presses; press-1 matches preview; no RT-active release warning.
**Why human:** Visual acceptance on the real asset; all mechanisms regression-tested, pixels are not.

### 2. CR-01 preview/material parity + Extracted Roughness channel

**Test:** Compare the shaded after-pane against the generated `.mat` for a collapsed-residual source; inspect the Extracted Roughness debug channel.
**Expected:** No double-multiply darkening in the preview; debug channel shows quiet=light/matte, edges=dark/glossy grayscale.
**Why human:** Binding-logic change marked "requires human verification" by its own fix report; render equivalence has no automated test.

### 3. WR-01 interactive cancel UX

**Test:** Cancel the fit progress bar during a live Process; drag sliders in the preview.
**Expected:** "NAMER: roughness fit cancelled ... assets written WITHOUT roughness extraction." warning appears; assets still written; no progress bar on preview slider drags.
**Why human:** The interactive `EditorUtility` branch only executes in the live editor; the propagation chain is already regression-tested.

The previous report's design-decision item (Sobel Blender-parity look) is RESOLVED: the user decided via UAT rounds 4/5 (p90 scale + anchored-inverted polarity), which spawned plans 04.1-06/04.1-07 — implemented and verified here.

### Gaps Summary

No code gaps. All 30 truths verified against the current tree: the 12 goal truths (under the plan-07 anchored-inverted design that supersedes plan-01's Blender-parity premise by documented user decision), the 4 redesign truths, the 7 gap-closure truths (no regressions), and the 7 review-fix truths including both post-plan change sets (84af782, c7ffda2..12d7de7). Executed-test evidence: full EditMode 124/124 / 0 fail / 0 skip, timestamped after the last source edit on a clean source tree. Four Info-level review leftovers (stale lerp doc, dead `RequestFit`, two vacuous assertions, `0..8` doc) remain open by recorded disposition — none affects the goal. UAT round 5 is intentionally PENDING: a `/gsd-explore` session (`.planning/notes/sobel-led-residual-guided-roughness.md`, `.planning/todos/pending/plan-sobel-led-roughness-redesign.md`) rejected the fit-driven LOOK and chose a downstream Gouraud-projection redesign — tracked follow-up outside this phase's implemented goal, not a 04.1 gap. Status is human_needed solely for the three visual/interactive acceptance items above.

---

_Verified: 2026-09-16T20:52:24Z_
_Verifier: Claude (gsd-verifier)_
