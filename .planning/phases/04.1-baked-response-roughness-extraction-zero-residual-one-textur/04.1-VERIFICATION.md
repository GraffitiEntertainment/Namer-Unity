---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
verified: 2026-09-06T17:49:04Z
status: human_needed
score: 7/12 must-haves verified
behavior_unverified: 5 # Present + wired; GPU round-trip tests authored + capability-gated but NOT executed (live editor holds project lock; open unrun-verify ids 1-4 in .planning/WINDOWS.md)
overrides_applied: 0
behavior_unverified_items:
  - truth: "A source with no authored roughness map (MetallicGlossMap == null) and extraction enabled packs per-texel Sobel roughness into surface alpha bits 0-5 instead of the scalar _Roughness"
    test: "Run NamerRoughnessExtractionTests.Sobel_FlatBase_YieldsNearZeroRoughness + Sobel_SharpEdge_YieldsHighRoughness in-editor (TestRunnerApi)"
    expected: "Flat base packs (alphaByte & 0x3F) <= 1; edge texel roughness > flat texel roughness"
    why_human: "GPU dispatch + packing round-trip; grep cannot see GPU output. Tests authored but unexecuted (no headless run possible — editor lock)."
  - truth: "A source with an authored MetallicGlossMap produces byte-identical packed surface output to the pre-04.1 path"
    test: "Run NamerRoughnessExtractionTests.AuthoredMetallicGlossMap_IsByteIdentical_NoExtraction in-editor"
    expected: "Packed surface RGBA bytes identical between RoughnessExtractStrength = 0f and 1f on an authored-map source"
    why_human: "Byte-identity is a runtime output invariant; the guard is statically provable (flag 0 => kernel no-op) but no executed test confirms GPU output."
  - truth: "Sobel roughness is normalized by the global max edge magnitude (Blender parity): flat ~0, sharp edge high"
    test: "Run the two Sobel GPU tests; independently verify a known-input fixture against Blender extract_roughness values"
    expected: "saturate(mag / max(globalMax, 1e-4)) behavior matches the kernel math read in NAMERRoughness.compute:130"
    why_human: "Hierarchical max-reduce + readback + normalize run on the GPU; the reduce chain's runtime correctness is not statically observable."
  - truth: "An extraction-processed asset whose refit collapses generates a material with _BaseResidualMap UNBOUND, no residual EXR, Base PNG still written"
    test: "Run NamerOneTextureTests.OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound (full NamerProcessor.Process on a decomposed baked-response no-map fixture with default settings)"
    expected: "GetTexture(\"_BaseResidualMap\") == null, ResidualTexturePath empty, BaseTexturePath written, MeshPath written (vertex-colored)"
    why_human: "End-to-end fit-driven collapse is a GPU + Burst + AssetDatabase runtime pipeline; only the mechanism (D-13 drop, unconditional base write) is statically verified."
  - truth: "An asset with genuine albedo detail that survives extraction still falls back to a bound residual (D-13 extended honest gate)"
    test: "Run NamerOneTextureTests.HonestGate_AlbedoDetail_StillRequiresResidual on a high-frequency base"
    expected: "ResidualRequired == true with a non-null residual and a bound _BaseResidualMap"
    why_human: "Depends on the runtime fit/residual threshold behavior under extraction, not on statically visible wiring."
human_verification:
  - test: "Execute the four phase test suites in the live editor via TestRunnerApi (filters: GraffitiEntertainment.Namer.Tests.NamerRoughnessExtractionTests, NamerRoughnessFitTests, NamerEditorWindowSmokeTests, NamerOneTextureTests) — or after closing the editor, headless: -batchMode -runTests -testPlatform EditMode"
    expected: "13/13 pass (3 extraction + 3 fit + 2 smoke + 4 one-texture + the second pure-CPU assertion block inside FitDriven_SelectsFirstPassingStrength); pool-returns-to-baseline assertions hold; then close open unrun-verify entries 1-4 in .planning/WINDOWS.md"
    why_human: "The live editor (PID 4715) holds Temp/UnityLockfile; no headless batchmode run is possible from this verifier. Test-execution claims are not evidence until run."
  - test: "Focus the live editor to force a recompile + reimport of the Wave-3 and review-fix changes (NamerOneTextureTests.cs, _RoughnessOffsetMap shader input in NAMER.shader/NamerSurface.hlsl/NamerDebugView.shader, CR-01 c84f6df, WR-01 7adfd41, WR-02 edf3b40) and inspect Editor.log"
    expected: "Zero 'error CS' entries; NAMER.shader, NamerDebugView.shader, NAMERRoughness.compute, NAMERPack.compute import with zero shader errors. (Editor.log mtime 10:18 predates these changes; only Waves 01-02 have compile evidence in the log.)"
    why_human: "Compile/import of the last wave + fixes is pending the editor's next refresh — cannot be confirmed from a stale log."
  - test: "Open Tools > NAMER > Processor with all sections collapsed and confirm the D-07 render claims"
    expected: "Window fits a small screen height with Source open + others collapsed on first launch; every control reachable by scrolling; Process button + status visible OUTSIDE the scroll view; 'Extracted Roughness' appears as the last debug toolbar entry and shows grayscale extracted roughness when extraction ran"
    why_human: "Render/layout quality has no automated UI test in the repo (stated plainly in PLAN 02 verification)."
  - test: "Neo acceptance pass: process a real baked-response no-map asset (decomposition ON, default settings) end-to-end"
    expected: "Output = one RGBA8 surface PNG + vertex-colored mesh + written-but-unbound Base PNG, NO residual EXR, material renders correctly"
    why_human: "The phase's motivating acceptance case is a visual/runtime outcome; static verification covers wiring only."
---

# Phase 04.1: Baked-Response Roughness Extraction + Zero-Residual One-Texture — Verification Report

**Phase Goal:** Extract material response (gloss/shading/cavity) baked into the base texture as 6-bit roughness (surface alpha bits 0-5) when no authored roughness map exists — via a Sobel Blender-parity estimator and a fit-driven strength search — then refit the sharp-removal-cleaned base into vertex colors so the Phase-4 D-13 residual auto-drop becomes the primary one-texture path (one RGBA8 surface PNG + vertex-colored mesh, no residual), with a D-06 optional roughness-offset escape hatch.
**Verified:** 2026-09-06T17:49:04Z
**Status:** human_needed
**Re-verification:** No — initial verification

**Verification posture:** All static/code-level checks were performed directly against the working tree at HEAD (003fe8a, clean). GPU `[UnityTest]` evidence is NOT claimed: the live editor holds the project lock, all four phase test suites are authored + capability-gated but unexecuted, and this is honestly recorded as open `unrun-verify` entries 1-4 in `.planning/WINDOWS.md` (matching the Phase-04 precedent: compile evidence + UAT routing). Editor.log (mtime 10:18) contains zero-error compile/import evidence for Waves 01-02 only; Wave-3 and the CR-01/WR-01/WR-02 fixes are pending the editor's next refresh.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | No-map source + extraction enabled packs per-texel Sobel roughness into surface alpha bits 0-5 (not scalar `_Roughness`) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Fully wired: `NAMERRoughness.compute` `CSRoughnessSobel/MaxReduce/Normalize` (Rec.601 luminance in `NAMERRoughness.hlsl`), `NamerRoughnessPipeline.ExtractSobel` staged dispatch, D-01 gate `shouldExtract = MetallicGlossMap == null && RoughnessExtractStrength > 0f` (`NamerComputePipeline.cs:115,161-166`), pack override `lerp(roughness, _RoughnessTex[id.xy].r, _RoughnessExtractStrength)` guarded by `_HasExtractedRoughness > 0.5` (`NAMERPack.compute:115-118`). GPU round-trip tests authored but unexecuted. |
| 2 | Authored `MetallicGlossMap` produces byte-identical packed output to pre-04.1 (extraction never runs — D-01) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Statically provable identity: authored map ⇒ `shouldExtract == false` ⇒ `roughnessTex == null` ⇒ `_HasExtractedRoughness = 0` ⇒ kernel guard is a no-op; scalar path uniforms untouched. Dedicated test `AuthoredMetallicGlossMap_IsByteIdentical_NoExtraction` (per-texel RGBA equality, lines 206-209) authored but unexecuted. |
| 3 | Sobel roughness normalized by global max edge magnitude (Blender parity: flat ~0, edge high) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `CSRoughnessNormalize`: `saturate(_RoughnessRaw.g / max(_GlobalMax, 1e-4))` — no `1 - x` inversion; hierarchical `.g = max` reduce copied from CSReduce; tiny blocking RGBAFloat readback. Runtime reduce/readback chain unexercised. |
| 4 | Fit-driven estimator (default) walks deterministic ascending ladder, picks FIRST passing strength (not global argmin) | ✓ VERIFIED | `NamerRoughnessFitter.cs:59` fixed `static readonly float[] StrengthLadder {0, .15, .3, .5, .7, .9, 1}`; `Fit` (72-110) returns the first strength with `MaxError <= threshold`, max-strength + `Passed=false` on no-pass, never `System.Random`. Pure-CPU deterministic loop — statically traced end-to-end. Test `FitDriven_SelectsFirstPassingStrength` (U-shaped curve asserting 0.3 not argmin 0.5) authored, unexecuted. |
| 5 | D-04 NamerAOBaker pattern: deterministic tables, injectable shouldCancel, off-debounce `RequestFit`, caching `HasCachedFit`, caller-owned buffers | ✓ VERIFIED | All mechanisms present: `shouldCancel` poll (fitter 85-90, `DisplayCancelableProgressBar` when null), `RequestFit`/`HasCachedFit`/`ClearFitCache` on `NamerRoughnessPipeline` (195-254) mirroring `NamerAOPipeline`, fitter owns no native buffers (T-04.1-04). Documented vehicle deviation (managed loop, not Burst IJobParallelFor) is in the plan's must-have wording itself. NOTE (warning): `RequestFit`/`HasCachedFit` are not yet called by `NamerEditorWindow` — disclosed in 04.1-02-SUMMARY. |
| 6 | Vertex-color refit consumes the post-extraction (sharp-removal-cleaned) base, not the AO-only cleaned base (D-05) | ✓ VERIFIED | `NamerComputeResult.NormalizedBaseColor = cleanedBase != null ? cleanedBase : baseColorOut` + `NormalizedBaseColorOwnedByRoughnessPool` flag (`NamerComputePipeline.cs:186-194`); refit reads `computeResult.NormalizedBaseColor` (`NamerProcessor.cs:204-215`); ownership flag routes release to the roughness pool (`ReleaseResult` 238-248) avoiding the foreign-pool silent no-op leak. |
| 7 | Fit-driven estimator skips extraction to identity legacy path when DecompositionEnabled == false or mesh unresolved (1A) | ✓ VERIFIED | Gate `shouldExtract && isFitDriven && BakeSourceMesh != null && evaluate != null` (`NamerComputePipeline.cs:167`); `NamerProcessor` composes `evaluate` only when `DecompositionEnabled && decomposeSourceMesh != null` (157-172) — decomposition off ⇒ evaluate null ⇒ skip. **CR-01 fix verified**: `BindAndDispatchSurfacePack(..., roughnessTex != null, ...)` (180-182) gates the flag on the PRODUCED texture, so the identity path preserves scalar roughness byte-identically instead of lerping toward white fill at strength 1 (the pre-fix default-config corruption). |
| 8 | Editor window exposes strength slider (0=off, default 1=on) + fit-driven/Sobel popup, EditorPrefs-persisted, debounced | ✓ VERIFIED | Slider + popup inside Roughness Extraction foldout with `MarkTweaking()` + `MarkDirty()` pattern (`NamerEditorWindow.cs:804-830`); `RebuildInspection` snapshot (206-207) + `RecomputePreview` copy with enum cast (319-320); settings keys persisted via `NamerProcessorSettings` (25-26, 112-122); defaults `DefaultRoughnessExtractStrength = 1f` / `DefaultRoughnessEstimator = 0` in `NamerEditorConstants.cs:49,52`. |
| 9 | Processor window: scrolling controls region, collapsible foldouts (Source / Preview-Debug / Roughness Extraction / AO / Decomposition / Output) with persisted state, extracted-roughness debug channel | ✓ VERIFIED (structure; render claims → human) | `BeginScrollView`/`EndScrollView` wrap with `DrawActionSection` (Process button + status) outside (`NamerEditorWindow.cs:619-628`); all six foldouts read/write persisted `Foldout*` properties (633/680/796/837/906/1004); defaults Source open, others collapsed (`NamerProcessorSettings` 126-164); `DebugChannelLabels` appends "Extracted Roughness" at index 10 (verified array read, 25-28); `NamerDebugView.shader` channel-9 `else` branch (`_DebugChannel >= 8.5`) samples `_ExtractedRoughness.r`, Error Heatmap narrowed to `< 8.5`; `SetExtractedRoughness` binding exists (371, `NamerDebugChannelMaterial.cs:67`). Smoke tests authored (foldout defaults, labels order + index/channel pairing) but unexecuted. Window-fits-screen is a render claim (human item 3). |
| 10 | Extraction-processed asset with collapsed fit: `_BaseResidualMap` UNBOUND, no residual EXR, Base PNG still written, vertex-colored mesh | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Mechanism statically verified: `writeResidual = decomp != null && Stats.ResidualRequired && Residual != null`; `baseResidualPath = decomp != null ? residualWritePath : basePath` yields null when dropped → unbound white default; `WriteBaseTexture` unconditional; split mesh always written (`AssetGenerator.cs:165-199`, git-diff-confirmed untouched this phase). End-to-end collapse is GPU/fit runtime behavior; acceptance test authored, unexecuted. |
| 11 | D-06 `_RoughnessOffsetMap` additive, neutral-when-unset ("black" {} default ⇒ .r == 0 ⇒ byte-identical decode) | ✓ VERIFIED | `NAMER.shader:13` `[NoScaleOffset] _RoughnessOffsetMap(...) = "black" {}`; `NamerSurface.hlsl:98-99` `roughness = saturate(roughness + SAMPLE_TEXTURE2D(...).r); smoothness = 1.0 - roughness;` applied AFTER `NAMER_DECODE_SURFACE` (macro signature unchanged, Meta pass untouched); guarded `material.SetTexture` only when `inspection.RoughnessOffsetMap != null` (`AssetGenerator.cs:577-580`, direct reference, no LoadAssetAtPath); matching hidden declaration in `NamerDebugView.shader:11` keeps the include's SRP-batcher layout complete. Black default math is fully visible: +0 is identity. |
| 12 | Genuine albedo detail surviving extraction still falls back to a bound residual (D-13 extended honest gate) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Pre-existing Phase-4 D-13 machinery drives it; fit returns `Passed=false` max-strength without forcing a drop; `HonestGate_AlbedoDetail_StillRequiresResidual` (asserts `ResidualRequired == true`, `NamerOneTextureTests.cs:188`) authored, unexecuted. |

**Score:** 7/12 truths verified (5 present, behavior-unverified — all routed to human verification)

**CR-01 re-verification (per instruction — highest-value static check):** VERIFIED. `NamerComputePipeline.cs:180-182` passes `roughnessTex != null` (the produced-texture check) as `hasExtractedRoughness`, not the `shouldExtract` intent flag. The default config (extraction on, fit-driven, decomposition off ⇒ 1A skip) now leaves `_HasExtractedRoughness = 0`, so `CSSurfacePack` keeps the scalar roughness byte-identically instead of lerping every texel to the white fill. Authored-map and strength-0 paths were already false-flagged, so no regression surface there.

**Review-fix verification:** CR-01 (`c84f6df`), WR-01 (`7adfd41` — `ReadBackGlobalMax` NativeArray now wrapped in try/finally dispose, `NamerRoughnessPipeline.cs:486-505`), WR-02 (`edf3b40` — fit cache re-keyed to the 5-tuple `(meshId, estimator, w, h, maxErrorThreshold)` and only `Passed` fits cached, lines 71-72/241-244/350-353; forwarder signature updated, no stale 4-tuple call sites) — all three statically verified as applied. The three info-level findings (IN-01 doc range, IN-02 stale comment clause "or strength == 0" at `NamerComputePipeline.cs:177`, IN-03 exception-path `baseColorOut` leak) remain open by scope choice — none block the goal.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `Compute/NAMERRoughness.compute` | 8 kernels (3 Sobel + 5 frequency-separation), `_Size` guards, linear writes | ✓ VERIFIED | All 8 `#pragma kernel` declarations present; bound guards on every kernel; Sobel Gx/Gy matrices match Blender; saturate-clamped sharp removal (Pitfall 5); remap `saturate(_Strength * length(sharpDetail))` no inversion. |
| `Compute/NAMERRoughness.hlsl` | Rec.601 luminance + helper | ✓ VERIFIED | `NAMER_ROUGH_LUMA_R/G/B = 0.299/0.587/0.114`, `NamerRoughnessLuminance`, include guard, doc comment distinguishing Rec.709 AO. |
| `Editor/Pipeline/NamerRoughnessPipeline.cs` | ExtractRoughness/ReleaseRoughness/Dispose + fit path + 3A cache | ✓ VERIFIED | 536 lines, estimator dispatch, `RunSharpRemoval` probe, `HasCachedFit`/`RequestFit`/`ClearFitCache`, WR-01/WR-02 fixes applied. |
| `Editor/Bake/NamerRoughnessFitter.cs` | Deterministic ladder minimizer | ✓ VERIFIED | Fixed ladder, `NamerRoughnessFitResult {Strength, MaxError, Passed, Cancelled}`, injectable cancel, progress bar + ClearProgressBar. |
| `Editor/Pipeline/NamerComputePipeline.cs` | D-01/1A gates, D-05 repoint, ownership, CR-01 fix | ✓ VERIFIED | All gates read in source; `ExtractedRoughness` nullable + released via roughness pool in `ReleaseResult`. |
| `Editor/Pipeline/NamerSourceModel.cs` | `RoughnessExtractStrength` (no inline default), enum field, `RoughnessOffsetMap` | ✓ VERIFIED | Line 98-99 (`NamerRoughnessEstimator.FitDriven` default), 109 (`Texture2D RoughnessOffsetMap`). |
| `Editor/Pipeline/NamerProcessor.cs` | Settings copy w/ cast, mesh-before-Process, composed evaluate | ✓ VERIFIED | Lines 137-138, 144-147, 156-174, `EvaluateRefitMaxError` (512-538) disposes all intermediates. |
| `Editor/Settings/NamerProcessorSettings.cs` | 2 roughness keys + 6 foldout keys w/ properties | ✓ VERIFIED | All present; Source defaults open, others collapsed. |
| `Editor/NamerEditorConstants.cs` | DefaultRoughnessExtractStrength = 1f, DefaultRoughnessEstimator = 0 | ✓ VERIFIED | Lines 49, 52. |
| `Editor/UI/NamerEditorWindow.cs` | Scroll + foldouts + controls + debug channel | ✓ VERIFIED | See truth 9 evidence. |
| `Editor/UI/NamerDebugChannelMaterial.cs` + `Shaders/NamerDebugView.shader` | SetExtractedRoughness + channel 9 | ✓ VERIFIED | Binding + property + `else` (>= 8.5) grayscale branch. |
| `Shaders/NAMER.shader` + `Shaders/NamerSurface.hlsl` | D-06 black-default additive offset | ✓ VERIFIED | See truth 11 evidence. |
| `Editor/Generation/AssetGenerator.cs` | Guarded `_RoughnessOffsetMap` binding; `baseResidualPath` untouched | ✓ VERIFIED | Git-diff confirms the D-13 line unmodified this phase. |
| `Tests/Editor/NamerRoughnessExtractionTests.cs` | 3 capability-gated GPU tests | ✓ PRESENT (authored; unexecuted) | Flat-base `(alphaByte & 0x3F) <= 1` max-scan, edge > flat, authored-map per-texel RGBA byte equality; leak-baseline assertions. |
| `Tests/Editor/NamerRoughnessFitTests.cs` | 3 fit tests incl. 2A default-on end-to-end | ✓ PRESENT (authored; unexecuted) | U-shaped first-passing CPU test, collapse test, full `NamerProcessor.Process` default-settings acceptance. |
| `Tests/Editor/NamerEditorWindowSmokeTests.cs` | Foldout defaults + labels pairing | ✓ PRESENT (authored; unexecuted) | Reflection read of labels array + index/channel math; EditorPrefs snapshot/restore isolation. |
| `Tests/Editor/NamerOneTextureTests.cs` | 4 one-texture/D-06/honest-gate tests | ✓ PRESENT (authored; unexecuted) | Full processor drive, material-binding assertions, prefs isolation. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| CSNormalize (`_BaseColorOut`) | NamerRoughnessPipeline | `ExtractRoughness(inspection, baseColorOut, w, h)` between normalize/encode and surface pack | ✓ WIRED | `NamerComputePipeline.cs:153-176` — extraction slot (b) sits between dispatch (a) and pack (c) exactly as planned. |
| Extracted roughness | CSSurfacePack | `_RoughnessTex` + `_HasExtractedRoughness` + `_RoughnessExtractStrength` | ✓ WIRED | `BindAndDispatchSurfacePack` (386-404) binds all three; kernel overrides `_PackInputs.y` at pack time (no CSNormalize re-dispatch — AO un-multiply not double-applied). |
| NamerRoughnessFitter ↔ NamerRoughnessPipeline ↔ NamerDecompPipeline | fit-driven strength search | composed `Func<float,float> evaluate` (3A) | ✓ WIRED | `NamerProcessor` composes the callback (owner of splitter/fitter/decomp state) → `ExtractSharpRemoval` → `EvaluateRefitMaxError` → `VertexColorFitter.Fit` + `GenerateResidual` → `Stats.MaxError`. Synchronous on cache miss inside `Process`. |
| NamerProcessor (D-05 cleaned-base feed) | VertexColorFitter.Fit → GenerateResidual | `computeResult.NormalizedBaseColor` (repointed) | ✓ WIRED | Lines 204-215; the evaluate callback and the final refit consume the same cleaned-base contract. |
| WriteMaterial `_RoughnessOffsetMap` | NamerSurface.hlsl decode | guarded `SetTexture` + `"black" {}` default + additive saturate | ✓ WIRED | See truth 11. |
| Window controls → preview | settings/inspection | snapshot → RecomputePreview copy → `Process(inspection)` | ✓ WIRED (with disclosed limitation) | Default FitDriven preview passes no evaluate ⇒ identity fallback (per 1A) ⇒ the "Extracted Roughness" debug channel binds null (black) in the default preview until `RequestFit` is wired into the window (disclosed in 04.1-02-SUMMARY "Next Phase Readiness"). Sobel-mode preview does extract. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| NamerRoughnessPipeline.ExtractSobel | `_RoughnessOut` | Sobel on `_BaseColorOut` + hierarchical global max | Yes (GPU compute over the real cleaned base) | ✓ FLOWING |
| ExtractFitDriven | `CleanedBase` / `Roughness` | blur → sharpDetail → sharp-removal/remap at fitted strength | Yes | ✓ FLOWING |
| CSSurfacePack | `_SurfaceOut` | `_PackInputs` + `_Octahedral` + extracted `_RoughnessTex` | Yes | ✓ FLOWING |
| NamerEditorWindow debug preview | `_liveResult.ExtractedRoughness` | `Process(inspection)` (no evaluate) | Null in default FitDriven preview (identity path) — channel renders black | ⚠️ STATIC in default preview config (disclosed; Sobel preview flows) |
| AssetGenerator.WriteMaterial | `_RoughnessOffsetMap` slot | user-assigned `inspection.RoughnessOffsetMap` | Optional by design (null ⇒ neutral black default) | ✓ FLOWING (by design optional) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All 4 suites + full workspace tests | Unity batchmode / TestRunnerApi | Not runnable — live editor (PID 4715) holds `Temp/UnityLockfile` | ? SKIP (routed to human verification) |
| Wave 01-02 compile/import | Editor.log grep (mtime 10:18) | `NAMERRoughness.hlsl`/`.compute` imported clean; 0 `error CS`; no shader errors | ✓ PASS (Waves 01-02 only) |
| Wave-3 + CR/WR fix compile | Editor.log | Log predates those commits — evidence pending editor refresh | ? SKIP (human item 2) |
| Task/fix commits exist | `git rev-parse` on all 11 hashes | All OK (58d03cb…edf3b40) | ✓ PASS |
| Phase diff scope | `git diff --stat 232b89c..HEAD` | 29 package files, +3010/−24; `baseResidualPath` line unmodified | ✓ PASS |
| Stale cache-key call sites | grep 4-tuple `HasCachedFit` | None (5-tuple consistent after WR-02) | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| (none) | — | Phase declares Unity Test Framework runs, not `scripts/*/tests/probe-*.sh`; no probe scripts exist for this phase | N/A |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| ENCD-02 | 01, 03 | Packed surface format (A bits 0-5 roughness) — unchanged contract, values re-sourced | ✓ SATISFIED (static) | `NamerPackSurfaceFromOct` not re-derived; override applied at pack time inside the existing encode; D-06 offset is outside the packed format (material-level input). |
| NORM-01 | 01, 02 | Cleaned base with baked response removed | ✓ SATISFIED (static) | Frequency-separation kernels produce the sharp-removal cleaned base consumed by the refit (D-05). |
| NORM-03 | 01, 02 | Per-pixel GPU compute, no per-pixel C# | ✓ SATISFIED (static) | All texture math in `NAMERRoughness.compute` kernels; CPU loops only over the ≤64-texel reduced max and mesh-vertex fitting (sanctioned Burst/mesh domain). |
| VCOL-03 | 02, 03 | Residual texture from fitted-vs-source difference | ✓ SATISFIED (static) | `EvaluateRefitMaxError` + final `GenerateResidual` wired on the cleaned base; D-13 drop promotes the zero-residual outcome. |
| VCOL-04 | 02 | Reconstruction-error statistics | ✓ SATISFIED (static) | `Stats.MaxError` is the fit objective; `Stats.ResidualRequired` drives the honest gate/drop. |
| VCOL-05 | 02 | Adaptive residual resolution | ✓ SATISFIED (static, carried) | `settings.ResidualResolution` plumbed through the evaluate callback and final `GenerateResidual` (pre-existing Phase-4 machinery exercised unchanged). |
| UI-03 | 02 | Window processing controls incl. roughness extraction | ✓ SATISFIED (static; render → human) | Strength slider + estimator popup + foldout/scroll restructure + debug channel. |
| SHDR-02 | 03 | Base/residual + vertex-color reconstruction; D-06 offset | ✓ SATISFIED (static) | Decode path untouched for the happy path; offset additive post-decode, neutral black default. |

No orphaned requirements — the union of plan `requirements` fields equals the phase's 8 declared IDs exactly.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `NamerComputePipeline.cs` | 177 | Stale comment clause "or strength == 0" (IN-02, unfixed) — `strength == 0` is excluded by `shouldExtract`; the branch IS identity post-CR-01, but the second clause misleads | ℹ️ Info | Doc drift only; behavior correct |
| `NamerComputePipeline.cs` | 196-223 | `baseColorOut` leaks on exception when `cleanedBase == null` (IN-03, unfixed) — bounded by pipeline disposal | ℹ️ Info | Leak only on GPU/shader exception mid-Process; long-lived window pipeline could accumulate on repeated preview failures |
| `NamerRoughnessPipeline.cs` | 195-254 | `RequestFit`/`HasCachedFit` exposed but no caller yet (window preview unwired) | ⚠️ Warning | Default FitDriven preview shows identity (no extraction, black debug channel); disclosed in SUMMARY; generation path unaffected |

Debt-marker scan (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) across all 19 phase source files: zero matches.

### Human Verification Required

See frontmatter `human_verification` (4 items): (1) in-editor execution of the four test suites + closing WINDOWS.md unrun-verify entries 1-4, (2) editor refresh + compile/import confirmation for Wave-3 and the CR-01/WR-01/WR-02 fixes, (3) D-07 render check (window height, scroll reachability, Process button outside scroll), (4) Neo end-to-end acceptance pass.

### Gaps Summary

No structural gaps. Every artifact exists, is substantive, and is wired end-to-end: the Sobel and fit-driven estimators, the frequency-separation kernels, the D-01/1A/D-05 gates, the settings/UI/debug-channel surface, the D-06 offset input, and the one-texture D-13 promotion are all present in code at HEAD (003fe8a), with the CR-01/WR-01/WR-02 review fixes statically confirmed applied. What is NOT yet evidence is runtime behavior: all 13 phase tests across four suites are authored and capability-gated but unexecuted (live editor holds the project lock), and the editor log's zero-error compile evidence covers Waves 01-02 only — Wave-3 and the review fixes are pending the editor's next refresh. Five truths therefore remain present-but-behavior-unverified and are routed to human verification, per the Phase-04 precedent. Two disclosed, non-blocking limitations are worth carrying forward: the interactive window does not yet invoke `RequestFit`/`HasCachedFit` (default FitDriven preview falls back to identity), and the three info-level review findings (IN-01/02/03) were left open by scope choice.

---

_Verified: 2026-09-06T17:49:04Z_
_Verifier: Claude (gsd-verifier)_
