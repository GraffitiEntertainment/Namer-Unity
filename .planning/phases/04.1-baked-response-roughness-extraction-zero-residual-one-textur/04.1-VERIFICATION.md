---
phase: 04.1-baked-response-roughness-extraction-zero-residual-one-textur
verified: 2026-09-15T19:31:24Z
status: human_needed
score: 19/19 must-haves verified (12 phase truths + 7 gap-closure truths)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 7/12
  gaps_closed:
    - "All four phase test suites EXECUTED and passing (previously authored-but-unexecuted): NamerRoughnessExtractionTests 3/3, NamerRoughnessFitTests 3/3, NamerEditorWindowSmokeTests 2/2, NamerOneTextureTests 4/4 — now inside a full EditMode regression of 120/120 (Temp/namer-regression5-results.txt, 2026-09-15 12:27, live editor)"
    - "Compile evidence: the 12:27 live-editor run postdates every source edit in the phase (latest mtime 12:25) — EditMode tests cannot execute against a non-compiling tree; 0 error CS / 0 shader errors per UAT test 2"
    - "Fit-driven strength search gating defect fixed (FitOnlyMaxError): EvaluateRefitMaxError now returns the fit-only D-13 statistic, not the vacuous with-residual MaxError (commit d7ea1c4)"
    - "WR-01..WR-05 code-review fixes applied (commit 0a68f72): 10-element fit-cache key, Resample _REENCODE_SRGB keyword disable, exception-path lease releases (catch-rethrow), shader-default 'black' pin replacing the tautological neutrality loop; WR-04 accepted with documented rationale"
    - "Double-Process vanish fixed: WriteMeshAsset overwrites the existing mesh asset IN PLACE (Clear + ApplySplitStreams + SetDirty + SaveAssets) — regression test DoubleProcessWithDecomposition_KeepsRendererMeshReferenceValid green"
    - "Preview/Process CR-01 divergence fixed: DecompositionSkipReason shared predicate + decompWillRun preview gating + warning HelpBox"
    - "RenderTexture.active unbind in ComputeTexturePool.DestroyRenderTexture"
    - "Test-suite independence from live user EditorPrefs (pref pins + PrefsSnapshot extension)"
    - "SourceInspector dedupes on the RESOLVED D-04 source — Dedupe_SourceAndItsGeneratedMaterialCountOnce green"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "In the live editor (FitDriven estimator, default settings, a no-map source with decomposition enabled): inspect the 'Extracted Roughness' debug toolbar entry and the shaded after-pane"
    expected: "The Extracted Roughness channel shows grayscale extracted roughness (not black), and the shaded preview applies that roughness (not uniformly glossy); preview and Process output agree"
    why_human: "Visual rendering quality of the debug channel and shaded preview has no automated test; the data flow is suite-verified but pixels on screen are not."
  - test: "Decide on the Sobel Blender-parity look (global-max normalization makes most materials near-mirror/glossy)"
    expected: "Accept as designed (Blender parity) or file a design change request — an explicit decision, not a bug report"
    why_human: "A design acceptance decision, not a code property; kernel parity is verified, desirability is not machine-checkable."
  - test: "Run Process TWICE on the Neo scene instance (tripo_node_d83278e6) in the live editor"
    expected: "Model stays visible after BOTH presses; press-1 output matches the preview window; no 'Releasing render texture that is set to be RenderTexture.active!' warning in the console; one-texture output (surface PNG + vertex-colored mesh, no residual EXR)"
    why_human: "End-to-end acceptance on the user's real scene asset; the mechanism is regression-tested on synthetic fixtures, the live-scene confirmation is the user's acceptance."
---

# Phase 04.1: Baked-Response Roughness Extraction + Zero-Residual One-Texture — Verification Report

**Phase Goal:** Extract material response (gloss/shading/cavity) baked into the base texture as 6-bit roughness (surface alpha bits 0-5) when no authored roughness map exists — via a Sobel Blender-parity estimator and a fit-driven strength search — then refit the sharp-removal-cleaned base into vertex colors so the Phase-4 D-13 residual auto-drop becomes the primary one-texture path (one RGBA8 surface PNG + vertex-colored mesh, no residual), with a D-06 optional roughness-offset escape hatch.
**Verified:** 2026-09-15T19:31:24Z (gap-closure re-verification)
**Status:** human_needed
**Re-verification:** Yes — after gap closure (previous: 2026-09-06, human_needed, 7/12)

---

## Gap-Closure Re-Verification (2026-09-15)

This round re-verified every must-have after two gap-closure waves (04.1-04 production closure, 04.1-05 fixture closure) plus a second live-user round (2026-09-15). **All code claims below were checked directly against the working tree** (not SUMMARY/UAT prose). Every previously behavior-unverified truth now has executed-test evidence: the full EditMode regression ran 120/120 PASS / 0 FAIL / 0 SKIP in the live editor (`Temp/namer-regression5-results.txt`, mtime 12:27) — the results file lists all four phase suites by name, all previously-failing tests (including `FitDriven_SelectsFirstPassingStrength`, all three collapse tests, `RoughnessOffset_Set_BindsMaterialTexture`, `BasePng_EncodesLinearToSrgb`) as PASS, and its mtime postdates the last source edit (SourceInspector.cs 12:25), so the run covers the exact tree verified here. The verifier inspected the results artifact directly (120 PASS lines, 0 FAIL, `SUMMARY pass=120 fail=0 skip=0`); the Unity suite itself cannot be re-run from this verifier without launching the editor (forbidden), so execution evidence rests on that artifact — consistent with the project's established TestRunnerApi file-channel pattern.

**Commit state:** FitOnlyMaxError (d7ea1c4) and WR-01..WR-05 (0a68f72) are committed on `gsd/phase-04.1-...`. The second-round UAT fixes (mesh in-place overwrite, DecompositionSkipReason sharing, RT-active unbind, pref pins, SourceInspector dedupe, +437/−50 across 9 files) exist ONLY in the uncommitted working tree — commits blocked by a locked 1Password vault (human action required). See Warnings.

> **Post-verification note (2026-09-15, after report finalized):** the vault unblocked and commit `18bc331` landed those working-tree fixes — the commit's 9-file, +437/−50 diff is exactly the set this report verified against the tree. Recorded here so the warning above isn't acted on; the verdict and evidence are unchanged.

### Observable Truths (original 12 — re-verified)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | No-map source + extraction enabled packs per-texel Sobel roughness into surface alpha bits 0-5 (not scalar `_Roughness`) | ✓ VERIFIED | Wiring previously verified (unchanged); now EXECUTED: `Sobel_FlatBase_YieldsNearZeroRoughness` + `Sobel_SharpEdge_YieldsHighRoughness` PASS in the 120/120 regression (results lines 76-77). |
| 2 | Authored `MetallicGlossMap` produces byte-identical packed output to pre-04.1 (extraction never runs) | ✓ VERIFIED | `AuthoredMetallicGlossMap_IsByteIdentical_NoExtraction` PASS (results line 75) — per-texel RGBA byte equality between strength 0 and 1 on an authored-map source. |
| 3 | Sobel roughness normalized by global max edge magnitude (Blender parity: flat ~0, edge high) | ✓ VERIFIED | Kernel reduce/normalize chain unchanged (static verification stands); both Sobel behavior tests PASS. Whether the resulting look is *desired* is human item 2 (design decision), not a code gap. |
| 4 | Fit-driven estimator walks deterministic ascending ladder, picks FIRST passing strength (not argmin) | ✓ VERIFIED | `FitDriven_SelectsFirstPassingStrength` PASS at the SHIPPED 0.02 threshold (fixture rescaled, 03e7b81) — AND the search now measures the right objective: `EvaluateRefitMaxError` returns `Stats.FitOnlyMaxError` (`NamerProcessor.cs:524-543`), the fit-only D-13 statistic (`NamerDecompPipeline.cs:214-216` computes fitStats with residual == identity; line 287 carries `fitOnlyMaxError: fitStats.MaxError` into the with-residual path). Doc comment states exactly why the with-residual MaxError must not feed the search. Committed d7ea1c4. |
| 5 | D-04 NamerAOBaker pattern: deterministic tables, injectable shouldCancel, off-debounce `RequestFit`, caching, caller-owned buffers | ✓ VERIFIED | `RequestFit` retained (`NamerRoughnessPipeline.cs:197`, keyed cache hit at 212); dead `HasCachedFit` deleted from both files (grep: zero references); preview wiring uses option 1 — `RecomputePreview` composes the same evaluate callback `Process` uses (WR-04 accepted disposition, REVIEW.md:186-188). |
| 6 | Vertex-color refit consumes the post-extraction (sharp-removal-cleaned) base (D-05) | ✓ VERIFIED | Unchanged from previous verification (quick regression: `NormalizedBaseColor` repoint + ownership routing intact); exercised end-to-end by the passing collapse tests. |
| 7 | Fit-driven estimator falls to identity legacy path when decomposition off or mesh unresolved (1A); authored/zero-strength paths byte-identical (CR-01) | ✓ VERIFIED | Unchanged gates; now STRENGTHENED — the CR-01 predicate is shared (`NamerProcessor.DecompositionSkipReason`, cs:653-673) between `Process` (cs:119-126) and the window preview, closing the preview/Process divergence (see gap truth G3). |
| 8 | Editor window: strength slider + estimator popup, EditorPrefs-persisted, debounced; preview actually extracts (FitDriven) | ✓ VERIFIED | Controls unchanged; `RecomputePreview` (window cs:305+) now sets `BakeSourceMesh` and composes `EvaluateRefitMaxError` for FitDriven — the previously-disclosed "identity preview / black debug channel" limitation is CLOSED. Smoke tests 2/2 PASS. |
| 9 | Processor window: scroll + 6 persisted foldouts + Extracted Roughness debug channel (D-07) | ✓ VERIFIED (static + smoke) | Structure verified previously; `DebugChannelLabels_AppendsExtractedRoughnessAtIndex10` PASS. Render/layout quality remains human item 1's neighborhood (visual confirmation of the channel's appearance). |
| 10 | Extraction-processed asset with collapsed fit: `_BaseResidualMap` UNBOUND, no residual EXR, Base PNG written, vertex-colored mesh | ✓ VERIFIED | `OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound` + `FitDriven_DefaultOn_DecomposedNoMapAsset_ExtractsAndDropsResidual` PASS (results lines 66, 79) — asserts empty ResidualTexturePath, unbound `_BaseResidualMap`, non-zero packed roughness bits, written MeshPath. |
| 11 | D-06 `_RoughnessOffsetMap` additive, neutral-when-unset | ✓ VERIFIED | `RoughnessOffset_Unset_DecodesIdentical` now PINS the shader's declared default via `shader.GetPropertyTextureDefaultName(...) == "black"` (`NamerOneTextureTests.cs:151-154`, WR-05 — no longer tautological); `RoughnessOffset_Set_BindsMaterialTexture` PASS with a persisted asset (c938c02). |
| 12 | Genuine albedo detail surviving extraction still falls back to a bound residual (D-13 honest gate) | ✓ VERIFIED | `HonestGate_AlbedoDetail_StillRequiresResidual` PASS (results line 65). |

**Score (original truths): 12/12** — all five previously behavior-unverified truths now have executed-test evidence.

### Gap-Closure Truths (from UAT rounds — full 3-level verification)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| G1 | Strength search gates on fit-only refit error (production defect) | ✓ VERIFIED | Code: `EvaluateRefitMaxError` returns `FitOnlyMaxError` (cs:542); statistic computed residual==identity (`NamerDecompPipeline.cs:215-216`) and carried through the with-residual branch (cs:287). Committed d7ea1c4. Tests: all 3 fit tests PASS. |
| G2 | Second Process press must not destroy the renderer's mesh reference (model vanish) | ✓ VERIFIED | Code: `AssetGenerator.WriteMeshAsset` (cs:463-488) — existing asset path takes `existing.Clear(false)` + `ApplySplitStreams` + `EditorUtility.SetDirty` + `SaveAssets` (in-place), never `CreateAsset` over an existing path; `ApplySplitStreams` (cs:531-563) shared by both branches, bindposes-before-boneWeights preserved. Test: `NamerReprocessTests.DoubleProcessWithDecomposition_KeepsRendererMeshReferenceValid` (cs:270-339) — two full Process runs, asserts sharedMesh non-null AND same instance as the regenerated asset. PASS (results line 72). Verified RED-pre-fix per UAT. |
| G3 | Preview shows the shape Process will generate (CR-01 shared predicate + user-visible warning) | ✓ VERIFIED | `DecompositionSkipReason` (processor cs:653) shared; window caches `_decompGuardReason` (cs:215), `decompWillRun` gates `BakeSourceMesh`, evaluate composition, and `RunDecompPreview` (cs:332-368); warning HelpBox rendered in DrawDecompositionSection (cs:971-978). User's 2026-09-15 console showed the live warning firing for Neo-T-Pose. |
| G4 | Pool dispose never releases a RenderTexture that is still active | ✓ VERIFIED | `ComputeTexturePool.DestroyRenderTexture` (cs:113-131): `if (RenderTexture.active == rt) RenderTexture.active = null;` before Release/DestroyImmediate. No dedicated test (single static guard) — covered by the full 120/120 regression; live confirmation folded into human item 3. |
| G5 | EditMode suite independent of live user EditorPrefs | ✓ VERIFIED | `AssetGeneratorTests`: `RoughnessExtractStrengthKey` + `PrefsSnapshot.RoughnessExtractStrength/Had` captured/restored (cs:549-580); ImportStamping pins `RoughnessExtractStrength = 0f` (cs:337-342). `NamerRoughnessFitTests.FitDriven_DefaultOn` pins `RoughnessEstimator = FitDriven` + `DefaultRoughnessExtractStrength` (cs:186-192, with explanatory comment). All previously machine-dependent tests green in regression5. |
| G6 | Inspection dedupes on the RESOLVED source (source + its generated NAMER material = ONE unit) | ✓ VERIFIED | `SourceInspector.Inspect` (cs:54-75): `resolvedSeen` HashSet on `source.GetInstanceID()` with a comment documenting the partial-bind reprocess case. Test: `Dedupe_SourceAndItsGeneratedMaterialCountOnce` (SourceInspectorTests.cs:231-272) mirrors the user's hierarchy (renderer on source + renderer on labeled/tagged generated) and asserts `Materials.Count == 1` + resolves to the source instance. PASS (results line 102). Verified RED-pre-fix (got 2) per UAT. |
| G7 | WR-01..WR-05 review fixes applied as dispositioned | ✓ VERIFIED | WR-01: `MakeFitKey` 10-tuple `(meshId, baseMapId, aoStrength, aoContrast, aoBlurRadius, aoUnmultiplyStrength, estimator, w, h, maxErrorThreshold)` (`NamerRoughnessPipeline.cs:510-518`), both call sites keyed. WR-02: `Resample` disables `_REENCODE_SRGB` before the blit (`NamerDecompPipeline.cs:518-524`). WR-03: catch-rethrow lease releases in `NamerComputePipeline.Process` (cs:196-221 — surfaceOut to compute pool, roughnessTex/cleanedBase via `ReleaseExtractedRoughness`, baseColorOut either/or with finally) and `ExtractSobel` (cs:292-301). WR-05: shader-default pin (truth 11). WR-04: ACCEPTED with documented rationale (REVIEW.md:186-188) — preview supplies the evaluate callback directly (option-1, recorded STATE.md decision); not a failed must-have. Committed 0a68f72. |

**Score (gap-closure truths): 7/7. Total: 19/19.**

### Behavioral Evidence

| Check | Evidence | Status |
| ----- | -------- | ------ |
| Full EditMode regression | `Temp/namer-regression5-results.txt`: 120 PASS lines, 0 FAIL, `SUMMARY pass=120 fail=0 skip=0`; all 4 phase suites present (3+3+2+4) plus reprocess (5), source-inspector (13), asset-generator (7); mtime 12:27 postdates every source edit (latest 12:25) | ✓ PASS (artifact inspected by verifier; independent re-run requires launching the editor — out of verifier scope) |
| Compile cleanliness | The 12:27 in-editor run requires the current tree to compile; UAT test 2 records 0 `error CS` / 0 shader errors | ✓ PASS |
| Commits exist | `git log`: d7ea1c4 (FitOnlyMaxError), 0a68f72 (WR-01..05), 03e7b81/3658783/c938c02 (fixture gaps 1a/1b/1c), 371e597 — all present | ✓ PASS (second-round UAT fixes still uncommitted — see Warnings) |
| Debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER, stub phrases) | grep across all 13 phase-modified source/test files: zero matches | ✓ PASS |

### Requirements Coverage

All 8 phase requirement IDs (ENCD-02, NORM-01, NORM-03, VCOL-03, VCOL-04, VCOL-05, UI-03, SHDR-02) — previously SATISFIED statically — now carry executed-test evidence through the 120/120 regression. No orphaned requirements.

### Anti-Patterns / Warnings (non-blocking)

| Item | Severity | Impact |
| ---- | -------- | ------ |
| Second-round UAT fixes uncommitted (+437/−50, 9 files: AssetGenerator, ComputeTexturePool, NamerProcessor, SourceInspector, NamerEditorWindow, 4 test files) — commits blocked by locked 1Password vault | ⚠️ Warning | Verified fixes exist only in the working tree; loss/reversion risk until committed. Human action: commit when the vault is unlocked. |
| WR-04 accepted cost: AO-slider continuous drag re-keys the fit cache per debounce tick and can re-fire the cancelable search (documented REVIEW.md disposition) | ⚠️ Warning | UX nit only; keyed cache makes steady-state recomputes hits |
| IN-01..IN-07 info findings open by disposition (noted, not fixed — out of gap-closure scope) | ℹ️ Info | None block the goal; recorded in 04.1-REVIEW.md |

### Human Verification Required

Three items remain (frontmatter `human_verification`): (1) visual confirmation of the fit-driven Extracted Roughness debug channel + shaded preview roughness; (2) the Sobel Blender-parity glossy-look design decision; (3) the user's double-Process confirmation on the live Neo scene instance (stays visible both presses, press-1 output matches preview, no RT-active warning). All three are visual/acceptance outcomes — every underlying mechanism is code-verified and regression-tested.

### Gaps Summary

No code gaps. All 12 original phase truths and all 7 gap-closure truths are verified in the working tree with executed-test evidence (120/120 full EditMode regression in the live editor, postdating every source edit). The FitOnlyMaxError production defect (the vacuous with-residual search objective that made the strength search always pass at rung 0) is fixed and committed; the WR-01..WR-05 review fixes are applied and committed; the second live-user round's three defects (mesh-asset destruction on reprocess, preview/Process CR-01 divergence, RT-active release warning), the pref-leak test isolation, and the resolved-source dedupe are fixed and regression-tested in the working tree (uncommitted — vault-blocked, flagged as a warning). Status is human_needed solely for the three visual/acceptance items above; per the phase's own precedent these route to the user, not back to the planner.

---

_Verified: 2026-09-15T19:31:24Z (gap-closure re-verification; original 2026-09-06T17:49:04Z report superseded by this document — its truth table is carried forward with updated statuses above)_
_Verifier: Claude (gsd-verifier)_
