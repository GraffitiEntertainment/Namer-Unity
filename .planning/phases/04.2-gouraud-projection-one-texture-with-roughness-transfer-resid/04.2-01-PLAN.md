---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute
  - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionTests.cs
autonomous: true
requirements: [VCOL-03, VCOL-04, VCOL-05]
must_haves:
  truths:
    - "GenerateResidual can write the rasterized Gouraud surface back as a float16 projected base with source alpha preserved and uncovered texels passed through from the source"
    - "The residual quotient computed against the projected base is white (within 1/255 per channel) everywhere, including below-VcFloor dark texels (symmetric floor)"
    - "A residual mode parameter can force the residual to always-keep or never-keep, overriding the D-13 gate in both directions"
    - "The existing EditMode suite stays green after the symmetric-floor oracle flip"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute"
      provides: "CSProjectBase write-back kernel + symmetric-floor CSResidual"
      contains: "CSProjectBase"
    - path: "Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs"
      provides: "projectedOut parameter + NamerResidualMode on GenerateResidual"
      contains: "NamerResidualMode"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionTests.cs"
      provides: "projection identity + residual-mode tests"
      min_lines: 120
  key_links:
    - from: "NamerDecompPipeline.GenerateResidual"
      to: "CSProjectBase"
      via: "kernel dispatch when projectedOut != null"
      pattern: "FindKernel\\(\"CSProjectBase\"\\)"
    - from: "CSResidual"
      to: "symmetric VcFloor"
      via: "max on both dividend and divisor"
      pattern: "max\\(_BaseLinear\\[id\\.xy\\]\\.rgb"
---

<objective>
Deliver the decomposition-engine half of the by-construction residual collapse: the
projection write-back kernel (rasterized Gouraud surface becomes the cleaned base), the
symmetric VcFloor in CSResidual (kills the measured 1.70% dark-texel exception class), and
the residual mode parameter that will become the residual on/off checkbox in plan 03.

Purpose: this is the mechanism that makes `base / vcInterp ≡ white` an IEEE x/x identity
instead of an approximation quality (04.2 RESEARCH Pattern 1/2, VCOL-03). The dividend for
the production residual-ON path becomes the pre-projection SOURCE base (locked user
decision 2026-09-16), and the adaptive ladder then keys on source-reconstruction error
automatically (VCOL-05, Pitfall 3 resolved without new machinery).
Output: extended NAMERDecomp.compute + NamerDecompPipeline (additive parameters, default
behavior unchanged), flipped ResidualPipelineTests oracle, new NamerProjectionTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-CONTEXT.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-RESEARCH.md

<interfaces>
<!-- Current CSResidual body (NAMERDecomp.compute, covered-texel branch) — the floor is
     asymmetric today: dividend is raw base, divisor is floored vc. -->
```hlsl
float3 b = _BaseLinear[id.xy].rgb;
float3 v = max(vc.rgb, _VcFloor);
_ResidualOut[id.xy] = float4(b / v, baseA);
```

<!-- CSRasterizeVertexColors contract (unchanged this plan): first covering triangle wins,
     uncovered texels stay (0,0,0,0); RGB = barycentric interp of quantized Color32;
     _VcInterp is R8G8B8A8_UNorm linear. Available buffers/uniforms in the same file:
     _Verts, _Uvs, _Tris, _Colors, _Size, _TriCount, _VcFloor, _BaseLinear (Texture2D). -->

<!-- GenerateResidual current signature (NamerDecompPipeline.cs). Both new params are
     optional so every existing call site (NamerProcessor, NamerEditorWindow, tests)
     compiles and behaves identically until plan 03 wires them. -->
```csharp
public NamerDecompOutput GenerateResidual(
    NamerSplitResult split,
    Color32[] quantizedColors,
    RenderTexture baseLinear,
    int w,
    int h,
    float errorThreshold,
    int manualResolution)
```

<!-- NamerDecompErrorStats fields (unchanged names; doc semantics update only):
     Coverage, AvgError, MaxError, FitOnlyMaxError, ResidualRequired, ChosenResolution,
     CannotDecompose. NamerConstants.VcFloor = 1e-3f (Core). -->

<!-- ResidualPipelineTests CPU oracle (lines ~57-59) pins the asymmetric floor today: -->
```csharp
float oracle = baseValue / Mathf.Max(vcInterp, 1e-3f);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSProjectBase write-back kernel + symmetric VcFloor in CSResidual</name>
  <files>Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute (whole file — kernel layout, buffer/uniform declarations, comment conventions)
    - .planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-RESEARCH.md (Patterns 1-2, Pitfalls 1-2)
  </read_first>
  <action>
    1. Symmetric floor (RESEARCH Pattern 2): in CSResidual's covered-texel branch, floor
       the dividend per channel before dividing: compute b as max(_BaseLinear[id.xy].rgb,
       _VcFloor.xxx) and v as max(vc.rgb, _VcFloor.xxx), write float4(b / v, baseA). Update
       the file-header comment block that documents the quotient so it states
       max(base, _VcFloor) / max(vcInterp, _VcFloor) and why (with base == vc under the
       4.2 projection the quotient is exactly 1.0 everywhere, including the below-floor
       class; reconstruction error stays sub-LSB per RESEARCH Pitfall 1).
    2. New kernel CSProjectBase (add `#pragma kernel CSProjectBase` alongside the
       existing pragmas): declare RWTexture2D<float4> _ProjectedOut
       (R16G16B16A16_SFloat linear — same contract text as _ResidualOut). Per texel, read
       vc = _VcInterp[id.xy]; when vc.a < 0.5 (uncovered) write _BaseLinear[id.xy]
       unchanged (RGB and alpha passthrough); otherwise write float4(vc.rgb,
       _BaseLinear[id.xy].a) — projected RGB from the rasterization, alpha from the
       source base (RESEARCH Pitfall 2: coverage-in-alpha must not leak into the cleaned
       base). Guard against _Size exactly like the other kernels; [numthreads(8,8,1)].
    3. Update the file-header kernel list comment to include CSProjectBase with a
       one-paragraph note: the projection is the SAME rasterization the residual kernel
       uses, so dividend == divisor bit-exactly and the quotient is the IEEE x/x identity
       (04.2 by-construction collapse); the float16 write target keeps the projected base
       out of an 8-bit intermediate (RESEARCH Pattern 1 constraint 1).
  </action>
  <verify>
    <automated>grep -c "^#pragma kernel CSProjectBase" Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute | grep -qx 1 && grep -E "max\(_BaseLinear\[id\.xy\]\.rgb" Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute | grep -qv "^//" && echo KERNELS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^#pragma kernel CSProjectBase" NAMERDecomp.compute` returns 1
    - CSProjectBase body contains the uncovered passthrough branch reading vc.a < 0.5 and writing _BaseLinear[id.xy]
    - CSProjectBase covered branch writes alpha from _BaseLinear[id.xy].a, never from vc.a
    - CSResidual covered branch floors BOTH operands: max(_BaseLinear[id.xy].rgb, _VcFloor.xxx) divided by max(vc.rgb, _VcFloor.xxx)
    - No new uniform or StructuredBuffer was added (kernel reuses _VcInterp, _BaseLinear, _Size)
  </acceptance_criteria>
  <done>CSProjectBase kernel compiles into NAMERDecomp.compute with source-alpha preservation and uncovered passthrough; CSResidual floors the dividend symmetrically</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GenerateResidual gains projectedOut + NamerResidualMode</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs (whole file — GenerateResidual step ordering, pool lease patterns, BuildStats, kernel handle fields)
    - Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute (Task 1 result)
  </read_first>
  <behavior>
    - projectedOut != null: after the CSRasterizeVertexColors dispatch, CSProjectBase is dispatched once (bound _VcInterp, _BaseLinear = baseLinear, _ProjectedOut = projectedOut) at the same w/h; the caller-owned RT is never released by this pipeline
    - projectedOut == null: behavior identical to today (no write-back dispatch)
    - mode == Gate (default): D-13 gate logic byte-identical to today — existing tests pass unmodified
    - mode == NeverKeep: after the CR-03 coverage guard and the step-2 fit-only heatmap/reduce, return NamerDecompOutput with Residual == null and stats ResidualRequired = false, ChosenResolution = 0 — no CSResidual dispatch, no ladder
    - mode == AlwaysKeep: skip the MaxError/opaque gate entirely; proceed to CSResidual + ChooseResolution + final stats with required: true
    - CannotDecompose path (coverage < kMinCoverageFraction) fires before any mode branch, unchanged
  </behavior>
  <action>
    1. Add a namespace-level public enum NamerResidualMode { Gate, AlwaysKeep, NeverKeep }
       in NamerDecompPipeline.cs with a doc comment: Gate is the legacy D-13 threshold
       gate (transitional, retired by plan 03); AlwaysKeep/NeverKeep are the 4.2 checkbox
       semantics — the residual is computed against the passed baseLinear, which in the
       4.2 wiring is the pre-projection SOURCE base (locked user decision 2026-09-16), so
       FitOnlyMaxError reads as the removed-detail magnitude and ChooseResolution's
       EvaluateResolution becomes the source-reconstruction criterion automatically
       (VCOL-05; RESEARCH Pitfall 3 needs no new code).
    2. Extend GenerateResidual with two trailing optional parameters:
       RenderTexture projectedOut = null, NamerResidualMode mode = NamerResidualMode.Gate.
       Existing call sites stay source-compatible.
    3. Add a _kernelProject handle (FindKernel("CSProjectBase")) in the constructor. In
       GenerateResidual immediately after the rasterize dispatch (step 1) and before the
       _VcFloor/_Threshold uniform setup: when projectedOut != null, bind
       _VcInterp/_BaseLinear/_ProjectedOut to _kernelProject and Dispatch at w/h.
    4. Restructure the step-3 gate to switch on mode: Gate keeps the existing
       MaxError/opaque drop; NeverKeep returns the drop-shaped output (reusing the
       existing coverage reduce so Coverage stats still populate); AlwaysKeep falls
       through to the residual computation. The step-2 fit-only heatmap still runs in
       every mode so FitOnlyMaxError stays populated for VCOL-04.
    5. Update the class-level and NamerDecompErrorStats doc comments: FitOnlyMaxError is
       the fit-only reconstruction error against the passed dividend; when the dividend is
       the pre-projection source it equals the removed-detail magnitude; document the
       projectedOut contract (caller-owned R16G16B16A16_SFloat linear RT, w/h, never
       released here).
  </action>
  <verify>
    <automated>[ "$(grep -c "NamerResidualMode" Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs)" -ge 4 ] && grep -q "projectedOut = null" Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs && grep -q "FindKernel(\"CSProjectBase\")" Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs && echo PIPELINE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NamerResidualMode" NamerDecompPipeline.cs` >= 4 (enum decl + param + doc + branch)
    - GenerateResidual signature ends with `RenderTexture projectedOut = null, NamerResidualMode mode = NamerResidualMode.Gate`
    - `grep -q 'FindKernel("CSProjectBase")'` matches; the CSProjectBase dispatch is gated on projectedOut != null
    - NeverKeep branch returns before any CSResidual dispatch and sets ChosenResolution = 0
    - AlwaysKeep branch cannot reach the MaxError/opaque drop return
    - No caller of GenerateResidual in Editor/ or Tests/ required modification for compilation (verify: git diff shows only NamerDecompPipeline.cs among non-test sources)
  </acceptance_criteria>
  <done>GenerateResidual supports projection write-back and both checkbox modes behind optional parameters; default path is behavior-identical</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip the residual oracle + add NamerProjectionTests</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs (whole file — fixture builders for split/colors/base RTs, capability gating, readback helpers)
    - Packages/com.graffitientertainment.namer/Tests/Editor/VertexColorDecompTests.cs (quad mesh + split fixture patterns)
    - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs (Task 2 result)
  </read_first>
  <behavior>
    - Test Projection_QuotientIsWhite_ByConstruction: gradient-base fixture -> split -> VertexColorFitter.Fit -> Color32[] -> first GenerateResidual call with projectedOut = leased float16 RT and mode AlwaysKeep (dividend = source), then a second GenerateResidual call with that same projected RT passed as baseLinear, projectedOut = null, mode AlwaysKeep; read back the second residual and assert max |quotient - 1| <= 1/255 per channel and Stats.MaxError <= 0.01
    - Test Projection_DarkVc_SymmetricFloorKeepsQuotientWhite: dark fixture (base values that quantize whole vc channels to 0, mirroring RESEARCH Pitfall 1: Neo B channel mean 5.9/255) -> same two-call sequence -> quotient white within 1/255 including below-floor texels
    - Test Projection_PreservesSourceAlpha_UncoveredPassthrough: partial-coverage UV layout (the Residual_UncoveredTexels_AreIdentity [0,0.5]-quad pattern) + varying source alpha -> assert projected RT alpha == source alpha everywhere, uncovered RGB == source RGB (read back the projected RT via AsyncGPUReadback RGBAFloat)
    - Test ResidualMode_NeverKeep_DropsDespiteError: gradient fixture (which Gate would keep) -> mode NeverKeep -> output.Residual is null, Stats.ResidualRequired false, Stats.ChosenResolution 0, Stats.Coverage > 0
    - Test ResidualMode_AlwaysKeep_KeepsDespitePerfectFit: constant-color fixture (which Gate would drop) -> mode AlwaysKeep -> output.Residual not null, Stats.ResidualRequired true
    - ResidualPipelineTests oracle flip: baseValue / Mathf.Max(vcInterp, 1e-3f) becomes Mathf.Max(baseValue, 1e-3f) / Mathf.Max(vcInterp, 1e-3f) per channel; reconstruction tolerances (2/255) unchanged
  </behavior>
  <action>
    1. In ResidualPipelineTests, flip the CPU oracle to the symmetric floor (both operands
       floored at 1e-3f) and update the suite doc comment to cite RESEARCH Pattern 2
       (measured 1.70% below-floor exception class removed; sub-LSB reconstruction cost).
       Do not touch the D-13 gate/coverage/ladder assertions — those still exercise Gate
       mode, which is the default.
    2. Create NamerProjectionTests.cs in namespace GraffitiEntertainment.Namer.Tests,
       class NamerProjectionTests, mirroring ResidualPipelineTests' capability gating
       (ComputeAvailable skip-with-report), fixture construction, and RGBAFloat readback
       helpers. Reuse the existing quad/partial-quad fixture builders where importable;
       otherwise copy the smallest shape. Implement the five behaviors above. Lease the
       projected RT with the same R16G16B16A16_SFloat linear enableRandomWrite descriptor
       the pipeline uses and release it in a finally.
    3. Keep all timing synchronous (WaitForCompletion + forcePlayerLoopUpdate, existing
       pattern). No sleep-based waits.
  </action>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer042_p01.xml -logFile /tmp/namer042_p01.log; grep -o '<test-run[^>]*' /tmp/namer042_p01.xml | grep -o 'result="[A-Za-z]*"' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - ResidualPipelineTests contains Mathf.Max(baseValue, 1e-3f) on the dividend side of the oracle
    - NamerProjectionTests.cs exists with the five tests named above, all [UnityTest] IEnumerator, capability-gated
    - Full EditMode run: test-run result="Passed" with failed="0" in /tmp/namer042_p01.xml (run inside the live editor via the unity-mcp RunCommand recipe from memory/live-editor-editmode-test-run.md when the interactive editor holds the lock; the batch command above is the fallback and must NOT use -nographics or -quit; snapshot/restore ProjectSettings.asset around batch runs per the memory recipe)
    - New suite has at least 5 passing test cases (grep 'fullname=".*NamerProjectionTests' in the XML)
  </acceptance_criteria>
  <done>Suite green: symmetric-floor oracle flipped, projection identity (white quotient incl. dark/below-floor texels), alpha preservation + uncovered passthrough, and both residual modes proven by automated EditMode tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unity-imported asset → editor pipeline | Mesh/texture data read by the decomposition; sizes drive GPU dispatches and buffer strides |
| Pipeline → GPU | ComputeBuffer sizes derived from split vertex/triangle counts (existing, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-01 | DoS | CSRasterizeVertexColors / CSProjectBase on huge meshes/textures | mitigate (existing) | Dispatch sizing already derived from w/h with 8x8 groups; no new per-texel loops beyond the existing rasterize walk; CSProjectBase is O(1) per texel |
| T-04.2-02 | Tampering | projectedOut caller-owned RT contract | mitigate | Documented contract: caller owns the lease; pipeline never releases a foreign RT (existing Release/pool discipline) |
| T-04.2-03 | Information disclosure | none introduced | accept | Editor-time texture math only; no new data leaves the project |
| T-04.2-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages this phase (RESEARCH Package Legitimacy Audit: not applicable); no install tasks exist |
</threat_model>

<verification>
- Kernel and pipeline changes are additive-with-defaults: `git diff --stat` shows no non-test source touched besides NAMERDecomp.compute and NamerDecompPipeline.cs
- Full EditMode suite green (batch command in Task 3, live-editor recipe preferred per project memory)
- Whiteness identity proven on-engine (not offline): NamerProjectionTests quotient test within 1/255 per channel
</verification>

<success_criteria>
- CSProjectBase + symmetric CSResidual exist and are dispatched from GenerateResidual
- NamerResidualMode { Gate, AlwaysKeep, NeverKeep } implemented; Gate default preserves today's behavior
- Projection identity, alpha preservation, uncovered passthrough, and both modes covered by automated tests
- EditMode suite green (existing suites + 1 new suite; the ResidualPipelineTests oracle flip is intentional and documented in-code)
</success_criteria>

<output>
Create `.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-01-SUMMARY.md` when done
</output>
