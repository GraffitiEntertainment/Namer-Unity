---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
plan: 03
type: execute
wave: 2
depends_on: ["04.2-01", "04.2-02"]
files_modified:
  - Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute
  - Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Bake/NamerRoughnessFitter.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs
  - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs
  - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs
autonomous: true
requirements: [VCOL-03, VCOL-05, UI-03, SHDR-02]
must_haves:
  truths:
    - "Default Process run with decomposition on produces a one-texture outcome: no residual EXR, vertex-colored mesh, packed surface PNG whose alpha carries the transferred roughness dip"
    - "With the Write Residual checkbox on, the residual EXR is written from the SOURCE albedo dividend (honest removed-detail error), and the adaptive ladder keys on source-reconstruction error"
    - "The estimator dropdown is gone: a Dip Source popup chooses Removed Detail (default) or Sobel Edge; no strength search, no fit cache, no frequency separation, no NamerRoughnessFitter"
    - "The dip-depth slider owns the dip magnitude end to end (taste, no precompute) and defaults to 0.25"
    - "The full EditMode suite is green after the intentional flips"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs"
      provides: "NamerProjectionContext + projection-driven Process flow + SourceBaseColor"
      contains: "NamerProjectionContext"
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs"
      provides: "NamerDipSource enum + DipSource field"
      contains: "NamerDipSource"
    - path: "Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs"
      provides: "DipSource + WriteResidual settings keys"
      contains: "WriteResidual"
  key_links:
    - from: "NamerComputePipeline.Process"
      to: "NamerRoughnessPipeline.ExtractTransferRoughness"
      via: "projection branch dispatch between normalize and pack"
      pattern: "ExtractTransferRoughness"
    - from: "NamerProcessor"
      to: "NamerDecompPipeline.GenerateResidual"
      via: "projection context Run delegate"
      pattern: "NamerResidualMode\\.(AlwaysKeep|NeverKeep)"
    - from: "NamerEditorWindow"
      to: "settings.WriteResidual"
      via: "checkbox in DrawDecompositionSection"
      pattern: "WriteResidual"
    - from: "CSSurfacePack"
      to: "transfer roughness map"
      via: "roughnessDipApplied adopt-as-is branch"
      pattern: "_RoughnessDipApplied"
---

<objective>
The rewire: route the projection (plan 01) and the transfer (plan 02) through the real
Process flow, delete the retired machinery, and land the UI controls. After this plan the
product behaves per the locked 04.2 design — decomposition on by default means the
cleaned base IS the Gouraud projection (residual white by construction, VCOL-03), the
removed detail's luminance is re-expressed as gloss through the dip-depth taste slider
(SHDR-02), one-texture is the default with an explicit Write Residual checkbox whose
dividend is the source albedo (VCOL-05), and the estimator split is replaced by a
two-option Dip Source (UI-03).

Retirements (locked): strength ladder search, FitOnlyMaxError threshold gamble, residual
correction mask (never built), fit cache, frequency separation (blur/sharp kernels),
NamerRoughnessFitter, the estimator enum split, and the EvaluateRefitMaxError estimator
helper. The Sobel chain survives as the alternate dip source (CONTEXT discretion +
research recommendation).
Output: rewired NamerComputePipeline/NamerProcessor/NamerEditorWindow, renamed enum +
new settings keys, deleted NamerRoughnessFitter.cs, flipped test suites, green suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-CONTEXT.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-RESEARCH.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-01-SUMMARY.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-02-SUMMARY.md

<interfaces>
<!-- From plan 01 (NamerDecompPipeline.cs): -->
```csharp
public enum NamerResidualMode { Gate, AlwaysKeep, NeverKeep }
public NamerDecompOutput GenerateResidual(
    NamerSplitResult split, Color32[] quantizedColors, RenderTexture baseLinear,
    int w, int h, float errorThreshold, int manualResolution,
    RenderTexture projectedOut = null, NamerResidualMode mode = NamerResidualMode.Gate)
// NamerDecompOutput fields: Residual (RenderTexture), Stats (NamerDecompErrorStats)
```

<!-- From plan 02 (NamerRoughnessPipeline.cs): -->
```csharp
public RenderTexture ExtractTransferRoughness(float scalarRoughness,
    RenderTexture sourceBase, RenderTexture projectedBase, int w, int h, float strength)
// returns caller-owned R8G8B8A8_UNorm map, dip encoded in .r; quiet texels == scalar
```

<!-- Existing contracts this plan rewires (read before editing):
     NamerComputePipeline.Process(inspection, Func<float,float> evaluate = null,
         Func<bool> shouldCancel = null, float maxErrorThreshold = 0f)
       - _currentBaseColorOut reentrancy field, shouldExtract gate
         (MetallicGlossMap == null && RoughnessExtractStrength > 0), isFitDriven branch,
         BindAndDispatchSurfacePack(..., roughnessTex, hasExtractedRoughness,
         roughnessExtractStrength, roughnessDipApplied, w, h),
         ExtractSharpRemoval/ReleaseExtractedRoughness/TryGetFitStrength forwarders,
         ReleaseResult routing via NormalizedBaseColorOwnedByRoughnessPool,
         NamerComputeResult {NormalizedBaseColor, PackedSurface, ExtractedRoughness,
         NormalizedBaseColorOwnedByRoughnessPool, RoughnessFitCancelled, Width, Height}
     NamerProcessor.Process static: evaluate callback composition (MeshVertexSplitter.Split
         + ExtractSharpRemoval + EvaluateRefitMaxError), decomp block
         (ReadBackBase -> VertexColorFitter.Fit -> ToColor32Array -> GenerateResidual),
         CR-01/CR-03 guard HelpBox routing
     NamerSourceModel.cs: enum NamerRoughnessEstimator { FitDriven = 0, Sobel = 1 },
         field RoughnessEstimator (line ~99)
     NamerProcessorSettings.cs: RoughnessEstimatorKey
         "NamerProcessor.RoughnessEstimator", RoughnessExtractStrengthKey
         "NamerProcessor.RoughnessExtractStrength", foldout keys
     NamerEditorConstants.cs: DefaultRoughnessExtractStrength = 1f,
         DefaultRoughnessEstimator = 0
     NamerEditorWindow.cs: _roughnessEstimator field (line 74), RebuildInspection sync
         (211-212), RecomputePreview evaluate composition (342-363), RunDecompPreview
         duplicate fit (572-605), DrawRoughnessExtractionSection (849-908),
         DrawDecompositionSection (979+)
     CSSurfacePack roughness branch: _RoughnessDipApplied > 0.5 -> adopt _RoughnessTex.r
         as-is; else dip saturate(roughness - _RoughnessExtractStrength * _RoughnessTex.r) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engine rewire — projection hook through Process + retirement sweep</name>
  <files>Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute, Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs, Packages/com.graffitientertainment.namer/Editor/Bake/NamerRoughnessFitter.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs, Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs, Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs (whole file)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (whole file)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs (whole file)
    - Packages/com.graffitientertainment.namer/Editor/Bake/NamerRoughnessFitter.cs (whole file — the deletion target)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs (enum + fields)
    - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs (whole file)
    - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs (whole file)
    - Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute (CSSurfacePack roughness branch comments)
  </read_first>
  <action>
    Context note: this is the context-heavy task of the phase (9 files, whole-file
    read_first list) — read each listed file once, in order, and keep edits surgical so
    tasks 2-3 stay inside the context budget.

    1. NamerSourceModel.cs — rename enum NamerRoughnessEstimator to NamerDipSource with
       values RemovedDetail = 0, SobelEdge = 1; rename field RoughnessEstimator to
       DipSource (default RemovedDetail); update doc comments (04.2: dip source selection
       replaces the estimator split).
    2. NamerProcessorSettings.cs — delete RoughnessEstimator property +
       RoughnessEstimatorKey; add DipSource (key "NamerProcessor.DipSource", int,
       default 0) and WriteResidual (key "NamerProcessor.WriteResidual", bool, default
       false). Do NOT migrate the stale estimator key (research: stale-key acceptance).
       NamerEditorConstants.cs — delete DefaultRoughnessEstimator; add DefaultDipSource =
       0 and DefaultWriteResidual = false; change DefaultRoughnessExtractStrength from 1f
       to 0.25f (research taste range 0.2-0.3) and update its doc comment to dip-depth
       semantics.
    3. NAMERRoughness.compute — delete the retired kernels CSRoughnessBlurH,
       CSRoughnessBlurV, CSRoughnessSharpDetail, CSRoughnessSharpRemoval, and the old
       CSRoughnessRemap, plus their #pragma lines, the _BlurPing/_Blurred/_SharpDetail/
       _CleanedBaseOut declarations, the _Radius/_Sigma uniforms, and related comments.
       Keep: CSRoughnessSobel, CSRoughnessMaxReduce, CSRoughnessNormalize (Sobel chain),
       and the plan-02 transfer kernels.
    4. NamerRoughnessPipeline.cs — delete NamerRoughnessExtractResult class,
       ExtractRoughness, the fit-driven extraction method, RunFrequencySeparation,
       RunBlurAndSharpDetail, RunSharpRemoval, RequestFit, ClearFitCache, TryGetFitStrength,
       _fitCache, MakeFitKey, the AssetContentStamp struct, blur constants, and the
       _kernelBlurH/_kernelBlurV/_kernelSharpDetail/_kernelSharpRemoval/_kernelRemap
       handles. Make ExtractSobel public (doc: alternate dip source when decomposition is
       off or DipSource == SobelEdge). Keep ReleaseRoughness/Dispose/ReduceToGlobalMax/
       RobustSobelScale + plan-02 additions. Update the class doc comment: 04.2 pipeline —
       transfer is primary, Sobel is the fallback dip source; the strength search retired.
    5. Delete Editor/Bake/NamerRoughnessFitter.cs AND its .meta file. Nothing may
       reference NamerRoughnessFitter or NamerRoughnessFitResult afterwards.
    6. NamerComputePipeline.cs — replace the evaluate/shouldCancel/maxErrorThreshold
       parameters of Process with a single NamerProjectionContext projection = null.
       Define public sealed class NamerProjectionContext in the same file holding:
       NamerSplitResult Split, Color32[] Colors, NamerDecompOutput Decomp, and a
       delegate-style Run(RenderTexture sourceBase, RenderTexture projectedOut) callback
       plus the inspection-derived knobs it needs (errorThreshold, manualResolution,
       writeResidual). New flow: after the normalize step, if projection != null — lease
       a projectedOut RT (R16G16B16A16_SFloat, enableRandomWrite, from the compute pool),
       invoke projection.Run(baseColorOut, projectedOut); extraction branches become:
       (a) projection != null && DipSource == RemovedDetail && shouldExtract — call
       ExtractTransferRoughness(inspection.Roughness, baseColorOut, projectedOut, w, h,
       inspection.RoughnessExtractStrength), set roughnessDipApplied = true;
       (b) shouldExtract otherwise (DipSource == SobelEdge or authored-map absence with
       no projection) — public ExtractSobel, roughnessDipApplied = false (CSSurfacePack
       dip branch, slider-scaled as today); (c) authored roughness map present — existing
       behavior unchanged. BindAndDispatchSurfacePack call shape unchanged. Result:
       NormalizedBaseColor = projectedOut when projection ran else baseColorOut;
       add SourceBaseColor = baseColorOut when projection ran else null (kept alive for
       the residual-ON dividend and the window preview); delete
       NormalizedBaseColorOwnedByRoughnessPool, RoughnessFitCancelled, _currentBaseColorOut,
       ExtractSharpRemoval, ReleaseExtractedRoughness, TryGetFitStrength forwarders;
       update ReleaseResult + catch/finally release routing for the two pool-owned
       normalized candidates (projected survives into the result; source base releases
       unless referenced by SourceBaseColor).
    7. NamerProcessor.cs — delete EvaluateRefitMaxError and the evaluate composition;
       build the projection context instead via a new internal static helper
       CreateProjectionContext(NamerSplitResult split, NamerDecompPipeline decomp, int w,
       int h, float errorThreshold, int residualResolution, bool writeResidual) whose Run
       does: ReadBackBase(sourceBase) -> using VertexColorFitter.Fit -> ToColor32Array ->
       decomp.GenerateResidual(split, colors, sourceBase, w, h, errorThreshold,
       residualResolution, projectedOut, writeResidual ? NamerResidualMode.AlwaysKeep :
       NamerResidualMode.NeverKeep). The Process decomp block consumes the context after
       pipeline.Process returns (build NamerDecompData from ctx.Split/ctx.Colors/
       ctx.Decomp.Residual + Stats; dispose ctx.Decomp in the existing finally). The
       legacy branch (DipSource == SobelEdge with decomposition on) keeps today's
       readback->fit->GenerateResidual flow but passes mode from settings.WriteResidual
       (AlwaysKeep/NeverKeep — Gate retires from call sites). Create/dispose the
       NamerDecompPipeline before/after pipeline.Process exactly as today.
    8. NAMERPack.compute — update the CSSurfacePack roughness-branch comment that
       references the old CSRoughnessRemap/FitDriven wording to describe the 04.2
       contract: dipApplied means the bound map is already dip-encoded (transfer path,
       adopted as-is); otherwise the inline dip consumes the Sobel map scaled by the
       slider.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && ! ls Packages/com.graffitientertainment.namer/Editor/Bake/NamerRoughnessFitter.cs >/dev/null 2>&1 && ! grep -rn --include=*.cs -e "NamerRoughnessFitter" -e "TryGetFitStrength" -e "ExtractSharpRemoval" -e "EvaluateRefitMaxError" -e "NamerRoughnessEstimator" -e "RoughnessFitCancelled" Packages/com.graffitientertainment.namer/Editor Packages/com.graffitientertainment.namer/Runtime 2>/dev/null && grep -q "NamerProjectionContext" Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs && grep -q "WriteResidual" Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs && grep -q "NamerDipSource" Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs && echo ENGINE-REWIRE-OK</automated>
  </verify>
  <acceptance_criteria>
    - NamerRoughnessFitter.cs and its .meta are gone; zero references to NamerRoughnessFitter, NamerRoughnessFitResult, TryGetFitStrength, ExtractSharpRemoval, EvaluateRefitMaxError, NamerRoughnessEstimator, RoughnessFitCancelled, RequestFit, _fitCache, RunFrequencySeparation, CSRoughnessRemap, CSRoughnessBlurH, CSRoughnessSharpRemoval anywhere under Packages/
    - grep -c "^#pragma kernel" NAMERRoughness.compute = Sobel chain (3) + transfer kernels (2) = 5
    - NamerDipSource { RemovedDetail = 0, SobelEdge = 1 } replaces the estimator enum; settings expose DipSource ("NamerProcessor.DipSource") and WriteResidual ("NamerProcessor.WriteResidual", default false); "NamerProcessor.RoughnessEstimator" key string absent from source
    - DefaultRoughnessExtractStrength == 0.25f
    - NamerComputePipeline.Process signature is (inspection, NamerProjectionContext projection = null); NamerComputeResult has SourceBaseColor and no RoughnessFitCancelled/NormalizedBaseColorOwnedByRoughnessPool
    - This task does NOT need to leave the editor compiling mid-way, but the file set above must compile together by end of task 2 (the Editor asmdef is compile-atomic; the window is edited in task 2 — run compile checks only after task 2)
  </acceptance_criteria>
  <done>Projection + transfer wired through the production Process flow; ladder search, fit cache, frequency separation, fitter, and estimator split deleted; enum/settings/defaults renamed per 04.2</done>
</task>

<task type="auto">
  <name>Task 2: Window rewire + Dip Source / Write Residual / dip-depth UI</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (whole file — fields, RebuildInspection, RecomputePreview, RunDecompPreview, DrawRoughnessExtractionSection, DrawDecompositionSection, residual binding, lifecycle)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs (task 1 result — Process + NamerProjectionContext contract)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (task 1 result — CreateProjectionContext)
  </read_first>
  <action>
    1. Fields: rename _roughnessEstimator to _dipSource (NamerDipSource); add private bool
       _writeResidual; load/save them via the new settings keys in the window's settings
       load/save paths (EditorPrefs int/bool), replacing the estimator key usage.
    2. RebuildInspection: assign inspection.DipSource = _dipSource (replaces the
       RoughnessEstimator sync at lines 211-212).
    3. RecomputePreview: delete the 04.1 evaluate composition (ExtractSharpRemoval +
       TryGetFitStrength callback at lines 342-363); instead, when decomposition will run
       AND _dipSource == RemovedDetail, build the projection context exactly like
       NamerProcessor does (call its internal static CreateProjectionContext — the window
       already references the NamerProcessor assembly) and pass it to
       _pipeline.Process(inspection, projection). Otherwise pass null. The
       _errorThreshold/_residualResolution settings feed the context.
    4. RunDecompPreview: stop duplicating the fit (delete the ReadBackBaseTexels ->
       Split -> Fit -> GenerateResidual block at lines 572-605). When the projection ran,
       consume the context: _previewSplitMesh from ctx.Split/ctx.Colors (existing preview
       mesh builder), _decompOutput = ctx.Decomp, stats readout from its Stats. Legacy
       branch (SobelEdge + decomp on): keep a minimal fit -> GenerateResidual with mode
       from _writeResidual. Dispose the decomp pipeline per the existing lifecycle.
    5. DrawRoughnessExtractionSection: replace the estimator popup with a Dip Source
       popup labeled "Dip Source", options { "Removed Detail", "Sobel Edge" } (enum values
       RemovedDetail/SobelEdge); delete the fit-driven searched-strength HelpBox
       (TryGetFitStrength readout — the API is gone); relabel the strength slider to
       "Roughness Dip Depth" with tooltip: taste control — how strongly removed-detail
       luminance is re-expressed as gloss (bright speckle dips toward gloss, dark
       occlusion raises toward matte; 0 = keep authored roughness scalar); when
       _dipSource == SobelEdge show an indented HelpBox noting the removed-detail path
       requires decomposition and Sobel is a fallback signal; when decomposition is off or
       CR-01-guarded, the effective dip source is Sobel — state that honestly.
    6. DrawDecompositionSection: add a "Write Residual Texture" toggle bound to
       _writeResidual (persisted via settings.WriteResidual) directly under the
       decomposition toggle. Tooltip copy (keep honest, concrete): default OFF = one-texture
       outcome (no residual EXR — the projection already makes base/vcInterp white by
       construction); ON = additionally write source ÷ vertex-color interpolation as an
       EXR carrying the removed detail (including its luminance, which is also re-expressed
       as gloss); UV-overlap texels use whichever triangle rasterized first, so overlapped
       regions may carry interpolation of the winning triangle only. When OFF, gray out or
       de-emphasize the Error Threshold and Residual Resolution controls (they only act
       when residual writing is on) — the 04.1 disable-state pattern is the reference.
    7. Residual preview binding (lines 377-395): unchanged logic — when the context
       produced a residual (checkbox ON) bind it; when _decompOutput == null bind base as
       today; else unbind (one-texture).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "Write Residual Texture" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "Dip Source" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "Roughness Dip Depth" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && ! grep -q "TryGetFitStrength\|FitDriven\|RoughnessEstimator" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && echo WINDOW-OK</automated>
  </verify>
  <acceptance_criteria>
    - Window compiles against the task-1 engine (this is the first compile gate — run the Task 3 suite command or a compile check now)
    - No occurrence of FitDriven, RoughnessEstimator, estimator popup strings, or searched-strength HelpBox in the window
    - Dip Source popup writes NamerDipSource values; Write Residual toggle persists via "NamerProcessor.WriteResidual"; dip-depth slider persists via the existing strength key and defaults to 0.25 for fresh installs
    - RunDecompPreview contains no second VertexColorFitter.Fit call in the projection path (grep VertexColorFitter in the window: only the legacy SobelEdge branch may retain it)
    - Error Threshold + Residual Resolution controls de-emphasized/disabled when Write Residual is off
  </acceptance_criteria>
  <done>Editor window drives the projection flow end to end; checkbox + dip source + taste slider are the only roughness controls left</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Intentional test flips + full EditMode suite green</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs (whole file — the six fit-driven tests + EditorPrefs snapshot helpers)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs (whole file — estimator enum usage)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs (lines 1-200 + prefs snapshot 578-642 — HonestGate test)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs (whole file)
    - .planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-RESEARCH.md (Pitfall 6: flip tests in the same wave as the deletion)
  </read_first>
  <behavior>
    - Deleted tests (search-retired, no successor): FitDriven_SelectsFirstPassingStrength, FitDriven_CancelledFit_SurfacesCancelledFlag — the ladder and the cancellation flag no longer exist
    - FitDriven_BakedResponse_ResidualCollapses -> Projection_BakedResponse_ResidualCollapsesByConstruction: default settings (decomp on, RemovedDetail, WriteResidual off) on the baked-response fixture -> no residual EXR, output mesh has Color32 vertex colors, packed surface exists
    - FitDriven_PackedRoughness_AdoptsSearchedStrengthNotSliderBlend -> Transfer_PackedRoughness_DipsBySliderDepth: baked-speckle fixture, two runs strength 0 vs strength 0.5 -> surface alpha bits differ (dip present at 0.5); at strength 0 alpha encodes the authored scalar exactly
    - FitDriven_Roughness_UsesSobelEdgeSignal -> Transfer_Roughness_UsesRemovedLumaNotSobel: fixture whose removed detail is a smooth interior gradient patch (Sobel-of-base interior ~0, removed luma != 0) -> roughness dips in the patch interior under RemovedDetail, proving the dip follows removed-luma rather than an edge signal
    - FitDriven_DefaultOn_DecomposedNoMapAsset_ExtractsAndDropsResidual -> DefaultOn_OneTextureWithDip: fresh prefs -> one-texture output + extracted roughness present
    - NamerRoughnessExtractionTests: NamerRoughnessEstimator.Sobel -> NamerDipSource.SobelEdge renames; the authored-map premise test passes strength 0 with DipSource RemovedDetail — behavior unchanged
    - NamerOneTextureTests.HonestGate_AlbedoDetail_StillRequiresResidual flips to HonestGate_AlbedoDetail_OneTextureByDefault: WriteResidual off + albedo detail -> no residual EXR; a sibling case with WriteResidual on -> EXR written
    - NamerDecompIntegrationTests.Process_WithDecomposition_WritesMeshAndResidualAndSwapsMesh: fixture sets WriteResidual = true (keeps testing mesh + EXR + swap); Process_ReducedResolutionResidual_StampsBilinearImporter: WriteResidual = true; a new/default case asserts the one-texture no-EXR default; Process_ConstantColorBase_AutoDropsResidual stays valid (NeverKeep drops)
    - Prefs snapshot helpers: swap "NamerProcessor.RoughnessEstimator" for the DipSource/WriteResidual keys
  </behavior>
  <action>
    1. Rewrite NamerRoughnessFitTests.cs per the behavior list: delete the two
       search-retired tests, rename the file's suite concept to the projection era (keep
       the filename — asmdef stability), keep the EditorPrefs snapshot/restore helpers
       with the new key set. Use NamerProcessor/NamerComputePipeline entry points, not
       the deleted APIs. Fixture note from project memory: luminance ramps flatten Sobel —
       for the UsesRemovedLumaNotSobel test use hash-noise-free smooth patches as stated
       (the discriminator IS smoothness).
    2. Apply the enum renames in NamerRoughnessExtractionTests.cs (Sobel ->
       NamerDipSource.SobelEdge; FitDriven references -> RemovedDetail or removal per
       test intent). The 04.1-07 dip-coupling premises that pinned
       saturate(scalar - strength * Sobel) semantics stay — the Sobel branch keeps that
       formula at CSSurfacePack.
    3. Flip NamerOneTextureTests: default one-texture expectations per the behavior list;
       update the prefs snapshot keys; HonestGate test becomes the two-case flip.
    4. Update NamerDecompIntegrationTests fixtures: WriteResidual = true where the EXR is
       the subject; add the default no-EXR case.
    5. Run the FULL EditMode suite. Fix source bugs at the root when tests fail (no test
       guards, no workarounds). The expected flip set is exactly the list above — any
       OTHER failing test means an implementation defect in tasks 1-2.
    6. Grep gate: confirm the retired-symbol sweep from task 1 still holds including the
       Tests directory, plus "NamerProcessor.RoughnessEstimator" returning zero matches
       repo-wide under Packages/.
  </action>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer042_p03.xml -logFile /tmp/namer042_p03.log; grep -o '<test-run[^>]*' /tmp/namer042_p03.xml | grep -oE 'result="[A-Za-z]+"|passed="[0-9]+"|failed="[0-9]+"' | head -3; grep -rn "NamerProcessor.RoughnessEstimator" Packages/ 2>/dev/null | wc -l | grep -qx 0 && echo SWEEP-CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Full EditMode result="Passed" failed="0" (live-editor RunCommand recipe preferred; batch fallback — no -nographics, no -quit, ProjectSettings.asset snapshot/restore)
    - passed count >= 124 minus deleted tests plus new tests; the flip set matches the behavior list exactly (documented in the SUMMARY)
    - Repo-wide grep for NamerRoughnessFitter, TryGetFitStrength, ExtractSharpRemoval, EvaluateRefitMaxError, NamerRoughnessEstimator, RoughnessFitCancelled, "NamerProcessor.RoughnessEstimator" returns 0 matches under Packages/
    - No test asserts the retired Gate mode through the production path (plan 01's direct GenerateResidual tests keep their Gate-mode coverage at the API level only if still compiling — Gate itself remains a valid enum value on the pipeline API)
  </acceptance_criteria>
  <done>Projection-era behavior pinned by flipped tests; the full EditMode suite is green; the retired-symbol sweep is clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unity-imported asset → processor | Source mesh/texture inspection drives split, fit, and dispatch sizing (existing) |
| Processor → NAMERGenerated writes | AssetGenerator writes material/mesh/textures under the destination folder; the destination guard + generated-asset-only overwrite stamping are the existing controls (unchanged this plan — writeResidual already keyed on decomp.Stats.ResidualRequired, which the checkbox now drives through AlwaysKeep/NeverKeep) |
| Pipeline → GPU | Two new dispatches per run (projection write-back, transfer) + retained pool leases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-07 | Tampering | Asset overwrite scope after rewire | mitigate (existing) | Destination-guard + overwrite-stamp checks from Phase 3 unchanged; the checkbox only flips ResidualRequired, and AssetGenerator still writes only under NAMERGenerated paths — covered by the existing source-immutability test which must stay green (it is NOT in the flip set) |
| T-04.2-08 | DoS | RT lifetime growth (SourceBaseColor + projected + transfer map) | mitigate | Compute-pool leases with finally-release exactly like the retired sharp-removal leases; ReleaseResult routing updated in the same edit; no net-new unbounded lease |
| T-04.2-09 | Repudiation | Stale settings key (RoughnessEstimator) silently reinterpreted | mitigate | Key deleted, not migrated — fresh default (RemovedDetail) is the documented 04.2 behavior; no cross-version promise exists for editor prefs in a pre-release plugin (research stale-key acceptance) |
| T-04.2-10 | DoS | Fitter deletion leaves dead Burst paths | mitigate | Full-file deletion + repo-wide grep gate for strays; suite green proves no dangling references |
| T-04.2-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages; no install tasks |
</threat_model>

<verification>
- Retired-symbol sweep clean (task 3 grep gate)
- Full EditMode suite green with the documented intentional flip set
- Default-path behavior: one-texture (no EXR), dip present in packed alpha at default strength 0.25
- Checkbox path: EXR written from source dividend; ladder acts (ChooseResolution on source-reconstruction error)
</verification>

<success_criteria>
- Process flows through NamerProjectionContext; transfer dip lands in packed surface alpha via the adopt-as-is branch
- NamerRoughnessFitter, ladder, cache, frequency separation, estimator split, EvaluateRefitMaxError all gone
- Dip Source popup + Write Residual checkbox + Roughness Dip Depth slider shipped with honest tooltips
- Suite green; flip set exactly as designed
</success_criteria>

<output>
Create `.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-03-SUMMARY.md` when done
</output>
