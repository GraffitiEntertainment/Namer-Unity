---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
plan: 04
type: execute
wave: 3
depends_on: ["04.2-03"]
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs
autonomous: true
requirements: [UI-03, VCOL-04, SHDR-02]
must_haves:
  truths:
    - "A processed baked-speckle asset ships one-texture by default and its packed-surface alpha visibly varies with the dip-depth slider (removed detail re-expressed as gloss, not lost)"
    - "With Write Residual on, the EXR exists and its quotient reflects the source dividend (non-white in the speckle region), and the stats panel reports removed-detail error semantics honestly"
    - "The UI states the accepted chroma loss and the UV-overlap honesty caveat with the measured numbers, and de-emphasizes controls that do not apply"
    - "The full EditMode suite ends green with the phase's intentional flip set audited and listed"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs"
      provides: "end-to-end acceptance tests for ROADMAP criteria 1-3"
      min_lines: 150
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs"
      provides: "settings round-trip + defaults tests for the new controls"
      min_lines: 60
  key_links:
    - from: "NamerProjectionAcceptanceTests"
      to: "NamerProcessor.Process"
      via: "full pipeline invocation on the baked-speckle fixture"
      pattern: "NamerProcessor\\.Process"
    - from: "DrawDecompStats"
      to: "NamerDecompErrorStats"
      via: "relabeled read-only rows"
      pattern: "Removed-detail"
    - from: "NamerUIControlsTests"
      to: "NamerProcessorSettings"
      via: "DipSource/WriteResidual key round-trip"
      pattern: "NamerProcessor\\.DipSource"
---

<objective>
Close the phase: prove the five ROADMAP success criteria end-to-end, finish the UI honesty
pass (stats relabeled to removed-detail semantics, chroma-loss bound and UV-overlap
caveat stated with the measured research numbers, control de-emphasis), and run the full
regression with an explicit audit of the intentional test flips.

Purpose: criteria 1-3 of the phase goal are user-visible outcomes — "residual white by
construction", "removed detail re-expressed as gloss with chroma loss bounded and
documented", "one-texture default + checkbox" — and they need acceptance-level tests that
exercise NamerProcessor.Process, not just kernels (VCOL-04, SHDR-02). Criterion 5 (green
suite + intentional flips) is the exit gate. The overlap-honesty requirement is satisfied
with honest copy carrying the measured numbers, per CONTEXT: "at most an honest statement,
not repair" — a computed overlap-depth statistic is deliberately NOT built (cost: a full
O(texels x tris) second walk per asset; documented decision, not a silent omission).
Output: two new test files, the relabeled/honest stats + tooltips, and the phase's
regression green light.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-CONTEXT.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-RESEARCH.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-03-SUMMARY.md

<interfaces>
<!-- From plan 03: NamerProcessor.Process now composes the projection internally from the
     inspection (DipSource, RoughnessExtractStrength) + settings (WriteResidual); the
     NamerComputeResult carries NormalizedBaseColor (projected) + SourceBaseColor; the
     decomp output flows to AssetGenerator keyed on Stats.ResidualRequired, which
     AlwaysKeep/NeverKeep drive. Settings keys: "NamerProcessor.DipSource" (int, 0),
     "NamerProcessor.WriteResidual" (bool, false),
     "NamerProcessor.RoughnessExtractStrength" (float, default 0.25). -->

<!-- DrawDecompStats currently renders five read-only rows from NamerDecompErrorStats:
     Coverage, AvgError, MaxError, FitOnlyMaxError, ResidualRequired/ChosenResolution.
     Relabel per this plan; do not add computed overlap statistics (documented decision
     above). -->

<!-- Measured numbers to carry into copy (04.2 RESEARCH, Neo measurements):
     removed-luma texel split 66.9% dark / 33.1% bright; |luma| p90 = 0.173;
     luminance retains ~55% of removed-signal energy (p10 37%); discarded chroma
     ~0.071 linear mean; 87.9% of covered texels multi-covered on stacked UVs
     (first-covering-triangle-wins rasterization). -->

<!-- Test-execution recipe: live editor unity-mcp RunCommand preferred; batch fallback
     (no -nographics, no -quit, snapshot/restore ProjectSettings.asset). Pitfall 4:
     never gate pipeline logic on re-read PNG values (sRGB round-trip noise in dark
     channels) — acceptance assertions use in-memory RTs and loose dark tolerances. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NamerProjectionAcceptanceTests — end-to-end criteria 1-3</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs (fixture + prefs snapshot + asset-existence assertion patterns)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs (full NamerProcessor.Process invocation patterns, generated-asset path helpers, cleanup)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (plan 03 result — entry signature)
    - .planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-RESEARCH.md (Pitfall 4 + acceptance sketch in the PRD-linked plan scope note)
  </read_first>
  <behavior>
    - Test Acceptance_OneTextureDefault_NoResidualExr_GlossReexpressed: baked-speckle fixture (hash-noise grain on brightness per the project-memory fixture lesson — luminance ramps flatten Sobel; here the grain must survive as removed detail), default settings -> _Namer_Residual.exr absent under the generated folder, mesh .asset exists with non-empty colors32, _Namer_Surface.png exists; capture packed-surface alpha (in-memory PackedSurface readback, NOT re-read PNG — Pitfall 4) for a companion strength-0 run -> alpha distribution differs (dip present at default 0.25 / at 0.6), and at strength 0 the alpha encodes the authored scalar within quantization
    - Test Acceptance_WriteResidualOn_SourceDividendExr: same fixture + WriteResidual true -> residual EXR exists; assert via the in-memory residual RT (or decomp Stats) that the covered speckle region's quotient is non-white (mean |q-1| above a loose floor, e.g. > 0.01) — the honest removed-detail error — while a projection-dividend quotient stays white (criterion 1 pairing)
    - Test Acceptance_WhitenessByIdentity_E2E: on the processed fixture, GenerateResidual with dividend = projected base (plan 01 API) -> max per-channel |q - 1| <= 1/255 over covered texels (UV-overlap texels included — dividend == divisor regardless of rasterize winner; the only deviation is float16 dust)
    - Test Acceptance_DipDepth_TasteSlider: strength 0 vs 0.6 runs -> alpha roughness spread (max-min over texels) strictly larger at 0.6; both within [0, 1]
    - All tests: capability-gated, prefs snapshotted/restored, generated assets cleaned in finally
  </behavior>
  <action>
    1. Create NamerProjectionAcceptanceTests.cs (namespace
       GraffitiEntertainment.Namer.Tests) following NamerDecompIntegrationTests'
       scaffolding: fixture material + mesh construction, NamerProcessor.Process
       invocation, generated-path resolution, [TearDown] cleanup.
    2. Build the baked-speckle fixture with hash-noise grain modulating brightness (the
       AO-fixture memory lesson inverted: here the grain IS the removable signal). Keep
       fixtures small (e.g. 128x128) for suite speed.
    3. Implement the four behaviors. Assertion discipline per Pitfall 4: every pixel
       assertion reads the in-memory RenderTexture via the existing readback helpers or
       the stats struct; file-existence checks are path-based only; dark-channel
       tolerances loose (>= 3/255 where quantitative).
    4. Document in the suite header which ROADMAP criterion each test maps to
       (criterion 1 -> WhitenessByIdentity, criterion 2 -> OneTextureDefault +
       DipDepth, criterion 3 -> WriteResidualOn).
  </action>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer042_p04a.xml -logFile /tmp/namer042_p04a.log; grep -o '<test-run[^>]*' /tmp/namer042_p04a.xml | grep -o 'result="[A-Za-z]*"' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Four named tests present and passing (result="Passed", failed="0"; live-editor recipe preferred, batch fallback per memory)
    - No assertion in the file reads PNG bytes back for pipeline gating (grep EncodeToPNG/PNG readback usage: only path-existence allowed)
    - One-texture default proven: no EXR file on disk after a default run
    - WriteResidual proven: EXR file on disk + non-white quotient in the speckle region
  </acceptance_criteria>
  <done>ROADMAP criteria 1-3 proven end-to-end through NamerProcessor.Process with the fixture and tolerance discipline of the research pitfalls</done>
</task>

<task type="auto">
  <name>Task 2: UI honesty pass — stats relabel, chroma bound, overlap caveat, controls tests</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (DrawDecompStats + DrawDecompositionSection + DrawRoughnessExtractionSection after plan 03)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerAOControlsTests.cs (controls-test pattern: settings round-trip, defaults, label presence via reflection or drawing)
    - .planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-RESEARCH.md (chroma-loss measurements + overlap prevalence numbers)
  </read_first>
  <action>
    1. DrawDecompStats relabel: the FitOnlyMaxError row becomes "Removed-detail max error"
       with a tooltip explaining it is the source-vs-reconstruction error (what the
       residual-ON EXR encodes and what the gloss transfer consumes); the residual row
       reads "not written (one-texture)" when Stats says not required / checkbox off, or
       "written @ {ChosenResolution}px" when written. Coverage and Avg/Max error rows keep
       their meaning with refreshed tooltips (avg/max of the same removed-detail error).
    2. Deepen the two honesty tooltips with the measured numbers (concrete, not vague):
       the Write Residual tooltip gains the overlap caveat — on stacked-UV assets most
       covered texels are covered by more than one triangle (Neo: 87.9%) and the
       rasterized surface keeps only the first-covering triangle's interpolation, so the
       EXR reads honestly but cannot attribute overlap blending; the dip-depth tooltip
       gains the chroma bound — luminance carries roughly half of the removed signal's
       energy (Neo: ~55%, p10 37%) and the discarded chroma grain averages ~0.07 linear,
       an accepted loss because a scalar gloss channel has no home for color.
    3. Control de-emphasis polish: when Write Residual is off, the Error Threshold and
       Residual Resolution rows render disabled (EditorGUI.DisabledScope) with a
       one-line "only applies when Write Residual is on" hint — matching the phase's
       disable-state conventions.
    4. Create NamerUIControlsTests.cs mirroring NamerAOControlsTests: settings round-trip
       for the three keys ("NamerProcessor.DipSource" int, "NamerProcessor.WriteResidual"
       bool, "NamerProcessor.RoughnessExtractStrength" float), fresh-prefs defaults
       (0 / false / 0.25), and the DrawDecompStats label presence ("Removed-detail max
       error", "not written (one-texture)") via the established pattern. No test may
       assert overlap-statistic computation — the honest-copy decision is deliberate.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "Removed-detail max error" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "not written (one-texture)" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "0.07" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "87.9" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "NamerProcessor.DipSource" Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs && echo UI-HONESTY-OK</automated>
  </verify>
  <acceptance_criteria>
    - Stats rows relabeled with removed-detail semantics; residual row distinguishes "not written (one-texture)" vs "written @ Npx"
    - Chroma-loss and overlap copy present in the window source with the measured numbers (0.07 / 87.9 / ~55%)
    - DisabledScope applied to threshold/resolution controls when Write Residual is off
    - NamerUIControlsTests green: round-trip + defaults (0 / false / 0.25) + label presence
    - No computed overlap-depth statistic was added (grep for new kernel names or an overlap counter: none)
  </acceptance_criteria>
  <done>The UI reports the new semantics honestly with measured numbers, inapplicable controls are visually retired, and the controls behavior is pinned by tests</done>
</task>

<task type="auto">
  <name>Task 3: Full regression + intentional-flip audit</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs</files>
  <read_first>
    - /tmp/namer042_p04a.xml (Task 1 result — the baseline for this run)
    - .planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-03-SUMMARY.md (the flip list documented by plan 03)
  </read_first>
  <action>
    1. Run the FULL EditMode suite (all assemblies, not a filter). Use the live-editor
       RunCommand recipe when the interactive editor is available; otherwise the batch
       fallback with the ProjectSettings.asset snapshot/restore dance. Confirm
       result="Passed" failed="0" and record the total passed count.
    2. Audit the intentional-flip set against plan 03's documented list: deleted
       (FitDriven_SelectsFirstPassingStrength, FitDriven_CancelledFit_SurfacesCancelledFlag),
       rewritten (BakedResponse_ResidualCollapses, PackedRoughness_AdoptsSearchedStrength,
       Roughness_UsesSobelEdgeSignal, DefaultOn_DecomposedNoMapAsset,
       HonestGate_AlbedoDetail_StillRequiresResidual), renamed (Sobel ->
       NamerDipSource.SobelEdge in the extraction suite), fixture-flagged (the two
       decomp integration EXR tests + the new default no-EXR case). Any diff outside
       this list is a defect — fix at the source.
    3. Spot-check the release-routing pitfall (research Pitfall 7): process one fixture
       with WriteResidual on then off and confirm no RT leak or double-release between
       runs (the acceptance tests running back-to-back in one session already exercise
       this; assert no editor errors leaked into the test log — grep the run log for
       the pipeline's release-assertion markers if present, else rely on suite green +
       no UnityEngine errors).
    4. Write the SUMMARY with: final suite counts, the audited flip list, the two
       documented accepted losses (chroma grain, overlap attribution) with their
       measured bounds, and the honest-copy decision rationale (no overlap-depth
       statistic).
  </action>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer042_p04.xml -logFile /tmp/namer042_p04.log; grep -o '<test-run[^>]*' /tmp/namer042_p04.xml | grep -oE 'result="[A-Za-z]+"|passed="[0-9]+"|failed="[0-9]+"' | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Full-suite result="Passed" failed="0"; passed count >= 124 (net of the phase's deletions + additions; record the number)
    - Flip audit: every test identity change maps 1:1 to the plan-03 documented list; zero unexplained diffs
    - Back-to-back WriteResidual on/off runs leave no leaked GPU resources and no editor errors in the log
    - SUMMARY documents counts, flips, accepted losses with bounds, and the honest-copy decision
  </acceptance_criteria>
  <done>Phase exit gate green: full suite passes, flips audited, losses bounded and documented — all five ROADMAP success criteria closed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → NAMERGenerated writes | Acceptance tests generate real assets under temp destination folders; the existing destination guard + source-immutability test remain the control |
| Test fixtures → editor prefs | Tests snapshot/restore EditorPrefs (established pattern) so runs cannot corrupt developer settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-11 | Tampering | Acceptance tests writing assets | mitigate | Tests use temp destination folders + TearDown cleanup; source-immutability suite (not in the flip set) must stay green, proving writes stay inside generated paths |
| T-04.2-12 | DoS | Acceptance fixtures inflating suite time | mitigate | Fixtures capped at 128x128; four tests total; suite budget guarded by the existing ~23s full-run expectation (batch) |
| T-04.2-13 | Repudiation | UI copy claiming unmeasured guarantees | mitigate | Copy carries only research-measured numbers with their source; whiteness claims scoped to "by construction + float16 dust" exactly as tested |
| T-04.2-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages; no install tasks |
</threat_model>

<verification>
- Full EditMode suite green with recorded counts
- All five ROADMAP criteria closed: whiteness by construction (identity test), gloss re-expression + bounded chroma loss (acceptance + tooltip numbers), one-texture default + checkbox (acceptance), consolidated dip source (plan 03 + UI tests), intentional flips audited (task 3)
</verification>

<success_criteria>
- Acceptance suite proves the phase goal end-to-end through NamerProcessor.Process
- UI stats/tooltips honest with measured bounds; inapplicable controls disabled
- Full regression green; flip audit clean; accepted losses documented in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-04-SUMMARY.md` when done
</output>
