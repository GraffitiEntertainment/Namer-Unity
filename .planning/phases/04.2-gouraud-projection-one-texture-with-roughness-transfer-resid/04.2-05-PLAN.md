---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
plan: 05
type: execute
wave: 4
depends_on: ["04.2-04"]
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs
autonomous: true
requirements: [UI-03, UI-04]
must_haves:
  truths:
    - "Both preview panes render through ONE orthographic camera — no fieldOfView anywhere in the preview path, so lighting and silhouette angle no longer shift between the compared objects"
    - "Dragging the preview rotates both objects in sync through ONE shared yaw+pitch rotation — deltaX drives yaw, deltaY drives pitch (clamped to ±89°, the clamp the current code already has), and both panes receive the identical rotation quaternion (amended 2026-09-16 decision: the invariant is pane sync, not axis restriction)"
    - "Scrolling zooms the pair in/out by changing the orthographic size; the camera's position and rotation never move"
    - "A newly framed selection sizes the view to both objects side by side plus a small gap using the rotation-invariant bounding-sphere extent — never cropped at ANY yaw+pitch at default zoom"
    - "Everything the preview already did correctly is untouched: debug-channel toolbar, debounced control-driven recomputes, CR-01 three-branch residual binding, non-destructive in-memory preview (full EditMode suite green)"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs"
      provides: "orthographic PreviewRenderUtility camera + synced two-axis (yaw+pitch) rotation + zoom-scale + rotation-invariant pair-plus-gap framing"
      contains: "orthographicSize"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs"
      provides: "orthographic/two-axis-orbit+pitch-clamp/zoom-clamp/framing-fit/shared-rotation render tests"
      min_lines: 120
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      provides: "drag wiring in HandlePreviewCameraInput (the existing Orbit(delta.x, delta.y) call site already IS the locked mapping — left unchanged) plus orthographic camera-model doc wording"
      contains: "Orbit(current.delta.x, current.delta.y)"
  key_links:
    - from: "NamerEditorWindow.HandlePreviewCameraInput"
      to: "NamerPreviewRenderer.Orbit"
      via: "MouseDrag event (deltaX→yaw, deltaY→pitch)"
      pattern: "Orbit\\(current\\.delta\\.x, current\\.delta\\.y\\)"
    - from: "NamerPreviewRenderer.Render"
      to: "_preview.camera.orthographicSize"
      via: "rect-aspect rotation-invariant pair-plus-gap fit times zoom scale"
      pattern: "orthographicSize"
    - from: "NamerPreviewRenderer.Render"
      to: "both DrawMesh calls"
      via: "one meshRotation quaternion shared by both panes"
      pattern: "DrawMesh\\(afterMesh, afterPosition, meshRotation"
    - from: "NamerPreviewRenderer.Frame"
      to: "HalfSeparation + bounds-sphere radius + PreviewGap"
      via: "rotation-invariant framed half-width/half-height for both objects plus gap"
      pattern: "PreviewGap"
---

<objective>
Rework the preview presentation per the locked mid-planning user decision "Preview
presentation — orthographic synced side-by-side (2026-09-16, AMENDED)": the preview camera
becomes orthographic (the perspective projection offset the lighting and silhouette angle
of the compared objects and biased the A/B judgment that rejected the 04.1 LOOK), the two
objects (source vs NAMER) rotate in sync through one shared rotation — yaw AND pitch both
(the amendment restores pitch; the invariant that matters is that both panes share ONE
rotation, not axis restriction) — zoom becomes orthographic size (scale in/out of the
pair, not camera distance or FOV), and the initial orthographic size fits both objects
wide plus a small gap under a bound that holds at ANY yaw+pitch.

Purpose: the preview is the acceptance instrument for the transferred-roughness LOOK
(UI-03) — criteria 2/4 of the phase goal are judged through these panes, so the judgment
must not be biased by per-pane perspective distortion. This is a camera/presentation
rework of NamerPreviewRenderer plus a doc-comment-only touch in NamerEditorWindow (the
existing `Orbit(current.delta.x, current.delta.y)` MouseDrag call site already implements
the locked drag mapping — deltaX→yaw, deltaY→pitch) — NOT a preview rewrite: the CR-01
three-branch residual binding on recompute, the debug-channel toolbar, the debounced
control-driven recomputes, and source-asset non-destructiveness are preserved
byte-for-byte in behavior.

Constraint that sets the wave: NamerEditorWindow.cs is in plan 04.2-04's files_modified
(Wave 3), so this plan runs in Wave 4 against the post-plan-04 window state (Dip Source
popup / Write Residual checkbox / relabeled stats are already landed by then — do not
touch them).

Finding from planning: NO existing test pins perspective-era behavior — a repo-wide grep
for NamerPreviewRenderer, .Orbit(, .Zoom(, .Frame(, cameraFieldOfView, fieldOfView,
orthographic across Tests/ returns zero matches (NamerEditorWindowSmokeTests.cs only pins
the FoldoutPreview EditorPrefs key). So this plan ADDS pinning tests instead of flipping
any; the intentional-flip discipline still applies as a guard (any failing test outside
NamerPreviewRendererTests means an implementation defect, not a flip).
Output: reworked NamerPreviewRenderer.cs, ortho camera-model wording in
NamerEditorWindow.cs (code paths untouched), new NamerPreviewRendererTests.cs, full
EditMode suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-CONTEXT.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-RESEARCH.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-04-SUMMARY.md

<interfaces>
<!-- Current NamerPreviewRenderer.cs contract being reworked (read the file first —
     signatures below are the load-bearing surface):

```csharp
public sealed class NamerPreviewRenderer : IDisposable
{
    private const float FieldOfView = 30f;          // DELETED (perspective)
    private const float HalfSeparation = 0.7f;      // KEPT, widened to public const
    private const float MinCameraDistance = 1.5f;   // DELETED
    private const float MaxCameraDistance = 100f;   // DELETED
    private const float ZoomSensitivity = 0.15f;    // KEPT, widened to public const
    private const float MinPitch = -89f;            // KEPT — the current pitch clamp
    private const float MaxPitch = 89f;             // KEPT — the current pitch clamp

    private PreviewRenderUtility _preview;
    private Mesh _framedMesh;
    private float _distance = 4f;                   // DELETED -> OrthoCameraDistance const
    private float _yaw;                             // KEPT
    private float _pitch;                           // KEPT (two-axis rotation restored)

    public NamerPreviewRenderer()                   // sets _preview.cameraFieldOfView — REPLACE
    public Texture Render(Mesh beforeMesh, Mesh afterMesh, Material before, Material after, Rect rect)
    public void Frame(Mesh mesh)                    // re-fit framing; resets yaw AND pitch
    public void Orbit(float yawDegrees, float pitchDegrees)  // KEPT — accumulated yaw + clamped pitch
    public void Zoom(float scrollDelta)             // reworked to zoom-scale
    public void Cleanup()
    public void Dispose()
}
```

Key implementation facts from the current source (NamerPreviewRenderer.cs):
- Constructor: `_preview = new PreviewRenderUtility(); _preview.cameraFieldOfView = FieldOfView;`
  plus `_preview.ambientColor = (0.1, 0.1, 0.1, 1)`.
- Render: BeginPreview(rect, GUIStyle.none) -> EnsureFramed -> ApplyCamera -> fixed light
  rotations (50,-30,0 and 340,218,177) -> `meshRotation = Quaternion.Euler(_pitch, _yaw, 0f)`
  -> both meshes drawn at ±HalfSeparation minus `meshRotation * beforeMesh.bounds.center`
  (rotation-in-place counter-offset) -> `_preview.Render(true)` (allowScriptableRenderPipeline
  REQUIRED for URP materials) -> EndPreview. The ONE meshRotation local feeding BOTH DrawMesh
  calls (current lines 86-92) IS the pane-sync invariant the amended decision locks — this
  plan keeps those lines verbatim.
- Orbit (current lines 134-139): `_yaw += yawDegrees; _pitch = Mathf.Clamp(_pitch +
  pitchDegrees, MinPitch, MaxPitch);` with MinPitch = -89f / MaxPitch = 89f (lines 30-31) —
  this two-axis API and its pitch clamp are KEPT as-is (minus the trailing ApplyCamera()
  call, which becomes a no-op once the camera is fixed).
- ApplyCamera: camera at (0, 0, -_distance), rotation identity — camera never moves in the
  new model either; only the clip-distance constant replaces the zoom distance.
- The two preview lights are fixed and shared by both panes — the A/B lighting parity that
  the ortho decision relies on; do not touch them.

<!-- NamerEditorWindow.cs touchpoints (post-plan-04 state — plan 04 will already have
     renamed the estimator UI; the members below are the ones this plan touches):
     - HandlePreviewCameraInput(Rect previewRect) — MouseDrag branch already calls
       `_preview.Orbit(current.delta.x, current.delta.y)` (deltaX→yaw, deltaY→pitch —
       exactly the locked drag mapping; UNCHANGED by this plan); ScrollWheel branch calls
       `_preview.Zoom(current.delta.y)` (unchanged).
     - RebuildInspection calls `_preview.Frame(_previewMesh)` — signature unchanged, no edit.
     - DrawPreviewSection builds the 2:1 previewRect, calls
       `_preview.Render(_previewMesh, afterMesh, beforeMaterial, afterMaterial, previewRect)`
       — signature unchanged, no edit.
     - Class doc comment (lines ~11-21) says "shared orbit/zoom camera" — update wording only.

<!-- Test-execution recipe (project memory): live interactive editor (TestRunnerApi via
     unity-mcp RunCommand) is preferred; batch fallback is
     Unity -batchmode -projectPath ... -runTests -testPlatform EditMode -testResults ... -logFile ...
     with NO -nographics, NO -quit, and ProjectSettings.asset snapshot/restore. Never kill
     the interactive editor. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orthographic renderer rework + NamerPreviewRendererTests</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs (whole file — every member is listed in the plan's interfaces block)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerAfterPanelStateTests.cs (test-file scaffolding convention: namespace GraffitiEntertainment.Namer.Tests, [SetUp]/[TearDown], Dispose discipline)
    - Packages/com.graffitientertainment.namer/Tests/Editor/ComputeSmokeTests.cs (graphics-capability gating pattern for GPU-dependent EditMode tests)
  </read_first>
  <behavior>
    - Test Construction_PreviewCameraIsOrthographic: a fresh NamerPreviewRenderer reports IsOrthographic == true (constructor sets _preview.camera.orthographic = true; cameraFieldOfView/FieldOfView are gone)
    - Test Orbit_AccumulatesYawAndPitch_ClampsPitch_ResetsOnFrame: Orbit(10f, 5f) then Orbit(-25f, 3f) -> YawDegrees == -15f AND PitchDegrees == 8f (tolerance 1e-4) — yaw AND pitch each move the one shared rotation; Orbit(0f, 200f) -> PitchDegrees == 89f and Orbit(0f, -200f) -> PitchDegrees == -89f (the preserved MinPitch/MaxPitch clamp); Frame(mesh) resets BOTH YawDegrees and PitchDegrees to 0f; a Rotate(float) call fails to compile (Orbit(float, float) is the sole rotation API — compile-level pinning)
    - Test Zoom_ClampedScaleFactor_SameWheelDirection: Zoom(1f) -> ZoomScale == 0.85f (1 - 1 * ZoomSensitivity, i.e. the same wheel direction that shortened the retired camera distance shrinks the ortho size); Zoom(-1f) -> ZoomScale grows to ~1.15f; repeated Zoom(10f) clamps at MinZoomScale and repeated Zoom(-10f) clamps at MaxZoomScale (values never escape [MinZoomScale, MaxZoomScale])
    - Test Frame_FitsPairPlusGapAtAnyYawPitch: mesh whose bounds extents are (1, 2, 0.5) -> bounding-sphere radius = sqrt(5.25) ≈ 2.29129, OrthographicSizeForAspect(2f) == 2.3913f (vertical: radius + PreviewGap = 2.39129 dominates (HalfSeparation + radius + PreviewGap)/2 ≈ 1.5456); wide-flat mesh with extents (4, 0.5, 0.1) -> radius = sqrt(16.26) ≈ 4.03237, OrthographicSizeForAspect(2f) == 4.1324f (the rotation-invariant bound dominates even for wide-flat meshes at 2:1 — under pitch the Y extent rotates into the horizontal footprint, so the yaw-only horizontal bound is gone) and OrthographicSizeForAspect(1f) == 4.8324f (square preview: the pair-plus-gap half-width HalfSeparation + radius + PreviewGap = 4.83237 dominates); rotation invariance: after Orbit(45f, 30f), OrthographicSizeForAspect(2f) is unchanged (tolerance 1e-6) — the framing bound cannot be broken by any yaw+pitch; tolerance 1e-3 on the pinned sizes
    - Test Render_OrthoCamera_OneSharedRotation_SizeMatchesFit (graphics-capability-gated): Orbit(30f, 20f) then Render(cubeMesh, cubeMesh, urpLitMaterial, urpLitMaterial, new Rect(0, 0, 512, 256)) returns a non-null texture, IsOrthographic stays true after the render, the camera's applied orthographicSize equals OrthographicSizeForAspect(2f), and YawDegrees/PitchDegrees are unchanged across the Render call (Render consumes — never mutates — the one shared rotation; combined with the both-draws-take-one-meshRotation grep gate in the verify step this pins pane sync)
    - All tests: renderer Dispose in [TearDown]; test meshes/materials created with HideAndDontSave and destroyed in [TearDown]; no writes anywhere (the preview path is disk-free by construction — D-10)
  </behavior>
  <action>
    RED first: create NamerPreviewRendererTests.cs with the five behaviors above (the new
    public accessors do not exist yet, so the file fails to compile — treat the compile
    failure as the RED state, then implement GREEN).

    NamerPreviewRenderer.cs rework (all names are exact):
    1. Constants: DELETE FieldOfView, MinCameraDistance, MaxCameraDistance. KEEP
       HalfSeparation = 0.7f and ZoomSensitivity = 0.15f, widening both to `public const`.
       KEEP `private const float MinPitch = -89f` and `private const float MaxPitch = 89f`
       (the pitch clamp the current Orbit already enforces — cited in the interfaces
       block). ADD `public const float PreviewGap = 0.1f` (the small gap between the two
       objects), `private const float OrthoCameraDistance = 4f` (fixed clip distance — the
       camera never moves for framing or zoom anymore), `private const float
       DefaultOrthographicSize = 1.5f`, `private const float MinZoomScale = 0.05f`,
       `private const float MaxZoomScale = 10f`.
    2. Fields: DELETE _distance. ADD `private float _zoomScale = 1f`, `private float
       _framedHalfWidth = HalfSeparation + 1f + PreviewGap` (= 1.8f), `private float
       _framedHalfHeight = 1f + PreviewGap` (= 1.1f) — the defaults mirror the
       degenerate-radius framing and only matter before the first Frame (Render always
       re-fits). KEEP _preview, _framedMesh, _yaw, _pitch.
    3. Constructor: replace `_preview.cameraFieldOfView = FieldOfView;` with
       `_preview.camera.orthographic = true;` and
       `_preview.camera.orthographicSize = DefaultOrthographicSize;`. Keep the
       ambientColor line unchanged.
    4. Test observability accessors (public read-only, Doxygen one-liners):
       `public bool IsOrthographic` (false when _preview is null, else
       `_preview.camera.orthographic`), `public float YawDegrees` => _yaw,
       `public float PitchDegrees` => _pitch, `public float ZoomScale` => _zoomScale, and
       `public float OrthographicSizeForAspect(float aspect)` => the same pure expression
       Render applies: `Mathf.Max(_framedHalfWidth / Mathf.Max(aspect, 0.01f),
       _framedHalfHeight) * Mathf.Clamp(_zoomScale, MinZoomScale, MaxZoomScale)`.
    5. Render(mesh, mesh, before, after, rect) overloads: signatures and both DrawMesh
       calls unchanged; KEEP `Quaternion meshRotation = Quaternion.Euler(_pitch, _yaw,
       0f)` VERBATIM — the two-axis synced rotation, one quaternion applied to BOTH panes
       (amended 2026-09-16 decision: pane sync is the invariant, not axis restriction;
       both panes already share this one rotation and the fixed shared lights, which is
       the A/B parity the decision wants). After `EnsureFramed(beforeMesh)` and
       `ApplyCamera()`, fit the orthographic size to the rect: `float aspect = rect.width /
       Mathf.Max(rect.height, 1f);` then `_preview.camera.orthographicSize =
       OrthographicSizeForAspect(aspect);` — horizontal fit depends on aspect, so the size
       is applied here where the rect is known, not in Frame. Do NOT touch the light
       rotations or the Render(true) call.
    6. Frame(Mesh mesh): keep the null guard, `_framedMesh = mesh`, `_yaw = 0f`, and
       `_pitch = 0f`; DELETE the FOV/distance math. Compute the rotation-invariant
       pair-plus-gap framing from the bounds: `float radius =
       mesh.bounds.extents.magnitude;` (bounding-sphere half-extent — the horizontal OR
       vertical silhouette of the object is at most this at ANY yaw+pitch, because under
       pitch the Y extent rotates into the horizontal footprint; this supersedes the
       yaw-only sqrt(e.x² + e.z²) reasoning), degenerate guard: `if (radius <= 0f) {
       radius = 1f; }` (the same guard the retired perspective Frame used). Store
       `_framedHalfWidth = HalfSeparation + radius + PreviewGap` (both objects wide plus
       the small gap) and `_framedHalfHeight = radius + PreviewGap`; reset `_zoomScale =
       1f`; call ApplyCamera().
    7. KEEP `public void Orbit(float yawDegrees, float pitchDegrees)` with its current
       body semantics: `_yaw += yawDegrees; _pitch = Mathf.Clamp(_pitch + pitchDegrees,
       MinPitch, MaxPitch);` — the camera never moves (rotation is applied per-draw in
       Render; the camera staying put IS the Unity Inspector-preview semantics, keep it).
       Drop only the trailing ApplyCamera() call — the camera position is fixed at
       OrthoCameraDistance, so there is nothing to re-apply.
    8. Zoom(float scrollDelta): `_zoomScale = Mathf.Clamp(_zoomScale * (1f -
       scrollDelta * ZoomSensitivity), MinZoomScale, MaxZoomScale);` — same multiplication
       shape and wheel direction as the retired distance zoom (positive scrollDelta shrank
       the distance = zoom in; now it shrinks the ortho size = zoom in), but nothing but
       the size changes: the camera position/rotation are fixed.
    9. ApplyCamera(): position the camera at `new Vector3(0f, 0f, -OrthoCameraDistance)`
       with rotation identity (the before/after midpoint stays the look target). Nothing
       else.
    10. Update the class doc comment: orthographic shared camera (the 04.2 user decision —
        perspective offset lighting/silhouette angle between the compared panes and biased
        the A/B judgment), synced two-axis rotation (yaw AND pitch through ONE shared
        quaternion — amended 2026-09-16: pane sync is the invariant, not axis
        restriction; pitch clamped to ±89°), zoom as orthographic size (scale of the pair,
        not camera distance/FOV), initial size = both objects wide plus PreviewGap under
        the rotation-invariant bounding-sphere bound. Keep the existing
        URP/allowScriptableRenderPipeline and rotation-in-place remarks — they still hold.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && ! grep -q "fieldOfView\|MinCameraDistance\|MaxCameraDistance\|public void Rotate" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "orthographicSize" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "Quaternion.Euler(_pitch, _yaw, 0f)" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "public void Orbit(float yawDegrees, float pitchDegrees)" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "Mathf.Clamp(_pitch + pitchDegrees, MinPitch, MaxPitch)" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "DrawMesh(beforeMesh, beforePosition, meshRotation" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "DrawMesh(afterMesh, afterPosition, meshRotation" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && echo RENDERER-ORTHO-OK</automated>
  </verify>
  <acceptance_criteria>
    - grep for fieldOfView, MinCameraDistance, MaxCameraDistance, "public void Rotate" in NamerPreviewRenderer.cs returns nothing; grep for orthographicSize, "Quaternion.Euler(_pitch, _yaw, 0f)", "public void Orbit(float yawDegrees, float pitchDegrees)", "Mathf.Clamp(_pitch + pitchDegrees, MinPitch, MaxPitch)", and both "DrawMesh(..., meshRotation" call sites succeeds
    - The two Render overloads, both DrawMesh calls, the light rotations, and Render(true) are unchanged (diff shows only the listed edits)
    - Constructor sets _preview.camera.orthographic = true (no cameraFieldOfView anywhere)
    - Frame computes _framedHalfWidth = HalfSeparation + bounds.extents.magnitude + PreviewGap and _framedHalfHeight = bounds.extents.magnitude + PreviewGap with the degenerate radius guard (fallback 1f); Frame resets _zoomScale to 1f and BOTH _yaw and _pitch to 0f
    - Orbit keeps the two-axis signature, accumulates yaw unclamped, clamps pitch to [MinPitch, MaxPitch] = [-89, 89], and never moves the camera; Zoom mutates only _zoomScale with the (1f - scrollDelta * ZoomSensitivity) shape, clamped to [MinZoomScale, MaxZoomScale]; ApplyCamera's camera position uses OrthoCameraDistance
    - Five named tests present in NamerPreviewRendererTests.cs and passing (live-editor RunCommand recipe preferred; batch fallback — no -nographics, no -quit, ProjectSettings.asset snapshot/restore)
    - Test 4 pins all three framing numbers with tolerance 1e-3 — 2.3913 (extents (1,2,0.5) at aspect 2), 4.1324 (extents (4,0.5,0.1) at aspect 2), 4.8324 (extents (4,0.5,0.1) at aspect 1) — plus the Orbit-leaves-size-unchanged invariance assertion; the "both objects wide plus a small gap, at any yaw+pitch" decision is pinned numerically
  </acceptance_criteria>
  <done>Preview renders both panes through one fixed orthographic camera; rotation is the one shared yaw+pitch quaternion (pitch clamped ±89°); zoom is orthographic size; rotation-invariant framing fits the pair plus gap; behavior pinned by five green tests</done>
</task>

<task type="auto">
  <name>Task 2: Window camera-model wording + full-suite green with no-flip verification</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (whole file, post-plan-04 state — locate HandlePreviewCameraInput and the class doc comment; the RecomputePreview residual binding, DrawPreviewSection, debug toolbar, and debounce are the preserved surface)
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs (task 1 result — the Orbit/Zoom contract being wired)
    - .planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-04-SUMMARY.md (the suite count this plan must not regress)
  </read_first>
  <action>
    1. HandlePreviewCameraInput(Rect previewRect): NO code change — the MouseDrag branch
       already calls `_preview.Orbit(current.delta.x, current.delta.y);` (deltaX→yaw,
       deltaY→pitch — exactly the locked drag mapping; the amended decision keeps both
       axes), and the ScrollWheel branch already calls `_preview.Zoom(current.delta.y)`
       (Zoom kept its parameter and wheel direction). Verify both branches keep
       current.Use() and Repaint(); do not edit them.
    2. Update the NamerEditorWindow class doc comment: "preview the actual mesh
       before/after with a shared orbit/zoom camera" becomes before/after side by side
       through one shared orthographic camera with synced yaw+pitch rotation (one shared
       rotation for both panes) and orthographic-size zoom. No other prose in the file
       references the camera model — verify with a grep for "orbit" (case-insensitive)
       and update any camera-model straggler to the new wording (references to the Orbit
       METHOD name in code are fine; only prose describing the camera changes).
    3. Change NOTHING else in the window: the MouseDrag/ScrollWheel input branches, the
       CR-01 three-branch residual/preview binding in RecomputePreview, the debug-channel
       toolbar in DrawPreviewSection, the debounced Tick/MarkDirty recompute,
       RebuildInspection's _preview.Frame call, and DrawPreviewSection's _preview.Render
       call all keep their exact current shape (the Frame/Render/Orbit/Zoom signatures are
       unchanged by task 1).
    4. Run the FULL EditMode suite (all assemblies, no filter). Expected diff vs the
       plan-04 exit count: exactly +5 (the new NamerPreviewRendererTests) and 0 failures.
       Planning found zero existing tests referencing NamerPreviewRenderer/.Orbit(/.Zoom(/
       .Frame(/fieldOfView, so there is NO intentional flip set in this plan — any failing
       test is an implementation defect: fix it at the source, never in the test.
    5. Grep gate for stragglers: no `cameraFieldOfView` and no `public void Rotate`
       anywhere under Packages/, and the window still contains the
       `Orbit(current.delta.x, current.delta.y)` call.
  </action>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer042_p05.xml -logFile /tmp/namer042_p05.log; grep -o '<test-run[^>]*' /tmp/namer042_p05.xml | grep -oE 'result="[A-Za-z]+"|passed="[0-9]+"|failed="[0-9]+"' | head -3; cd /Users/Shared/SSDevelopment/Development/Namer-Unity && ! grep -rn "cameraFieldOfView\|public void Rotate" Packages/com.graffitientertainment.namer --include=*.cs && grep -q "Orbit(current.delta.x, current.delta.y)" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && echo WINDOW-ORTHO-OK</automated>
  </verify>
  <acceptance_criteria>
    - Full EditMode result="Passed" failed="0"; passed count == plan-04 exit count + 5 (live-editor RunCommand recipe preferred; batch fallback — no -nographics, no -quit, ProjectSettings.asset snapshot/restore)
    - NamerEditorWindow.cs still contains "Orbit(current.delta.x, current.delta.y)" in the MouseDrag branch; repo-wide grep for cameraFieldOfView and "public void Rotate" under Packages/ returns nothing
    - Diff audit: NamerEditorWindow.cs changes are limited to the class doc comment and (if grep found any) camera-wording stragglers — HandlePreviewCameraInput's code branches, RecomputePreview, DrawPreviewSection, Tick/MarkDirty, and the Frame/Render call sites are byte-identical in behavior
    - Zero unexplained test-identity changes (this plan has no flip set — verified by the suite going green without editing any pre-existing test file)
    - SourceImmutabilityTests pass untouched (preview instantiation stays non-destructive: DrawMesh only, in-memory HideAndDontSave meshes/materials, no disk writes)
  </acceptance_criteria>
  <done>The window drives the orthographic synced preview end to end (input wiring already correct, wording updated); the whole EditMode suite is green with +5 tests and zero flips; every preserved behavior is verified unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Selection → preview scene | Source/inspected meshes and materials are instantiated into the PreviewRenderUtility scene purely for rendering (existing; unchanged) |
| Preview → GPU | One orthographic camera render per Repaint through PreviewRenderUtility (Render(true) — SRP allowed); no compute, no readback added |
| Preview → disk | NONE — the preview path never writes (D-10); the sole writers remain NamerProcessor/AssetGenerator behind the Process button (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-14 | Tampering | Preview instantiation of selection assets | mitigate (existing) | DrawMesh-only consumption — the renderer never mutates source meshes/materials; preview meshes stay in-memory HideAndDontSave; SourceImmutabilityTests are NOT edited by this plan and must end green (explicit acceptance criterion) |
| T-04.2-15 | DoS | Degenerate input to framing/zoom (zero-extents mesh, runaway scroll) | mitigate | Degenerate-radius guard in Frame (fallback 1f, same guard the retired perspective Frame used); zoom clamped to [MinZoomScale, MaxZoomScale]; pitch clamped to [MinPitch, MaxPitch]; aspect guarded against zero-height rects via Mathf.Max(rect.height, 1f) |
| T-04.2-16 | Repudiation | Preview biasing the transferred-roughness LOOK judgment (the defect this plan fixes) | mitigate | Orthographic projection removes the perspective silhouette/lighting-angle offset between panes; both panes share one camera, one rotation (yaw+pitch through a single quaternion), and the fixed preview lights — pinned by the ortho/two-axis-sync/framing tests so a regression to perspective-era bias fails the suite |
| T-04.2-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages; no install tasks |
</threat_model>

<verification>
- Renderer is orthographic end to end: constructor flag, per-render orthographicSize fit, zero fieldOfView references (task 1 grep gate)
- Rotation is synced two-axis: Orbit(float, float) is the sole rotation API; the draw rotation is ONE Quaternion.Euler(_pitch, _yaw, 0f) shared by both panes; pitch clamped to [-89, 89] (the clamp the current code already has)
- Zoom is orthographic size only: camera position/rotation fixed at OrthoCameraDistance; zoom-scale clamped
- Initial size = both objects wide plus a small gap, at any yaw+pitch: rotation-invariant bounding-sphere framing; numeric test covers the vertical-dominant (aspect 2) and horizontal pair-plus-gap (aspect 1) regimes plus the Orbit-invariance assertion
- Full EditMode suite green (+5 tests, 0 flips, 0 failures); source-immutability suite untouched and green
</verification>

<success_criteria>
- The locked (amended) preview decision is fully implemented: orthographic camera, two objects side by side rotating in sync through one shared yaw+pitch rotation (pitch clamped ±89°), orthographic-size zoom, rotation-invariant pair-plus-gap initial size
- Every preserved behavior verified: CR-01 three-branch residual binding, debug-channel toolbar, debounced recomputes, non-destructive preview
- Suite green with the no-flip guarantee stated in the SUMMARY (planning grep evidence + green run)
</success_criteria>

<output>
Create `.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-05-SUMMARY.md` when done
</output>
