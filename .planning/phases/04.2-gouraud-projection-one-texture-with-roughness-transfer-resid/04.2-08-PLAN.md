---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.hlsl
autonomous: true
requirements: [VCOL-04, VCOL-05, UI-03]
gap_closure: true
must_haves:
  truths:
    - "The residual resample preserves the source aspect — a non-square base (e.g. 2048x1024) yields a residual at the aspect-correct (w', h'), never a square target"
    - "The preview projection branch mirrors the CR-03 CannotDecompose guard — a tiling/zero-coverage UV selection previews the non-decomposed Phase-3 shape Process actually writes"
    - "ReleaseDecompPreview disposes the projection context's Decomp before nulling — no pool-leased residual RT leaks on a thrown Process"
    - "NAMER_DECOMP_HEATMAP has a single exit point with bit-identical piecewise-lerp math — no Metal 'potentially uninitialized variable' warning"
    - "Zero of the four warnings in the live editor (console + shader compilation); full EditMode suite green"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs"
      provides: "ResolveAspectSize helper + aspect-correct ChooseResolution/resample/ChosenResolution (long-edge convention)"
      contains: "ResolveAspectSize"
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      provides: "CR-03 mirror guard in RunDecompPreview + Decomp dispose in ReleaseDecompPreview"
      contains: "CannotDecompose"
    - path: "Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.hlsl"
      provides: "single-exit-point NAMER_DECOMP_HEATMAP (nested ternary select)"
      contains: "seg3"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs"
      provides: "non-square aspect-preservation test"
      contains: "GenerateResidual_NonSquareBase_PreservesSourceAspect"
  key_links:
    - from: "NamerDecompPipeline.ChooseResolution / EvaluateResolution / final resample"
      to: "ResolveAspectSize(longEdge, w, h, out w', out h')"
      via: "aspect-preserving (w', h') in place of square (n, n)"
      pattern: "ResolveAspectSize"
    - from: "NamerEditorWindow.RunDecompPreview (projection branch)"
      to: "ctx.Decomp.Stats.CannotDecompose"
      via: "CR-03 mirror guard that returns before consuming ctx.Decomp"
      pattern: "CannotDecompose"
    - from: "NamerEditorWindow.ReleaseDecompPreview"
      to: "NamerDecompOutput.Dispose (idempotent)"
      via: "_projectionContext?.Decomp?.Dispose() before nulling"
      pattern: "_projectionContext?.Decomp?.Dispose()"
    - from: "NAMER_DECOMP_HEATMAP"
      to: "single return of a nested ternary over four precomputed lerp segments"
      via: "no early returns -> clean MSL translation"
      pattern: "return (x < 0.25)"
---

<objective>
Close GAP-3 (04.2-REVIEW.md Warnings WR-01..WR-03 + the user-reported Metal shader
warning) — the user-directed warning-closure batch. Four small, independent fixes, each
confined to one file (except the two NamerEditorWindow.cs fixes, which are disjoint
lines and combined in one task):

- WR-01 (NamerDecompPipeline.cs:329/398/440): the residual ladder resamples to a square
  target clamped by width only, aspect-distorting residuals for non-square bases.
- WR-02 (NamerEditorWindow.cs:600-614): the preview projection branch consumes
  ctx.Decomp without the CR-03 CannotDecompose check NamerProcessor.cs:210-223 applies,
  so tiling-UV selections preview a decomposition Process will not write.
- WR-03 (NamerEditorWindow.cs:571): ReleaseDecompPreview nulls _projectionContext without
  disposing Decomp, leaking the pool-leased residual RT on a thrown Process.
- Metal shader warning: NAMER_DECOMP_HEATMAP (NAMERDecomp.hlsl:23-49) is an early-return
  chain the HLSL->MSL translator turns into predicated assignment, emitting a false-
  positive "potentially uninitialized variable" on the function result.

Out of scope (do NOT plan/do): review Infos IN-01/IN-02/IN-03 and 04.1's four Info
leftovers — keep their recorded dispositions.

Purpose: close every open review Warning and the live-editor shader warning before this
code is relied on for production asset generation (correctness for non-square bases,
preview/output-truth parity, an error-path resource leak, and a clean Metal compile).

Output: four surgical fixes + one new aspect-preservation test, full EditMode green, and
a live-editor smoke confirming zero console + shader-compile warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-REVIEW.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-HUMAN-UAT.md

<interfaces>
<!-- WR-01 current code (NamerDecompPipeline.cs, read the file first):
     - Manual branch (:393-399): `return Mathf.Min(ResolutionLadder[idx], w);`
     - Auto search (:406-424): `int chosen = w; ... if (r >= w) continue; ... chosen = r;`
     - Final resample (:322-332): `if (chosenResolution >= w) returnedResidual = fullResidual;
       else returnedResidual = Resample(fullResidual, chosenResolution, chosenResolution);`
     - Stats upsample (:337): `if (chosenResolution < w) eval = Resample(returnedResidual, w, h);`
     - EvaluateResolution (:440): `RenderTexture down = Resample(fullResidual, resolution, resolution);`
     - ResolutionLadder = { 2048, 1024, 512, 256, 128 } (line 120).
     - Existing tests are SQUARE-source (256x256, 512x512) so the long-edge convention is
       backward-compatible: ChosenResolution == Residual.width still holds for square. -->

<!-- WR-02 current code (NamerEditorWindow.cs:600-614, projection branch of RunDecompPreview):
       `NamerProjectionContext ctx = _projectionContext; _decompOutput = ctx.Decomp;
        _decompStats = ctx.Decomp != null ? ctx.Decomp.Stats : null;
        if (ctx.Split != null && ctx.Colors != null) { _previewSplitMesh = BuildPreviewSplitMesh(...); }
        return;`
     Mirror of NamerProcessor.cs:210-223 (the CR-03 CannotDecompose guard). -->

<!-- WR-03 current code (NamerEditorWindow.cs:563-580, ReleaseDecompPreview):
       `if (_decompOutput != null) { _decompOutput.Dispose(); _decompOutput = null; }
        _projectionContext = null;  // <-- line 571: add Decomp dispose before this
        ...`
     NamerDecompOutput.Dispose is idempotent (guarded by _disposed, NamerDecompPipeline.cs:81-91). -->

<!-- WR-04 current code (NAMERDecomp.hlsl:23-49): the early-return chain. Replace the body
     with a single-exit nested-ternary form; keep the c0..c4 constants and the exact lerp
     coefficients (x/0.25, (x-0.25)/0.25, (x-0.5)/0.25, (x-0.75)/0.25) bit-identical.
     Consumers: NAMERDecomp.compute:203,214 (CSErrorHeatmap) and NamerDebugView.shader:143.
     There is NO existing test pinning the ramp values — the ramp math must stay
     bit-identical and is verified by grep on the anchors/coefficients plus the suite. -->

<!-- Test-execution recipe (project memory): live interactive editor (TestRunnerApi via
     unity-mcp RunCommand) preferred; batch fallback Unity -batchmode ... -runTests
     -testPlatform EditMode ... with NO -nographics, NO -quit, snapshot/restore
     ProjectSettings.asset. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 — aspect-correct residual resample + non-square test</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs, Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs (whole file — ChooseResolution, EvaluateResolution, the step-6 resample at :322-332, the stats upsample at :337, Resample at :576-587, ResolutionLadder at :120)
    - Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs (whole file — CreateSplitQuad/ConstantColors/CreateGradientBase/Release helpers and the existing GenerateResidual_AdaptiveAndManualOverride square-source assertions that must keep passing)
  </read_first>
  <action>
    Test-first RED: add to ResidualPipelineTests.cs a [UnityTest]
    GenerateResidual_NonSquareBase_PreservesSourceAspect (behind the existing
    ComputeAvailable gate). Fixture: const int w = 256, h = 128; NamerSplitResult split =
    CreateSplitQuad(0f, 1f); Color32[] colors = ConstantColors(split.VertexCount, 128);
    RenderTexture baseRt = CreateGradientBase(w, h); call
    pipeline.GenerateResidual(split, colors, baseRt, w, h, 0.05f, 5) (manual popup index 5
    -> ResolutionLadder[4] = 128). Assert: output.Stats.ResidualRequired is true;
    output.Stats.ChosenResolution == 128; output.Residual.width == 128 AND
    output.Residual.height == 64 (aspect preserved; before the fix the height is 128 —
    a square target — so this assertion FAILS). Dispose output and Release(baseRt) in
    finally. Commit: test(04.2-08): add non-square aspect-preservation test (RED).

    GREEN — NamerDecompPipeline.cs (minimal, no refactor):
    1. Add a private static helper near Resample (line ~576):
       ```
       private static void ResolveAspectSize(int longEdge, int w, int h, out int outW, out int outH)
       {
           if (w >= h)
           {
               outW = Mathf.Max(1, longEdge);
               outH = Mathf.Max(1, Mathf.RoundToInt(longEdge * (h / (float)w)));
           }
           else
           {
               outH = Mathf.Max(1, longEdge);
               outW = Mathf.Max(1, Mathf.RoundToInt(longEdge * (w / (float)h)));
           }
       }
       ```
       The convention: the ResolutionLadder value is the LONG edge of the residual; the
       short edge scales to preserve the source aspect.
    2. Manual branch (line 398): `Mathf.Min(ResolutionLadder[idx], w)` ->
       `Mathf.Min(ResolutionLadder[idx], Mathf.Max(w, h))`.
    3. Auto search (lines 406-424): compute `int longEdge = Mathf.Max(w, h);` once, set
       `int chosen = longEdge;`, and change the skip guard `if (r >= w)` -> `if (r >=
       longEdge)`.
    4. EvaluateResolution (line 440): replace the square down-resample with
       `ResolveAspectSize(resolution, w, h, out int cw, out int ch); RenderTexture down =
       Resample(fullResidual, cw, ch);` (the up-resample back to (w, h) stays — already
       aspect-correct).
    5. Step-6 final resample (lines 322-332): hoist `int longEdge = Mathf.Max(w, h);` above
       the if; change the condition to `if (chosenResolution >= longEdge)` and the else to
       `ResolveAspectSize(chosenResolution, w, h, out int cw, out int ch); returnedResidual
       = Resample(fullResidual, cw, ch);`.
    6. Stats upsample (line 337): `if (chosenResolution < w)` -> `if (chosenResolution <
       longEdge)` (reuse the hoisted longEdge).
    7. ChosenResolution convention: it now reports the chosen LONG edge. For square sources
       (w == h == longEdge) this equals the width, so the existing square-source assertions
       (GenerateResidual_AdaptiveAndManualOverride) keep passing unchanged.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "ResolveAspectSize" Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs && grep -q "Mathf.Max(w, h)" Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs && grep -q "GenerateResidual_NonSquareBase_PreservesSourceAspect" Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs && ! grep -q "Resample(fullResidual, chosenResolution, chosenResolution)\|Resample(fullResidual, resolution, resolution)" Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs && echo WR01-OK</automated>
  </verify>
  <acceptance_criteria>
    - ResolveAspectSize helper exists and every residual down-resample goes through it (no square Resample(fullResidual, n, n) remains in NamerDecompPipeline.cs)
    - Manual branch clamps against Mathf.Max(w, h); auto search uses longEdge for both the fallback and the skip guard; final resample and the stats upsample use the aspect-correct (cw, ch) and longEdge respectively
    - GenerateResidual_NonSquareBase_PreservesSourceAspect passes (Residual.width == 128, Residual.height == 64, ChosenResolution == 128)
    - The existing square-source tests GenerateResidual_AdaptiveAndManualOverride and the rest of ResidualPipelineTests pass unchanged
    - Commit messages: test(04.2-08): add non-square aspect-preservation test (RED); fix(04.2-08): preserve source aspect in residual resample (GREEN)
  </acceptance_criteria>
  <done>Non-square bases produce aspect-correct residuals; ChosenResolution reports the long edge; the new test is green and square-source tests are untouched</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 + WR-03 — CR-03 preview guard + Decomp dispose on release</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (RunDecompPreview :590-647 and ReleaseDecompPreview :563-580)
    - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (:205-229 the CR-03 CannotDecompose guard this mirrors)
    - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs (NamerDecompOutput.Dispose idempotence at :81-91)
  </read_first>
  <action>
    Two disjoint, one-file edits — no refactor.

    WR-02 (RunDecompPreview projection branch, insert after `NamerProjectionContext ctx =
    _projectionContext;` at line 605, before `_decompOutput = ctx.Decomp;` at line 606):
    ```
    if (ctx.Decomp != null && ctx.Decomp.Stats.CannotDecompose)
    {
        // CR-03 mirror: Process generates the Phase-3 shape here — preview must match.
        _decompStats = ctx.Decomp.Stats;
        return;
    }
    ```
    Keeping `_decompStats` populated lets DrawDecompStats still show the honest 0% coverage
    numbers; `_decompOutput` stays null so the CR-01 three-branch residual binding at
    :390-408 shows the non-decomposed Phase-3 shape and `_previewSplitMesh` stays null so
    the after pane draws the source mesh — exactly what Process writes. No dedicated unit
    test exists for this window-internal path (consistent with how the CR-01 mirror was
    verified); it is pinned by the grep gate + the full suite.

    WR-03 (ReleaseDecompPreview, replace line 571 `_projectionContext = null;`):
    ```
    _projectionContext?.Decomp?.Dispose();
    _projectionContext = null;
    ```
    On the happy path _decompOutput == ctx.Decomp was already disposed above (idempotent
    — safe); on a thrown Process (post-projection.Run) _decompOutput is null and this
    disposes the leaked pool-leased residual RT. Do NOT dispose ctx.Split/ctx.Colors — they
    are plain data (NamerSplitResult / Color32[]), not pool leases.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "ctx.Decomp.Stats.CannotDecompose" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "_projectionContext?.Decomp?.Dispose()" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && echo WR0203-OK</automated>
  </verify>
  <acceptance_criteria>
    - RunDecompPreview's projection branch contains the `if (ctx.Decomp != null && ctx.Decomp.Stats.CannotDecompose) { _decompStats = ctx.Decomp.Stats; return; }` guard BEFORE any `_decompOutput = ctx.Decomp` assignment (grep for "CannotDecompose" in NamerEditorWindow.cs matches the new guard)
    - ReleaseDecompPreview contains `_projectionContext?.Decomp?.Dispose();` immediately before `_projectionContext = null;` (grep for the exact expression)
    - No other line in NamerEditorWindow.cs changes (diff audit limited to the two hunks)
    - Full EditMode suite green (no new tests — window internals verified by grep + suite)
    - Commit message: fix(04.2-08): mirror CR-03 guard in preview and dispose Decomp on release
  </acceptance_criteria>
  <done>The preview mirrors the processor's CannotDecompose guard and no longer leaks the projection Decomp on error paths</done>
</task>

<task type="auto">
  <name>Task 3: WR-04 — single-exit-point NAMER_DECOMP_HEATMAP</name>
  <files>Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.hlsl</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.hlsl (whole 52-line file — the include guard, NAMER_VC_FLOOR, and the NAMER_DECOMP_HEATMAP early-return chain at :23-49)
    - Packages/com.graffitientertainment.namer/Shaders/NamerDebugView.shader (:143 the shared-include consumer — read only, do NOT edit)
    - Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute (:203, :214 the CSErrorHeatmap call sites — read only, do NOT edit)
  </read_first>
  <action>
    Replace the NAMER_DECOMP_HEATMAP body (NAMERDecomp.hlsl:23-49) with a single-exit-point
    form that preserves the EXACT piecewise-lerp math and the c0..c4 anchor constants
    (bit-identical output; the four lerp coefficients x/0.25, (x-0.25)/0.25,
    (x-0.5)/0.25, (x-0.75)/0.25 stay verbatim):
    ```
    float3 NAMER_DECOMP_HEATMAP(float x)
    {
        x = saturate(x);

        const float3 c0 = float3(0.2667, 0.0039, 0.3294); // #440154 deep violet
        const float3 c1 = float3(0.2314, 0.3216, 0.5451); // #3B528B blue
        const float3 c2 = float3(0.1294, 0.5686, 0.5490); // #21918C teal
        const float3 c3 = float3(0.3686, 0.7882, 0.3843); // #5EC962 green
        const float3 c4 = float3(0.9922, 0.9059, 0.1451); // #FDE725 yellow

        // Single exit point (Metal MSL translation): the four piecewise-linear segments
        // are computed unconditionally (lerp is total, no side effects) and selected by a
        // nested ternary, so the translated code has no early returns the Metal frontend
        // can misread as an uninitialized result. Segment math is IDENTICAL to the
        // early-return form.
        float3 seg0 = lerp(c0, c1, x / 0.25);
        float3 seg1 = lerp(c1, c2, (x - 0.25) / 0.25);
        float3 seg2 = lerp(c2, c3, (x - 0.5) / 0.25);
        float3 seg3 = lerp(c3, c4, (x - 0.75) / 0.25);
        return (x < 0.25) ? seg0 : (x < 0.5) ? seg1 : (x < 0.75) ? seg2 : seg3;
    }
    ```
    Keep the include guard and NAMER_VC_FLOOR untouched. Update the function's doc comment
    (the 5-anchor viridis description) to note the single-exit-point structure exists for
    the Metal MSL translation. Do NOT edit NamerDebugView.shader or NAMERDecomp.compute.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && ! grep -q "if (x < 0.25)\|if (x < 0.5)\|if (x < 0.75)" Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.hlsl && grep -q "return (x < 0.25)" Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.hlsl && grep -q "lerp(c0, c1, x / 0.25)" Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.hlsl && grep -q "lerp(c3, c4, (x - 0.75) / 0.25)" Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.hlsl && echo WR04-OK</automated>
  </verify>
  <acceptance_criteria>
    - NAMERDecomp.hlsl has NO `if (x < ...)` early returns and exactly one `return (x < 0.25) ? ...` single exit point
    - The c0..c4 constants and the four lerp coefficient expressions are byte-identical to the original (grep the exact expressions)
    - The two CSErrorHeatmap call sites (NAMERDecomp.compute:203,214) and the NamerDebugView.shader:143 consumer are untouched (the include's contract is unchanged)
    - No new test (no existing ramp-value test — the math is preserved by the grep gate); full EditMode suite green (the decomp integration/acceptance tests still exercise CSErrorHeatmap)
    - Live-editor smoke: recompiling the shaders produces ZERO Metal "potentially uninitialized variable" warnings
    - Commit message: fix(04.2-08): single-exit-point NAMER_DECOMP_HEATMAP for Metal
  </acceptance_criteria>
  <done>The viridis ramp has a single exit point with bit-identical math and compiles clean on Metal</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → GPU | Residual resample + heatmap dispatches (existing); the resample change is a CPU-side target-size decision |
| Preview → disk | NONE (WR-02/WR-03 are in-memory window lifecycle) |
| Shader compile | NAMERDecomp.hlsl is a shared include (compute + debug shader) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-23 | Tampering | Aspect-distorted residual for non-square bases (WR-01) | mitigate | ResolveAspectSize preserves source aspect in every down-resample; pinned by GenerateResidual_NonSquareBase_PreservesSourceAspect |
| T-04.2-24 | Repudiation | Preview promises a decomposition Process will not write (WR-02) | mitigate | CR-03 CannotDecompose guard mirrors NamerProcessor.cs:210-223 before ctx.Decomp is consumed |
| T-04.2-25 | DoS | Pool-leased residual RT leak on thrown Process (WR-03) | mitigate | `_projectionContext?.Decomp?.Dispose()` before nulling; NamerDecompOutput.Dispose is idempotent |
| T-04.2-26 | Tampering | Heatmap ramp drift during single-exit restructure (WR-04) | mitigate | Segment lerp coefficients and anchors byte-identical; grep gate pins them |
| T-04.2-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages; no install tasks |
</threat_model>

<verification>
- WR-01: no square Resample(fullResidual, n, n) remains; ResolveAspectSize drives every down-resample; the non-square test pins 128x64 from a 256x128 base
- WR-02: grep "CannotDecompose" in NamerEditorWindow.cs shows the new CR-03 mirror guard in the projection branch
- WR-03: grep "_projectionContext?.Decomp?.Dispose()" present immediately before the null
- WR-04: no `if (x < ...)` early returns in NAMERDecomp.hlsl; single nested-ternary return; anchors/coefficients identical
- Full EditMode suite green (+1 new test; the rest of the 146 unchanged except this plan's additions); commit per task
- Live-editor smoke: zero console warnings and zero Metal shader-compile warnings after the fixes; the four review Warnings recorded as resolved in the SUMMARY
</verification>

<success_criteria>
- All three review Warnings (WR-01/02/03) and the Metal heatmap warning are closed with surgical, no-refactor edits
- Full EditMode suite green including the new non-square aspect-preservation test
- Live editor shows zero of these warnings (console + shader compilation)
</success_criteria>

<output>
Create `.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-08-SUMMARY.md` when done
</output>
