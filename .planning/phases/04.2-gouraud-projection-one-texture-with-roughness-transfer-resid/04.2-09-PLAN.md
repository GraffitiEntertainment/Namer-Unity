---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs
autonomous: true
requirements: [UI-03, UI-04]
gap_closure: true
must_haves:
  truths:
    - "Default framing places the two models adjacent at the center divider with the pinned 10 px inner margins — the LEFT model is right-aligned in the left pane, the RIGHT model left-aligned in the right pane (tight neutral-rotation AABB fit, divider-anchored placement)"
    - "Zooming in grows each model OUTWARD from the divider — the inner edge stays exactly InnerMarginPx from the divider at every zoom level AND every orbit (clipping only at the OUTER edges/top/bottom, which is accepted window behavior); never across the divider"
    - "A visible 1 px pane outline (outer window border + center divider) renders over the preview texture"
    - "Synced yaw+pitch orbit (pitch clamped ±89°) and orthographic-size zoom semantics are byte-identical; the debug-channel toolbar and debounced recomputes behave as before"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs"
      provides: "InnerMarginPx constant, _framedHalfWidth/_framedHalfHeight (neutral AABB), tight OrthographicSizeForAspect(aspect, rectHeightPx), HorizontalHalfExtentWorld(), PaneAnchorWorld(aspect, rectHeightPx), divider-anchored ±PaneAnchorWorld draw positions"
      contains: "InnerMarginPx"
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      provides: "DrawPreviewPaneOutline (1 px outer border + center divider) called after GUI.DrawTexture"
      contains: "DrawPreviewPaneOutline"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs"
      provides: "Frame_FitsTightAabbWithInnerMargin (2.1622 / 4.2667 / 9.1429), HorizontalHalfExtentWorld_TracksCurrentRotation (4.0 / 0.1 / 2.8991), PaneAnchor_InnerEdgeStaysAtInnerMarginAcrossZoomSweep"
      contains: "PaneAnchor_InnerEdgeStaysAtInnerMarginAcrossZoomSweep"
  key_links:
    - from: "NamerPreviewRenderer.Render"
      to: "OrthographicSizeForAspect(aspect, rect.height)"
      via: "tight neutral-AABB fit applied to the orthographic camera"
      pattern: "OrthographicSizeForAspect\\(aspect, rect\\.height\\)"
    - from: "NamerPreviewRenderer.Render"
      to: "both DrawMesh calls"
      via: "±PaneAnchorWorld(aspect, rect.height) draw positions (divider anchor)"
      pattern: "PaneAnchorWorld"
    - from: "NamerEditorWindow.DrawPreviewSection"
      to: "DrawPreviewPaneOutline"
      via: "1 px IMGUI overlay drawn after the texture"
      pattern: "DrawPreviewPaneOutline\\(previewRect\\)"
    - from: "NamerPreviewRenderer.Frame"
      to: "bounds.extents.x / bounds.extents.y"
      via: "neutral-rotation AABB half-extents stored for the tight fit"
      pattern: "_framedHalfWidth"
---

<objective>
Close GAP-4 (04.2-HUMAN-UAT.md, round 2) — the preview pane framing/anchoring.

**Both prior framing decisions are SUPERSEDED by user UAT verdicts** (checker note — neither
is a locked-decision violation):
1. The locked 04.2-05 "both objects wide plus PreviewGap" pair framing was superseded by the
   round-1 UAT verdict and replaced by 04.2-07 per-pane framing.
2. 04.2-07's per-pane-CENTERED placement (MarginPx = 15 spherical fit) is now superseded by
   the round-2 UAT verdict: "each frame window is too large? ... models are still far apart
   from each other in default, and the zoom they start to overlap each other ... left is
   right aligned and right is left aligned with 10 pixel left/right margins" plus "Probably
   need a outline to tell".

The three defects to close: (a) default framing wastes the middle because each object is
centered in its pane; (b) zooming past the fit spills each object across the divider
(nothing constrains them to their panes); (c) no visible pane bounds.

Purpose: the preview is the acceptance instrument for the transferred-roughness LOOK
(UI-03/UI-04) — the A/B judgment must compare two models that hug the divider with a known
10 px gap and visible pane bounds, at any zoom.

Output: reworked NamerPreviewRenderer.cs (tight divider-anchored framing), a minimal pane
outline in NamerEditorWindow.cs, flipped + new framing tests, full EditMode green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-HUMAN-UAT.md
@.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-07-SUMMARY.md

<interfaces>
<!-- Current NamerPreviewRenderer.cs contract being reworked (read the whole file first;
     the load-bearing surface):
     public const float MarginPx = 15f;            // KEPT, re-scoped to outer/vertical margin
     public const float ZoomSensitivity = 0.15f;   // KEPT
     private float _framedRadius = 1f;             // DELETED -> _framedHalfWidth/_framedHalfHeight
     public float OrthographicSizeForAspect(float aspect, float rectHeightPx)  // REWORKED formula
     public Texture Render(Mesh beforeMesh, Mesh afterMesh, Material before, Material after, Rect rect)
     public void Frame(Mesh mesh)                  // re-fits; resets zoom/yaw/pitch
     public void Orbit(float, float)               // UNCHANGED
     public void Zoom(float)                       // UNCHANGED
     public void Cleanup() / Dispose()             // UNCHANGED

<!-- Geometry that drives the fix (derive from the code, do not guess):
     - The camera is fixed orthographic at (0,0,-OrthoCameraDistance) looking at world origin
       (the divider). Screen px -> world: pixelsPerWorldUnit = rectHeightPx / (2 * orthoSize);
       worldPerPixel = 2 * orthoSize / rectHeightPx. The divider is world x = 0.
     - One pane = half the full rect: paneWidthPx = aspect * rectHeightPx / 2.
     - TIGHT neutral fit uses the mesh AABB half-extents (hw = bounds.extents.x, hh =
       bounds.extents.y) at Frame's neutral rotation, NOT the bounding sphere. The sphere
       over-approximates a near-cubic mesh by ~1.7x (r = sqrt(ex^2+ey^2+ez^2) vs ex), which
       is exactly why the models rendered too small/far apart in 04.2-07.
     - The rotated AABB horizontal half-extent (bounds the inner edge at ANY orbit) is the
       absolute first row of the rotation matrix dotted with the extents:
         xExtent = |m00|*ex + |m01|*ey + |m02|*ez,  m = Matrix4x4.Rotate(Euler(pitch,yaw,0)).
       At neutral xExtent == ex; at yaw 90 on extents (4,0.5,0.1) xExtent == ez == 0.1; its
       max over yaw+pitch is the sphere radius r (so it NEVER exceeds r).
     - Anchor: place the left object's inner (right) edge at world -innerMarginWorld where
       innerMarginWorld = InnerMarginPx * worldPerPixel = InnerMarginPx * 2*orthoSize/rectHeightPx,
       so the inner edge is exactly InnerMarginPx px from the divider in screen space at any
       orthoSize (zoom) and any rotation. center.x = -(xExtent + innerMarginWorld) for before,
       +(xExtent + innerMarginWorld) for after; each minus rotatedBoundsCenter as today. -->

<!-- FIT derivation (InnerMarginPx = 10, MarginPx = 15 outer/vertical, rectHeightPx = 400):
       orthoSize = max(horizFit, vertFit) * zoomScale
       horizFit = hw * H / (paneWidthPx - InnerMarginPx - MarginPx)
       vertFit  = hh * H / (H - 2*MarginPx)
     Pin table (the numbers the flipped test must assert):
       tall  extents (1, 2, 0.5)  aspect 2: horiz 400/375=1.0667, vert 800/370=2.16216 -> 2.1622
       wide  extents (4, 0.5, 0.1) aspect 2: horiz 1600/375=4.26667, vert 200/370=0.5405 -> 4.2667
       wide  extents (4, 0.5, 0.1) aspect 1: pane=200, horiz 1600/175=9.14286 -> 9.1429
     Anchor pins (HorizontalHalfExtentWorld): neutral 4.0, yaw90 0.1, yaw45 0.70710678*4.1 = 2.8991 -->

<!-- Existing test pinning values that FLIP intentionally (NamerPreviewRendererTests.cs
     Frame_FitsPerPaneObjectWithMargin): 2.4771 / 4.3593 / 9.4879 are retired. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Divider-anchored tight-AABB framing in NamerPreviewRenderer + flipped/new tests</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs (whole file — constants, fields, Frame, Render, OrthographicSizeForAspect, Orbit/Zoom, ApplyCamera)
    - Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs (whole file — the framing test to flip, the Render test, CreateBoundsMesh/CreateCubeMesh helpers)
    - .planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-HUMAN-UAT.md (GAP-4 entry: verbatim user spec, three defects, acceptance)
  </read_first>
  <action>
    Test-first RED — rewrite/extend NamerPreviewRendererTests.cs BEFORE the production change
    (the new public members do not exist yet, so the file fails to compile; treat the compile
    failure as RED):

    1. RENAME Frame_FitsPerPaneObjectWithMargin -> Frame_FitsTightAabbWithInnerMargin and flip
       its body to the tight-AABB formula with the new pins (tolerance 1e-3):
       - tall mesh CreateBoundsMesh(1f, 2f, 0.5f): Frame(mesh) then
         Assert.AreEqual(2.1622f, _renderer.OrthographicSizeForAspect(2f, 400f), 1e-3f,
         "tight AABB: vertical ey=2 dominates (800/370)")
       - wide mesh CreateBoundsMesh(4f, 0.5f, 0.1f): Frame(mesh) then
         Assert.AreEqual(4.2667f, _renderer.OrthographicSizeForAspect(2f, 400f), 1e-3f, ...)
         and Assert.AreEqual(9.1429f, _renderer.OrthographicSizeForAspect(1f, 400f), 1e-3f,
         "tight AABB horizontal ex=4 in a 200px pane (1600/175)")
       - fit rotation invariance (message updated to the tight-AABB rationale): float before =
         _renderer.OrthographicSizeForAspect(2f, 400f); _renderer.Orbit(45f, 30f);
         Assert.AreEqual(before, _renderer.OrthographicSizeForAspect(2f, 400f), 1e-6f,
         "the neutral-AABB fit is fixed at Frame — anchoring, not fitting, absorbs rotation")

    2. ADD HorizontalHalfExtentWorld_TracksCurrentRotation:
       - wide mesh CreateBoundsMesh(4f, 0.5f, 0.1f); Frame(mesh);
         Assert.AreEqual(4f, _renderer.HorizontalHalfExtentWorld(), 1e-4f, "neutral: X extent");
         _renderer.Orbit(90f, 0f); Assert.AreEqual(0.1f, _renderer.HorizontalHalfExtentWorld(), 1e-4f,
         "yaw 90: wide axis rotates into depth, X extent = Z extent");
         _renderer.Frame(mesh); _renderer.Orbit(45f, 0f);
         Assert.AreEqual(2.8991f, _renderer.HorizontalHalfExtentWorld(), 1e-3f,
         "yaw 45: |cos45|*4 + |sin45|*0.1 = 0.70710678*4.1")

    3. ADD PaneAnchor_InnerEdgeStaysAtInnerMarginAcrossZoomSweep:
       - wide mesh CreateBoundsMesh(4f, 0.5f, 0.1f); Frame(mesh);
       - loop 24 times: compute `float ortho = _renderer.OrthographicSizeForAspect(2f, 400f);
         float anchor = _renderer.PaneAnchorWorld(2f, 400f); float xExtent =
         _renderer.HorizontalHalfExtentWorld(); float innerEdgePx = (anchor - xExtent) *
         (400f / (2f * ortho)); Assert.AreEqual(NamerPreviewRenderer.InnerMarginPx, innerEdgePx,
         1e-3f, "inner edge must stay InnerMarginPx from the divider at every zoom"); _renderer.Zoom(1f);`
         (Zoom(1f) multiplies zoomScale by 0.85 each step, sweeping the clamp range)
       - then _renderer.Frame(mesh); _renderer.Orbit(35f, 20f); repeat the same 24-step sweep and
         assertion — the inner edge stays at InnerMarginPx even with a rotated silhouette
         (the anchor recomputes the rotated horizontal half-extent per Render).

    Then GREEN — NamerPreviewRenderer.cs (minimal, surgical):
    1. Constants: ADD `public const float InnerMarginPx = 10f;` (the pinned divider-side margin —
       the user's verbatim "10 pixel left/right margins" this round). KEEP `public const float
       MarginPx = 15f` re-scoped to the OUTER/vertical margin (the surviving round-1 "10-20 px
       around the edges" gutter on the non-divider sides). KEEP ZoomSensitivity/OrthoCameraDistance/
       DefaultOrthographicSize/MinZoomScale/MaxZoomScale/MinPitch/MaxPitch VERBATIM.
    2. Fields: REPLACE `private float _framedRadius = 1f;` with
       `private float _framedHalfWidth = 1f;` and `private float _framedHalfHeight = 1f;`
       (the degenerate defaults; Render always re-fits via EnsureFramed).
    3. Frame(Mesh mesh): keep the null guard, `_framedMesh = mesh`, `_zoomScale = 1f`,
       `_yaw = 0f`, `_pitch = 0f`, ApplyCamera(). REPLACE the radius computation with the
       neutral-AABB half-extents: `Vector3 extents = mesh.bounds.extents;
       _framedHalfWidth = extents.x > 0f ? extents.x : 1f;
       _framedHalfHeight = extents.y > 0f ? extents.y : 1f;`.
    4. OrthographicSizeForAspect(float aspect, float rectHeightPx) — REWORK the formula to the
       tight neutral AABB with the asymmetric horizontal margins:
       ```
       float paneWidthPx = aspect * rectHeightPx / 2f;   // one pane = half the full rect
       float horizFit = _framedHalfWidth * rectHeightPx / Mathf.Max(paneWidthPx - InnerMarginPx - MarginPx, 1f);
       float vertFit  = _framedHalfHeight * rectHeightPx / Mathf.Max(rectHeightPx - 2f * MarginPx, 1f);
       return Mathf.Max(horizFit, vertFit) * Mathf.Clamp(_zoomScale, MinZoomScale, MaxZoomScale);
       ```
    5. ADD `public float HorizontalHalfExtentWorld()`:
       ```
       if (_framedMesh == null) { return 0f; }
       Matrix4x4 m = Matrix4x4.Rotate(Quaternion.Euler(_pitch, _yaw, 0f));
       Vector3 e = _framedMesh.bounds.extents;
       return Mathf.Abs(m.m00) * e.x + Mathf.Abs(m.m01) * e.y + Mathf.Abs(m.m02) * e.z;
       ```
       (m.m00/m.m01/m.m02 are the rotation matrix's first row = the rotated X basis; the
       absolute-dot gives the rotated AABB X half-extent, which is what bounds the inner edge.)
    6. ADD `public float PaneAnchorWorld(float aspect, float rectHeightPx)`:
       ```
       float orthoSize = OrthographicSizeForAspect(aspect, rectHeightPx);
       return HorizontalHalfExtentWorld() + InnerMarginPx * (2f * orthoSize / Mathf.Max(rectHeightPx, 1f));
       ```
       (world-x distance of each pane's object center from the divider; recomputed per Render
       so zoom grows each model outward from the divider.)
    7. Render(beforeMesh, afterMesh, before, after, rect): keep the aspect/orthoSize lines
       (`float aspect = ...; float orthoSize = OrthographicSizeForAspect(aspect, rect.height);
       _preview.camera.orthographicSize = orthoSize;`) and the light rotations VERBATIM. REPLACE
       `float paneCenterWorld = orthoSize * aspect / 2f;` and the two positions with:
       `float anchor = PaneAnchorWorld(aspect, rect.height);` then
       `Vector3 beforePosition = new Vector3(-anchor, 0f, 0f) - rotatedBoundsCenter;` and
       `Vector3 afterPosition = new Vector3(anchor, 0f, 0f) - rotatedBoundsCenter;`. Keep the ONE
       shared `Quaternion meshRotation = Quaternion.Euler(_pitch, _yaw, 0f)`, both DrawMesh calls,
       `_preview.Render(true)`, and `_preview.EndPreview()` VERBATIM.
    8. Update the class doc comment: initial size now frames the object's neutral AABB tightly
       (InnerMarginPx toward the divider, MarginPx on the outer/vertical edges), each pane's
       object is divider-anchored (inner edge exactly InnerMarginPx from the divider), zoom grows
       each object outward from the divider (never across it; outer/top/bottom clipping is
       accepted window behavior), and the anchor recomputes the rotated horizontal half-extent
       per Render so orbit never crosses the divider.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "public const float InnerMarginPx = 10f" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "float anchor = PaneAnchorWorld(aspect, rect.height)" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "public float PaneAnchorWorld(float aspect, float rectHeightPx)" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "public float HorizontalHalfExtentWorld()" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && grep -q "_framedHalfWidth \* rectHeightPx / Mathf.Max(paneWidthPx - InnerMarginPx - MarginPx, 1f)" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs && ! grep -q "paneCenterWorld\|_framedRadius\|HalfSeparation\|PreviewGap" Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs && grep -q "Frame_FitsTightAabbWithInnerMargin" Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs && grep -q "PaneAnchor_InnerEdgeStaysAtInnerMarginAcrossZoomSweep" Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs && grep -q "HorizontalHalfExtentWorld_TracksCurrentRotation" Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs && echo GAP4-FRAMING-OK</automated>
  </verify>
  <acceptance_criteria>
    - InnerMarginPx = 10f public const exists; MarginPx = 15f stays (outer/vertical); paneCenterWorld, _framedRadius, HalfSeparation, PreviewGap are gone from both the renderer and the tests (repo-wide grep under Packages/ returns zero matches)
    - OrthographicSizeForAspect has the exact tight-AABB formula (asymmetric horizontal margins: paneWidthPx - InnerMarginPx - MarginPx; vertical: rectHeightPx - 2*MarginPx); Render applies OrthographicSizeForAspect(aspect, rect.height) and draws both meshes at ±PaneAnchorWorld(aspect, rect.height)
    - Frame stores _framedHalfWidth/_framedHalfHeight from bounds.extents.x/.y (per-axis fallback 1f), resets _zoomScale to 1f and BOTH _yaw and _pitch to 0f
    - HorizontalHalfExtentWorld() returns the rotated-AABB X half-extent from Matrix4x4.Rotate(Euler(_pitch,_yaw,0)); PaneAnchorWorld(aspect, rectHeightPx) = HorizontalHalfExtentWorld() + InnerMarginPx * 2*orthoSize/rectHeightPx
    - Frame_FitsTightAabbWithInnerMargin pins 2.1622 / 4.2667 / 9.1429 at tolerance 1e-3 plus the Orbit-leaves-fit-unchanged assertion; HorizontalHalfExtentWorld_TracksCurrentRotation pins 4.0 / 0.1 / 2.8991; PaneAnchor_InnerEdgeStaysAtInnerMarginAcrossZoomSweep asserts the inner edge == InnerMarginPx (10 px) across a 24-step zoom sweep at neutral AND after Orbit(35f, 20f)
    - The other tests (Construction_PreviewCameraIsOrthographic, Orbit_AccumulatesYawAndPitch_ClampsPitch_ResetsOnFrame, Zoom_ClampedScaleFactor_SameWheelDirection, Render_OrthoCamera_OneSharedRotation_SizeMatchesFit) pass unchanged — Orbit/Zoom/Render(true) semantics untouched and the Render test's OrthographicSizeForAspect(2f, 256f) assertion is signature-compatible (no hardcoded number)
    - Full NamerPreviewRendererTests green; commit messages: test(04.2-09): flip framing tests to divider-anchored tight AABB (RED) then feat(04.2-09): divider-anchored tight-AABB preview framing with 10px inner margin (GREEN)
  </acceptance_criteria>
  <done>Framing is divider-anchored: default places both models adjacent at the 10 px inner margin; zoom grows each outward from the divider with no cross-pane overlap; the anchor recomputes the rotated extent per Render; behavior pinned by the flipped + 2 new tests</done>
</task>

<task type="auto">
  <name>Task 2: Pane outline overlay in NamerEditorWindow + full-suite green</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs</files>
  <read_first>
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (DrawPreviewSection lines 779-871 — the previewRect layout, the Render call, the GUI.DrawTexture draw; HandlePreviewCameraInput lines 873-893 — the Orbit/Zoom wiring to leave untouched)
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs (task 1 result — the anchor contract being outlined)
    - .planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-07-SUMMARY.md (the 148 exit count this plan builds on)
  </read_first>
  <action>
    Outline placement decision (documented for the checker): the outline is drawn by the WINDOW
    as a 1 px IMGUI overlay on top of the returned preview texture, NOT baked into the RT by the
    renderer. Rationale: baking it into the RT would require GL immediate-mode lines issued
    between `_preview.Render(true)` and `_preview.EndPreview()` — an order-sensitive pass that
    needs an internal shader material (`Hidden/Internal-Colored`) and is fragile/hard to verify
    headlessly. The IMGUI overlay is pixel-exact, needs no GL material, and the window already
    owns `previewRect` (the exact bounds the renderer anchors against), so the divider is
    trivially `previewRect.x + previewRect.width / 2f`. The window change is strictly the
    outline the placement decision requires — nothing else in the window is edited.

    1. In NamerEditorWindow.DrawPreviewSection, immediately AFTER
       `GUI.DrawTexture(previewRect, previewTexture, ScaleMode.StretchToFill);` (line 848), ADD
       one call: `DrawPreviewPaneOutline(previewRect);`. (The `else { GUI.Box(...) }` empty
       branch at 852 stays as-is — the outline only draws over a real render.)
    2. ADD a private method next to DrawPreviewSection:
       ```
       private void DrawPreviewPaneOutline(Rect previewRect)
       {
           Color outline = new Color(0.4f, 0.4f, 0.4f, 1f);
           EditorGUI.DrawRect(new Rect(previewRect.x, previewRect.y, previewRect.width, 1f), outline);                       // top
           EditorGUI.DrawRect(new Rect(previewRect.x, previewRect.yMax - 1f, previewRect.width, 1f), outline);                // bottom
           EditorGUI.DrawRect(new Rect(previewRect.x, previewRect.y, 1f, previewRect.height), outline);                       // left
           EditorGUI.DrawRect(new Rect(previewRect.xMax - 1f, previewRect.y, 1f, previewRect.height), outline);               // right
           EditorGUI.DrawRect(new Rect(previewRect.x + previewRect.width / 2f - 0.5f, previewRect.y, 1f, previewRect.height), outline); // center divider
       }
       ```
       (outer 1 px border marks the window bounds; the 1 px center divider marks the pane
       boundary the models hug — both "tell" the user where the fit targets, per the user's
       "Probably need a outline to tell".)
    3. Change NOTHING else in the window: the class doc comment, HandlePreviewCameraInput's
       MouseDrag/ScrollWheel branches, the debug-channel toolbar, the debounced recompute, the
       Render call, and the Frame call all keep their exact current shape.
    4. Run the FULL EditMode suite (all assemblies, no filter). Expected: 151 passed / 0 failed /
       0 skipped (the 149 exit + 2 new tests from task 1; the framing test is a rename+flip, the
       anchor tests are the +2).
    5. Intentional-flip audit: the ONLY changed test file is NamerPreviewRendererTests.cs
       (rename+re-pin of the framing test + 2 new tests). NO other test file may change; any
       failure outside NamerPreviewRendererTests is an implementation defect — fix it at the
       source, never in the test.
    6. Grep gates: no paneCenterWorld / _framedRadius anywhere under Packages/; the window still
       contains `_preview.Orbit(current.delta.x, current.delta.y)` and `_preview.Zoom(current.delta.y)`
       in HandlePreviewCameraInput, and `DrawPreviewPaneOutline(previewRect);` after GUI.DrawTexture.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "DrawPreviewPaneOutline(previewRect);" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "private void DrawPreviewPaneOutline(Rect previewRect)" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "Orbit(current.delta.x, current.delta.y)" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "Zoom(current.delta.y)" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && ! grep -rn "paneCenterWorld\|_framedRadius" Packages/com.graffitientertainment.namer --include=*.cs && echo GAP4-OUTLINE-OK</automated>
  </verify>
  <acceptance_criteria>
    - DrawPreviewPaneOutline(Rect previewRect) exists and is called once, immediately after GUI.DrawTexture in DrawPreviewSection; the outline is a 1 px outer border + 1 px center divider in Color(0.4, 0.4, 0.4, 1)
    - Full EditMode run green: passed=151, failed=0, skipped=0 (live-editor TestRunnerApi recipe preferred per project memory; batch fallback — no -nographics, no -quit, ProjectSettings.asset snapshot/restore)
    - Diff audit: NamerEditorWindow.cs changes are limited to the DrawPreviewPaneOutline method + its one call line; HandlePreviewCameraInput, the debug-channel toolbar, RecomputePreview, DrawPreviewSection's render path, and the Frame/Render/Orbit/Zoom call sites are byte-identical in behavior
    - Only NamerPreviewRendererTests.cs changed among test files (flip + 2 additions); SourceImmutabilityTests pass untouched (preview stays DrawMesh-only, disk-free)
    - Commit message: feat(04.2-09): 1px pane outline overlay in the preview window
  </acceptance_criteria>
  <done>A visible 1 px pane outline (outer border + center divider) renders over the preview; the full EditMode suite is green at 151/0/0 with the flip audit clean; live-editor UAT round 3 is the remaining human gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Selection → preview scene | Source/inspected meshes instantiated for rendering only (unchanged) |
| Preview → GPU | One orthographic camera render per Repaint (Render(true)); no compute/readback added |
| Preview → disk | NONE — the preview path never writes (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-23 | DoS | Degenerate input to tight fit/anchor (zero-extents mesh, zero-height rect, tiny margin) | mitigate | Per-axis fallback 1f for _framedHalfWidth/_framedHalfHeight; denominators clamped via Mathf.Max(..., 1f); aspect/height guarded via Mathf.Max(rect.height, 1f); HorizontalHalfExtentWorld returns 0 for a null framed mesh |
| T-04.2-24 | Repudiation | Preview biasing the LOOK judgment via wrong framing/anchoring (the GAP-4 defect) | mitigate | Divider-anchored tight neutral-AABB fit + recomputed rotated extent; PaneAnchor_InnerEdgeStaysAtInnerMarginAcrossZoomSweep pins the inner edge at exactly 10 px at every zoom and every orbit, so a regression to the 04.2-07 gap/overlap fails the suite |
| T-04.2-25 | Tampering | Regressing synced orbit/zoom or the debug toolbar | mitigate | Orbit/Zoom/Render(true)/light paths untouched; the anchor offset is derived inside Render only; the window diff is limited to the outline method + one call line |
| T-04.2-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages; no install tasks |
</threat_model>

<verification>
- Divider-anchored tight-AABB framing: InnerMarginPx = 10, MarginPx = 15 outer/vertical, _framedHalfWidth/_framedHalfHeight from the neutral AABB, OrthographicSizeForAspect with the asymmetric horizontal margins, ±PaneAnchorWorld draw positions
- Framing test pins 2.1622 / 4.2667 / 9.1429 (tolerance 1e-3) + fit Orbit-invariance; anchor tests pin HorizontalHalfExtentWorld 4.0 / 0.1 / 2.8991 and the inner-edge == InnerMarginPx invariant across a zoom sweep (neutral + rotated)
- Full EditMode suite green at 151/0/0 (149 exit + 2 new tests) with the flip audit clean; source-immutability untouched
- Live-editor smoke (user UAT round 3 on the framing): frame a selection — the two models sit adjacent at the divider with the 10 px inner margins and a visible pane outline; zoom in — each model grows outward from the divider with no cross-pane overlap; synced orbit and the debug-channel toolbar feel unchanged
</verification>

<success_criteria>
- Default framing places the two models adjacent at the divider with 10 px inner margins; zoom grows each outward from the divider with no cross-pane overlap at any zoom; a visible 1 px pane outline renders
- Framing tests flipped intentionally to the divider-anchored tight-AABB numbers, 2 new anchor tests added, full EditMode suite green at 151/0/0
</success_criteria>

<output>
Create `.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-09-SUMMARY.md` when done
</output>
