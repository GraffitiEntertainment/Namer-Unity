# Phase 4.2: Gouraud-projection one-texture with roughness transfer - Context

**Gathered:** 2026-09-16
**Status:** Ready for planning
**Source:** PRD Express Path (.planning/notes/sobel-led-residual-guided-roughness.md)

<domain>
## Phase Boundary

This phase replaces the fit-driven sharp-removal search (the 04.1 estimator whose LOOK was rejected in UAT round 5) with a by-construction residual collapse and a roughness-transfer path:

- The cleaned base becomes the **per-triangle Gouraud-representable projection of the albedo**, so the residual quotient (`cleanedBase / vcInterp`) is white up to Color32 quantization — the ladder search, the honest-threshold gamble, and any correction-mask pass retire.
- The removed detail (albedo − projected) has its **luminance transferred into the 6-bit roughness map** via the surviving D-08 dip consume site; dip depth is a pure taste slider.
- A **residual on/off checkbox** overrides the (now near-always-passing) residual gate explicitly.
- The **estimator dropdown consolidates** to one extraction path + decomposition on/off.
- **UV-overlap texels** are the one known exception to "white by construction" and get an honest treatment (research-gated).
- The **preview becomes an orthographic, synced side-by-side A/B** (user decision added mid-planning) so the redesign's look is judged without perspective bias.

Out of scope: stylization (Phase 5), Blender literal fixtures (v2), any change to the packed format contract itself.
</domain>

<decisions>
## Implementation Decisions

### Residual collapse
- The cleaned base is the projection of the albedo onto the Gouraud-representable space (per-triangle, reusing the existing 16×16 barycentric fit machinery, writing the fitted surface back) — the base says exactly what the VCs can say, so `base/vcInterp ≡ white` up to Color32 quantization.
- The strength ladder search, the FitOnlyMaxError threshold gamble, and a residual-correction mask pass all RETIRE (locked — round-2 design supersedes round-1's mask idea).

### Roughness transfer
- The removed signal (albedo − projected) is split luminance/chroma; the **luminance component modulates the roughness map** through the surviving D-08 consume site (`saturate(scalar − strength·mag)`); the *source signal* changes from Sobel-of-base to the transferred removed-detail luminance.
- **Chroma grain is an accepted loss** — a scalar gloss channel has no home for it; the loss must be bounded and documented.
- **Polarity by phenomenon — SIGNED transfer (user decision, 2026-09-16, off the phase research)**: bright baked speckle dips toward gloss AND dark baked occlusion raises toward matte (one signed removed-luma signal; Neo is 67% dark / 33% bright removed-luma texels — bright-only would waste two-thirds of the signal).
- **6-bit roughness saturation headroom**: heavy transfer can clip at both ends; the plan must handle it.

### Dip depth control
- **Dip depth is a pure taste slider** (user decision, round 2) — no precompute, no search; nothing is precomputed against residual whiteness.

### Residual gate
- An explicit **residual on/off checkbox** overrides the near-always-passing gate ("drop it, I accept the quantization dust"); one-texture outcome (no residual EXR) is the default. **When ON, the residual's dividend is the SOURCE albedo (user decision, 2026-09-16)** — it writes the honest reconstruction error (source ÷ vcInterp, including the transferred detail), never a dead-white projected-base quotient.

### Estimator UX
- The estimator split **largely retires**: one extraction path + decomposition on/off. Whether Sobel-of-base survives as an alternate dip source is a planning call (see Discretion).

### Preview presentation — orthographic synced side-by-side (user decision, 2026-09-16, added mid-planning)
- The preview camera becomes **orthographic**: the perspective projection offsets the lighting and silhouette angle of the compared objects and biases the A/B judgment (the fit-driven LOOK was rejected through that lens).
- **Two objects side by side** (source vs NAMER), **rotating in sync — yaw AND pitch both** (amended 2026-09-16: pitch restored after an initial Y-only lock; the invariant that matters is that both panes share one rotation, not axis restriction).
- **Zoom is orthographic size** (scale in/out of the pair), not camera distance or FOV.
- **Initial orthographic size = both objects wide plus a small gap** between them.

### Test posture
- Existing pinned 04.1-07 dip-coupling tests **flip again, intentionally**; the full EditMode suite must end green.

### Claude's Discretion
- Exact projection kernel form (likely reuses the 16×16 fit machinery; how the fitted surface is evaluated/written back into the base texture).
- Whether the Sobel-of-base estimator survives as an alternate dip source, and what the estimator dropdown becomes (renaming is a UX decision). Research recommends KEEPING the Sobel kernels as a two-option dip source (near-zero cost, preserves passing tests).
- The transfer function shape (how removed-luminance magnitude maps to dip depth at the consume site) short of the locked polarity/headroom constraints. Research quantifies: |luma| p90 = 0.173 → full-clip at strength 1.0 under p90 normalization (RobustSobelScale precedent).
- UV-overlap strategy: research answered the blocker — overlaps cannot break whiteness under projection (dividend = divisor regardless of rasterize winner); they only matter to the residual-ON path's fidelity. Plan needs at most an honest statement, not repair.
- Adopt the research's remaining recommendations unless contradicted in planning: symmetric VcFloor (`max(base,F)/max(vc,F)` — kills the 1.7% dark-texel floor exception; check no test pins asymmetric values), ChooseResolution criterion redefined to source-reconstruction error when the checkbox is ON (fit-only error becomes identically 0 and would collapse to minimum resolution), projected base stays in the float path (an 8-bit intermediate reintroduces ~10% quotient deviation on dark VCs).
- Implementation structure across compute kernels / C# pipeline / UI, test organization.
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Design + decisions
- `.planning/notes/sobel-led-residual-guided-roughness.md` — the PRD itself: motivation, research-verified mechanics with file:line cites, the round-2 design table, transfer caveats
- `.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-CONTEXT.md` — locked D-01..D-13 decisions this phase builds on (D-05 cleaned-base refit, D-08 dip formula, D-09 polarity ownership, D-10 full adoption, D-13 residual gate)
- `.planning/phases/04.1-baked-response-roughness-extraction-zero-residual-one-textur/04.1-VERIFICATION.md` — the verified 04.1 state this phase modifies (including the code-review-fix round)

### Open research
- `.planning/research/questions.md` — the overlapping-UV texels question that blocks the "white by construction" claim (prevalence on Neo, candidate strategies, 6-bit/8-bit quantization interaction)

### Scope + acceptance
- `.planning/todos/pending/plan-sobel-led-roughness-redesign.md` — the 5-part plan scope (projection kernel, transfer kernel, checkbox, dropdown consolidation, UV-overlap handling) and the acceptance sketch
</canonical_refs>

<specifics>
## Specific Ideas

Research-verified mechanics the design depends on (from the PRD, with its file:line evidence):
- Residual quotient = `cleanedBase / vcInterp` in linear space, vc floor 1e-3 (`Compute/NAMERDecomp.compute:130-141`) — the dividend is the CLEANED base, never the source albedo.
- VCs are per-triangle barycentric LSQ on a 16×16 interior grid, unweighted vertex mean, piecewise-linear Gouraud output, Color32-quantized (`Editor/Bake/VertexColorFitter.cs:104,265-270,389-415`) — mid frequencies are structurally unrepresentable, which is why projection (not better smoothing) is the collapse mechanism.
- Overlapping UVs rasterize first-covering-triangle-wins (`Compute/NAMERDecomp.compute:111-113`) — unfixable by projection; likely part of Neo's scattered non-white residual spots.
- The current smoother is a single global texel-space gaussian (radius `clamp(max(w,h)/32, 8, 64)`, σ = radius/3; `NamerRoughnessPipeline.cs:48-51`); `saturate()` in `CSRoughnessSharpRemoval` is mathematically inert.
- Literature pointers flagged ASSUMED/unverified in the PRD: bilateral filtering (Tomasi & Manduchi 1998), Mesh Colors (Yuksel et al. 2010), xatlas-style seam-aware dilation.
</specifics>

<deferred>
## Deferred Ideas

None beyond the PRD's own deferrals — round-1's slider-owns-dip + ladder-owns-removal + residual-mask design is superseded (survivors: taste slider, checkbox, root-cause analysis; already folded into the decisions above). Stylization stays Phase 5.
</deferred>

---

*Phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid*
*Context gathered: 2026-09-16 via PRD Express Path*
