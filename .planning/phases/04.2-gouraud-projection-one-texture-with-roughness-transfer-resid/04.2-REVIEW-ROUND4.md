---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
round: 4 (GAP-5 closure, plan 04.2-10)
reviewed: 2026-09-17T00:00:00Z
depth: standard
diff_base: ca9c020..HEAD
files_reviewed: 3
files_reviewed_list:
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs
findings:
  critical: 0
  warning: 1
  warning_resolved: 1
  info: 5
  total: 6
status: resolved (WR-01 closed as invalid premise — see WR-01 Resolution; Info items fenced per precedent)
---

# Phase 4.2 Round 4 (GAP-5) — Code Review Report

**Reviewed:** 2026-09-17
**Depth:** standard (per-file analysis + cross-file call-site/grep verification)
**Diff base:** `ca9c020..HEAD` (exactly the three in-scope files changed under `Packages/`)
**Files Reviewed:** 3
**Status:** issues_found

## Summary

| Severity | Count |
|----------|-------|
| Critical | 0 |
| Warning | 1 |
| Info | 5 |
| **Total** | **6** |

The GAP-5 implementation itself is sound. `PaneAnchorWorld` now derives from the
Frame-captured neutral `_framedHalfWidth` exactly as planned; `HorizontalHalfExtentWorld`
is gone repo-wide; the two-cycle `Render` → `PreviewRenderResult` rework matches the
vetted design in every load-bearing detail I could attack: the `ref`-field realloc chain
in `BlitToPane` genuinely reassigns `_beforePaneTexture`/`_afterPaneTexture`, realloc
happens only on size mismatch (no per-Repaint churn), `RenderTexture.active` is restored
after each blit, `Cleanup` is idempotent, the window's half-rect/texcoord math is a
correct 1:1 mapping with the far half of each RT discarded (genuine per-pane clipping),
and the flipped tests are provably discriminating (delta 0.5714 world units vs a 1e-6
tolerance against the round-3 code — verified numerically below).

The one Warning is a phase-record integrity defect, not a code defect: the diff ADDS
`GetPaneDrawOffsets_MirrorsDividerAnchor` (it did not exist at `ca9c020`), while the
plan's acceptance criteria and the 04.2-10 summary both describe it as an existing test
"flipped in place" and claim "no new test added, suite drift 152 -> 151 (one deleted,
none added)". The diff deletes one test and adds one test in the same file (7 -> 7
methods; every other test file is byte-identical in the range), so it cannot produce a
-1 suite drift. Either the 04.2-09 baseline count (152) or the round-4 live-run count
(151) is misrecorded, and the summary's drift explanation is provably false. This needs
reconciliation before ship because the plan's own audit treats unexplained count changes
as implementation defects.

## Critical Issues

None found.

## Warnings

### WR-01: Test-audit record contradicts the diff — a test was ADDED, not flipped, and the claimed 152 -> 151 drift is arithmetically impossible for this diff

**File:** `Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs:180-251` (added test); record at `.planning/phases/04.2-gouraud-projection-one-texture-with-roughness-transfer-resid/04.2-10-SUMMARY.md:38,68`
**Issue:** `git show ca9c020:<file>` proves `GetPaneDrawOffsets_MirrorsDividerAnchor` did not exist before this round (old file: 7 tests — Construction, Orbit, Zoom, Frame_Fits, HorizontalHalfExtent, PaneAnchor_InnerEdge, Render; it contained no GetPaneDrawOffsets test and no mirror assertions to "KEEP"). The diff deletes `HorizontalHalfExtentWorld_TracksCurrentRotation` and adds `GetPaneDrawOffsets_MirrorsDividerAnchor` — net zero change in test methods (7 -> 7), and `git diff --name-only ca9c020..HEAD -- Packages/` shows no other source file changed, with per-file `[Test]`/`[UnityTest]` counts identical across all 27 test files between the two commits. Therefore the summary's claims — "flipped `GetPaneDrawOffsets_MirrorsDividerAnchor`", "no new test (suite drift 152 -> 151)", "one test deleted, none added" — cannot all be true: a delete-plus-add in an otherwise frozen tree cannot move the suite count by -1. Either the 04.2-09 baseline report (152) or the round-4 gate report (151) is misrecorded, or the gate's counting method changed between rounds. The added test itself is good quality (its orbit-stability assertions are discriminating; see Verified clean) — the defect is the verification record, which downstream gates and UAT rely on, and an unexplained -1 is exactly the signal the plan's intentional-flip audit exists to catch.
**Fix:** Amend the 04.2-10 summary (and the plan's flip-audit line if the plan is ever revisited): describe `GetPaneDrawOffsets_MirrorsDividerAnchor` as added (not flipped), and re-derive the drift line from the actual gate output — re-check the round-4 results file (Temp marker per the gate recipe) and the 04.2-09 results file to determine which of 151/152 is wrong. No code change required; the suite itself stayed at failed=0 and tree-level coverage is demonstrably intact (counts identical between the commits).

## Info

### IN-01: Plan-artifact "(Task 2)" leaked into a shipped doc comment

**File:** `Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs:47`
**Issue:** The class doc says "...is clipped invisible by the two-cycle per-pane render (Task 2) (GAP-5)". "(Task 2)" is an internal plan-task reference with no meaning to a reader of the package. (The text was dictated verbatim by the plan, so it is plan-faithful — but it is still a doc defect in the shipped file.)
**Fix:** Drop "(Task 2)" → "...clipped invisible by the two-cycle per-pane render (GAP-5)." (The GAP-N tags match existing in-file style and can stay.)

### IN-02: Test-class doc summary is stale for round 4

**File:** `Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs:12-19`
**Issue:** The `<summary>` still describes only the 04.2-09 semantics ("divider-anchored tight neutral-AABB fit (each pane's object hugs the divider with an InnerMarginPx inner margin)") with no mention of what this round pins: the rotation-stable fixed center (GAP-5) and the two-pane `PreviewRenderResult`. Unqualified, "hugs the divider" is now neutral-rotation-only semantics — under orbit the inner edge intentionally moves away from the divider while the center stays fixed.
**Fix:** Add one sentence: "Round 4 (04.2-10) additionally pins the rotation-stable fixed center — anchor/offsets unchanged under Orbit — and the two per-pane render targets returned by Render (distinct, non-null)."

### IN-03: Unused `using UnityEditor;` in the test file (pre-existing)

**File:** `Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs:5`
**Issue:** Nothing in the file resolves from `UnityEditor` (Shader/SystemInfo/GraphicsDeviceType/Material/Mesh all come from `UnityEngine`/`UnityEngine.Rendering`). Present at `ca9c020` too, so not introduced this round.
**Fix:** Remove the line in a future touch of this file; not worth a dedicated commit.

### IN-04: Render test could also pin the pane RT dimensions

**File:** `Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs:278-283`
**Issue:** The in-place update asserts non-null + `AreNotSame` but not that the persistent RTs match the requested rect pixel size. If `BlitToPane`'s realloc ever sized the pane from the wrong dimension (e.g. height vs width swap), the window draw would still "work" (stretch-mapped) and no test would notice. A one-line pin closes that hole.
**Fix:** Add `Assert.AreEqual(512, ((RenderTexture)result.Before).width, ...)` and the 256-height equivalent for `After` (the test rect is `new Rect(0, 0, 512, 256)`).

### IN-05: Pre-existing `Graphics.Blit` in the window leaves `RenderTexture.active` set (out-of-diff, same hazard class C-3 fixed)

**File:** `Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs:522-525`
**Issue:** `ResolvePreviewBaseMap`'s `Graphics.Blit(_liveResult.NormalizedBaseColor, _previewBaseRt, rawCopy)` does not restore `RenderTexture.active`. It runs from `Tick`/`EditorApplication.update` (not mid-OnGUI), so it is currently harmless — IMGUI binds its own target on the next OnGUI — but it is the only remaining blit in the changed file outside the C-3 discipline this round established (`NamerPreviewRenderer.BlitToPane:206` now restores it).
**Fix:** Optional one-liner for consistency: `RenderTexture.active = null;` after the blit at line 525. Clearly labeled pre-existing — not a defect of plan 04.2-10.

## Verified clean

Checked specifically and found sound (adversarial passes that did NOT produce findings):

1. **Anchor formula vs plan**: `NamerPreviewRenderer.PaneAnchorWorld` (NamerPreviewRenderer.cs:286-290) is exactly `_framedHalfWidth + InnerMarginPx * (2f * OrthographicSizeForAspect(...) / Mathf.Max(rectHeightPx, 1f))`; `HorizontalHalfExtentWorld` has zero matches under `Packages/` (grep, both renderer and tests).
2. **Ref-field realloc path**: `RenderPane(..., ref _beforePaneTexture/_afterPaneTexture)` → `BlitToPane(ref pane, ...)` — C# ref-through-field aliasing is legal and `pane = new RenderTexture(...)` reassigns the field. Realloc condition is `null || width != w || height != h` — int-vs-int against the same utility-RT source each Repaint, so no per-Repaint churn at stable size; `Release()` before realloc; `HideFlags.HideAndDontSave`, depth 0, `source.graphicsFormat` matched (faithful full-frame copy).
3. **C-3 active-RT restore**: `RenderTexture.active = null` immediately after each `Graphics.Blit` (NamerPreviewRenderer.cs:206); the second cycle's blit is the last graphics op of `Render`, so the mid-OnGUI GUI draws (DrawPreviewPaneTexture, DrawPreviewPaneOutline, toolbar) target the window.
4. **Leak/early-return paths**: no early return between `BeginPreview` and `EndPreview` inside `RenderPane`; `Cleanup` releases both pane RTs after `_preview.Cleanup()`, nulls the fields, and is idempotent (second call no-ops); `Dispose` delegates; `Render` after `Cleanup` returns `default` via the `_preview == null` guard (no NRE), and the window's `IsValid` branch falls to the preserved empty-state `GUI.Box` else.
5. **4-arg overload**: return type changed to `PreviewRenderResult`, delegation intact; grep confirms the window (5-arg call, NamerEditorWindow.cs:845) is the ONLY caller of `Render` in the package — no orphaned call sites for the changed return type; `PreviewRenderResult` is consumed only by the three in-scope files.
6. **Half-rect/texcoord math as written** (NamerEditorWindow.cs:884-890): `leftPane = (x, y, w*0.5, h)` with texcoords `(0, 0, 0.5, 1)`; `rightPane = (x + w*0.5, y, w*0.5, h)` with texcoords `(0.5, 0, 0.5, 1)` — exact halves, no off-by-one or gap; before mesh at `-anchor` lives in the LEFT half of its RT (world x=0 → RT center), after at `+anchor` in the RIGHT half; far halves are never drawn, so rotation overflow past the divider is genuinely clipped (not the withdrawn single-texture no-op crop). The 1-px divider outline at `x + w/2 - 0.5` covers the seam.
7. **Two Render(true) per Repaint — no double-applied state**: each `RenderPane` re-establishes camera transform (`ApplyCamera`), `orthographicSize`, and both light rotations after its own `BeginPreview`; ambient color is set once in the constructor and persists (not re-applied); DrawMesh submissions are consumed per render; cycle 2 does not bleed cycle 1 (camera clear). Light indices `[0]`/`[1]` are the standard PreviewRenderUtility pair (pre-existing usage).
8. **Guard + empty-state branch at the call site**: `previewResult.IsValid` gate with the `else { GUI.Box(...) }` branch preserved verbatim (NamerEditorWindow.cs:845-854); `ScaleMode.StretchToFill` has zero matches in the window.
9. **W-1 doc sentences**: the Render inline comment (NamerPreviewRenderer.cs:158-161) and the `GetPaneDrawOffsets` summary (292-299) now say neutral `_framedHalfWidth`/zoom-only ("rotation-stable — fixed at Frame"), and the class doc GAP-4 sentence is rewritten to GAP-5 fixed-center language (44-48). No stale "rotated half-extent" rotation-dependence sentence remains in the renderer.
10. **Window wiring untouched**: `HandlePreviewCameraInput`'s `Orbit(current.delta.x, current.delta.y)` (902) and `Zoom(current.delta.y)` (908) byte-identical; `DrawPreviewPaneTexture` called after the Render call and before `DrawPreviewPaneOutline` (848-849).
11. **Test discrimination (numerically verified)**: at Orbit(35, 20) the wide fixture's rotated X extent is 3.4286, so the round-3 anchor would be 3.6419 vs the fixed 4.2133 — delta 0.5714 world units, versus the tests' 1e-6 tolerance: both orbit-stability blocks (`PaneAnchor_FixedCenter...` lines 168-177; `GetPaneDrawOffsets...` lines 217-227) provably FAIL against the round-3 code. The `AreNotEqual(orthoSize * aspect / 2f, anchor)` GAP-4 discriminator is 4.2667 vs 4.2133 — meaningful. The neutral zoom sweeps pass under both rounds by design (the neutral invariant is unchanged).
12. **`AreNotSame` is meaningful**: `Before`/`After` are the two distinct persistent fields, so the assertion fails under the C-2 regression (both cycles returning the utility's cached RT — same instance) and under a wrong-ref bug (both `RenderPane` calls passing the same field). Content-level aliasing (distinct RTs, stale second blit) is not assertable headlessly — no test pretends to catch it.
13. **No never-failing assertions**: every assertion pins a computed relation; the orbit pins are trivially true against current code only because the anchor is now pure w.r.t. yaw/pitch — which is the pinned invariant.
14. **Untouched fit math**: `Frame`, `OrthographicSizeForAspect`, `Zoom`, `Orbit`, `EnsureFramed`, `ApplyCamera` are behavior-identical to round 3; the `Frame_FitsTightAbbWithInnerMargin` pins (2.1622/4.2667/9.1429) and the shared-rotation/ortho-size/yaw/pitch assertions in the Render test are unaffected.
15. **Degenerate rect**: unreachable from the only call site (`GUILayoutUtility.GetRect` with width >= 256, height = width/2); the renderer's exposure to a zero-size rect is unchanged from round 3 (same `BeginPreview(rect, ...)` input). Debounce interaction: the `Tick`/`RecomputePreview` path never touches the pane RTs and cannot alias them with the window-owned `_previewBaseRt`.

---

## WR-01 Resolution (orchestrator, same day — RESOLVED AS INVALID PREMISE, no code or summary change required)

WR-01's arithmetic treated `ca9c020` as plan 10's baseline. It is not: the WR-04 fix commit
`ff9daec` (2026-09-17 12:56:59 PDT) sits BETWEEN `ca9c020` and plan 10, and it is the commit
that ADDED `GetPaneDrawOffsets_MirrorsDividerAnchor` (+81 lines to this test file, verified:
`git show ff9daec -- <file>` adds the method; `git show ca9c020:<file>` contains zero matches
for it). Plan 10's true baseline is post-`ff9daec`.

Reconciliation from the gate files themselves (marker-test presence pins each file's era):

| Gate file | mtime | PASS | Mirrors test | HorizontalHalfExtent test | Era |
|---|---|---|---|---|---|
| Temp/namer042-09-results.txt | 12:44:57 | 151 | absent | present | post-plan-09, pre-WR-04-fix |
| Temp/namer042-wr04-results.txt | 13:00:36 | 152 | present (PASS) | present | post-ff9daec |
| Temp/namer042-10-results.txt | 16:45:02 | 151 | present (PASS) | absent | post-plan-10 |

Drift: 151 → +1 (WR-04 fix) → 152 → −1 (plan 10 deletes HorizontalHalfExtentWorld_TracksCurrentRotation) → 151 — exactly the
"152 → 151, one deleted, none added" the plan and 04.2-10-SUMMARY record. Round-3
VERIFICATION.md:35 already documented this arithmetic (149 + 2 plan-09 + 1 WR-04 = 152).
Plan 10's own commits confirm the flip claim: `6e2ce72` (RED) modified
`GetPaneDrawOffsets_MirrorsDividerAnchor` IN PLACE (method signature line untouched; body
rewritten) and deleted `HorizontalHalfExtentWorld_TracksCurrentRotation` ([Test] count 7 → 6);
`fd98dac` touched no test method. Both criticized records are correct as written.

**Disposition:** WR-01 closed as invalid premise (diff base one commit too early). Critical 0,
Warning 0 (resolved), Info 5 recorded below with dispositions. IN-01..IN-05 remain fenced
unless the developer opts in (standing Info-fencing precedent from rounds 1-3).

---

_Reviewed: 2026-09-17_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
_Diff base: ca9c020..HEAD (WR-01 note: plan 10's baseline is ff9daec, not ca9c020 — see resolution)_
