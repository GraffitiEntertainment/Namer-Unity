---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
reviewed: 2026-09-16T21:45:00Z
depth: standard
files_reviewed: 22
files_reviewed_list:
  - Packages/com.graffitientertainment.namer/Compute/NAMERDecomp.compute
  - Packages/com.graffitientertainment.namer/Compute/NAMERPack.compute
  - Packages/com.graffitientertainment.namer/Compute/NAMERRoughness.compute
  - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerSourceModel.cs
  - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerPreviewRenderer.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerOneTextureTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerPreviewRendererTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessExtractionTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessFitTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerRoughnessTransferTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs
findings:
  critical: 0
  warning: 3
  info: 3
  total: 6
status: issues_found
---

# Phase 04.2: Code Review Report

**Reviewed:** 2026-09-16T21:45:00Z
**Depth:** standard
**Files Reviewed:** 22
**Status:** issues_found

## Summary

All 22 in-scope files were read in full: the three compute assets (NAMERDecomp, NAMERPack, NAMERRoughness), the nine editor C# files carrying the projection/transfer rewire, and the ten phase test suites. Cross-module contracts were traced against supporting code outside the scope list (ComputeTexturePool null/no-op semantics, NamerConstants mirrors, AssetGenerator's one-texture output path, NamerDebugChannelMaterial bindings) and against `git diff a4906b4..HEAD` to separate in-phase changes from pre-existing behavior.

The core 04.2 machinery is sound. The Gouraud write-back, symmetric VcFloor quotient, mode gate, removed-luma polarity (verified against the Rec.601 weights in NAMERRoughness.compute), result-shape change (NormalizedBaseColor = projection, SourceBaseColor = normalize output), full-adoption vs slider-scaled dip switch, and the WR-03-style catch-block releases in NamerComputePipeline.Process were all traced end-to-end and held up under adversarial probing. No Critical findings survived investigation.

Three Warnings remain: two preview/output-truth mismatches and one resource-lifecycle leak on an error path, plus three Info-level quality defects. None block the phase goal, but WR-01 through WR-03 should be fixed before this code is relied on for production asset generation.

## Narrative Findings (AI reviewer)

### WR-01: Non-square base maps produce aspect-distorted square residuals

**File:** `Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs:329` (with `:398`, `:440`)
**Issue:** `ChooseResolution` clamps the ladder against `w` only (`Mathf.Min(ResolutionLadder[idx], w)` in the manual branch; the auto search behaves the same), and every path resamples to a square target: `Resample(fullResidual, chosenResolution, chosenResolution)`. For a non-square base (e.g. 2048x1024), the residual EXR is resampled with unequal horizontal/vertical scale factors, so residual texel (u, v) no longer corresponds to base texel (u, v) — the runtime reconstruction multiplies an aspect-correct base against an aspect-distorted quotient, smearing the removed detail along one axis. The phase's new projection path inherits and sharpens this: `projectedOut` stays full-resolution and aspect-correct while the residual written beside it is distorted, so the residual-ON outcome is geometrically inconsistent with the one-texture outcome for the same mesh. Confirmed pre-existing via `git diff a4906b4..HEAD` (this phase only added the projection/mode plumbing), but the file is in scope and the defect is a correctness risk for every non-square base.
**Fix:** Preserve the source aspect in the ladder clamp and the resample. Either clamp by the long edge and scale the short edge:
```csharp
int chosenW = Mathf.Min(ResolutionLadder[idx], Mathf.Max(w, h));
int chosenH = Mathf.Max(1, Mathf.RoundToInt(chosenW * (h / (float)w)));
return Resample(fullResidual, chosenW, chosenH);
```
or resolve `chosenW`/`chosenH` independently against the ladder per axis. The `Stats.ChosenResolution` reporting (square assumption) should follow whatever convention is chosen.

### WR-02: Preview shows a decomposed split mesh for CannotDecompose selections where Process writes the Phase-3 shape

**File:** `Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs:600-614`
**Issue:** `RunDecompPreview`'s projection branch consumes `ctx.Decomp` unconditionally and builds `_previewSplitMesh` from `ctx.Split`/`ctx.Colors`. `NamerProcessor.Process` deliberately checks `ctx.Decomp.Stats.CannotDecompose` (NamerProcessor.cs:210-223) and leaves `decomp` null so the non-decomposed Phase-3 shape is generated with a UV-coverage warning (CR-03). For a zero-coverage selection (tiling/out-of-range UVs) with decomposition enabled and Dip Source = Removed Detail, the window's after pane renders the split mesh with fitted vertex colors and an unbound residual slot, while Process actually writes a Phase-3 material — exactly the preview-must-not-promise-what-Process-will-not-deliver class the window's own CR-01 mirror (NamerEditorWindow.cs:352-356) exists to enforce. The CR-03 fallback was added to the processor this phase but not mirrored into the new preview branch.
**Fix:** Mirror the processor's check at the top of the projection branch:
```csharp
NamerProjectionContext ctx = _projectionContext;
if (ctx.Decomp != null && ctx.Decomp.Stats.CannotDecompose)
{
    // CR-03 mirror: Process generates the Phase-3 shape here — preview must match.
    _decompStats = ctx.Decomp.Stats;
    return;
}
_decompOutput = ctx.Decomp;
_decompStats = ctx.Decomp != null ? ctx.Decomp.Stats : null;
```
(Keeping `_decompStats` populated lets DrawDecompStats still show the honest 0% coverage numbers.)

### WR-03: Leaked pool-live residual when Process throws after the projection ran (window error path)

**File:** `Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs:571` (context lifecycle at `:367-376`)
**Issue:** `RecomputePreview` stores the context in `_projectionContext` (line 372) before calling `_pipeline.Process(inspection, _projectionContext)` (line 376). If `Process` throws after `projection.Run` succeeded — e.g. `ExtractTransferRoughness`'s removed-luma p90 readback failure or a surface-pack dispatch failure — `RunDecompPreview` (line 386) never executes, so `_decompOutput` stays null while `_projectionContext.Decomp` holds a residual RT leased from the persistent `_decompPipeline`'s pool. `ReleaseDecompPreview` then drops the reference at line 571 (`_projectionContext = null;`) without disposing `Decomp`: the RT remains in the pool's live set indefinitely, never returns to the reuse stack, and every subsequent failed recompute leaks another one (the window recomputes are debounced control-driven, so a persistent failure accumulates live RTs across a session). This is the same accumulation class the phase's WR-03 catch-block releases were introduced to prevent for the compute pool; the decomp-pool lease reachable through the new context field was missed. (`NamerProcessor` is not affected: its per-material `finally` disposes the whole decomp pipeline, whose pool teardown destroys live targets.)
**Fix:** Dispose the context's output before nulling — `NamerDecompOutput.Dispose` is idempotent (guarded by `_disposed`), so the happy path where `_decompOutput == ctx.Decomp` (already disposed above) is safe:
```csharp
_projectionContext?.Decomp?.Dispose();
_projectionContext = null;
```

## Info

### IN-01: Duplicate `<summary>` blocks above DecompositionSkipReason; CountDistinctSourceMeshes undocumented

**File:** `Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs:678-699`
**Issue:** Two `<summary>` elements sit back-to-back before `DecompositionSkipReason` (line 700). The first block (describing the distinct-source-mesh count) was clearly written for `CountDistinctSourceMeshes` (line 722) but no longer attaches to it, leaving malformed XML doc on the member and the helper below undocumented.
**Fix:** Delete the first `<summary>` block and move it directly above `CountDistinctSourceMeshes` (line 722).

### IN-02: Three readback helpers omit `forcePlayerLoopUpdate = true`

**File:** `Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs:260` (also `:333`, `:381`)
**Issue:** `RemovedLumaP90`, `ReadBackGlobalMax`, and `RobustSobelScale` call `request.WaitForCompletion()` directly, unlike the established pattern in this codebase (e.g. `NamerDecompPipeline.ReadBackStats`, `NamerEditorWindow.ReadBackBaseTexels:652`) which sets `request.forcePlayerLoopUpdate = true` before waiting, so the request is serviced even when no player-loop tick is pending. Without the flag, `WaitForCompletion` can block indefinitely in editor contexts that are not pumping the loop.
**Fix:** Add `request.forcePlayerLoopUpdate = true;` immediately after each `AsyncGPUReadback.Request(...)` call in all three helpers.

### IN-03: `_Verts` StructuredBuffer uploaded and bound but never read by the kernel

**File:** `Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs:237` (allocation/upload above; `NAMERDecomp.compute` declares `_Verts` but `CSRasterizeVertexColors` never reads it)
**Issue:** The vertex-positions buffer is allocated, filled, and bound to the rasterize kernel on every `GenerateResidual` invocation, but the kernel interpolates only `_Colors`/`_Uvs`/`_Tris`. Dead per-call GPU upload and a misleading shader-side contract.
**Fix:** Remove the `_Verts` declaration from `NAMERDecomp.compute` and the corresponding allocation/SetBuffer in `NamerDecompPipeline` (or bind positions only once a kernel actually consumes them).

---

_Reviewed: 2026-09-16T21:45:00Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_

---

## Gap-Closure Round (plans 06/07/08)

**Reviewed:** 2026-09-17T19:45:00Z
**Depth:** standard
**Scope:** the 9 source files of `git diff 8e9a512..HEAD` (commits `7c272fa..7aa092b` — plans 04.2-06/07/08): `Compute/NAMERRoughness.compute`, `Editor/Pipeline/NamerComputePipeline.cs`, `Tests/Editor/NamerRoughnessTransferTests.cs`, `Editor/UI/NamerPreviewRenderer.cs`, `Tests/Editor/NamerPreviewRendererTests.cs`, `Editor/Decompose/NamerDecompPipeline.cs`, `Tests/Editor/ResidualPipelineTests.cs`, `Editor/UI/NamerEditorWindow.cs`, `Compute/NAMERDecomp.hlsl`.

The round-1 content above is unchanged; the round-1 frontmatter remains the round-1 record. Its `status: issues_found` is still accurate overall: the three round-1 Warnings are now closed (verdicts below), but the three round-1 Infos remain open and three new Infos were found in this round.

### Method

Every hunk was read both in diff and in full file context, and each fix was traced end-to-end across module boundaries: the soft-clip against `CSRemovedLuma`, the `ExtractTransferRoughness(scalar, sourceBase, projectedBase, ...)` parameter order, and the Rec.601 luminance; the per-pane fit re-derived analytically (world-units-per-pixel = 2·orthoSize/rectHeightPx) with the pinned test values recomputed from the mesh extents; `ResolveAspectSize`'s ripple into every `Resample` call site and every `ChosenResolution` consumer (window stats string, EXR write path, existing tests); the window guard against `NamerProcessor.cs:210-228` plus the `_decompOutput == null` material-binding branch and after-pane mesh fallback; the dispose fix against `NamerDecompOutput.Dispose` idempotence and the `RecomputePreview` → `ReleaseDecompPreview` lifecycle; and the ternary chain's segment membership at the x = 0.25/0.5/0.75 knots versus the retired early returns.

### Fix Verification (GAP closure)

**GAP-1 soft-clip — `NAMERRoughness.compute:187`.** Verified correct. `mag = clamp(mag, -1.0, 1.0)` sits exactly between the p90 division (`max(_LumaP90, 1e-4)`) and the saturate dip. The signed dark-occlusion path is handled symmetrically: negative outliers clamp to −1, so the matte ceiling is `saturate(scalar + strength)` — the mirror of the gloss floor `saturate(scalar − strength)` — and in-range texels (|mag| ≤ 1) are bit-identical to the previous behavior (the six pre-existing transfer tests are byte-unchanged and green per the live-editor run). The corrected comment at `NamerComputePipeline.cs:173-175` is accurate: `ExtractTransferRoughness` binds `sourceBase = baseColorOut` (2nd parameter) and `CSRemovedLuma` computes `luma(source − projected)` — the SOURCE base is the minuend, the projected base the subtrahend. Test quality: the 2×2 outlier on a 32×32 flat field drives |mag| ≈ 4500 above the 1e-4 p90 floor (a faithful seam/boundary-outlier model), the floor test asserts `scalar − strength` within 0.03 (8-bit quantization headroom), and the monotonic test asserts strength 0.6 dips strictly deeper than 0.25 — pinning the user-visible slider behavior GAP-1 asked for. Code-level closure verified; the Neo-asset LOOK re-validation remains the pending UAT round-2 item (HUMAN-UAT test 1).

**GAP-2 per-pane framing — `NamerPreviewRenderer.cs`.** Verified correct. Derivation: an orthographic camera of half-height `s` over a rect of height H px has world-units-per-pixel 2s/H; requiring the bounding-sphere radius r to fit a pane half-width (aspect·H/2 px) minus `MarginPx` on each edge yields exactly `horizFit = r·H/(aspect·H/2 − 2·margin)`; the vertical fit over the full rect height is the same form — the implementation matches both. The expression is homogeneous in (H, paneWidthPx, margin), so points-vs-physical-pixels cancels (only the rect aspect must be exact, and `rect.width/rect.height` is) — no HiDPI unit bug. `paneCenterWorld = orthoSize·aspect/2` is the exact center of each half of the visible `2·orthoSize·aspect` world width, recomputed from the ZOOMED size on every `Render`, so each object stays centered in its own pane while zooming — the specific UAT complaint. Pinned values recomputed independently: √5.25·400/370 = 2.47709, √16.26·400/370 = 4.35932, √16.26·400/170 = 9.48794 — all match the test pins to 1e-3. Signature flip is regression-safe: the only non-test caller of `OrthographicSizeForAspect` is `Render` itself (grep across `Packages/`), and all test call sites are two-arg. Degenerate guards present (`Mathf.Max(rect.height, 1f)`, `Mathf.Max(denominator, 1f)`); no `HalfSeparation`/`PreviewGap` remnants anywhere under `Packages/`. The pane-center tracking is not directly test-pinned, but it is a pure function of the pinned orthographic size, so the pin covers it.

**GAP-3 / Metal heatmap ("WR-04" per 04.2-08-PLAN) — `NAMERDecomp.hlsl:36-48`.** Verified mathematically equivalent to the retired early-return chain. Boundary membership is unchanged and exact: at x = 0.25/0.5/0.75 both forms select the next segment at t = 0, and `lerp(c, c', 0) = c` exactly in IEEE, so the knot values are c1/c2/c3 either way (the ternaries use strict `<`, same as the retired `if`s). The unconditionally computed off-segment lerps are finite (constant 0.25 divisors; `lerp` is total) and discarded by the select, so no NaN/Inf can propagate through the discarded branches. The c0..c4 anchors and all four lerp coefficients are byte-identical to the previous form (checked hunk-by-hunk); only the control flow changed — which is exactly what removes the predicated-assignment shape the Metal frontend misread as an uninitialized result.

### Warning Closure Verdicts

- **WR-01: FIXED.** Every former square-resample path now routes through the long-edge convention: the step-6 final resample (`NamerDecompPipeline.cs:332-333`), the stats-upsample gate (`:341`, keyed on `longEdge`), the manual-override clamp (`:402`), the adaptive skip/fallback (`:410-411`), and `EvaluateResolution` (`:445-446`). Grep confirms no `Resample(x, n, n)` call remains in the file. Portrait sources resolve correctly through the `w < h` branch of `ResolveAspectSize` (short edge = long edge scaled by `w/h`). `ChosenResolution`'s only non-test consumers are the window stats string (`NamerEditorWindow.cs:1199`, "written @Npx" now honestly means the long edge) and the EXR write, which is dimension-agnostic (RT readback → `EncodeToEXR`); existing square-source assertions (`ResidualPipelineTests.cs:244-258, 287-289`) hold because width == long edge for squares. `Mathf.RoundToInt` midpoint behavior can deviate the short edge by ≤1 texel in exact-.5 aspect cases — immaterial for a bilinear-sampled quotient. The new test pins 256×128 + manual index 5 → `ChosenResolution == 128`, residual 128×64.
- **WR-02: FIXED.** The guard at `NamerEditorWindow.cs:607-612` mirrors `NamerProcessor.cs:210-218` — identical condition (`ctx.Decomp != null && ctx.Decomp.Stats.CannotDecompose`) and identical consequence (do not consume the decomposition). Null-safety verified: `Stats` is a reference type, but all three `NamerDecompOutput` construction sites pass non-null stats, and the pre-existing line 615 dereferences `Stats` the same way, so the guard adds no new null surface. Stale-state safety verified: `RecomputePreview` calls `ReleaseDecompPreview` (`:342`) before rebuilding, so the early return leaves `_decompOutput`/`_previewSplitMesh` genuinely null; the after pane then falls back to the source mesh (`:832`, `:839-843`) and the material binding takes the `_decompOutput == null` branch (`:394-398` — base PNG at `_BaseResidualMap`), which is the Phase-3 shape `NamerProcessor` generates with `decomp` null (`:267`). `_decompStats` stays populated so `DrawDecompStats` shows the honest 0% coverage.
- **WR-03: FIXED.** `NamerEditorWindow.cs:571` disposes the context's output immediately before nulling. Idempotence verified at `NamerDecompPipeline.cs:81-91` (`_disposed` guard; `Residual` nulled after release), so the happy-path double-dispose (`_decompOutput == ctx.Decomp`, disposed at `:567`) is a no-op. The leak scenario is closed on both exits: the next `RecomputePreview` releases at `:342` before creating a new context, and window teardown releases at `:179` — one live RT max per failure, no cross-recompute accumulation. Ordering vs `_previewSplitMesh` teardown is safe: this is a teardown path, and the next material rebind happens before the next `Repaint` (the same pattern the pre-existing `_decompOutput` dispose already established).

### Prior Infos

IN-01/IN-02/IN-03 verified intentionally untouched, per the HUMAN-UAT GAP-3 "out of scope" disposition: `NamerProcessor.cs` and `NamerRoughnessPipeline.cs` have zero hunks in `8e9a512..HEAD`, and the `_Verts` upload block in `NamerDecompPipeline` is unchanged.

### New Findings

No Critical and no Warning findings survived adversarial probing in this round. Three Infos:

#### IN-04: `ChosenResolution` XML doc still says "width" after the long-edge semantics change

**File:** `Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs:38-39`
**Issue:** The `<see cref="ChosenResolution"/>` doc on `NamerDecompErrorStats` still reads "otherwise the chosen residual width." This round changed the field's semantics to the residual LONG edge (short edge derives by aspect); for non-square sources the doc is now wrong, and the next reader of the public stats contract will mis-derive the residual shape.
**Fix:** One-line doc edit: "otherwise the chosen residual LONG edge (the short edge preserves the source aspect; equals the width for square sources)."

#### IN-05: Transfer-formula doc drift — the [-1,1] soft-clip is missing from two formula comments

**File:** `Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerRoughnessPipeline.cs:166-167` (also `Compute/NAMERRoughness.compute:171`)
**Issue:** The `ExtractTransferRoughness` XML doc still states the consume-site formula as `saturate(scalarRoughness - strength * removedLuma / p90)` without the new clamp, and the kernel one-liner above `CSRoughnessTransferRemap` (`// Transfer remap: roughness = saturate(scalar - strength * removedLuma / p90).`) has the same omission — even though the extended header block above it (lines 149-155) was updated. The two formula strings nearest the code now under-document the GAP-1 behavior the kernel actually implements.
**Fix:** Append "with mag = removedLuma/p90 clamped to [-1, 1]" to both formula comments.

#### IN-06: CannotDecompose preview surfaces no skip notice, and the residual row copy mislabels the outcome

**File:** `Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs:607-612` (copy at `:49`, `:1199-1200`)
**Issue:** For a CannotDecompose selection, `NamerProcessor` records an explicit warning ("Vertex-color decomposition skipped ... generating the non-decomposed Phase-3 shape instead", `NamerProcessor.cs:215-217`), but the window's preview path surfaces no equivalent status — the only hints are the 0% coverage stat and the residual row reading `ResidualNotWrittenLabel` ("not written (one-texture)"), which mislabels a skipped decomposition as a one-texture collapse. The rendered shape parity WR-02 demanded is fixed; this is the remaining message-level parity gap, in the same preview-must-not-mislead class.
**Fix:** When `_decompStats.CannotDecompose` is true, show a short status/helpbox line mirroring the processor's wording (or extend the residual-row copy to "not written (decomposition skipped)").

**Summary (this round): 0 Critical / 0 Warning / 3 Info.**

Residual risk notes (not findings): GAP-1's outlier texels above the clamp now all receive the identical extreme dip `saturate(scalar ∓ strength)` — the deliberate soft-clip trade-off accepted in planning; its final acceptance is the user's Neo-asset UAT re-run, not the unit tests. The framing tests pin the orthographic size only; pane-center tracking and the 15px screen margin are verified by derivation plus the executor's live suite, and get their real confirmation in UAT test 2's re-run.

---

_Reviewed: 2026-09-17T19:45:00Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
