---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
verified: 2026-09-17T20:30:00Z
status: human_needed
score: 8/8 machine-checkable must-have groups verified (5 roadmap SCs + 3 gap acceptance sets)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 27/27 (5 roadmap SCs + 22 plan truths)
  gaps_closed:
    - "GAP-1 Removed Detail soft-clip (plan 06): shiny edges + inert dip-depth slider"
    - "GAP-2 per-pane screen-space framing (plan 07): 15px margin, pane centers track zoom"
    - "GAP-3 warning closure (plan 08): WR-01 aspect resample, WR-02 preview guard, WR-03 dispose, Metal heatmap"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "UAT round 2 — GAP-1 re-validation: transferred-roughness LOOK on the real Neo asset via the default Removed Detail path"
    expected: "Process the Neo selection with decomposition on + defaults (Removed Detail, Write Residual off, dip depth 0.25): no shiny/glossy edges at UV seams or bright baked speckle (outliers now floor at roughness = scalar - strength, never gloss 0); the dip-depth slider produces a visible monotonic change swept 0 -> 0.25 -> 0.6; quiet regions stay near the authored scalar; Sobel Edge remains selectable as fallback (still clean at 0.352 if compared); console stays free of the NAMER_DECOMP_HEATMAP shader warning"
    why_human: "The soft-clip, its monotonicity, and the Sobel-fallback integrity are code- and test-verified (Transfer_SeamOutlier_* PASS in the 149/0 run), but gloss appearance on the user's real asset is a visual judgment no automated test encodes — this is the exact 04.1 round-5 / 04.2 round-1 rejection the phase exists to fix"
  - test: "UAT round 2 — GAP-2 re-validation: per-pane framing look and interaction in the live editor"
    expected: "A newly framed selection shows each pane's own object individually framed with ~15px of screen-space margin (no large gap between before/after objects); zooming in keeps BOTH objects framed in their own panes (pane centers track the zoomed orthographic size — the mate object no longer slides off-pane); synced drag rotation (pitch clamps at +-89), scroll zoom with no perspective shift, nothing crops at extreme yaw+pitch; toolbar/debounce behave as before"
    why_human: "The fit expression is analytically re-derived and pinned (2.4771 / 4.3593 / 9.4879 recomputed independently from the mesh extents), and the render test pins the applied orthographic size, but 'framing looks right' at real window DPI/aspect and zoom feel need the live window"
  - test: "UAT round 2 — Write Residual checkbox end-to-end (round-1 item, still unexercised) + long-edge semantics on a real asset"
    expected: "Checkbox ON: _Residual.exr written, stats row reads 'written @ Npx' (N = residual LONG edge under the WR-01 fix — for a non-square base like 2048x1024 the EXR itself is aspect-correct, not square), removed-detail rows (Coverage/Avg/Max/max error) populate; checkbox OFF: no EXR, residual row reads 'not written (one-texture)', threshold/resolution controls visibly disabled; tooltips readable; prefs persist across an editor restart; console clean"
    why_human: "Fixture-level behavior is automated (GenerateResidual_NonSquareBase_PreservesSourceAspect + the one-texture acceptance tests), but live control states, tooltip readability, prefs-across-restart, and a real non-square asset's EXR shape need the real UI"
---

# Phase 04.2: Gouraud-projection One-texture with Roughness Transfer — Round-2 Verification (Gap Closure)

**Phase Goal:** Replace the fit-driven sharp-removal search with a by-construction residual collapse — the cleaned base becomes the per-triangle Gouraud-representable projection of the albedo (residual quotient == white up to quantization, no ladder search or threshold gamble), while the removed detail's luminance transfers into the 6-bit roughness map via the surviving D-08 dip consume site with a pure-taste dip-depth slider, a residual on/off override checkbox, and a consolidated estimator path; UV-overlap texels handled honestly (research-gated).
**Verified:** 2026-09-17T20:30:00Z
**Status:** human_needed
**Re-verification:** Yes — round 2, after gap closure (plans 06/07/08) for UAT round 1's GAP-1/2/3

## Verification Method

Goal-backward, codebase-first. Every gap claim was checked against the working tree and git history, not the SUMMARYs: `git diff 8e9a512..HEAD` (the round-1 verification baseline) read in full for all 9 changed files — the diff itself is the proof of "Sobel untouched", "call sites byte-identical", "comment-only", "anchors byte-identical"; both compute assets and the heatmap include read in full; the window/decomp/pipeline files read at every cited hunk; all three new/flipped tests read for assertion substance (vacuousness check); the per-pane fit math and the three framing pins re-derived analytically; the seam-outlier fixture's |mag| arithmetic checked against `RemovedLumaP90`'s actual constants (subsample 256, p90, floor 1e-4); retired-symbol sweep re-run repo-wide under `Packages/`. Suite evidence audited directly rather than trusted: `Temp/namer042-{06,07,08}-results.txt` are complete per-test records (06: 148 PASS lines / failed=0; 07: 148/0; 08: 149 PASS lines / 0 fail / 0 skip, TOTAL line matches, FINISHED marker present, per-test PASS lines confirmed for every new/flipped test). Metal-warning evidence audited in `~/Library/Logs/Unity/Editor.log`: 24 historical occurrences of `potentially uninitialized variable (NAMER_DECOMP_HEATMAP)`, the LAST at line 87661 of 94123, followed by the NAMERDecomp.hlsl/.compute reimports, script recompile, assembly reload, and ~6400 lines of subsequent test-fixture activity containing ZERO further occurrences (log mtime 11:49:19 PDT = 18:49 UTC, matching plan-08 completion). `gsd-sdk query verify.artifacts` passed 9/9 across plans 06/07/08; `verify.key-links` again reports "Source file not found" for all 8 links (the plans' `from:` fields are member names — same tool artifact as round 1), substituted by manual tracing at the exact lines cited below. The 8 gap-closure commits (`7c272fa..50c3d12` + docs) all exist on the branch.

## Goal Achievement — Checks Table

| # | Criterion (ROADMAP SC / gap acceptance) | Evidence (file:line, personally verified) | Verdict |
|---|----------------------------------------|------------------------------------------|---------|
| 1 | **SC-1** Residual white by construction; projection machinery intact after gap changes | `NAMERDecomp.compute` is NOT in the `8e9a512..HEAD` diff stat — CSProjectBase/CSResidual symmetric-floor quotient untouched since round 1's full verification. WR-01 changed only the ladder/resample logic (diff starts at line 319, after the kernel dispatches); for square sources `ResolveAspectSize` returns (n, n) — byte-equivalent to the old square resample. `Acceptance_WhitenessByIdentity_E2E` PASS in the audited 149/0 run | ✓ VERIFIED |
| 2 | **SC-2** Removed detail re-expressed as gloss via dip-depth slider; chroma loss bounded/documented — now WITH GAP-1 closure | Soft-clip `mag = clamp(mag, -1.0, 1.0);` at NAMERRoughness.compute:187, exactly between the p90 division (:180) and `saturate(_ScalarRoughness - _Strength * mag)` (:188). Monotonicity: outlier floor is `saturate(scalar - strength)` (gloss side) and ceiling `saturate(scalar + strength)` (matte side, signed clamp symmetric) — slider modulates both monotonically; in-range texels (\|mag\| <= 1) are bit-identical (clamp is identity), so the six pre-existing transfer tests hold (Transfer_P90Headroom_SaturatesBothEnds PASS in all three results files). Corrected comment at NamerComputePipeline.cs:173-175 matches the code: `ExtractTransferRoughness(inspection.Roughness, baseColorOut, projectedOut, ...)` with signature `(scalar, sourceBase, projectedBase)` (NamerRoughnessPipeline.cs:176-179) binding `_BaseColorOut = sourceBase` (:208) — SOURCE base is the minuend. Chroma copy intact (window :930 ~55%/p10 37%/0.07; :1080 87.9%). The Neo-asset LOOK itself: UAT round 2, item 1 | ✓ VERIFIED (machine level; LOOK = human) |
| 3 | **SC-3** One-texture default + explicit on/off checkbox | `DefaultWriteResidual = false` (NamerEditorConstants.cs:57), `DefaultRoughnessExtractStrength = 0.25f` (:51); checkbox drives `AlwaysKeep/NeverKeep` at NamerProcessor.cs:243 and :587; honesty copy intact (`not written (one-texture)` :49, `written @ Npx` :1199). One-texture acceptance tests PASS in the audited 149/0 run | ✓ VERIFIED |
| 4 | **SC-4** Estimator consolidation, single extraction path | Retired-symbol sweep re-run across `Packages/`: 0 matches for all 13 identifiers (NamerRoughnessFitter, TryGetFitStrength, ExtractSharpRemoval, EvaluateRefitMaxError, NamerRoughnessEstimator, RoughnessFitCancelled, RequestFit, _fitCache, RunFrequencySeparation, CSRoughnessRemap, NormalizedBaseColorOwnedByRoughnessPool, HalfSeparation, PreviewGap). NAMERRoughness.compute = exactly 5 pragma kernels (Sobel 3 + transfer 2) | ✓ VERIFIED |
| 5 | **SC-5** 04.1-07 flips intentional; full suite green | Flip set unchanged since round 1 (no test files touched by 06/07/08 beyond the three named). Suite arithmetic reconciles: 146 (round-1 w4) + 2 (plan-06 seam tests) + 0 (plan-07 = rename+flip of the framing test) + 1 (plan-08 aspect test) = 149. Audited: namer042-08-results.txt contains exactly 149 PASS lines, 0 FAIL/Skip, TOTAL matches. The round-1 staged Zoom-test fix was committed (ad21615) — Packages/ tree is clean vs HEAD | ✓ VERIFIED |
| 6 | **GAP-1 acceptance** No shiny edges on default Removed Detail output; slider visibly/monotonically live; Sobel fallback intact; tests genuinely pin seam-outlier behavior | Kernel + monotonicity + comment: see SC-2 row. Tests substantive: `Transfer_SeamOutlier_SoftClipsAtUnitDepth` (NamerRoughnessTransferTests.cs:344-390) — 2x2 outlier (0.95) on a flat 0.5 field, p90 ~= 0 floored at 1e-4, so \|mag\| ~= 4500 >> 1; asserts `AreEqual(scalar - strength, r, 0.03)` — the old unclamped form pins to roughness 0 and FAILS this. `Transfer_SeamOutlier_DipIsMonotonicInStrength` (:392-436) asserts r06 < r025 - 0.05 and both < scalar. Sobel path untouched: the `8e9a512..HEAD` diff of NAMERRoughness.compute contains ONLY the soft-clip hunk + comments — CSRoughnessSobel/MaxReduce/Normalize and CSRemovedLuma have zero hunks. Visual acceptance on Neo: UAT round 2, item 1 | ✓ VERIFIED (code/tests; visual = human) |
| 7 | **GAP-2 acceptance** Per-pane screen-space framing, 10-20px margin, objects stay framed while zooming; window call sites unchanged | `MarginPx = 15f` (NamerPreviewRenderer.cs:31 — mid-range of the user's 10-20 spec). Two-param `OrthographicSizeForAspect(aspect, rectHeightPx)` (:198-205): pane = `aspect * rectHeightPx / 2` (:201); `horizFit = radius * H / max(paneWidthPx - 2*margin, 1)` (:202), `vertFit = radius * H / max(H - 2*margin, 1)` (:203), `max(...) * clamped zoom` (:204) — matches the analytic derivation (world-units-per-pixel = 2*orthoSize/H; diameter 2*radius fits the margin-reduced pane) exactly. Pane centers track zoom: `paneCenterWorld = orthoSize * aspect / 2f` (:125) recomputed from the ZOOMED size each Render (:111-112), draw positions `±paneCenterWorld - rotatedBoundsCenter` (:126-127). `Frame` stores the rotation-invariant `extents.magnitude` radius with degenerate fallback (:153-163). Flipped pins recomputed independently: sqrt(5.25)*400/370 = 2.47707, sqrt(16.26)*400/370 = 4.35932, sqrt(16.26)*400/170 = 9.48794 — all match `Frame_FitsPerPaneObjectWithMargin` (NamerPreviewRendererTests.cs:125/133/135) at 1e-3; orbit-invariance asserted (:139-142); render test pins the applied size via the two-arg call (:174-178). Old framing symbols swept to zero (HalfSeparation/PreviewGap/_framedHalfWidth/_framedHalfHeight: grep exit 1). Window diff `8e9a512..HEAD` contains ONLY the WR-02/WR-03 hunks — `_preview.Render/Orbit/Zoom` call sites (:845/:883/:889) byte-identical. Framing look: UAT round 2, item 2 | ✓ VERIFIED (math/tests; look = human) |
| 8 | **GAP-3 acceptance** All four warnings closed at root cause; no new warnings (3 new Infos recorded, not warnings); suite green | **WR-01:** `ResolveAspectSize` (NamerDecompPipeline.cs:582-597) — ladder value = LONG edge, short edge scaled by aspect, Max(1,..) guards; every down-resample routes through it (step-6 :332-333, EvaluateResolution :445-446) or longEdge (:324-325, :341, :410-411, manual clamp :402); no `Resample(x, n, n)` remains (all call sites read: (cw,ch) or full-res (w,h) upsamples). `ChosenResolution` = long edge; square sources unchanged (w == h == longEdge). New test `GenerateResidual_NonSquareBase_PreservesSourceAspect` (ResidualPipelineTests.cs:388-432): 256x128 + manual index 5 -> ChosenResolution 128 (ResolutionLadder[4] = 128, clamp vs max(256,128)), Residual 128x64 — discriminates against the old 128x128. **WR-02:** guard at NamerEditorWindow.cs:607-612 is condition-identical to NamerProcessor.cs:210 (`ctx.Decomp != null && ctx.Decomp.Stats.CannotDecompose`), keeps `_decompStats` populated, returns before consuming `ctx.Decomp` — preview matches the Phase-3 shape Process writes. **WR-03:** `_projectionContext?.Decomp?.Dispose();` immediately before nulling (:571-572); `NamerDecompOutput.Dispose` is `_disposed`-guarded (NamerDecompPipeline.cs:81-91) so the happy-path double-dispose is a no-op and the thrown-Process path no longer leaks the pool-leased residual RT. **Metal warning:** single-exit-point `return (x < 0.25) ? seg0 : (x < 0.5) ? seg1 : (x < 0.75) ? seg2 : seg3;` (NAMERDecomp.hlsl:45); git diff confirms the c0..c4 anchors and all four lerp coefficients are byte-identical to the retired early-return chain and boundary semantics preserved (strict `<` both forms; knots -> next segment at t=0). Editor.log audit: last of 24 occurrences precedes the shader reimport; zero occurrences after, through a recompile + full 149-test run that dispatches CSErrorHeatmap. Review round-2 verdicts (WR-01/02/03 FIXED, 0C/0W/3I) corroborated by this independent reading. New Infos IN-04/05/06 are documentation-level, recorded in 04.2-REVIEW.md | ✓ VERIFIED |

**Score:** 8/8 machine-checkable groups verified. Zero FAILED, zero UNCERTAIN at the machine level.

### Round-1 Facts — Regression Status

All 27 round-1 must-haves (5 SCs + 22 plan truths) remain true: the only source changes since the round-1 baseline are the 9 files above; `NAMERDecomp.compute`, `NAMERPack.compute`, `NamerProcessor.cs` (except none — untouched), `NamerRoughnessPipeline.cs`, `NamerSourceModel.cs`, settings, and 7 of 10 test suites have zero hunks in `8e9a512..HEAD`. Round-1 housekeeping resolved: the staged Zoom-test neutral-reset fix is committed (`ad21615`); `Packages/` is clean vs HEAD. Still open (informational): live-editor generated artifacts under `Assets/NAMERGenerated/Neo-T-Pose/` and scene/ProjectSettings modifications remain uncommitted working-tree state — user-generated outputs, not phase deliverables.

## Required Artifacts (gap-closure round)

| Artifact | Expected | Status | Details |
|----------|-----------|--------|---------|
| `Compute/NAMERRoughness.compute` | soft-clipped CSRoughnessTransferRemap | ✓ VERIFIED | gsd-sdk PASS; clamp at :187 with GAP-1 comment; header updated; Sobel/CSRemovedLuma diff-clean |
| `Editor/Pipeline/NamerComputePipeline.cs` | corrected minuend comment | ✓ VERIFIED | gsd-sdk PASS; comment-only diff, branch logic byte-identical |
| `Tests/Editor/NamerRoughnessTransferTests.cs` | 2 seam-outlier tests, >=60 lines | ✓ VERIFIED | gsd-sdk PASS; 8 tests total; both new tests substantive (discriminating assertions read) |
| `Editor/UI/NamerPreviewRenderer.cs` | MarginPx/_framedRadius/two-param fit/pane centers | ✓ VERIFIED | gsd-sdk PASS; all four elements at :31/:50/:198-205/:125-127 |
| `Tests/Editor/NamerPreviewRendererTests.cs` | flipped per-pane pins | ✓ VERIFIED | gsd-sdk PASS; 2.4771/4.3593/9.4879 pinned; old pair-plus-gap name/asserts gone |
| `Editor/Decompose/NamerDecompPipeline.cs` | ResolveAspectSize + long-edge ChosenResolution | ✓ VERIFIED | gsd-sdk PASS; helper + 5 routed sites; no square resample remains |
| `Editor/UI/NamerEditorWindow.cs` | CannotDecompose guard + dispose-before-null | ✓ VERIFIED | gsd-sdk PASS; :607-612 and :571-572 |
| `Compute/NAMERDecomp.hlsl` | single-exit heatmap | ✓ VERIFIED | gsd-sdk PASS; nested ternary, anchors/coefficients byte-identical (diff-confirmed) |
| `Tests/Editor/ResidualPipelineTests.cs` | non-square aspect test | ✓ VERIFIED | gsd-sdk PASS; 256x128 -> 128x64 assertions |

## Key Link Verification

`gsd-sdk query verify.key-links`: 0/8 — "Source file not found" on every link (member-name `from:` fields; same tool artifact as round 1). Manual tracing at exact lines:

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| CSRoughnessTransferRemap | saturate dip | clamp(mag, -1, 1) | ✓ WIRED | NAMERRoughness.compute:180 -> :187 -> :188 |
| NamerComputePipeline.Process | ExtractTransferRoughness | source base minuend binding | ✓ WIRED | NamerComputePipeline.cs:176-177 -> NamerRoughnessPipeline.cs:176-179, :208-209 |
| NamerPreviewRenderer.Render | OrthographicSizeForAspect(aspect, rect.height) | per-pane fit applied to camera | ✓ WIRED | NamerPreviewRenderer.cs:110-112 |
| NamerPreviewRenderer.Render | both DrawMesh calls | ±paneCenterWorld from zoomed size | ✓ WIRED | :125-130 |
| ChooseResolution/EvaluateResolution/final resample | ResolveAspectSize | aspect-preserving (w', h') | ✓ WIRED | NamerDecompPipeline.cs:332, :402, :410-411, :445-446 -> :582 |
| RunDecompPreview (projection branch) | ctx.Decomp.Stats.CannotDecompose | CR-03 mirror early return | ✓ WIRED | NamerEditorWindow.cs:607-612 == NamerProcessor.cs:210-218 |
| ReleaseDecompPreview | NamerDecompOutput.Dispose (idempotent) | dispose before nulling | ✓ WIRED | NamerEditorWindow.cs:571-572 -> NamerDecompPipeline.cs:81-91 |
| NAMER_DECOMP_HEATMAP | single nested-ternary return | no early returns | ✓ WIRED | NAMERDecomp.hlsl:41-45; shared include consumed by NAMERDecomp.compute:203/:214 and NamerDebugView.shader:143 |

## Behavioral Spot-Checks (verifier-executed, read-only)

| Behavior | Command/Method | Result | Status |
|----------|----------------|--------|--------|
| Full EditMode suite (post-08) | Audit of `Temp/namer042-08-results.txt` | 149 PASS lines / 0 fail / 0 skip; TOTAL line matches; FINISHED present; new/flipped tests individually PASS | ✓ PASS |
| Suite gates 06/07 | Audit of results files | 06: 148/0/0 (11:32); 07: 148/0/0 (11:41) — count arithmetic 146+2+0 consistent | ✓ PASS |
| Metal warning absence | Editor.log audit | 24 occurrences, last at line 87661/94123; reimport+recompile+6462 subsequent lines (incl. full test run) contain 0 further occurrences; log mtime matches plan-08 completion | ✓ PASS |
| Sobel path untouched | `git diff 8e9a512..HEAD -- NAMERRoughness.compute` | Only soft-clip + comment hunks | ✓ PASS |
| Window call sites unchanged | `git diff 8e9a512..HEAD -- NamerEditorWindow.cs` | Only WR-02/WR-03 hunks | ✓ PASS |
| Heatmap math preserved | `git diff 8e9a512..HEAD -- NAMERDecomp.hlsl` | Anchors/coefficients byte-identical; only control flow changed | ✓ PASS |
| Framing pins | Analytic recomputation | 2.47707 / 4.35932 / 9.48794 vs pins at 1e-3 | ✓ PASS |
| Retired-symbol sweep | grep across Packages/ | 0 matches (13 identifiers + 2 framing symbols) | ✓ PASS |
| Gap commits exist | `git log` | 7c272fa, f00885d, c23c908, 4bc94a6, fc31ba0, 3a4d14f, aae52e8, 50c3d12 all present | ✓ PASS |
| Direct Unity re-run by verifier | — | Not run (interactive editor holds the project lock; driving it is out of verifier scope) | ? SKIP — covered by the audited results-file channel + UAT round 2 |

## Requirements Coverage

Unchanged from round 1 (VCOL-03/04/05, UI-03, SHDR-02, UI-04 all SATISFIED) and strengthened by the gap round: VCOL-05's residual resolution is now aspect-correct for non-square bases (WR-01); UI-03/UI-04's preview framing matches the user's round-1 verdict (GAP-2); SHDR-02's transferred-gloss default path no longer saturates outliers to gloss 0 (GAP-1). Orphaned requirements: none.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none of the 9 modified files) | — | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER, no placeholder prose, no empty returns | — | — |
| NamerRoughnessPipeline.cs | 166-167, NAMERRoughness.compute:171 | IN-05 (recorded): two formula comments omit the new [-1,1] clamp phrase | ℹ️ Info | Doc-only; the extended header (:149-155) documents GAP-1 correctly |
| NamerDecompPipeline.cs | 38-39 | IN-04 (recorded): ChosenResolution XML doc still says "width" after the long-edge change | ℹ️ Info | Doc-only; stats string at window :1199 now honestly means long edge |
| NamerEditorWindow.cs | 607-612 | IN-06 (recorded): CannotDecompose preview surfaces no explicit skip notice; residual row reads "not written (one-texture)" for a skipped decomposition | ℹ️ Info | Message-level parity gap; shape parity (the WR-02 requirement) is fixed |
| Working tree | — | Untracked/modified live-editor artifacts (NAMERGenerated/Neo-T-Pose/* incl. new Residual.exr, NamerSmoke.unity, ProjectSettings, .sln.DotSettings.user) | ℹ️ Info | Generated outputs/user settings, not phase deliverables; decide commit vs discard |

## Round-2 Delta (what changed since round 1)

- Plans 06/07/08 executed (commits 7c272fa..50c3d12): 9 files, +272/-85 — exactly the review's round-2 scope, nothing else moved.
- Round-1 verification was human_needed with 3 UAT items. UAT round 1 exercised items 1 and 2 and reported issues (GAP-1, GAP-2); item 3 (Write Residual) was never exercised and remains pending. All three gaps are now closed in code with machine verification; the human items are re-issued as UAT round 2 above (item 3 now also covers the WR-01 long-edge/aspect semantics on a real asset).
- Round-1's only in-tree action item (commit the staged Zoom-test fix) is resolved (`ad21615`).
- Code review round 2: WR-01/02/03 verdicts FIXED — corroborated by this verification's independent reading of every fix; 3 new Infos (IN-04/05/06) recorded, none blocking.
- Suite count moved 146 -> 149 with reconciling arithmetic and per-test-audited evidence; Metal heatmap warning verified absent after the restructure via Editor.log forensics.

## Gaps Summary

No gaps found. All 8 machine-checkable must-have groups (5 roadmap SCs + 3 gap acceptance sets) verify against the shipped code, with regression evidence that every round-1 fact still holds. The remaining acceptance is inherently visual/interactive: the Neo-asset transferred-roughness LOOK (GAP-1's user-visible acceptance), the per-pane framing experience (GAP-2's), and the never-yet-exercised Write Residual end-to-end flow including the new long-edge/aspect behavior (GAP-3's "console clean during real use" rides along with all three). Per the decision tree, human verification items take priority: status is human_needed with UAT round 2 ready.

---

_Verified: 2026-09-17T20:30:00Z_
_Verifier: Claude (gsd-verifier)_
