---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
verified: 2026-09-17T20:04:39Z
status: human_needed
score: 11/11 machine-checkable must-have groups verified (5 roadmap SCs + GAP-1/GAP-3 regression sets + GAP-4 acceptance incl. WR-04 + regression sweep)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 8/8 machine-checkable groups (round 2)
  gaps_closed:
    - "GAP-4 divider-anchored framing + pane outline (plan 09, commits 147654f/ca7e0ad/8f84433) with the WR-04 draw-offset pin (ff9daec)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "UAT round 3 — GAP-4 framing look: divider adjacency, zoom-outward growth, outline visibility in the live editor"
    expected: "A newly framed Neo selection shows the two models hugging the center divider with ~10 px inner margins (LEFT right-aligned, RIGHT left-aligned) under the tight neutral-AABB fit — no large empty middle; scrolling zoom grows each model OUTWARD from the divider with no cross-pane overlap at any zoom (outer/top/bottom clipping is accepted window behavior); the 1 px pane outline (outer border + center divider, gray 0.4) is visible against typical albedo content; synced yaw+pitch orbit (pitch clamp +-89) and the debug-channel toolbar behave as before"
    why_human: "The anchor/fit/camera math is analytically re-derived and pinned (fit pins 2.1622/4.2667/9.1429, extent pins 4.0/0.1/2.8991, inner-edge == InnerMarginPx across 24-step zoom sweeps neutral+rotated, GetPaneDrawOffsets mirrored/rotation-tracking/zoom-consistent with a discriminating AreNotEqual vs the retired center-anchored placement), but 'looks right' at real window DPI/aspect and the zoom feel need the live window"
  - test: "UAT round 3 — IN-07 conscious check: extreme-pitch top/bottom cropping on the Neo asset"
    expected: "At extreme pitch (near +-89) on the Neo mesh the model may crop top/bottom (and outer edge) — the fit is pinned to the NEUTRAL AABB while the rotated vertical half-extent is |cos pitch|*ey + |sin pitch|*ez; this is the documented trade-off (class doc NamerPreviewRenderer.cs:21-23) accepted because GAP-4 only forbids CROSS-pane overlap, which the X-axis anchor guarantees at every orbit/zoom. Confirm it reads as acceptable window behavior on the real (near-cubic tripo) mesh, or flag it for the vertical re-fit the review sketched (IN-07 recommendation)"
    why_human: "Crop acceptability is a visual judgment; the code guarantee (no cross-pane overlap) is machine-verified, but whether the top/bottom crop at extreme pitch is acceptable to the user is the review's explicitly routed conscious check (IN-07, 04.2-REVIEW.md gap-closure round 2)"
  - test: "UAT round 3 — Write Residual checkbox end-to-end on a real asset (carried item 3, never exercised in rounds 1-2)"
    expected: "Checkbox ON: _Residual.exr written, stats row reads 'written @ Npx' (N = residual LONG edge per the WR-01 fix — a non-square base gets an aspect-correct EXR, not a square-distorted resample), removed-detail rows (Coverage/Avg/Max/max error) populate; checkbox OFF: no EXR, residual row reads 'not written (one-texture)', threshold/resolution controls visibly disabled; tooltips readable; prefs persist across an editor restart; console clean (incl. no NAMER_DECOMP_HEATMAP Metal warning)"
    why_human: "Fixture-level behavior is automated (GenerateResidual_NonSquareBase_PreservesSourceAspect + one-texture acceptance tests PASS in the audited 152/0 run), but live control states, tooltip readability, prefs-across-restart, and a real non-square asset's EXR shape need the real UI"
---

# Phase 04.2: Gouraud-projection One-texture with Roughness Transfer — Round-3 Verification (GAP-4 Closure)

**Phase Goal:** Replace the fit-driven sharp-removal search with a by-construction residual collapse — the cleaned base becomes the per-triangle Gouraud-representable projection of the albedo (residual quotient == white up to quantization, no ladder search or threshold gamble), while the removed detail's luminance transfers into the 6-bit roughness map via the surviving D-08 dip consume site with a pure-taste dip-depth slider, a residual on/off override checkbox, and a consolidated estimator path; UV-overlap texels handled honestly (research-gated).
**Verified:** 2026-09-17T20:04:39Z
**Status:** human_needed
**Re-verification:** Yes — round 3, after GAP-4 closure (plan 09 + the WR-04 fix, commits `147654f`/`ca7e0ad`/`8f84433`/`ff9daec`)

## Verification Method

Goal-backward, codebase-first, adversarial to SUMMARY claims. Round-2 record: 8/8 machine groups verified, UAT round 2 confirmed GAP-1/GAP-3 closed and reported GAP-4 (item 2 ISSUES; item 1 PASSED; item 3 never exercised). This round re-verified the full phase contract against the working tree, not the SUMMARYs: `git diff 6e5e61d..HEAD` (post-plan-09-docs delta = the WR-04 fix) read hunk-by-hunk — exactly 2 files (`NamerPreviewRenderer.cs` +20/-3, `NamerPreviewRendererTests.cs` +81), nothing else, `Packages/` clean vs HEAD; `git diff 8e9a512..HEAD` (round-2 baseline) re-read for the regression sweep — 9 files, all accounted for as round-2 fixes + plan 09 + WR-04, zero hunks in `NAMERDecomp.compute`, `NAMERPack.compute`, `NamerProcessor.cs`, `NamerRoughnessPipeline.cs`, `NamerSourceModel.cs`, or settings since round 2 verified them. All three plan-09 files read in full current state. Every pinned number recomputed analytically (python): fit pins 400-max(400-25,1) forms -> 2.16216 / 4.26667 / 9.14286, yaw45 extent 0.70710678*4.1 = 2.89914, and the WR-04 discriminator anchor 4.21333 vs center-anchored 4.26667 (differ 0.053 — the `AreNotEqual` regression guard is genuine, not tolerance noise). Suite evidence audited directly: `Temp/namer042-09-results.txt` (mtime 12:44 PDT) = 151 `^PASS ` lines, 0 FAIL/IGNORED, `TOTAL passed=151 failed=0 skipped=0`, FINISHED present, the three plan-09 framing tests individually PASS; `Temp/namer042-wr04-results.txt` (mtime 13:00 PDT, after commit `ff9daec` at 12:56:59 PDT) = 152 `^PASS ` lines, 0 FAIL/IGNORED, TOTAL matches, FINISHED present, `GetPaneDrawOffsets_MirrorsDividerAnchor` PASS — the run exercised the final code state. Count arithmetic reconciles: 149 (post-08) + 2 (plan-09 anchor tests) + 1 (WR-04) = 152. The Unity editor was NOT driven by the verifier (user's interactive session holds the project lock) — the results files are executor-attested gates corroborated by on-disk per-test evidence. Editor.log re-audited: still 24 total `NAMER_DECOMP_HEATMAP` occurrences, last at line 87661 (the same line round 2 recorded), with 15,160 subsequent lines including BOTH new suite runs and zero further occurrences (log mtime 13:02 PDT). All four round-3 commits exist on the branch.

## Goal Achievement — Checks Table

| # | Criterion (ROADMAP SC / GAP-4 acceptance / WR-04) | Evidence (file:line, personally verified) | Verdict |
|---|----------------------------------------|------------------------------------------|---------|
| 1 | **SC-1** Residual white by construction; projection machinery intact after plan 09 + WR-04 | `NAMERDecomp.compute` has ZERO hunks in `8e9a512..HEAD` — untouched since round 1's full verification; WR-01 changed only ladder/resample logic downstream of the kernel dispatches. `Acceptance_WhitenessByIdentity_E2E` PASS in the audited 152/0 wr04 results file | ✓ VERIFIED |
| 2 | **SC-2** Removed detail re-expressed as gloss via dip-depth slider; chroma bounded — GAP-1 closure NOT regressed | Soft-clip `mag = clamp(mag, -1.0, 1.0);` intact at NAMERRoughness.compute:187 between the p90 division and the saturate dip; `Transfer_SeamOutlier_SoftClipsAtUnitDepth` + `Transfer_SeamOutlier_DipIsMonotonicInStrength` PASS in the wr04 results file; `NAMERRoughness.compute` total delta since round-2 baseline is still only the round-2 soft-clip hunks (16 lines — plan 09/WR-04 added none). The Neo LOOK itself: UAT-confirmed PASSED round 2 (HUMAN-UAT item 1) | ✓ VERIFIED (machine level; LOOK = UAT-confirmed) |
| 3 | **SC-3** One-texture default + explicit on/off checkbox — not regressed | Window diff `8e9a512..HEAD` contains ONLY: WR-03 dispose hunk (:571), WR-02 guard hunk (:607-612), `DrawPreviewPaneOutline` + its one call line (+11) — the checkbox/stats copy and processor paths are untouched; `Process_WriteResidualOff_WritesOneTextureNoExr`, `Acceptance_OneTextureDefault_NoResidualExr_GlossReexpressed`, `OneTexture_ResidualDropped_LeavesBaseResidualMapUnbound` all PASS in the wr04 results file | ✓ VERIFIED |
| 4 | **SC-4** Estimator consolidation, single extraction path — not regressed | Retired-symbol sweep re-run across `Packages/` (cs+compute): 0 matches for `paneCenterWorld`/`_framedRadius`/`HalfSeparation`/`PreviewGap` (plus the 13 round-2 identifiers swept then; no new retirement-introducing code landed since) | ✓ VERIFIED |
| 5 | **SC-5** 04.1-07 flips intentional; full suite green | Flip set unchanged since round 2 (the only test file touched since is `NamerPreviewRendererTests.cs`: rename+flip of the framing test + 3 additions — plan-09's own intentional flip is documented in the 09-SUMMARY flip-audit table and independently re-derived here). Suite: 152/152 PASS / 0 fail / 0 skip audited in `Temp/namer042-wr04-results.txt` (TOTAL + FINISHED + 0 FAIL/IGNORED lines) | ✓ VERIFIED |
| 6 | **GAP-1/GAP-3 regression set** All four round-2 warning closures still in place | Soft-clip (row 2); `ResolveAspectSize` at NamerDecompPipeline.cs:582 with routed call sites :332/:445 (grep-verified live); `GenerateResidual_NonSquareBase_PreservesSourceAspect` PASS in wr04 results; WR-02 guard + WR-03 dispose visible in the window diff above; single-exit heatmap `return (x < 0.25) ? seg0 : (x < 0.5) ? seg1 : (x < 0.75) ? seg2 : seg3;` at NAMERDecomp.hlsl:45 with ZERO hunks since round 2; Editor.log: 0 new Metal-warning occurrences through both new suite runs | ✓ VERIFIED |
| 7 | **GAP-4a** Models adjacent at divider with pinned 10 px inner margins under a tight neutral-AABB fit (near-cubic ~1.7x larger than the old sphere fit) | `InnerMarginPx = 10f` (:35), `MarginPx = 15f` re-scoped to outer/vertical (:34); `_framedHalfWidth`/`_framedHalfHeight` captured from `bounds.extents.x/.y` with per-axis 1f fallback (:53-54, :162-164 — no `_framedRadius` anywhere); `OrthographicSizeForAspect` (:201-207): `horizFit = hw * H / max(paneWidthPx - InnerMarginPx - MarginPx, 1)`, `vertFit = hh * H / max(H - 2*MarginPx, 1)`, `max * clamp(zoom)`. Pins analytically recomputed and exact: tall (1,2,0.5)@2:1 -> 800/370 = 2.16216; wide (4,0.5,0.1)@2:1 -> 1600/375 = 4.26667; wide@1:1 -> 1600/175 = 9.14286 — match `Frame_FitsTightAabbWithInnerMargin` (tests :125/:133/:135) at 1e-3, with the orbit-invariance assertion (:140-143). The tight AABB replaces the sphere radius sqrt(ex^2+ey^2+ez^2) — the ~1.7x over-approximation for near-cubic meshes is exactly the removed quantity | ✓ VERIFIED |
| 8 | **GAP-4b** NO cross-pane overlap at ANY zoom — anchor consumed by Render, same clamped ortho size as the camera, zoom-sweep invariant pinned | `Render` consumes the offsets: `Vector2 drawOffsets = GetPaneDrawOffsets(aspect, rect.height);` (:130) with before/after positions `drawOffsets.x`/`.y` minus rotatedBoundsCenter (:131-132) — CONSUMED, not just defined (WR-04's exact complaint, closed by `ff9daec`). `PaneAnchorWorld` (:233-237) calls the SAME `OrthographicSizeForAspect(aspect, rectHeightPx)` `Render` applies to the camera (:115-116 vs :235) — identical arguments, identical `Mathf.Clamp(_zoomScale, MinZoomScale, MaxZoomScale)` inside, no state mutation between :115 and :130, so camera size and anchor size cannot diverge. `HorizontalHalfExtentWorld` (:215-225) = abs row-0 of `Matrix4x4.Rotate(Euler(_pitch, _yaw, 0))` dotted with extents — an upper bound of the rotated silhouette's X half-extent at ANY orbit, so the inner edge at `anchor - xExtent` is exactly `InnerMarginPx` (world) from the divider: inner edges sit at +-10 px, gap 20 px, overlap impossible. Pinned twice: `PaneAnchor_InnerEdgeStaysAtInnerMarginAcrossZoomSweep` (24-step sweeps, neutral AND rotated, tests :164-192) and `GetPaneDrawOffsets_MirrorsDividerAnchor` (:195-273): mirrored negatives (1e-6), `anchor != orthoSize * aspect / 2` discriminator (4.21333 vs 4.26667 — fails on the retired center-anchored placement), rotation tracking after Orbit(35,20), and a 24-step zoom sweep re-asserting the inner edge. Extent pins recomputed: neutral 4.0, yaw90 0.1, yaw45 2.89914 (tests :151/:154/:159) | ✓ VERIFIED |
| 9 | **GAP-4c** Visible pane outline; synced orbit + debug toolbar unchanged | `DrawPreviewPaneOutline(Rect)` (NamerEditorWindow.cs:874-882): five `EditorGUI.DrawRect` calls — 1 px top/bottom/left/right border inside `previewRect` + 1 px center divider at `x + width/2 - 0.5`, color (0.4, 0.4, 0.4, 1) — called ONCE immediately after `GUI.DrawTexture(previewRect, previewTexture, ScaleMode.StretchToFill)` (:848-849); same GUI-space rect, so outline and rendered panes align (divider lands on world x = 0: camera at origin, identity rotation, :287-288). Window diff `8e9a512..HEAD` shows NO hunks touching `HandlePreviewCameraInput`'s `Orbit(current.delta.x, current.delta.y)` (:894) / `Zoom(current.delta.y)` (:900), the `_preview.Render` call (:845), `_preview.Frame` (:242), the toolbar, or the debounced recompute — "byte-identical" is diff-proven, not SUMMARY-trusted. Outline VISIBILITY against real content: UAT round 3, item 1 | ✓ VERIFIED (geometry/wiring; visibility = human) |
| 10 | **WR-04** Render's divider-anchored draw placement pinned (review gap-closure-round-2 Warning) | `ff9daec` = exactly the review's recommended minimal fix: pure public helper `GetPaneDrawOffsets(aspect, rectHeightPx)` returning `(-anchor, +anchor)` (:247-251) + `Render` consuming it (:130-132) + one test (`GetPaneDrawOffsets_MirrorsDividerAnchor`) pinning the mirrored/divider-anchored/rotation-tracking/zoom-consistent contract with the center-anchored regression guard. The one link grep cannot hold (Render's consumption) is now the smallest possible surface — a 3-line consumption verified by reading, with the formula pinned by test. PASS in the wr04 results file (152/0 run, post-commit) | ✓ VERIFIED |
| 11 | **Regression sweep** Nothing outside the claimed scope moved since round 2 | `git diff 6e5e61d..HEAD` = 2 files (renderer, tests) — the WR-04 fix only; `git status --porcelain -- Packages/` empty (clean tree); round-2's 9-file diff `8e9a512..HEAD` fully accounted for (round-2 fixes + plan 09 + WR-04); working-tree dirt remains only the user's live-editor artifacts (Assets/NAMERGenerated/Neo-T-Pose/*, NamerSmoke.unity, ProjectSettings, .sln.DotSettings.user) — unchanged disposition from round 2 | ✓ VERIFIED |

**Score:** 11/11 machine-checkable groups verified. Zero FAILED, zero UNCERTAIN at the machine level.

### Behavioral Spot-Checks (verifier-executed, read-only)

| Behavior | Command/Method | Result | Status |
|----------|----------------|--------|--------|
| Plan-09 suite gate | Audit of `Temp/namer042-09-results.txt` | 151 `^PASS ` / 0 FAIL / 0 IGNORED; TOTAL matches; FINISHED; the 3 framing tests individually PASS | ✓ PASS |
| WR-04 suite gate | Audit of `Temp/namer042-wr04-results.txt` | 152 `^PASS ` / 0 FAIL / 0 IGNORED; TOTAL matches; FINISHED; `GetPaneDrawOffsets_MirrorsDividerAnchor` PASS; file mtime 13:00 PDT > commit 12:56:59 PDT (final state exercised) | ✓ PASS |
| Fit pins | Analytic recomputation (python) | 2.16216 / 4.26667 / 9.14286 vs test pins at 1e-3 | ✓ PASS |
| Extent/anchor pins | Analytic recomputation | 4.0 / 0.1 / 2.89914; anchor 4.21333 vs center 4.26667 (discriminator genuine) | ✓ PASS |
| Metal warning absence | Editor.log audit | 24 total (unchanged since round 2), last at line 87661, 15,160 lines after incl. both new suite runs, 0 further occurrences | ✓ PASS |
| Retired symbols | grep across `Packages/` | 0 matches (`paneCenterWorld`/`_framedRadius`/`HalfSeparation`/`PreviewGap`) | ✓ PASS |
| Round-3 commits exist | `git log` | 147654f, ca7e0ad, 8f84433, ff9daec all present | ✓ PASS |
| Scope containment | `git diff --stat 6e5e61d..HEAD` | Exactly renderer (+20/-3) + tests (+81); window untouched by WR-04 | ✓ PASS |
| Direct Unity re-run by verifier | — | Not run (interactive editor holds the project lock; driving it is out of verifier scope) | ? SKIP — covered by the audited results-file channel + UAT round 3 |

## Required Artifacts (round-3 delta)

| Artifact | Expected | Status | Details |
|----------|-----------|--------|---------|
| `Editor/UI/NamerPreviewRenderer.cs` | InnerMarginPx=10, tight-AABB fit, HorizontalHalfExtentWorld, PaneAnchorWorld, GetPaneDrawOffsets consumed by Render, per-Render recompute | ✓ VERIFIED | :35, :53-54/:162-164, :201-207, :215-225, :233-237, :247-251, :130-132 |
| `Editor/UI/NamerEditorWindow.cs` | DrawPreviewPaneOutline after GUI.DrawTexture; Orbit/Zoom/Render sites unchanged | ✓ VERIFIED | :874-882, :849; :894/:900/:845/:242 diff-proven untouched |
| `Tests/Editor/NamerPreviewRendererTests.cs` | fit pins 2.1622/4.2667/9.1429; extent pins 4.0/0.1/2.8991; zoom-sweep invariant; GetPaneDrawOffsets_MirrorsDividerAnchor | ✓ VERIFIED | :119-144, :147-161, :164-192, :195-273; 8 tests in file, 8 PASS lines in wr04 results |

## Key Link Verification (manual — `verify.key-links` reports "Source file not found" on member-name `from:` fields, the known tool artifact)

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| NamerPreviewRenderer.Render | GetPaneDrawOffsets(aspect, rect.height) | draw positions `±anchor - rotatedBoundsCenter` | ✓ WIRED | :130-132 (WR-04 link — previously grep-only, now helper + pin) |
| NamerPreviewRenderer.Render | OrthographicSizeForAspect | camera orthographic size | ✓ WIRED | :114-116; same clamped expression re-derived inside PaneAnchorWorld :235 |
| NamerEditorWindow OnGUI | DrawPreviewPaneOutline | call after GUI.DrawTexture | ✓ WIRED | :848-849 -> :874-882 |
| NamerEditorWindow HandlePreviewCameraInput | Orbit/Zoom | synced rotation + scroll zoom | ✓ WIRED | :894/:900 unchanged (diff-proven) |
| (round-2 links) | soft-clip / ResolveAspectSize / CannotDecompose guard / dispose / single-exit heatmap | | ✓ WIRED | All re-confirmed live at the exact lines (row 6 of the checks table) |

## Requirements Coverage

Unchanged in substance from round 2 and strengthened by GAP-4: VCOL-03/04/05, SHDR-02 carry their round-2 verified state (zero code hunks since); UI-04's preview interaction now matches the user's round-2 verdict (divider-anchored panes, no cross-pane overlap, visible bounds) and UI-03's preview controls are untouched. Plan 09 claims UI-03/UI-04 — both SATISFIED at this phase's slice (REQUIREMENTS.md keeps them In Progress spanning Phase 5 stylization, as designed). Orphaned requirements: none.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (3 round-3 modified files) | — | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER, no placeholder prose, no empty returns | — | — |
| NamerEditorWindow.cs | 874-882 | IN-08 (recorded): outline color/thickness literals repeated across five DrawRect calls | ℹ️ Info | Cosmetic; behavior correct; review-recorded, no action required |
| NamerPreviewRenderer.cs | 201-207, 215-225 | IN-07 (recorded): neutral-AABB fit + X-only rotated extent guarantees extreme-pitch top/bottom crops on depth-heavy meshes — documented accepted trade-off | ℹ️ Info | Routed to UAT round 3 as a conscious check (human item 2) |
| Working tree | — | Untracked/modified live-editor artifacts (NAMERGenerated/Neo-T-Pose/*, NamerSmoke.unity, ProjectSettings, .sln.DotSettings.user) | ℹ️ Info | User-generated outputs, unchanged disposition since round 2 |

## Round-3 Delta (what changed since round 2)

- Plan 09 executed (commits `147654f` RED -> `ca7e0ad` GREEN -> `8f84433` outline) + the WR-04 hardening fix (`ff9daec`) — total scope since round 2: 3 files (renderer, window, tests), all verified above.
- Code review gap-closure round 2 (04.2-REVIEW.md, 19:53Z) approved GAP-4's math with one Warning (WR-04: Render's anchor consumption untested) and two Infos (IN-07 crop trade-off, IN-08 outline literals). WR-04 was closed AFTER the review by `ff9daec`, implementing the reviewer's exact recommended extraction + pin — verified here (row 10). IN-07/IN-08 remain recorded open as Infos, neither blocking; IN-07 is routed to UAT round 3.
- Suite count moved 149 -> 152 with reconciling arithmetic (+2 plan-09 anchor tests, +1 WR-04 test) and per-test-audited evidence at both gates (151/0/0 and 152/0/0).
- GAP-2's round-2 closure (per-pane-centered placement) was superseded by the user's round-2 UAT verdict and replaced by GAP-4's divider-anchored placement — an intentional supersession chain (04.2-05 pair-plus-gap -> 04.2-07 per-pane-centered -> 04.2-09 divider-anchored), each flip user-directed and test-pinned.
- UAT round 2 outcome imported: item 1 (transferred-roughness LOOK) PASSED — GAP-1 UAT-confirmed closed; item 2 ISSUES -> GAP-4 (this round's machine closure); item 3 (Write Residual) still never exercised — carried to UAT round 3.

## Gaps Summary

No gaps found. All 11 machine-checkable groups — the 5 roadmap SCs, the GAP-1/GAP-3 closures with regression evidence, all GAP-4 acceptance terms (adjacent-at-divider 10 px margins, tight-AABB fit, no cross-pane overlap at any zoom with the WR-04 consumption pin, outline + unchanged interactions), and the scope sweep — verify against the shipped code. GAP-4 is closed at machine level. The remaining acceptance is inherently visual/interactive: the framing/outline look on the real Neo asset (including the IN-07 extreme-pitch crop trade-off the review asked the user to consciously re-validate), and the never-yet-exercised Write Residual end-to-end flow. Per the decision tree, human verification items take priority: status is human_needed with UAT round 3 ready.

---

_Verified: 2026-09-17T20:04:39Z_
_Verifier: Claude (gsd-verifier)_
