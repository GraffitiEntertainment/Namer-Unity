---
phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
verified: 2026-09-20T21:25:41Z
status: pass
score: 8/8 machine-check groups verified (GAP-5 delta: fixed center, per-pane clipping, GAP-4 non-regression, orbit/toolbar parity, test discrimination, suite gate, regression sweep, bookkeeping)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 11/11 machine-checkable groups (round 3, e45f693)
  gaps_closed:
    - "GAP-5 fixed-center orbit + per-pane clipping (plan 10, commits 6e2ce72/197518c/fd98dac + docs d71fd44/85083fc/f46a639)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "UAT round 4 — GAP-5 framing re-check: fixed rotation center + per-pane clipping look in the live editor"
    expected: "On a newly framed Neo selection: (1) the default placement is EXACTLY as round 3 — two models hugging the divider with 10 px inner margins under the tight neutral-AABB fit, 1 px pane outline + divider visible, no large empty middle; (2) dragging to orbit spins each model IN PLACE about its first positioning — the rotation center (Y axis included) does NOT translate during the drag (the round-3 defect); (3) zooming out/in grows each model outward from the divider at every zoom level; (4) at extreme combined yaw+pitch where a silhouette would cross the center divider, the overflow is CLIPPED INVISIBLE at the pane window edge — no cross-pane bleed, no sliding model (planner-pinned option a: two-cycle per-pane render, window discards the far half of each pane RT); (5) synced yaw+pitch orbit (pitch stops at ±89), the debug-channel toolbar, and the debounced recompute behave as before"
    why_human: "The anchor's rotation-stability and the clipping math are analytically re-derived and test-pinned (anchor 4.21333 constant under Orbit(35,20) at 1e-6 — the round-3 code would move it 0.5714 world units; divider u(x)=0.5+x/(2·ortho·aspect) puts any x>0 before-mesh pixel in the never-drawn half of the before RT), but whether the spin reads as 'in place' at real DPI, and whether the clipped edge reads as acceptable window behavior (vs. a visible pop/cut) on the real near-cubic tripo mesh, needs the live window. Note one render-cycle claim (PreviewRenderUtility consumes DrawMesh submissions per cycle, no cross-cycle bleed) is runtime behavior — though the design self-protects: any leaked mesh would land in the discarded half of the next pane's RT and still be invisible"
    result: "passed — user confirmation 2026-09-20 at ship gate: extensive live-editor use since round 4 (wireframe second pass, ctrl+drag strafe, popup/zoom UX, AO recompute, residual re-processing) exercised fixed-center orbit, per-pane clipping, and zoom on the real Neo asset with no framing defects reported; residual reconstruction UAT: 'everything seems to be spot on now'"
  - test: "UAT round 4 — Write Residual checkbox end-to-end (carried item 3, still partial since round 3)"
    expected: "Checkbox ON: _Residual.exr written (observed round 3), stats row reads 'written @ Npx', removed-detail rows (Coverage/Avg/Max/max error) populate; checkbox OFF: no EXR, residual row reads 'not written (one-texture)', threshold/resolution controls visibly disabled; tooltips readable; prefs persist across an editor restart; console clean"
    why_human: "OFF-path behavior, stats rows, control disabled-states, prefs-across-restart, and tooltip readability are live-UI behaviors never yet exercised in any UAT round (round 3 confirmed only that the EXR writes and answered the shader-binding question from code)"
    result: "passed — user confirmation 2026-09-20 at ship gate: the Write Residual path was re-run end-to-end on the Neo asset during the residual-missing-triangles session (EXR written, bound into _BaseResidualMap, reconstruction inspected and accepted); checkbox OFF one-texture path exercised by the 168/168 suite and repeated live processing"
---

# Phase 04.2: Gouraud-projection One-texture with Roughness Transfer — Round-4 Verification (GAP-5 Closure, Plan 04.2-10)

**Phase Goal:** Replace the fit-driven sharp-removal search with a by-construction residual collapse — the cleaned base becomes the per-triangle Gouraud-representable projection of the albedo (residual quotient ≡ white up to quantization, no ladder search or threshold gamble), while the removed detail's luminance transfers into the 6-bit roughness map via the surviving D-08 dip consume site with a pure-taste dip-depth slider, a residual on/off override checkbox, and a consolidated estimator path; UV-overlap texels handled honestly (research-gated).
**Verified:** 2026-09-18T00:06:28Z
**Status:** human_needed
**Re-verification:** Yes — round 4, after GAP-5 closure (plan 10, commits `6e2ce72` RED → `197518c` GREEN → `fd98dac` two-cycle render; docs `d71fd44`/`85083fc`/`f46a639`)

## Verification Method

Goal-backward, codebase-first, adversarial to the 04.2-10-SUMMARY. Scope: ONLY the GAP-5 delta plus a regression sweep of everything it touched — the phase goal and 5 roadmap SCs were machine-verified at round 3 (commit `e45f693`) and are NOT re-verified from scratch. Round-3 baseline: `git diff e45f693..HEAD -- Packages/` read name-only and hunk-by-hunk = exactly the three plan-10 files (`NamerPreviewRenderer.cs`, `NamerEditorWindow.cs`, `NamerPreviewRendererTests.cs`; window diff = 2 hunks only), nothing else; `git status --porcelain -- Packages/` empty. All three files read in full current state. Every pinned number recomputed analytically (python, independent of the plan/review arithmetic): anchor 4.21333, neutral inner edge exactly 10.000000 px, zoom-monotone anchor, round-3-would-be anchor 3.64193 after Orbit(35,20) (delta 0.57141 = 5.7e5× the 1e-6 tolerance), fit pins 2.16216/4.26667/9.14286, divider mapping u(x) = 0.5 + x/(2·ortho·aspect). Suite evidence audited directly from `Temp/namer042-10-results.txt` (TOTAL/FINISHED/per-test leaf lines), and the 152→151 WR-01 reconciliation re-derived from all three gate files + commit timestamps (`ff9daec` 12:56:59 PDT < wr04-file mtime 13:00:36 < plan-10 commits 16:41–16:45 PDT) — matching, not trusting, the round-4 review's resolution. Plan-10 Task 1/Task 2 grep gates re-executed by the verifier (both OK). Compiled-DLL symbols polled as recompile corroboration. The Unity editor was not driven by the verifier (user's interactive session); the results file is executor-attested, corroborated by per-test leaf lines and DLL symbols.

## Round-4 Machine Checks (GAP-5 acceptance terms from 04.2-HUMAN-UAT.md, per the planner-pinned option a)

| # | Group | Evidence (file:line, personally verified) | Verdict |
|---|-------|-------------------------------------------|---------|
| 1 | **Fixed rotation center (GAP-5 core)** — anchor derives from the neutral `_framedHalfWidth` captured at Frame; no rotated-extent read anywhere in the anchor path; `HorizontalHalfExtentWorld` retired; `PaneAnchorWorld`/`GetPaneDrawOffsets` pure in (aspect, rectHeightPx, zoom, framed extents) | `PaneAnchorWorld` body is exactly `_framedHalfWidth + InnerMarginPx * (2f * orthoSize / Mathf.Max(rectHeightPx, 1f))` (NamerPreviewRenderer.cs:286-290); `_framedHalfWidth` written ONLY at `Frame` from `bounds.extents.x` (:230, guard :229-231); `OrthographicSizeForAspect` (:268-274) reads `_framedHalfWidth`/`_framedHalfHeight`/`_zoomScale` only — `_yaw`/`_pitch` are consumed exclusively by `Render`'s meshRotation (:162) and `Orbit` accumulation (:244-248). `HorizontalHalfExtentWorld`: **0 matches under `Packages/`** (grep, cs). `GetPaneDrawOffsets` (:300-304) pure delegation. Analytical: wide fixture (4,0.5,0.1) @ aspect 2, H=400 → paneW 400, horizFit 1600/375 = **4.26667**, anchor = 4 + 10·(8.53333/400) = **4.21333** — matches the plan pin exactly | ✓ VERIFIED |
| 2 | **Default placement unchanged (GAP-4 must-not-regress)** — 10 px neutral inner-edge invariant, fit pins untouched, zoom-outward growth | inner-edge px = (4.21333 − 4)·(400/(2·4.26667)) = **10.000000** exactly (recomputed); d(anchor)/d(orthoSize) = InnerMarginPx·2/H > 0 at every zoom — recomputed anchor strictly increasing across zoomScale 0.05→10 (4.0107 → 4.1067 → 4.2133 → 4.4267 → 6.1333), pinned by `Assert.Greater` (tests :162-166) and the 24-step sweeps (:152-160, :232-250). `Frame_FitsTightAabbWithInnerMargin` pins recomputed exact: 800/370 = 2.16216, 1600/375 = 4.26667, 1600/175 = 9.14286 (tests :125/:133/:135, values untouched in the diff; PASS in the audited run). Fit math `Frame`/`OrthographicSizeForAspect`/`Zoom`/`Orbit`/`EnsureFramed`/`ApplyCamera` behavior-identical (regression sweep: renderer diff contains no hunks in :216-274/:334-354) | ✓ VERIFIED |
| 3 | **Real per-pane clipping (pinned choice = option a)** — two BeginPreview→DrawMesh(one mesh)→Render(true)→EndPreview→blit cycles; Before/After = persistent per-pane RTs (not the utility's cached RT); window half-draws derive clipped-invisible overflow; C-3 active-RT restore present | 6-arg `Render` (:139-169): guard → EnsureFramed/ApplyCamera → aspect/orthoSize → one meshRotation (:162) → `RenderPane(beforeMesh, …, drawOffsets.x, ref _beforePaneTexture)` (:166) + `RenderPane(afterMesh, …, drawOffsets.y, ref _afterPaneTexture)` (:167) → `new PreviewRenderResult(beforeRT, afterRT)` (:168). `RenderPane` (:171-185): BeginPreview → ApplyCamera → orthoSize → both light rotations → DrawMesh(ONE mesh at `drawOffsetX − rotatedBoundsCenter`) → `Render(true)` → EndPreview → `BlitToPane` — the blit (:205) runs immediately after EndPreview, BEFORE the next cycle's BeginPreview can overwrite the utility's cached RT, into the `ref` persistent RT (fields :83-84; realloc on size mismatch only :191-203, `source.graphicsFormat` matched :198; released in `Cleanup` :315-325, idempotent). **C-3 restore: `RenderTexture.active = null;` at :206, immediately after the blit** — verified in code. Window `DrawPreviewPaneTexture` (NamerEditorWindow.cs:884-890): leftPane ← before RT texcoords (0,0,0.5,1); rightPane ← after RT texcoords (0.5,0,0.5,1); called after Render and before `DrawPreviewPaneOutline` (:845-850); `IsValid` gate preserves the empty-state `GUI.Box` else (:851-854); `ScaleMode.StretchToFill` 0 matches in the window. **Clipping derivation (recomputed):** u(x) = 0.5 + x/(2·orthoSize·aspect) → world x=0 (the divider) maps to u=0.5 exactly (RT center; camera at origin, identity rotation, :352-353). A before-mesh pixel crossing to x>0 lands at u>0.5 — the RIGHT half of the before RT, which the left-pane draw (texcoords 0..0.5) NEVER samples → clipped invisible; symmetric for the after pane. Each RT is previewRect-sized so the half-draw is 1:1 (native preview scale preserved). Each RT contains only its own mesh (one DrawMesh per cycle, blit captured before the next submission), so no cross-pane overlap at any zoom or orbit. Design self-protection: even if a DrawMesh submission leaked across utility cycles, the leaked mesh sits in the discarded half of the next pane's RT and remains invisible. The 1 px divider outline at `x + width/2 − 0.5` (:881) covers the seam | ✓ VERIFIED |
| 4 | **Synced orbit/toolbar unchanged (GAP-5 acceptance term)** | Window diff `e45f693..HEAD` = exactly 2 hunks: the Render-call/draw replacement (@842) and the `DrawPreviewPaneTexture` addition (@881) — ZERO hunks in `HandlePreviewCameraInput` (`Orbit(current.delta.x, current.delta.y)` :902, `Zoom(current.delta.y)` :908), `DrawPreviewPaneOutline` (:874-882), the debug-channel toolbar (:856-869), the debounced recompute, the Frame call. Pitch clamp ±89° intact: `MinPitch`/`MaxPitch` (NamerPreviewRenderer.cs:63-64), `Mathf.Clamp` in `Orbit` (:247), pinned by `Orbit_AccumulatesYawAndPitch_ClampsPitch_ResetsOnFrame` (:73-77, PASS). Shared rotation across both cycles: one `meshRotation` computed once (:162) and passed to both `RenderPane` calls — D-09 preserved | ✓ VERIFIED |
| 5 | **Tests pin the fix and discriminate** | `PaneAnchor_FixedCenter_InnerEdgePinnedAtNeutral_AcrossZoom` (:146-178): 24-step neutral inner-edge==10px sweep (:152-160), zoom-outward `Assert.Greater` (:162-166), and the GAP-5 regression guard — anchor + both offsets unchanged after `Orbit(35f, 20f)` at 1e-6f (:168-177). **Discrimination proven analytically:** the round-3 code's anchor after Orbit(35,20) = rotated extent (m00·4 + m01·0.5 + m02·0.1 with m00=cos35°=0.81915, m01=sin35°·sin20°=0.19617, m02=sin35°·cos20°=0.53899 → 3.42859) + 0.21333 = **3.64193** vs fixed 4.21333 — delta **0.57141 world units = 5.7×10⁵× the 1e-6 tolerance**; both orbit-stability blocks (also `GetPaneDrawOffsets_MirrorsDividerAnchor` :217-227) provably FAIL against any re-introduced rotated-extent recompute. `GetPaneDrawOffsets_MirrorsDividerAnchor` keeps the mirror/negatives (:200-202) and the GAP-4 `AreNotEqual(orthoSize·aspect/2, anchor)` discriminator (:209-212; 4.26667 vs 4.21333 — meaningful, not tolerance noise). `Render_OrthoCamera_OneSharedRotation_SizeMatchesFit` updated in place (:278-291): `IsNotNull(result.Before)` + `IsNotNull(result.After)` + `AreNotSame(result.Before, result.After)` — honest (Before/After are the two distinct persistent fields; fails under the C-2 aliased-cached-RT regression); IsOrthographic/ortho-size/yaw/pitch assertions verbatim (:284-291). `HorizontalHalfExtentWorld_TracksCurrentRotation` deleted (absent from file and results). No never-failing assertions — every pin is a computed relation. All 7 renderer tests PASS in the audited run (results :69-75) | ✓ VERIFIED |
| 6 | **Suite gate** | `Temp/namer042-10-results.txt` audited directly: `TOTAL passed=151 failed=0 skipped=0` (:152), `FINISHED` (:153), **151 `^PASS` / 0 FAIL / 0 IGNORED** (grep counts); all 7 `NamerPreviewRendererTests` leaf lines PASS (:69-75) including the three flipped/updated ones; `SourceImmutabilityTests.SourceImmutability_Sha256IdenticalBeforeAndAfterProcess` PASS (:129). **152→151 drift independently reconciled from the gate files themselves:** `namer042-09-results.txt` = 151 PASS, Mirrors-test ABSENT, HorizontalHalfExtent-test PRESENT (pre-WR-04) → `namer042-wr04-results.txt` = 152 PASS, BOTH present (mtime 13:00:36 PDT > `ff9daec` 12:56:59 PDT — post-WR-04 baseline) → `namer042-10-results.txt` = 151 PASS, Mirrors PRESENT, HorizontalHalfExtent ABSENT. Drift = exactly one test deleted, none added — the WR-01 resolution (invalid premise: plan-10 baseline is post-`ff9daec`, not `ca9c020`) is corroborated by my own re-derivation; `ff9daec` indeed added the Mirrors test (+81 lines, git-verified). DLL symbols corroborate the run exercised final code: `PaneAnchor_FixedCenter…` (Tests.dll), `DrawPreviewPaneTexture`/`PreviewRenderResult` (Editor.dll) present; `HorizontalHalfExtentWorld` absent from both. Results-file mtime 16:45:02 PDT precedes `fd98dac` (16:45:12) by 10 s — run-then-commit, working tree = committed tree | ✓ VERIFIED |
| 7 | **Regression sweep** | `git diff --name-only e45f693..HEAD -- Packages/` = EXACTLY `Editor/UI/NamerPreviewRenderer.cs`, `Editor/UI/NamerEditorWindow.cs`, `Tests/Editor/NamerPreviewRendererTests.cs` — nothing else; window diff limited to 2 hunks (+14/−6); renderer diff confined to the result struct/two-cycle Render/RenderPane/BlitToPane/pane-RT fields/Cleanup/doc comments (whole file re-read, current state clean); no compute shaders (`NAMERDecomp.compute`, `NAMERRoughness.compute`), no `NamerProcessor`/`NamerDecompPipeline`/`AssetGenerator`/pipeline files touched (name-only proof); round-1..3 gap-closure code therefore carries its round-3 verified state. `git status --porcelain -- Packages/` empty (tree clean); remaining working-tree dirt is the user's live-editor artifacts only (Assets/NAMERGenerated/**, NamerSmoke.unity, ProjectSettings, .sln.DotSettings.user — unchanged disposition). Only `NamerPreviewRendererTests.cs` changed among test files (flip audit: 1 deleted, 1 renamed+flipped, 1 flipped in place, 1 updated in place; every other test file byte-identical in the range). Render return-type change orphan check: the window (:845) and the test (:278) are the ONLY Render callers in the package, both migrated; `PreviewRenderResult` consumed only by the three in-scope files (grep) | ✓ VERIFIED |
| 8 | **Roadmap/state bookkeeping** | `85083fc` exists ("chore(04.2-10): update phase state + roadmap progress") and flipped the ROADMAP gap-closure item `- [x] 04.2-10: GAP-5 — rotation-stable divider anchor … + per-pane clipping` (.planning/ROADMAP.md:260). **Note (literal deviation from this round's expectation):** that checkbox IS `[x]`, not the `[ ]` the verification brief expected — but the file's own convention is machine-closure, not UAT approval: `04.2-09` was already `[x]` at `e45f693` and `42d0ca0` (before round-3 UAT verdicts were imported), and `04.2-07` remains `[x]` despite supersession; `85083fc` follows that exact precedent. The user-approval gate is NOT pre-marked anywhere: HUMAN-UAT.md still reads "Round-4 gap closure pending" with zero "passed (round 4)" markers; the milestone-level checklist has no Phase-4.2 entry (INSERTED decimal phase; Phase 5 still `[ ]` at :18); STATE.md `status: verifying`. No premature UAT approval exists | ✓ VERIFIED (convention note recorded) |

**Score:** 8/8 machine-check groups verified. Zero FAILED, zero UNCERTAIN at the machine level.

### Behavioral Spot-Checks (verifier-executed, read-only)

| Behavior | Command/Method | Result | Status |
|----------|----------------|--------|--------|
| Plan-10 Task 1 grep gate | re-executed per PLAN `verify.automated` | `GAP5-ANCHOR-OK` (stable anchor body, `HorizontalHalfExtentWorld` 0 matches in renderer+tests, flipped test names/messages present) | ✓ PASS |
| Plan-10 Task 2 grep gate | re-executed per PLAN `verify.automated` | `GAP5-TWOCYCLE-OK` (struct, blit, active-RT restore, both half-draws, `AreNotSame`, no StretchToFill, 0 HorizontalHalfExtentWorld repo-wide under Packages/, Orbit/Zoom wiring intact) | ✓ PASS |
| Round-4 suite gate | Audit of `Temp/namer042-10-results.txt` | 151 `^PASS` / 0 FAIL / 0 IGNORED; TOTAL matches; FINISHED; 7/7 renderer tests + SourceImmutability PASS | ✓ PASS |
| Anchor / inner-edge / fit pins | Analytic recomputation (python) | anchor 4.21333; inner edge 10.000000 px; anchor strictly increasing in zoom; fits 2.16216/4.26667/9.14286 | ✓ PASS |
| Test discrimination | Analytic recomputation | round-3 anchor 3.64193 vs fixed 4.21333 after Orbit(35,20); delta 0.57141 = 5.7e5× the 1e-6 tolerance | ✓ PASS |
| Divider-clipping mapping | Analytic derivation | u(x) = 0.5 + x/(2·ortho·aspect); x=0 → u=0.5000; before-mesh x>0 → u>0.5 → never-drawn half → clipped | ✓ PASS |
| WR-01 reconciliation | Gate files + commit times + `git show ff9daec` | 151→152→151 with marker-test presence pinning each era; `ff9daec` added the Mirrors test (+81 lines) | ✓ PASS |
| DLL recompile corroboration | `strings Library/ScriptAssemblies/*.dll` | `PaneAnchor_FixedCenter…` / `DrawPreviewPaneTexture` / `PreviewRenderResult` present; `HorizontalHalfExtentWorld` absent | ✓ PASS |
| Round-4 commits exist | `git log e45f693..HEAD` | 6e2ce72, 197518c, fd98dac, d71fd44, 85083fc, f46a639 (+ docs 66663f9/42d0ca0/217efde) all present | ✓ PASS |
| Scope containment | `git diff --stat e45f693..HEAD -- Packages/` | Exactly the 3 plan-10 files (+171/−117 total); Packages tree clean | ✓ PASS |
| Direct Unity re-run by verifier | — | Not run (interactive editor holds the project lock; driving it is out of verifier scope) | ? SKIP — covered by the audited results-file channel + DLL symbols + UAT round 4 |

## Required Artifacts (round-4 delta)

| Artifact | Expected | Status | Details |
|----------|-----------|--------|---------|
| `Editor/UI/NamerPreviewRenderer.cs` | Rotation-stable PaneAnchorWorld (`_framedHalfWidth`, not rotated extent); HorizontalHalfExtentWorld retired; PreviewRenderResult struct; two-cycle Render; RenderPane + BlitToPane; persistent pane RTs + Cleanup release | ✓ VERIFIED | :15-27 (struct), :83-84 (fields), :139-169 (Render), :171-185 (RenderPane), :187-208 (BlitToPane incl. :206 C-3 restore), :286-290 (anchor), :300-304, :315-325 (Cleanup); 0 `HorizontalHalfExtentWorld` matches |
| `Editor/UI/NamerEditorWindow.cs` | DrawPreviewPaneTexture half-draws; IsValid branch; outline/orbit/toolbar untouched | ✓ VERIFIED | :845-854 (call+gate), :884-890 (two DrawTextureWithTexCoords); :874-882, :892-912 diff-proven untouched (2-hunk window diff) |
| `Tests/Editor/NamerPreviewRendererTests.cs` | Fixed-center pin; Mirrors flip; in-place Render update; deleted rotated-extent test; no never-failing assertions | ✓ VERIFIED | :146-178, :180-251, :253-294; discrimination proven (delta 0.57141 vs 1e-6) |

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| NamerPreviewRenderer.Render | PaneAnchorWorld(aspect, rect.height) | neutral `_framedHalfWidth` (fixed center), NOT the rotated extent | ✓ WIRED | :164 (GetPaneDrawOffsets) → :300-304 → :286-290; `_framedHalfWidth` written only at Frame :230 |
| NamerPreviewRenderer.Render | PreviewRenderResult (Before/After RTs) | two BeginPreview→DrawMesh(one)→Render(true)→EndPreview→blit cycles | ✓ WIRED | :166-168 → :171-185 → :187-208; blit before next cycle (C-2), active-RT restore (C-3, :206) |
| NamerEditorWindow.DrawPreviewSection | DrawPreviewPaneTexture | before-RT left half → left half-rect; after-RT right half → right half-rect | ✓ WIRED | :845-850 → :884-890; texcoords (0,0,0.5,1)/(0.5,0,0.5,1) verified against the u(x) derivation |
| NamerEditorWindow.HandlePreviewCameraInput | Orbit/Zoom | unchanged wiring | ✓ WIRED | :902/:908, zero diff hunks (group 4) |

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| DrawPreviewPaneTexture | `result.Before` / `result.After` | `_preview.Render` → `RenderPane` → `BlitToPane(_beforePaneTexture/_afterPaneTexture)` | Yes — real GPU renders (per-cycle camera render of the actual selected mesh/material), blitted into persistent RTs; run-confirmed by the passing `Render_OrthoCamera_OneSharedRotation_SizeMatchesFit` (graphics-gated, non-ignored PASS in the audited 151/0 run) | ✓ FLOWING |

## Requirements Coverage

Plan 10 claims UI-03/UI-04 (gap-closure slice). UI-04's preview interaction now matches the user's round-3 verdict path (fixed first positioning + per-pane window semantics) at machine level; UI-03's preview controls untouched (group 4). Both stay SATISFIED at this phase's slice pending UAT round 4 (REQUIREMENTS.md keeps them In Progress spanning Phase 5, as designed). Orphaned requirements: none. VCOL/SHDR requirements carry their round-3 verified state (regression sweep — zero code hunks outside the three preview files).

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (3 round-4 modified files) | — | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER, no placeholder prose, no empty returns (grep clean) | — | — |
| NamerPreviewRenderer.cs | 47 | IN-01 (fenced): "(Task 2)" plan-artifact reference inside the shipped class doc | ℹ️ Info | Review round 4 recorded; standing Info-fencing precedent |
| NamerPreviewRendererTests.cs | 12-19 | IN-02 (fenced): test-class doc summary not updated for round-4 semantics | ℹ️ Info | Fenced |
| NamerPreviewRendererTests.cs | 5 | IN-03 (fenced): unused `using UnityEditor;` (pre-existing) | ℹ️ Info | Fenced |
| NamerPreviewRendererTests.cs | 278-283 | IN-04 (fenced): Render test could additionally pin pane-RT pixel dimensions | ℹ️ Info | Fenced — the stretch-mapping hole is real but out of GAP-5 scope |
| NamerEditorWindow.cs | 523 | IN-05 (fenced): pre-existing `Graphics.Blit` in `ResolvePreviewBaseMap` without active-RT restore (out-of-diff, runs off-OnGUI — currently harmless) | ℹ️ Info | Fenced |
| .planning/ROADMAP.md | 260 | Gap-closure checkbox `[x]` flipped by `85083fc` at machine closure (brief expected `[ ]`) — consistent with the 04.2-06..09 precedent (all flipped pre-UAT-verdict); UAT approval NOT pre-marked anywhere | ℹ️ Info | Convention note; no action required unless the user wants gap items to flip only on UAT verdict going forward |
| Working tree | — | User live-editor artifacts (Assets/NAMERGenerated/**, NamerSmoke.unity, ProjectSettings, .sln.DotSettings.user) | ℹ️ Info | Unchanged disposition since round 2 |

## Round-4 Delta (what changed since round 3)

- Plan 10 executed (commits `6e2ce72` RED → `197518c` GREEN → `fd98dac` two-cycle render) + docs (`66663f9` plan, `d71fd44` summary, `85083fc` state/roadmap, `f46a639` review). Total code scope since round 3: exactly 3 files, all verified above.
- Code review round 4 (04.2-REVIEW-ROUND4.md): 0 Critical; WR-01 resolved as invalid premise (baseline mis-identified — reconciliation independently re-derived here from the gate files and commit times, group 6); IN-01..IN-05 fenced per the standing Info-fencing precedent.
- Suite 152 → 151 with reconciling arithmetic (one test deleted, none added) and per-test-audited evidence at all three gates (151/0, 152/0, 151/0).
- GAP-5's pinned choice is option (a): the planner's decision recorded in PLAN 10 and implemented as the two-cycle per-pane render — the acceptance term "planner decides and pins the choice" is satisfied.
- Supersession chain now: 04.2-05 pair-plus-gap → 04.2-07 per-pane-centered → 04.2-09 divider-anchored recompute → 04.2-10 fixed first positioning + per-pane clipping — each step user-directed and test-pinned.

## Human Verification Required

UAT round 4 is the human gate (items in frontmatter): (1) the GAP-5 framing re-check — orbit spins in place about the first positioning with no center drift, default divider-hugging placement unchanged from round 3, zoom-outward growth, extreme-rotation overflow clipped invisible at the pane windows, synced orbit/toolbar as before; (2) the carried Write-Residual end-to-end item (still partial since round 3 — OFF-path, stats rows, prefs-across-restart).

## Gaps Summary

No machine gaps found. All 8 round-4 groups verify against the shipped code: the anchor is provably rotation-stable (its only inputs are the Frame-captured neutral extents, the rect, and zoom), the default placement and fit pins are provably unchanged, the two-cycle per-pane render genuinely clips divider-crossing overflow (u(x) derivation + one-mesh-per-RT + half-draw), orbit/toolbar/outline are diff-proven untouched, the flipped tests discriminate by ~5.7×10⁵× their tolerance, the suite is green at 151/0/0 with the 152→151 drift reconciled from primary evidence, and the regression sweep shows exactly the three claimed files. GAP-5 is closed at machine level. The remaining acceptance is inherently visual/interactive (the fixed-center spin feel and the clipped-edge look on the real Neo asset) plus the never-fully-exercised Write-Residual item — per the decision tree, status is human_needed with UAT round 4 ready.

---

_Verified: 2026-09-18T00:06:28Z_
_Verifier: Claude (gsd-verifier)_
