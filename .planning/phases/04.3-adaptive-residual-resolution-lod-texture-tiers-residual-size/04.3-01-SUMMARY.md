---
phase: 04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size
plan: 01
subsystem: decomposition
tags: [unity, csharp, residual, percentile-gate, adaptive-resolution, coverage, gpu-reduce]

# Dependency graph
requires:
  - phase: 04.2-gouraud-projection-one-texture-with-roughness-transfer-resid
    provides: GenerateResidual optional-trailing-parameter precedent (projectedOut/mode), _CoverageStat reduce chain, ResidualPipelineTests fixture helpers
provides:
  - Percentile coverage gate in NamerDecompPipeline.ChooseResolution (D-01/D-02): a rung passes when >= coverageTarget of UV-covered texels reconstruct within errorThreshold
  - NamerDecompErrorStats.AchievedCoverage field populated on every output path (D-03/D-07; 0 on CannotDecompose)
  - GenerateResidual trailing coverageTarget parameter (default NamerEditorConstants.DefaultCoverageTarget) — all existing call sites compile and behave byte-identically
  - NamerEditorConstants.DefaultCoverageTarget = 0.99f (D-05 default)
  - Discriminating percentile-gate tests (IgnoresErrorTail, TargetStrictnessMovesTheRung) + re-derived adaptive assertions
affects: [04.3-02, 04.3-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-rung gate statistic = covered-normalized within-tolerance fraction (covStats.MeanErr / errorStat CoverageFraction), identical math to BuildStats — the gate fraction is over COVERED texels"
    - "Gate knob threads as an optional trailing parameter with the constant default, keeping every pre-existing call site byte-identical (04.2 precedent)"
    - "Out-of-range gate inputs degrade deterministically via a single up-front Mathf.Clamp (T-04.3-01), never mis-gate"

key-files:
  created: []
  modified:
    - Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs
    - Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs
    - Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs

key-decisions:
  - "EvaluateResolution keeps its float return (no new struct): it now returns the coverage statistic; per-rung max error has no reader under the pure percentile gate and D-02 honest MaxError reporting already lives in BuildStats final stats"
  - "Coverage and AchievedCoverage are distinct fields that currently derive from the same coverage reduction — the distinct name exists so the gate statistic is never conflated with the legacy Coverage label in stats consumers/UI (D-03/D-07)"
  - "A programmatic coverageTarget <= 0 clamps to always-fallback (every rung fails -> long edge), > 1 clamps to near-strict; ChooseResolution clamps once at method start"
  - "Discriminating fixture = flat 0.5 gray + 4x4 white corner block (16 of 65536 texels, 0.024%): passes 0.99 target at rung 128, rejected by 1.0 target -> 256 fallback"

patterns-established:
  - "Percentile-gate tests prove BOTH directions with one fixture: tail-ignoring at the default target and target-knob-moves-the-rung at 1.0"

requirements-completed: [VCOL-05, VCOL-04]

# Metrics
duration: 49min
completed: 2026-09-22
---

# Phase 04.3 Plan 01: Percentile Coverage Gate Summary

**Adaptive residual-resolution search now gates on the within-tolerance fraction of UV-covered texels (percentile gate, default 0.99) instead of strict max error, with a distinct AchievedCoverage stat reported on every path — C#-only, NAMERDecomp.compute byte-identical**

## Performance

- **Duration:** 49 min
- **Started:** 2026-09-22T20:33:07Z
- **Completed:** 2026-09-22T21:22:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Replaced the strict `evalMaxError > errorThreshold` break in ChooseResolution with `evalCoverage < target` (D-01): a handful of outlier texels no longer forces a full-resolution residual — a 0.024% error tail now resolves to rung 128 on a 256 source
- EvaluateResolution returns the covered-normalized within-tolerance fraction (same math as BuildStats: covStats.MeanErr / CoverageFraction), reusing the already-dispatched _CoverageStat texture — third `ReduceToStats(coverageStat` occurrence, zero compute-shader changes
- NamerDecompErrorStats.AchievedCoverage populated on drop path, final-stats path, and zeroed on both CannotDecompose paths (D-03/D-07); MaxError stays reduced and reported even when the gate ignores it (D-02 honest reporting)
- GenerateResidual gained trailing `coverageTarget = NamerEditorConstants.DefaultCoverageTarget` (0.99f, D-05) — every existing call site compiles unchanged and keeps default behavior
- Two discriminating tests: IgnoresErrorTail (128 chosen at target 0.99 while MaxError > threshold) and TargetStrictnessMovesTheRung (256 fallback at target 1.0, D-08 shape); adaptive test re-derived with the AchievedCoverage >= 0.99 assert
- Full EditMode suite green in the live editor: 188 passed / 0 failed / 0 skipped

## Task Commits

Each task was committed atomically:

1. **Task 1: Percentile coverage gate + AchievedCoverage stat in NamerDecompPipeline** - `0da52b2` (feat)
2. **Task 2: Re-derived adaptive test + percentile tail and target-strictness tests** - `59682bf` (test)

## Files Created/Modified
- `Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs` - percentile gate in ChooseResolution, coverage-returning EvaluateResolution, AchievedCoverage on all stats paths, coverageTarget threading
- `Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs` - DefaultCoverageTarget = 0.99f (D-05)
- `Packages/com.graffitientertainment.namer/Tests/Editor/ResidualPipelineTests.cs` - re-derived adaptive block, two new percentile-gate tests, CreateTailBlockBase fixture helper

## Decisions Made
None beyond the plan - the plan's fixture analysis held exactly; both new tests passed on the first run with the wide margins the plan predicted (tail + smear halo orders of magnitude below the 1% budget).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Live-editor EditMode run friction (environmental, not plan): the TestRunnerApi run queued ~17 min behind editor busy-state (licensing entitlement checks + unfocused-editor update throttling) before RUN-STARTED appeared, and mid-run RunCommand probes hung until the suite finished. The relay/file-marker recipe itself worked: results at `Temp/gsd043-p01-results.txt` (188/0/0). No code impact.
- Tests-source recompile required an explicit `CompilationPipeline.RequestScriptCompilation()` RunCommand (auto-refresh had not picked up the test edit); verified via new-symbols-in-dll check before launching the run.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Pipeline gate + AchievedCoverage are ready for 04.3-02 (CoverageTarget persistence in NamerProcessorSettings + NamerProjectionContext/CreateProjectionContext plumbing) and 04.3-03 (UI slider 0.90-1.00, error-threshold max 0.10 -> 0.25, achieved-coverage readout on the Residual stats row)
- Production call sites still pass no coverageTarget (default 0.99) — plan 02 threads the persisted value through
- NamerUIControlsTests reflection coupling on DecompStatLabels is untouched and must be updated by whichever plan adds the D-07 stats row

## Self-Check: PASSED

All 3 modified files exist on disk; both task commits (0da52b2, 59682bf) present in git log; compute-shader diff across the two task commits is empty (zero lines).

---
*Phase: 04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size*
*Completed: 2026-09-22*
