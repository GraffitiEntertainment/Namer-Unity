---
phase: 04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size
plan: 02
type: execute
wave: 2
depends_on: ["04.3-01"]
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchReproTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs
autonomous: true
requirements:
  - VCOL-05
  - UI-03

must_haves:
  truths:
    - "The coverage target persists across editor sessions via EditorPrefs under the key NamerProcessor.CoverageTarget with default 0.99"
    - "NamerProcessor.Process passes settings.CoverageTarget through CreateProjectionContext into GenerateResidual on the projection path"
    - "The legacy SobelEdge branch passes settings.CoverageTarget into GenerateResidual"
    - "Every test suite that snapshots NamerProcessor.* EditorPrefs keys restores the new key too (no pref leaks)"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs"
      provides: "CoverageTargetKey const + CoverageTarget property (D-05 persistence)"
      contains: "CoverageTarget"
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs"
      provides: "NamerProjectionContext.CoverageTarget field"
      contains: "public float CoverageTarget;"
    - path: "Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs"
      provides: "CreateProjectionContext coverageTarget param + pass-through at all 3 call sites"
      contains: "coverageTarget"
  key_links:
    - from: "NamerProcessorSettings.CoverageTarget"
      to: "EditorPrefs NamerProcessor.CoverageTarget"
      via: "GetFloat/SetFloat with NamerEditorConstants.DefaultCoverageTarget default"
      pattern: "NamerProcessor\\.CoverageTarget"
    - from: "NamerProcessor.CreateProjectionContext"
      to: "NamerDecompPipeline.GenerateResidual"
      via: "context.CoverageTarget trailing argument inside context.Run"
      pattern: "context\\.CoverageTarget"
    - from: "NamerDipSwitchReproTests RunPinnedRepro"
      to: "CreateProjectionContext (reflection)"
      via: "8-element args array appending NamerEditorConstants.DefaultCoverageTarget"
      pattern: "DefaultCoverageTarget"
---

<objective>
Persist the coverage target (D-05) in NamerProcessorSettings and plumb it end-to-end: NamerProcessor.Process -> CreateProjectionContext -> NamerProjectionContext.CoverageTarget -> GenerateResidual's trailing coverageTarget parameter (added by plan 04.3-01). Update the three test couplings PATTERNS flags: the reflection invocation in NamerDipSwitchReproTests and the EditorPrefs snapshot/restore sandboxes in NamerDecompIntegrationTests and NamerProjectionAcceptanceTests.

Purpose: The gate exists (04.3-01) but every production path still passes the 0.99 default until the value travels from settings to the pipeline. This plan is the wiring; the window slider arrives in 04.3-03.

Output: Persisted setting, threaded parameter, updated reflection/prefs-sandbox tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-CONTEXT.md
@.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-PATTERNS.md
@.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-01-SUMMARY.md

<interfaces>
<!-- Verified against source 2026-09-22; assumes 04.3-01 landed (GenerateResidual trailing
     coverageTarget param + NamerEditorConstants.DefaultCoverageTarget). -->

ErrorThreshold persistence pattern to mirror (NamerProcessorSettings.cs):
  key const (line 24):  private const string ErrorThresholdKey = "NamerProcessor.ErrorThreshold";
  property (130-135):
      /// <summary>Max reconstruction error before a residual is required (D-15; default 0.02).</summary>
      public float ErrorThreshold
      {
          get { return EditorPrefs.GetFloat(ErrorThresholdKey, NamerEditorConstants.DefaultErrorThreshold); }
          set { EditorPrefs.SetFloat(ErrorThresholdKey, value); }
      }

NamerProjectionContext (NamerComputePipeline.cs:582-607) — fields ErrorThreshold (593-594),
ManualResolution, WriteResidual; Add `public float CoverageTarget;` after ErrorThreshold.

CreateProjectionContext (NamerProcessor.cs:586-601) current signature:
  internal static NamerProjectionContext CreateProjectionContext(
      NamerSplitResult split, NamerDecompPipeline decomp, int w, int h,
      float errorThreshold, int residualResolution, bool writeResidual)
  context.Run lambda calls (611-614):
      context.Decomp = decomp.GenerateResidual(
          split, context.Colors, sourceBase, w, h,
          context.ErrorThreshold, context.ManualResolution, projectedOut,
          context.WriteResidual ? NamerResidualMode.AlwaysKeep : NamerResidualMode.NeverKeep);

CreateProjectionContext callers:
  - NamerProcessor.cs:180-182 (Process, projection path): passes settings.ErrorThreshold, settings.ResidualResolution, settings.WriteResidual
  - NamerEditorWindow.cs:422-423 (preview) — 04.3-03's file; DO NOT touch the window in this plan

Legacy SobelEdge branch GenerateResidual call (NamerProcessor.cs:250-255):
      decompOutput = decompPipeline.GenerateResidual(
          split, colors, computeResult.NormalizedBaseColor,
          computeResult.Width, computeResult.Height,
          settings.ErrorThreshold, settings.ResidualResolution,
          projectedOut: null,
          mode: settings.WriteResidual ? NamerResidualMode.AlwaysKeep : NamerResidualMode.NeverKeep);

Reflection coupling (NamerDipSwitchReproTests.cs:80-92):
  MethodInfo createCtx = typeof(NamerProcessor).GetMethod("CreateProjectionContext", ...);
  NamerProjectionContext ctx = (NamerProjectionContext)createCtx.Invoke(
      null, new object[] { fitSplit, decompPipeline, w, h, threshold, ResidualRes, true });

EditorPrefs sandboxes to extend (both snapshot struct + Capture + Restore):
  - NamerDecompIntegrationTests.cs: keys block ~line 20-32, snapshot struct ~887-923, restore + line-978 area; header comment says "the seven NamerProcessor.* EditorPrefs keys" -> eight
  - NamerProjectionAcceptanceTests.cs: PrefsSnapshot/CapturePrefs/RestorePrefs ~795-870
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CoverageTarget setting + context DTO + factory pass-through</name>
  <files>Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs, Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs</files>
  <read_first>
  - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs (full file, 207 lines — key const block at 21-37, ErrorThreshold property at ~130)
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs (lines 574-608, NamerProjectionContext)
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (lines 155-190 Process projection path; 240-260 legacy Sobel branch; 575-625 CreateProjectionContext)
  </read_first>
  <action>
  1. NamerProcessorSettings.cs: add `private const string CoverageTargetKey = "NamerProcessor.CoverageTarget";`
     directly after ErrorThresholdKey (line 24). Add the property beside ErrorThreshold (~130-135)
     with Doxygen `<summary>Coverage target for the percentile residual-resolution gate — the fraction of UV-covered texels that must stay within the error threshold (D-05; default 0.99).</summary>`:
     get returns `EditorPrefs.GetFloat(CoverageTargetKey, NamerEditorConstants.DefaultCoverageTarget)`,
     set calls `EditorPrefs.SetFloat(CoverageTargetKey, value);` — the exact ErrorThreshold shape.

  2. NamerComputePipeline.cs NamerProjectionContext (after the ErrorThreshold field at 593-594):
     add `/// <summary>Coverage target passed to <c>GenerateResidual</c> (D-05 percentile gate).</summary>`
     and `public float CoverageTarget;`.

  3. NamerProcessor.cs CreateProjectionContext (586-601): add TRAILING parameter
     `float coverageTarget = NamerEditorConstants.DefaultCoverageTarget` after `bool writeResidual`
     (trailing-with-default so the not-yet-updated NamerEditorWindow.cs:422 call site keeps
     compiling byte-identically until 04.3-03 extends it — the GenerateResidual trailing-param
     precedent); initialize `CoverageTarget = coverageTarget,` in the object initializer beside
     `ErrorThreshold = errorThreshold,`; in the context.Run lambda's GenerateResidual call
     (611-614) append `context.CoverageTarget` as the final positional argument after the mode
     ternary (it fills 04.3-01's trailing coverageTarget parameter). Extend the method's doc
     comment with coverageTarget (D-05).

  4. NamerProcessor.cs Process projection-path call (180-182): append `settings.CoverageTarget`
     as the final argument to CreateProjectionContext.

  5. NamerProcessor.cs legacy Sobel-branch GenerateResidual call (250-255): append named argument
     `coverageTarget: settings.CoverageTarget` after the `mode:` argument.

  Call-site inventory (verified): CreateProjectionContext is invoked only from
  NamerProcessor.cs:180 and NamerEditorWindow.cs:422 (04.3-03's file — untouched here, still
  compiles via the trailing default). GenerateResidual production call sites are
  NamerProcessor.cs:250 (updated in item 5) and NamerProcessor.cs:611 (updated in item 3);
  the window's call at NamerEditorWindow.cs:715-720 also compiles via its own trailing default
  from 04.3-01 until 04.3-03 threads the slider value.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "CoverageTargetKey = \"NamerProcessor.CoverageTarget\"" Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs && grep -q "public float CoverageTarget" Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs && grep -q "public float CoverageTarget;" Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs && grep -c "coverageTarget\|CoverageTarget" Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs | grep -qv 0 && grep -q "context.CoverageTarget" Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs && echo PLUMB-OK</automated>
  </verify>
  <acceptance_criteria>
  - NamerProcessorSettings.cs declares CoverageTargetKey = "NamerProcessor.CoverageTarget" and a CoverageTarget float property defaulting to NamerEditorConstants.DefaultCoverageTarget (0.99) via EditorPrefs GetFloat/SetFloat, with a Doxygen summary citing D-05
  - NamerProjectionContext has `public float CoverageTarget;` with a Doxygen summary, placed after ErrorThreshold
  - CreateProjectionContext signature ends with `float coverageTarget = NamerEditorConstants.DefaultCoverageTarget` and assigns it into the context
  - The context.Run lambda's GenerateResidual call passes context.CoverageTarget as the final argument
  - NamerProcessor.cs:180-182 call passes settings.CoverageTarget; the legacy branch call (250-255) passes coverageTarget: settings.CoverageTarget
  - The Editor assembly compiles (live-editor compile check per memory recipe: force CompilationPipeline.RequestScriptCompilation and confirm GraffitiEntertainment.Namer.Editor.dll rebuilds with the symbol CoverageTarget present)
  </acceptance_criteria>
  <done>Coverage target persists under NamerProcessor.CoverageTarget (default 0.99) and travels Process -> CreateProjectionContext -> NamerProjectionContext -> GenerateResidual on both the projection and legacy Sobel branches; assembly compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Reflection invoke + EditorPrefs sandbox test couplings</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchReproTests.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs</files>
  <read_first>
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchReproTests.cs (lines 55-127, RunPinnedRepro and the createCtx.Invoke call)
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs (lines 15-35 keys/docs, 875-990 snapshot/capture/restore)
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs (lines 795-875, PrefsSnapshot/CapturePrefs/RestorePrefs)
  - Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs (post-Task-1 CreateProjectionContext signature — the reflection args must match parameter ORDER)
  </read_first>
  <action>
  1. NamerDipSwitchReproTests.cs RunPinnedRepro (line 91-92): the `createCtx.Invoke(null, new object[] { ... })`
     args array must match the new CreateProjectionContext parameter order. Append
     `NamerEditorConstants.DefaultCoverageTarget` as the 8th element after `true` (writeResidual):
     `{ fitSplit, decompPipeline, w, h, threshold, ResidualRes, true, NamerEditorConstants.DefaultCoverageTarget }`.
     Add a one-line comment: pinned 0.99 default (D-05).

  2. NamerDecompIntegrationTests.cs EditorPrefs sandbox: in the keys/constants block add
     `private const string CoverageTargetKey = "NamerProcessor.CoverageTarget";`; in the snapshot
     struct add `public float CoverageTarget;` and `public bool HadCoverageTarget;`; in Capture
     add `CoverageTarget = EditorPrefs.GetFloat(CoverageTargetKey, 0.99f),` and
     `HadCoverageTarget = EditorPrefs.HasKey(CoverageTargetKey),`; in Restore add the matching
     if/else SetFloat-or-DeleteKey pair. Update the class doc comment count "the seven
     NamerProcessor.* EditorPrefs keys" -> "the eight NamerProcessor.* EditorPrefs keys".

  3. NamerProjectionAcceptanceTests.cs: identical three-part extension of
     PrefsSnapshot (`public float CoverageTarget;` + `public bool HadCoverageTarget;`),
     CapturePrefs (`EditorPrefs.GetFloat("NamerProcessor.CoverageTarget", 0.99f)` +
     `EditorPrefs.HasKey("NamerProcessor.CoverageTarget")`), and RestorePrefs (SetFloat or
     DeleteKey) — this file uses inline key literals, mirror that style.

  No behavior asserts change — these suites prove no prefs leak and keep the reflection repro
  running end-to-end (its console log already prints coverage stats; it now exercises the
  coverage-capable path).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "DefaultCoverageTarget" Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchReproTests.cs && grep -c "NamerProcessor.CoverageTarget" Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs && grep -c "NamerProcessor.CoverageTarget" Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs && grep -q "HadCoverageTarget" Packages/com.graffitientertainment.namer/Tests/Editor/NamerProjectionAcceptanceTests.cs && echo COUPLINGS-OK</automated>
  </verify>
  <acceptance_criteria>
  - NamerDipSwitchReproTests createCtx.Invoke args array length is 8 with NamerEditorConstants.DefaultCoverageTarget in the position matching the new parameter order
  - NamerDecompIntegrationTests snapshot struct/capture/restore each mention CoverageTarget once (key const + get + haskey + set/delete), and the doc comment says "eight" keys
  - NamerProjectionAcceptanceTests PrefsSnapshot has HadCoverageTarget; CapturePrefs and RestorePrefs each handle NamerProcessor.CoverageTarget
  - EditMode suites NamerDipSwitchReproTests, NamerDecompIntegrationTests, NamerProjectionAcceptanceTests all green (live-editor recipe primary; batch fallback in plan-level verification)
  </acceptance_criteria>
  <done>Reflection repro invokes the new signature; both prefs sandboxes round-trip the new key; the three coupled suites pass with zero pref leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EditorPrefs (user machine, local) | persisted float setting — user-scoped, local-only, no cross-process trust |
| settings -> pipeline parameter | in-process typed float pass-through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-04 | Tampering | NamerProcessor.CoverageTarget EditorPrefs value | accept | local user preference; out-of-range values are already clamped inside ChooseResolution (04.3-01 T-04.3-01 mitigation); worst case is a different rung choice, not a security failure |
| T-04.3-05 | Information Disclosure | EditorPrefs key | accept | non-sensitive float, machine-local storage, same class as the 17 existing NamerProcessor.* keys |
| T-04.3-06 | DoS | reflection invoke signature drift | mitigate | Task 2 pins the args array to the new parameter order; the DipSwitch repro fails loudly (TargetParameterCountException) on any future drift — that is its established purpose |

No third-party packages are installed in this plan (no npm/pip/cargo legitimacy gate applies).
</threat_model>

<verification>
- grep gates in Task 1/2 verify blocks pass (PLUMB-OK, COUPLINGS-OK)
- EditMode run of the three coupled suites green. Primary: live editor via unity-mcp RunCommand per
  ~/.claude/projects/-Users-Shared-SSDevelopment-Development-Namer-Unity/memory/live-editor-editmode-test-run.md.
  Fallback (live editor closed / relay dead):
  `/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer043_p02.xml -logFile /tmp/namer043_p02.log`
  (no -nographics, no -quit; snapshot/restore ProjectSettings.asset; grep the test-run result line)
</verification>

<success_criteria>
- A fresh NamerProcessorSettings instance reads CoverageTarget == 0.99f with no key present
- Round-trip write 0.95f via one instance, read back 0.95f from a second instance
- Process -> CreateProjectionContext -> GenerateResidual carries the value (reflection repro runs green)
- Both prefs sandboxes restore NamerProcessor.CoverageTarget; suites green with no leaked keys
</success_criteria>

<output>
Create `.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-02-SUMMARY.md` when done
</output>
