---
phase: 04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size
plan: 03
type: execute
wave: 3
depends_on: ["04.3-01", "04.3-02"]
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs
autonomous: true
requirements:
  - UI-03
  - VCOL-04

must_haves:
  truths:
    - "User can drag a Coverage Target slider (0.90-1.00, default 0.99, 0.005 steps) beside the Error Threshold slider while Write Residual is on, and it persists across sessions"
    - "User can push the Error Threshold slider up to 0.25 (was 0.10 max; default 0.02 unchanged)"
    - "When a residual is written, the stats Residual row reads 'written @Npx, coverage X%' using the AchievedCoverage statistic at the chosen rung"
    - "Both sliders trigger the debounced in-memory preview recompute (MarkTweaking + MarkDirty), never a disk write"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      provides: "_coverageTarget field + load + D-05 slider + D-06 max bump + D-07 readout + call-site threading"
      contains: "_coverageTarget"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs"
      provides: "CoverageTarget persistence + fresh-defaults tests"
      contains: "CoverageTarget"
  key_links:
    - from: "NamerEditorWindow coverage slider"
      to: "NamerProcessorSettings.CoverageTarget"
      via: "Mathf.Approximately write-back + MarkTweaking + MarkDirty"
      pattern: "_settings\\.CoverageTarget"
    - from: "NamerEditorWindow preview call (line ~422)"
      to: "NamerProcessor.CreateProjectionContext"
      via: "_coverageTarget appended to the argument list"
      pattern: "_coverageTarget"
    - from: "DrawDecompStats Residual row"
      to: "NamerDecompErrorStats.AchievedCoverage"
      via: "'written @Npx, coverage X%' value string"
      pattern: "AchievedCoverage"
---

<objective>
Add the two UI knobs and the achieved-coverage readout: the Coverage Target slider (D-05, range 0.90-1.00, default 0.99, 0.005 step), the Error Threshold slider max raise 0.10 -> 0.25 (D-06, default unchanged), and the achieved-coverage-at-chosen-rung readout on the stats Residual row (D-07). Thread _coverageTarget through the window's two pipeline call sites (preview projection context and legacy Sobel branch).

Purpose: Tolerance ("how wrong is wrong") and coverage ("how much of the image may be wrong") are the two axes of the percentile gate from 04.3-01; this plan makes both visible and user-tunable, and reports what the gate achieved.

Output: Updated NamerEditorWindow (slider, slider max, readout, plumbing) + persistence tests in NamerUIControlsTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-CONTEXT.md
@.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-PATTERNS.md
@.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-01-SUMMARY.md
@.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-02-SUMMARY.md

<interfaces>
<!-- Verified against source 2026-09-22 (pre-phase line numbers); assumes 04.3-01 + 04.3-02 landed
     (GenerateResidual trailing coverageTarget param; CreateProjectionContext trailing
     coverageTarget param; NamerProcessorSettings.CoverageTarget; AchievedCoverage stat). -->

Error-threshold slider block to extend (NamerEditorWindow.cs:1668-1698), inside
`using (new EditorGUI.DisabledScope(!_writeResidual))`:
    float newThreshold = EditorGUILayout.Slider(
        new GUIContent("Error Threshold", "<tooltip>"),
        _errorThreshold, 0f, 0.10f);              // line 1676 — D-06 changes 0.10f -> 0.25f
    if (!Mathf.Approximately(newThreshold, _errorThreshold))
    {
        _errorThreshold = newThreshold;
        _settings.ErrorThreshold = newThreshold;
        _afterPanelState.MarkTweaking();
        MarkDirty();
    }
    // then the Residual Resolution popup (1685-1697) — leave in place

Slider write-back contract (every control in this window): local new* value, Mathf.Approximately
compare, assign field, assign _settings.*, _afterPanelState.MarkTweaking(), MarkDirty(). Never
write to disk from a slider.

Window plumbing sites for _coverageTarget:
  - field:      line 131  `private float _errorThreshold = NamerEditorConstants.DefaultErrorThreshold;`
  - load:       line 286  `_errorThreshold = _settings.ErrorThreshold;`
  - preview:    lines 422-423 `NamerProcessor.CreateProjectionContext(fitSplit, _decompPipeline, baseW, baseH, _errorThreshold, _residualResolution, _writeResidual)`
  - legacy:     lines 715-720 `_decompPipeline.GenerateResidual(split, colors, _liveResult.NormalizedBaseColor, _liveResult.Width, _liveResult.Height, _errorThreshold, _residualResolution, projectedOut: null, mode: ...)`

Stats Residual row (NamerEditorWindow.cs:1766-1771):
    EditorGUILayout.LabelField(
        new GUIContent(DecompStatLabels[4], "Whether the residual EXR was written (the 04.2 Write Residual checkbox)."),
        new GUIContent(residualWritten ? "written @" + _decompStats.ChosenResolution + "px" : ResidualNotWrittenLabel));

DecompStatLabels (lines 52-59) is UNCHANGED in this plan — the D-07 readout rides the Residual
row's VALUE string (PATTERNS-blessed option), so the reflection test's 5-label expectation and
ResidualNotWrittenLabel assert stay valid.

Persistence test pattern (NamerUIControlsTests.cs:31-69 ProjectionControls_PersistViaEditorPrefs,
76-110 FreshPrefsDefaults): snapshot EditorPrefs value + HasKey, act, restore in finally
(Set if had key, else DeleteKey).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coverage Target slider, threshold max 0.25, achieved-coverage readout, call-site threading</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs</files>
  <read_first>
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs (lines 45-60 DecompStatLabels; 125-135 fields; 280-292 load; 415-430 preview call; 700-725 legacy call; 1660-1712 slider block; 1727-1772 DrawDecompStats)
  - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs (the CoverageTarget property added by 04.3-02)
  - .planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-PATTERNS.md (section 3)
  </read_first>
  <action>
  Six edits, all mirroring neighboring code exactly (Allman, Doxygen where public/internal):

  1. Field (~131): add `private float _coverageTarget = NamerEditorConstants.DefaultCoverageTarget;`
     directly after `_errorThreshold`.

  2. Load (~286): add `_coverageTarget = _settings.CoverageTarget;` directly after
     `_errorThreshold = _settings.ErrorThreshold;`.

  3. D-06 — slider max: line 1676, change the slider bounds `_errorThreshold, 0f, 0.10f` to
     `_errorThreshold, 0f, 0.25f`. Default stays NamerEditorConstants.DefaultErrorThreshold
     (0.02) — only the reachable max changes. One-line change, nothing else in the block moves.

  4. D-05 — new slider: insert a Coverage Target slider block immediately after the threshold
     slider's closing brace and BEFORE the Residual Resolution popup, inside the same
     `DisabledScope(!_writeResidual)` (it only acts when Write Residual is on — the 04.2
     disable-state convention). Add a private const `kCoverageSliderStep = 0.005f` with the
     other window constants (kCamelCase). Shape:
     - GUIContent label "Coverage Target" with tooltip stating: fraction of UV-covered texels
       that must stay within the Error Threshold at a candidate residual resolution (the
       percentile gate, D-05); higher targets keep larger residuals; recompute is debounced and
       in-memory only.
     - `float newCoverage = EditorGUILayout.Slider(label, _coverageTarget, 0.90f, 1.00f);`
     - Quantize to the step: `newCoverage = Mathf.Round(newCoverage / kCoverageSliderStep) * kCoverageSliderStep;`
     - Write-back identical to the threshold slider: `if (!Mathf.Approximately(newCoverage, _coverageTarget)) { _coverageTarget = newCoverage; _settings.CoverageTarget = newCoverage; _afterPanelState.MarkTweaking(); MarkDirty(); }`
     - Update the block comment above the DisabledScope (1663-1667) to mention both sliders key
       on the residual-ON search.

  5. Call-site threading:
     - Preview call (~422-423): append `_coverageTarget` as the final argument to
       `NamerProcessor.CreateProjectionContext(...)`.
     - Legacy call (~715-720): append named argument `coverageTarget: _coverageTarget` after
       the `mode:` argument.

  6. D-07 — readout: in DrawDecompStats, the Residual row's residualWritten value string
     (1769-1770) becomes `"written @" + _decompStats.ChosenResolution + "px, coverage "
     + (_decompStats.AchievedCoverage * 100f).ToString("0.#") + "%"` (e.g. "written @512px,
     coverage 99.4%"). Extend the row's label tooltip: the coverage is the percentile-gate
     statistic at the chosen resolution (the fraction of UV-covered texels within the Error
     Threshold); when no ladder rung meets the Coverage Target the full source long edge is
     kept (D-08) — the row then shows the source long edge, honestly. The not-written branch
     (`ResidualNotWrittenLabel`) and DecompStatLabels array are UNCHANGED.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "private float _coverageTarget = NamerEditorConstants.DefaultCoverageTarget" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "_coverageTarget = _settings.CoverageTarget" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "_errorThreshold, 0f, 0.25f" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && ! grep -q "0f, 0.10f" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "Coverage Target" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "coverageTarget: _coverageTarget" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && grep -q "AchievedCoverage" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs && echo UI-OK</automated>
  </verify>
  <acceptance_criteria>
  - NamerEditorWindow.cs declares `private float _coverageTarget = NamerEditorConstants.DefaultCoverageTarget;` and loads it from _settings beside _errorThreshold
  - The threshold slider call reads `_errorThreshold, 0f, 0.25f` and the string `0f, 0.10f` appears nowhere in the file
  - A "Coverage Target" EditorGUILayout.Slider with bounds 0.90f/1.00f sits inside the DisabledScope(!_writeResidual) between the threshold slider and the Residual Resolution popup, quantized via Mathf.Round to kCoverageSliderStep = 0.005f, with the full write-back chain (field, _settings.CoverageTarget, MarkTweaking, MarkDirty)
  - The preview CreateProjectionContext call and the legacy GenerateResidual call both pass _coverageTarget
  - The residualWritten value string contains "px, coverage " and reads _decompStats.AchievedCoverage; ResidualNotWrittenLabel branch and the 5-entry DecompStatLabels array are byte-unchanged
  - Live-editor compile check passes (GraffitiEntertainment.Namer.Editor.dll rebuilds; NamerEditorWindowSmokeTests green)
  </acceptance_criteria>
  <done>Both knobs render beside each other under the Write Residual disable scope, persist, and feed the gate; the stats block reports achieved coverage at the chosen rung; no disk writes from sliders.</done>
</task>

<task type="auto">
  <name>Task 2: CoverageTarget persistence + fresh-defaults tests</name>
  <files>Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs</files>
  <read_first>
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs (full file, 155 lines — key consts at 21-23, persist test 31-69, fresh-defaults test 76-112, labels test 114-139)
  - Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs (CoverageTarget property)
  - ~/.claude/projects/-Users-Shared-SSDevelopment-Development-Namer-Unity/memory/live-editor-editmode-test-run.md (run recipe)
  </read_first>
  <action>
  Add `private const string CoverageTargetKey = "NamerProcessor.CoverageTarget";` beside the
  existing key consts (line 21-23), then two tests mirroring the file's established patterns:

  1. `CoverageTarget_PersistsViaEditorPrefs` (clone ProjectionControls_PersistViaEditorPrefs
     structure): snapshot `EditorPrefs.GetFloat(CoverageTargetKey, 0.99f)` + HasKey; in try,
     write 0.95f via one NamerProcessorSettings instance, read a fresh instance, assert
     `Assert.AreEqual(0.95f, second.CoverageTarget, "CoverageTarget must persist via EditorPrefs (D-05)")`;
     finally restores SetFloat-or-DeleteKey exactly like the sibling tests.

  2. `CoverageTarget_FreshPrefsDefault` (clone ProjectionControls_FreshPrefsDefaults): snapshot
     + HasKey; delete the key; assert a fresh NamerProcessorSettings reads
     `NamerEditorConstants.DefaultCoverageTarget` (0.99f) — assert against the constant, not a
     literal, plus one explicit `Assert.AreEqual(0.99f, ...)` pinning the shipped default;
     finally restores.

  Update the class doc comment to mention the 04.3 coverage-target knob. Do NOT touch
  DecompStatLabels_CarryRemovedDetailSemantics — the 5-label array is intentionally unchanged
  (the D-07 readout rides the Residual row's value string, not a new row).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/Namer-Unity && grep -q "CoverageTargetKey = \"NamerProcessor.CoverageTarget\"" Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs && grep -c "public void CoverageTarget_" Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs | grep -qx 2 && grep -q "DefaultCoverageTarget" Packages/com.graffitientertainment.namer/Tests/Editor/NamerUIControlsTests.cs && echo UI-TESTS-OK</automated>
  </verify>
  <acceptance_criteria>
  - NamerUIControlsTests.cs declares CoverageTargetKey = "NamerProcessor.CoverageTarget" and contains exactly two new [Test] methods: CoverageTarget_PersistsViaEditorPrefs and CoverageTarget_FreshPrefsDefault
  - Both tests snapshot/restore the key (Set if HadKey else DeleteKey) in try/finally — no pref leakage
  - Fresh-defaults asserts both the constant equality and the literal 0.99f
  - DecompStatLabels_CarryRemovedDetailSemantics is byte-unchanged and still passes
  - Full EditMode suite green (memory recipe primary; batch fallback below)
  </acceptance_criteria>
  <done>Coverage target persistence and the 0.99 fresh default are test-enforced; full EditMode suite green including the unchanged labels test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMGUI slider -> _coverageTarget/_errorThreshold | editor-user drag input, range-bounded by the slider widget |
| window state -> EditorPrefs | local persistence of two floats |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-07 | Tampering | slider input | mitigate-by-construction | EditorGUILayout.Slider clamps to [0.90, 1.00]; quantization to 0.005 keeps persisted values tidy; ChooseResolution clamps again downstream (04.3-01) |
| T-04.3-08 | Tampering | EditorPrefs floats (threshold/coverage) | accept | local user preferences; out-of-range values degrade to gate fallback, not a security failure |
| T-04.3-09 | Repudiation | stats readout | accept | read-only display of measured GPU statistics; tooltips state semantics honestly (D-08) |

No third-party packages are installed in this plan (no npm/pip/cargo legitimacy gate applies).
</threat_model>

<verification>
- grep gates in Task 1/2 verify blocks pass (UI-OK, UI-TESTS-OK)
- Full EditMode suite green (157+ tests). Primary: live editor via unity-mcp RunCommand per
  ~/.claude/projects/-Users-Shared-SSDevelopment-Development-Namer-Unity/memory/live-editor-editmode-test-run.md.
  Fallback (live editor closed / relay dead):
  `/Applications/Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath /Users/Shared/SSDevelopment/Development/Namer-Unity -runTests -testPlatform EditMode -testResults /tmp/namer043_p03.xml -logFile /tmp/namer043_p03.log`
  (no -nographics, no -quit; snapshot/restore ProjectSettings.asset)
- Manual smoke (optional, after green tests): open Tools > NAMER > Processor, enable
  Decomposition + Write Residual, confirm the Coverage Target slider sits under Error Threshold
  (max now 0.25) and the stats Residual row reads "written @Npx, coverage X%" after a process
</verification>

<success_criteria>
- Coverage Target slider: bounds 0.90-1.00, default 0.99, 0.005 steps, disabled when Write Residual off, persists across window reload
- Error Threshold slider reaches 0.25; persisted default remains 0.02
- Residual row shows achieved coverage at the chosen rung from NamerDecompErrorStats.AchievedCoverage
- Both window pipeline call sites pass _coverageTarget; full EditMode suite green
</success_criteria>

<output>
Create `.planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-03-SUMMARY.md` when done
</output>
