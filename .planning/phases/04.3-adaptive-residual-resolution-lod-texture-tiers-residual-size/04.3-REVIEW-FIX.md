---
phase: 04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size
fixed_at: 2026-09-22T22:40:00Z
review_path: .planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-REVIEW.md
iteration: 1
findings_in_scope: 9
fixed: 8
skipped: 1
status: partial
---

# Phase 04.3: Code Review Fix Report

**Fixed at:** 2026-09-22T22:40:00Z
**Source review:** .planning/phases/04.3-adaptive-residual-resolution-lod-texture-tiers-residual-size/04.3-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 9 (0 Critical, 3 Warning, 6 Info; scope = all)
- Fixed: 8
- Skipped: 1

**Verification note:** This fixer cannot run Unity EditMode tests (the interactive
editor holds the project lock; no unity-mcp relay available). Every fix was verified
by careful reading of the before/after code plus a delimiter-balance (brace/paren/
bracket) sanity check against the pre-fix baseline — no live test or compile run was
performed. **Live EditMode test execution is deferred to the orchestrator.**

## Fixed Issues

### WR-01: Percentile gate at target <= 0 picks the SMALLEST rung — comment claims the opposite

**Files modified:** `Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs`
**Commit:** 46eb2cd
**Applied fix:** Comment-only correction (per run constraints) at the `ChooseResolution`
clamp. The reviewer's analysis of the code was confirmed: with `target = 0` the pass
condition (`evalCoverage < 0`) is never true, so every rung passes and the walk ends on
the SMALLEST rung; a `> 1` target clamps to 1 (the strictest gate) and keeps the
long-edge fallback. The new comment documents both degenerate cases accurately. The
clamp code itself is unchanged. The two paths that could actually feed a 0 into this
code were closed by WR-02 (context field default) and IN-03 (pref clamp on read).

### WR-02: `NamerProjectionContext.CoverageTarget` defaults to 0f — silent smallest-rung trap

**Files modified:** `Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerComputePipeline.cs`
**Commit:** 8cd9e83
**Applied fix:** Field now initializes to `NamerEditorConstants.DefaultCoverageTarget`
(preferred minimal fix per run constraints), with the doc summary extended to say so.
`NamerEditorConstants` resolves via the shared `GraffitiEntertainment.Namer.Editor`
namespace — no new using required.

### WR-03: Self-described temporary repro harness updated instead of deleted

**Files modified (deleted):** `Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchReproTests.cs`, `Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchReproTests.cs.meta`
**Commit:** 42fcb44
**Applied fix:** Clean removal of the file and its `.meta` via `git rm` (both tracked;
verified nothing else in the package references the class first). The finding's
optional follow-up ("fold a synthetic-fixture version into ResidualPipelineTests") was
conditional ("if wanted") and is NOT done — the related wiring coverage is instead
provided by the IN-06 test below. Note: `gsd-sdk query commit` cannot stage deleted
paths (it errors with "pathspec did not match any files"), so the already-staged
deletion was committed directly with the same conventional message format.
`NamerDipSwitchTests.cs` (the permanent suite) was left untouched.

### IN-01: Stale gate descriptions in UI tooltip and CreateProjectionContext doc

**Files modified:** `Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs`, `Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs`
**Commit:** ff0f153
**Applied fix:** Residual Resolution tooltip now reads "…while the Coverage Target
fraction of covered texels stays within the Error Threshold…"; the
`CreateProjectionContext` doc's stale "defaults to the locked 0.99 while the window
slider arrives in 04.3-03" clause replaced with a cref to
`NamerEditorConstants.DefaultCoverageTarget`.

### IN-02: Gate statistic is quantized through the float16 reduce chain

**Files modified:** `Packages/com.graffitientertainment.namer/Editor/Decompose/NamerDecompPipeline.cs`
**Commit:** b0dde3f
**Applied fix:** Took the reviewer's "accept and document the tolerance" branch: the
comment above the gate-statistic normalization in `EvaluateResolution` now documents
the ~2^-11 (~5e-4) relative half-float error from the `R16G16B16A16_SFloat`
ping-pong reduce and the possible one-rung boundary flip. Switching the reduce chain
to R32 would change shared GPU pool formats — out of scope for a minimal,
non-verifiable-in-editor fix run.

### IN-03: `CoverageTarget` pref is unclamped relative to the UI range

**Files modified:** `Packages/com.graffitientertainment.namer/Editor/NamerEditorConstants.cs`, `Packages/com.graffitientertainment.namer/Editor/Settings/NamerProcessorSettings.cs`
**Commit:** 8386628
**Applied fix:** Added `NamerEditorConstants.MinCoverageTarget = 0.90f` (named constant,
no new magic number) and clamped the `NamerProcessorSettings.CoverageTarget` getter to
`[MinCoverageTarget, 1f]` on read, so an out-of-band pref can no longer make the
slider display disagree with Process; property summary updated. Added
`using UnityEngine;` for `Mathf`. Verified existing `NamerUIControlsTests` expectations
(0.95 round-trip, 0.99 fresh default) are unaffected by the clamp.

### IN-05 (pre-existing, out of phase diff): Duplicated `<summary>` on DecompositionSkipReason

**Files modified:** `Packages/com.graffitientertainment.namer/Editor/Pipeline/NamerProcessor.cs`
**Commit:** b62fff0
**Applied fix:** Moved the first `<summary>` block (the CountDistinctSourceMeshes
description) off `DecompositionSkipReason` and onto `CountDistinctSourceMeshes`, which
had no doc. `DecompositionSkipReason` keeps its own (previously second) summary. Pure
doc-comment move; no code changes.

### IN-06: No integration test for the settings -> Process coverage plumbing

**Files modified:** `Packages/com.graffitientertainment.namer/Tests/Editor/NamerDecompIntegrationTests.cs`
**Commit:** c6cd849
**Applied fix: fixed — requires human verification (new test, not executed live).**
Added `Process_CoverageTargetPref_ChangesGeneratedResidualResolution` mirroring
`GenerateResidual_PercentileGate_TargetStrictnessMovesTheRung`
(ResidualPipelineTests) through the settings path: a 256x256 linear-imported
tail-block fixture (new `TailBlock` pixel helper + `TailBaseValue`/`TailBlockSize`
consts, same shape as the pipeline fixture) is Processed twice via the existing
harness (`NewSettings`, `CapturePrefs`/`RestorePrefs`, temp-folder cleanup) — once at
`NamerEditorConstants.DefaultCoverageTarget` (expects residual EXR width 128) and once
at 1f (expects the D-08 long-edge fallback 256). `WriteResidual`, `ErrorThreshold
0.05f`, and `OverwriteGenerated = true` are pinned so the EXR is written and the
strict rerun replaces the lenient output. Margins mirror the analytically-verified
pipeline tests (~16-texel tail vs 655-texel budget). The test was NOT executed — run
it in the next live EditMode session.

## Skipped Issues

### IN-04: Constant naming drift in NamerEditorWindow

**File:** `Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs:84`
**Reason:** The fix is a pure rename (`kCoverageSliderStep` -> `CoverageSliderStep`,
2 sites: declaration + one usage). This run's project constraints explicitly prohibit
renames ("no refactors, no renames, no architecture changes"), so it is deferred to a
dedicated cleanup rather than applied here.
**Original issue:** `kCoverageSliderStep` uses kCamelCase while sibling constants in
the same file are PascalCase (`ChannelPaneSize`, `ChannelPopupSize`).

---

_Fixed: 2026-09-22T22:40:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
