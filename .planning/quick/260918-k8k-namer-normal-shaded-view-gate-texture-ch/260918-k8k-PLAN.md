---
phase: quick-k8k-namer-normal-shaded-view-gate-texture-ch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/com.graffitientertainment.namer/Shaders/NAMER.shader
  - Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl
  - Packages/com.graffitientertainment.namer/Shaders/NamerChannelView.shader
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchTests.cs
  - Packages/com.graffitientertainment.namer/Tests/Editor/NamerEditorWindowSmokeTests.cs
autonomous: true
requirements: [DIP-02, D-12]

must_haves:
  truths:
    - "A 7th shaded-view toggle 'Normal' neutralizes the decoded tangent-space normal to flat (0,0,1) in the full shaded After view; it defaults to neutral (1.0) so an untouched material decodes byte-identically"
    - "A row of six small channel panes (Base, Roughness, AO, Metallic, Emissive, Normal) sits directly under the shaded-view toggle row, each showing its channel decoded from the After material's packed textures via the shared NAMER_DECODE_SURFACE"
    - "Clicking a pane opens a temporary popup showing that channel at large size; clicking away closes it"
    - "The channel panes are neutral no-op boxes when the After material has no generated textures yet"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Shaders/NAMER.shader"
      provides: "7th hidden neutral-default normal gate property"
      contains: "_DbgEnableNormal"
    - path: "Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl"
      provides: "CBUFFER entry + normalTS flat-normal neutralization after NAMER_DECODE_SURFACE"
      contains: "_DbgEnableNormal"
    - path: "Packages/com.graffitientertainment.namer/Shaders/NamerChannelView.shader"
      provides: "unlit channel-view blit shader reusing NAMER_DECODE_SURFACE (D-12)"
      contains: "NAMER_DECODE_SURFACE"
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      provides: "7th toggle, channel pane row, popup viewer, RT/material disposal"
      contains: "DrawChannelPanes"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchTests.cs"
      provides: "7-gate neutral-default pin"
      contains: "_DbgEnableNormal"
    - path: "Packages/com.graffitientertainment.namer/Tests/Editor/NamerEditorWindowSmokeTests.cs"
      provides: "7 toggle labels + 6 pane labels pinned"
      contains: "ChannelPaneLabels_AreSixChannelPanes"
  key_links:
    - from: "NamerEditorWindow.cs"
      to: "NamerChannelView.shader"
      via: "Shader.Find(\"GraffitiEntertainment.Namer/NamerChannelView\") + Graphics.Blit"
      pattern: "NamerChannelView"
    - from: "NamerEditorWindow.cs"
      to: "After material _SurfaceMap/_BaseResidualMap"
      via: "afterMaterial.GetTexture into the channel-view material"
      pattern: "afterMaterial.GetTexture"
    - from: "NamerSurface.hlsl _DbgEnableNormal"
      to: "normalTS"
      via: "lerp to flat (0,0,1) after NAMER_DECODE_SURFACE"
      pattern: "lerp(half3(0.0h, 0.0h, 1.0h), normalTS, _DbgEnableNormal)"
---

<objective>
Add the 7th (Normal) shaded-view debug gate to the existing DIP-02 dip-switch set, and a six-pane
texture-channel viewer under the shaded-view toggles (small ~48px panes + click-to-open large
popup) backed by a new unlit channel-view blit shader that reuses the shared NAMER decode.

Purpose: Give the artist a shader-only Normal neutralization (flat-normal bisect for the 04.2
residual-smear isolation instrument) and a visual per-channel view of exactly the packed surface
the After material decodes, without any decode drift (D-12).
Output: `_DbgEnableNormal` in NAMER.shader + NamerSurface.hlsl + the window's 7th toggle; a new
`Shaders/NamerChannelView.shader`; the pane row + popup viewer in NamerEditorWindow; and EditMode
test updates pinning 7 toggle labels, 7 shader gates, and 6 pane labels.

Locked decisions implemented here (traceability):
- DIP-02 (extended): the Normal gate follows the EXACT existing _DbgEnable* pattern (hidden
  neutral-default float, CBUFFER entry, neutralization inside InitializeNamerSurfaceData after
  NAMER_DECODE_SURFACE, window toggle row entry, ApplyDebugGates write, pinning test).
- D-12: the channel-view shader includes NamerSurface.hlsl and reuses NAMER_DECODE_SURFACE so
  channel views cannot drift from the runtime/material decode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260918-fcx-namer-debug-dip-switches/260918-fcx-PLAN.md

# Source files this plan edits (read first — the plan references their exact symbols):
@Packages/com.graffitientertainment.namer/Shaders/NAMER.shader
@Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl
@Packages/com.graffitientertainment.namer/Shaders/NamerDebugView.shader
@Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
@Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchTests.cs
@Packages/com.graffitientertainment.namer/Tests/Editor/NamerEditorWindowSmokeTests.cs

<interfaces>
<!-- Key contracts the executor uses — no codebase exploration needed. -->

NAMER.shader already declares 6 hidden `_DbgEnable*` floats (Residual/Roughness/AO/Metallic/
Emissive/VertexColor) plus `_DbgRoughnessNeutral`, all `[HideInInspector] X("__x", Float) = 1.0`
style (0.5 for roughness-neutral). The Normal gate is the 7th, inserted after
`_DbgEnableVertexColor` and before `_DbgRoughnessNeutral`.

NamerSurface.hlsl `CBUFFER_START(UnityPerMaterial)` currently ends with the 6 `_DbgEnable*` halves
then `_DbgRoughnessNeutral`. `InitializeNamerSurfaceData` calls
`NAMER_DECODE_SURFACE(surface, metallic, emissive, roughness, smoothness, normalTS, ao);` and the
existing gates are applied INSIDE InitializeNamerSurfaceData only (NAMER_DECODE_SURFACE and the
Meta-pass call site stay untouched). Insert the normal gate directly after that decode call.

NamerDebugView.shader is the reuse template for the new blit shader: it includes NamerSurface.hlsl,
declares `_SurfaceMap`/`_BaseResidualMap` (NoScaleOffset "white"), hidden `_RoughnessOffsetMap`
("black"), `_OcclusionStrength` (Range 0-1, default 1.0), and hidden CBUFFER-completeness props
`_BaseColor`/`_EmissionColor`/`_Cutoff`/`_Surface`; Tags are "RenderType"="Opaque" +
"RenderPipeline"="UniversalPipeline" + "IgnoreProjector"="True"; `FallBack Off`. Its `DebugFrag`
uses an if/else chain over a float channel selector after `NAMER_DECODE_SURFACE` — the new shader
mirrors that chain but over `_Channel` with the channel meanings in this plan's Task 2.

NamerEditorWindow.cs symbols:
- `ShaderInputToggleLabels` = `{ "Base/Residual", "Roughness", "AO", "Metallic", "Emissive", "Vertex Color" }` (6) — extend to 7 by appending "Normal".
- Cached ids near line 59: `DbgEnableResidualId` ... `DbgEnableVertexColorId`, `DbgRoughnessNeutralId` (all `Shader.PropertyToID`). Add `DbgEnableNormalId` and a new `ChannelId` for `_Channel`.
- Fields: `_dbgResidualEnabled` ... `_dbgVertexColorEnabled` (all `= true`). Add `_dbgNormalEnabled = true`.
- `ApplyDebugGates()` writes the 6 gate floats onto `_namerMaterial` AND `_generatedMaterial` (transient; no SetDirty). Add the Normal write to both.
- `DrawPreviewSection` draws the toggle row as `DrawShaderInputToggle(0..5, ref _dbg*Enabled)` inside one horizontal group; insert `DrawChannelPanes();` immediately after that row's `EditorGUI.EndDisabledGroup();` (line ~949), before the existing "Toggles neutralize..." mini-label.
- Existing disposal patterns to match in `OnDisable`: `_previewBaseRt.Release(); DestroyImmediate(_previewBaseRt); _previewBaseRt = null;` and `DestroyImmediate(_namerMaterial); ... = null;`. The new `_channelPaneRt` and `_channelViewMaterial` follow the same shape.
- `using UnityEngine.Experimental.Rendering;` is already present (for `GraphicsFormat`); `using UnityEditor;` and `using UnityEngine.Rendering;` too.

Tests:
- NamerDipSwitchTests.`ShaderDebugGates_DefaultNeutral` has a `string[] gates` array with 6 `_DbgEnable*` entries plus a separate `_DbgRoughnessNeutral` check. Add `_DbgEnableNormal` to the array (7 entries) and update the doc comments that say "six".
- NamerEditorWindowSmokeTests.`ShaderInputToggleLabels_AreFiveNeutralToggles` reflects
  `ShaderInputToggleLabels` via `typeof(NamerEditorWindow).GetField("ShaderInputToggleLabels",
  BindingFlags.NonPublic | BindingFlags.Static)` and asserts a 6-entry expected array. Update
  `expected` to 7 entries and DO NOT rename the method (scope-locked).
- The smoke test file is where the new pane-label pin goes (same reflection pattern against a new
  `ChannelPaneLabels` static).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 7th (Normal) DIP-02 shaded-view gate</name>
  <files>Packages/com.graffitientertainment.namer/Shaders/NAMER.shader, Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl, Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchTests.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerEditorWindowSmokeTests.cs</files>
  <action>
Add the Normal gate using the EXACT existing DIP-02 `_DbgEnable*` pattern (per locked decision 1).
Do this in order:

A. `Shaders/NAMER.shader` — in the `Properties` block, immediately after the
`_DbgEnableVertexColor` line and before `_DbgRoughnessNeutral`, add one hidden property:
`[HideInInspector] _DbgEnableNormal("__dbgEnableNormal", Float) = 1.0`.

B. `Shaders/NamerSurface.hlsl`:
- In `CBUFFER_START(UnityPerMaterial)`, add `half _DbgEnableNormal;` immediately after
  `half _DbgEnableVertexColor;` (before `_DbgRoughnessNeutral`), keeping the SRP-batcher layout
  stable (no ifdefs).
- In `InitializeNamerSurfaceData`, DIRECTLY after the existing
  `NAMER_DECODE_SURFACE(surface, metallic, emissive, roughness, smoothness, normalTS, ao);` line
  (the decode stays BEFORE the gate so the gate is shader-only like the others), insert one line
  with a short DIP-02-style comment (neutral = flat tangent-space normal):
  `normalTS = lerp(half3(0.0h, 0.0h, 1.0h), normalTS, _DbgEnableNormal);`
  Touch NOTHING else in the function — do NOT edit `NAMER_DECODE_SURFACE` or the Meta pass.

C. `Editor/UI/NamerEditorWindow.cs`:
- Add `private static readonly int DbgEnableNormalId = Shader.PropertyToID("_DbgEnableNormal");`
  immediately after the existing `DbgEnableVertexColorId` static.
- Add `private bool _dbgNormalEnabled = true;` immediately after `_dbgVertexColorEnabled`.
- Extend `ShaderInputToggleLabels` to 7 entries by appending `"Normal"` (keep the existing 6 in order).
- In `ApplyDebugGates()`, add `float normal = _dbgNormalEnabled ? 1f : 0f;` next to the other
  local gate floats, then write `SetFloat(DbgEnableNormalId, normal);` on BOTH `_namerMaterial`
  and `_generatedMaterial` (mirroring the existing gate writes; still no `EditorUtility.SetDirty`).
- In `DrawPreviewSection`, append `DrawShaderInputToggle(6, ref _dbgNormalEnabled);` to the
  existing toggle-row horizontal group (after the `DrawShaderInputToggle(5, ...)` call).

D. Tests:
- `Tests/Editor/NamerDipSwitchTests.cs` — in `ShaderDebugGates_DefaultNeutral`, add
  `"_DbgEnableNormal"` to the `gates` array (now 7 entries). Update the doc comments on the
  class and on that test that currently say "six _DbgEnable*" to "seven".
- `Tests/Editor/NamerEditorWindowSmokeTests.cs` — in
  `ShaderInputToggleLabels_AreFiveNeutralToggles`, change `expected` to
  `{ "Base/Residual", "Roughness", "AO", "Metallic", "Emissive", "Vertex Color", "Normal" }` and
  update the assertion message accordingly. KEEP the method name (scope-locked: no renames).

Do NOT hand-write any `.meta` for the edited files (they already exist); the shader/texture/tests
edits are to existing tracked files.
</action>
  <verify>
    <automated>test $(grep -c "_DbgEnableNormal" Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl) -ge 2 && test $(grep -c "_DbgEnableNormal" Packages/com.graffitientertainment.namer/Shaders/NAMER.shader) -ge 1 && test $(grep -c "DbgEnableNormalId" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs) -ge 1 && test $(grep -c "_DbgEnableNormal" Packages/com.graffitientertainment.namer/Tests/Editor/NamerDipSwitchTests.cs) -ge 1 && test $(grep -c '"Normal"' Packages/com.graffitientertainment.namer/Tests/Editor/NamerEditorWindowSmokeTests.cs) -ge 1 && grep -q "lerp(half3(0.0h, 0.0h, 1.0h), normalTS, _DbgEnableNormal)" Packages/com.graffitientertainment.namer/Shaders/NamerSurface.hlsl && echo NORMAL_GATE_OK</automated>
  </verify>
  <done>NAMER.shader declares the 7th hidden `_DbgEnableNormal` (default 1.0); NamerSurface.hlsl carries the CBUFFER entry and neutralizes `normalTS` to flat (0,0,1) when the gate is 0 (byte-identical when 1); the window shows the 7th "Normal" toggle and writes the gate onto both preview and generated After materials; the two pinning tests now assert 7 gates and 7 labels.</done>
</task>

<task type="auto">
  <name>Task 2: Create NamerChannelView.shader and wire the channel panes + popup viewer</name>
  <files>Packages/com.graffitientertainment.namer/Shaders/NamerChannelView.shader, Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs, Packages/com.graffitientertainment.namer/Tests/Editor/NamerEditorWindowSmokeTests.cs</files>
  <action>
Create the channel-view blit shader and wire the six pane row + popup viewer (per locked
decisions 2, 3, 4).

A. NEW `Shaders/NamerChannelView.shader` — `Shader "GraffitiEntertainment.Namer/NamerChannelView"`.
Follow NamerDebugView.shader's structure with these exact contents:
- Properties: `[NoScaleOffset] _SurfaceMap("Surface (Packed)", 2D) = "white" {}`,
  `[NoScaleOffset] _BaseResidualMap("Base/Residual", 2D) = "white" {}`, hidden
  `_RoughnessOffsetMap("Roughness Offset", 2D) = "black" {}` (CBUFFER/texture completeness, not
  sampled), `_Channel("Channel", Float) = 0`, `_OcclusionStrength("Occlusion Strength",
  Range(0.0, 1.0)) = 1.0`, and hidden `_BaseColor`/`_EmissionColor`/`_Cutoff`/`_Surface` exactly
  like NamerDebugView's completeness set.
- SubShader Tags identical to NamerDebugView: "RenderType"="Opaque",
  "RenderPipeline"="UniversalPipeline", "IgnoreProjector"="True"; LOD 100.
- One Pass `Name "NamerChannelView"` with Tags "LightMode"="UniversalForward"; blit render state
  `Cull Off ZWrite Off ZTest Always`; `#pragma target 2.0`; `#pragma vertex ChannelVert`;
  `#pragma fragment ChannelFrag`; `#include "NamerSurface.hlsl"` (D-12 — no drift). Declare
  `float _Channel;`.
- `ChannelVert`: Attributes `{ float4 positionOS : POSITION; float2 uv : TEXCOORD0; }`, Varyings
  `{ float4 positionCS : SV_POSITION; float2 uv : TEXCOORD0; }`;
  `output.positionCS = TransformObjectToHClip(input.positionOS.xyz); output.uv = input.uv;`
  (standard Graphics.Blit quad vertex — do NOT use TRANSFORM_TEX; panes show the raw texture).
- `ChannelFrag`: sample `_SurfaceMap` and `_BaseResidualMap` at `input.uv`; call
  `NAMER_DECODE_SURFACE(surface, metallic, emissive, roughness, smoothness, normalTS, ao);`; then
  an if/else chain on `_Channel` (mirroring DebugFrag's chain style):
  0 (`_Channel < 0.5`) -> `baseResidual.rgb` (Base);
  1 (`< 1.5`) -> `roughness.rrr` (packed alpha bits 0-5 /63 grayscale);
  2 (`< 2.5`) -> `ao.rrr` (surface.b * _OcclusionStrength grayscale);
  3 (`< 3.5`) -> `(metallic ? 1.0 : 0.0).rrr` (alpha bit 7 flag);
  4 (`< 4.5`) -> `(emissive ? 1.0 : 0.0).rrr` (alpha bit 6 flag);
  else -> `normalTS * 0.5 + 0.5` (decoded normal RGB).
  Return `half4(channel, 1.0)`. Unlit — no lighting, no InitializeNamerSurfaceData. `FallBack Off`.
- Do NOT hand-write a `.meta` (Unity generates it on import; the live editor is running). After
  saving, `git add -f` the new .shader (and its generated .meta once present) — the Packages/
  force-add quirk in STATE.md.

B. `Editor/UI/NamerEditorWindow.cs` — channel panes + popup:
- Add `private static readonly int ChannelId = Shader.PropertyToID("_Channel");` next to the other
  cached ids.
- Add `internal static readonly string[] ChannelPaneLabels = { "Base", "Roughness", "AO", "Metallic", "Emissive", "Normal" };` right after `ShaderInputToggleLabels` (reflection-pinned by the smoke test).
- Add `private const float ChannelPaneSize = 48f;` and `private const float ChannelPopupSize = 384f;` (named constants — no magic numbers).
- Add fields `private Material _channelViewMaterial;` and `private RenderTexture _channelPaneRt;`.
- In `DrawPreviewSection`, insert `DrawChannelPanes();` immediately after the toggle row's
  `EditorGUI.EndDisabledGroup();` (before the existing "Toggles neutralize..." mini-label).
- Add `private void DrawChannelPanes()`:
  - Resolve `Material afterMaterial = (_afterPanelState.PreferGenerated && _generatedMaterial != null) ? _generatedMaterial : _namerMaterial;` (NO beforeMaterial fallback — the panes are the After material's textures).
  - `EditorGUILayout.BeginHorizontal();` then for each `i` in `0..ChannelPaneLabels.Length-1`, get a pane rect via `GUILayoutUtility.GetRect(ChannelPaneSize, ChannelPaneSize, GUILayout.Width(ChannelPaneSize), GUILayout.Height(ChannelPaneSize))`.
  - If `afterMaterial == null` OR `afterMaterial.GetTexture(SurfaceMapId) == null` (no generated textures yet): draw a neutral `GUI.Box(paneRect, GUIContent.none)` placeholder and skip the blit/tooltip/click for that pane.
  - Else: `EnsureChannelViewMaterial();` then set `_channelViewMaterial.SetTexture(SurfaceMapId, afterMaterial.GetTexture(SurfaceMapId))`, `SetTexture(BaseResidualMapId, afterMaterial.GetTexture(BaseResidualMapId))`, `SetFloat(OcclusionStrengthId, afterMaterial.GetFloat(OcclusionStrengthId))`, `SetFloat(ChannelId, i)`; `EnsureChannelPaneRt();`; `Graphics.Blit(Texture2D.whiteTexture, _channelPaneRt, _channelViewMaterial, 0);`; `GUI.DrawTexture(paneRect, _channelPaneRt);`.
  - Attach a per-pane tooltip via an empty-content label overlay: `GUI.Label(paneRect, new GUIContent(string.Empty, ChannelPaneLabels[i] + " — source: " + (i == 0 ? "_BaseResidualMap" : "_SurfaceMap")));` and `EditorGUIUtility.AddCursorRect(paneRect, MouseCursor.Zoom);`.
  - On `Event.current.type == EventType.MouseDown && paneRect.Contains(Event.current.mousePosition)`: `Event.current.Use();` then `ShowChannelPopup(paneRect, i, afterMaterial);`.
  - `EditorGUILayout.EndHorizontal();`.
- Add `EnsureChannelViewMaterial()`: if null, `Shader.Find("GraffitiEntertainment.Namer/NamerChannelView")` (throw `InvalidOperationException` if not found, matching `EnsureMaterials`), then `new Material(shader) { hideFlags = HideFlags.HideAndDontSave };`.
- Add `EnsureChannelPaneRt()`: if null, create `new RenderTexture((int)ChannelPaneSize, (int)ChannelPaneSize, 0, GraphicsFormat.R8G8B8A8_UNorm)`.
- Add `ShowChannelPopup(Rect paneRect, int channel, Material afterMaterial)`: capture `afterMaterial.GetTexture(SurfaceMapId)`, `GetTexture(BaseResidualMapId)`, `GetFloat(OcclusionStrengthId)`, then `PopupWindow.Show(paneRect, new ChannelViewPopup(surface, baseResidual, occlusionStrength, channel));`.
- Add a NESTED `private sealed class ChannelViewPopup : PopupWindowWithoutFocus`:
  - Ctor stores the three captured values + channel; creates its OWN material via `Shader.Find("GraffitiEntertainment.Namer/NamerChannelView")` with `HideFlags.HideAndDontSave` and a null `RenderTexture _rt`.
  - `public override Vector2 GetWindowSize()` returns `new Vector2(ChannelPopupSize, ChannelPopupSize)`.
  - `public override void OnGUI(Rect rect)`: set the material's `_SurfaceMap`/`_BaseResidualMap`/`_OcclusionStrength`/`_Channel` (using the outer `SurfaceMapId`/`BaseResidualMapId`/`OcclusionStrengthId`/`ChannelId` statics — nested types may access them); ensure `_rt` matches `(int)rect.width`/`(int)rect.height` (release + `DestroyImmediate` + recreate on size change, `GraphicsFormat.R8G8B8A8_UNorm`); `Graphics.Blit(Texture2D.whiteTexture, _rt, _material, 0);`; `GUI.DrawTexture(rect, _rt, ScaleMode.ScaleToFit);`.
  - `public override void OnClose()`: release + `DestroyImmediate` both `_rt` and the popup's material. (Click-away close is automatic for `PopupWindowWithoutFocus` — no extra close code.)
- In `OnDisable()`, add disposal matching the existing patterns: release + `DestroyImmediate` + null for `_channelPaneRt`, and `DestroyImmediate` + null for `_channelViewMaterial` (the popup owns/disposes its own material + RT).

C. `Tests/Editor/NamerEditorWindowSmokeTests.cs` — add a new test
`ChannelPaneLabels_AreSixChannelPanes` using the same reflection pattern as the toggle-label test:
reflect `ChannelPaneLabels`, assert `{ "Base", "Roughness", "AO", "Metallic", "Emissive", "Normal" }`.
</action>
  <verify>
    <automated>test -f Packages/com.graffitientertainment.namer/Shaders/NamerChannelView.shader && test $(grep -c "NAMER_DECODE_SURFACE" Packages/com.graffitientertainment.namer/Shaders/NamerChannelView.shader) -ge 1 && test $(grep -c "_Channel" Packages/com.graffitientertainment.namer/Shaders/NamerChannelView.shader) -ge 1 && test $(grep -c "DrawChannelPanes" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs) -ge 2 && test $(grep -c "ChannelPaneLabels" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs) -ge 1 && test $(grep -c "ChannelViewPopup" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs) -ge 2 && test $(grep -c "Graphics.Blit" Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs) -ge 1 && grep -q "ChannelPaneLabels_AreSixChannelPanes" Packages/com.graffitientertainment.namer/Tests/Editor/NamerEditorWindowSmokeTests.cs && echo CHANNEL_PANES_OK</automated>
  </verify>
  <done>NamerChannelView.shader exists (unlit blit, reuses NAMER_DECODE_SURFACE, `_Channel` 0-5 selector, URP tags, FallBack Off); the window draws six 48px channel panes directly under the toggle row, blitting each through a hidden NamerChannelView material into a small RT (neutral boxes when no generated textures); clicking a pane opens a PopupWindowWithoutFocus that blits the channel at popup size and closes on click-away; all window-owned RTs/material and the popup's own RT/material are released/disposed; the smoke test pins the 6 pane labels.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full EditMode suite in the live Unity editor (TestRunnerApi marker-file recipe)</name>
  <files>Temp/260918-k8k-editmode-results.txt</files>
  <action>
Verify the whole change set by running the FULL EditMode suite inside the live editor via the
unity-mcp `RunCommand` tool, following the project memory recipe exactly (the batch-mode runner is
blocked — the interactive editor holds the project lock). This task is the plan's required
verification gate.

Steps:
1. From Bash, pre-clean any stale marker: `rm -f Temp/260918-k8k-editmode-results.txt` (never
   delete files inside the RunCommand script body — unity-mcp treats destructive ops in the
   command body as requiring user interaction and fails).
2. After Task 1/Task 2 wrote the C# + shader edits, let the live editor's automatic recompile
   settle. Do NOT call `AssetDatabase.Refresh()` before the run — a pending recompile + Refresh
   triggers a domain reload that wipes the dynamic assembly and the static callbacks.
3. Submit ONE RunCommand script to unity-mcp with these exact constraints:
   - Top-level `internal class CommandScript : IRunCommand` plus a TOP-LEVEL
     `internal class K8kRunCallbacks : ICallbacks` (top-level, not nested).
   - Create the API with `ScriptableObject.CreateInstance<TestRunnerApi>()` (NOT `new`).
   - `api.Execute(new ExecutionSettings(new Filter { testMode = TestMode.EditMode }));` — this UTF
     version has NO `ExecuteTestRun`, and the `RegisterCallbacks` 2-arg overload takes an int, not
     a string id.
   - Inside the ICallbacks bodies (RunFinished / TestFinished), write results to
     `Temp/260918-k8k-editmode-results.txt` via `File.WriteAllText`/`File.AppendAllText` — NOT
     console logs (Unity_GetConsoleLogs is unreliable after domain reloads). Leaf failure check:
     `!result.HasChildren && result.FailCount > 0`. Final RunFinished write is a single summary
     line of the form `FINISHED passed=<P> failed=<F> skipped=<S>`.
   - The script must NOT reference `System.Reflection` and must NOT reference any
     `GraffitiEntertainment.*` package namespaces (RunCommand sandbox bans both). Use only
     `UnityEditor.TestTools.TestRunner.Api` / `UnityEngine` APIs.
4. From Bash, watch the marker file with a background loop
   (`until grep -q FINISHED Temp/260918-k8k-editmode-results.txt; do sleep 2; done`) — do not poll
   Unity_GetConsoleLogs (it can hang to the 1800s MCP idle timeout). Confirm active run via
   `tail ~/Library/Logs/Unity/Editor.log` if desired.
5. Read the results file. If the `FINISHED` line shows duplicate entries (every line appearing
   twice) that means NO domain reload happened and the old assembly re-ran — do NOT accept the
   run; force a clean recompile and re-run. Accept the run only when a single `FINISHED` line
   reports `failed=0`.
</action>
  <verify>
    <automated>test -f Temp/260918-k8k-editmode-results.txt && grep -q "FINISHED" Temp/260918-k8k-editmode-results.txt && grep -q "failed=0" Temp/260918-k8k-editmode-results.txt && echo EDITMODE_SUITE_OK</automated>
  </verify>
  <done>The full EditMode suite ran inside the live editor via TestRunnerApi and reported a single FINISHED summary line with failed=0; the marker file Temp/260918-k8k-editmode-results.txt exists with the passing summary; no AssetDatabase.Refresh was issued and no console-log channel was used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor UI -> material shader floats | Debug gate (`_DbgEnableNormal`) and channel selector (`_Channel`) floats written by the editor window onto editor-owned materials; editor-authoritative, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | `_DbgEnableNormal` / `_Channel` material floats | accept | Editor-only debug instrument; values are editor-authored (0/1 gate, 0-5 channel) with neutral 1.0 defaults; no external/PII input; gates are transient (no SetDirty) so on-disk materials keep neutral defaults. |
| T-quick-02 | Information Disclosure | channel popup preview | accept | The popup only re-renders textures already bound to the selected material's generated assets — no new data leaves the editor; editor-local UI. |
| T-quick-SC | Tampering | npm/pip/cargo installs | N/A | No package-manager installs in this plan. |
</threat_model>

<verification>
- `_DbgEnableNormal` appears >= 2x in NamerSurface.hlsl (CBUFFER + lerp) and >= 1x in NAMER.shader; the exact `lerp(half3(0.0h, 0.0h, 1.0h), normalTS, _DbgEnableNormal)` line is present.
- NamerChannelView.shader exists, includes NAMER_DECODE_SURFACE and `_Channel`.
- NamerEditorWindow.cs contains `DrawChannelPanes`, `ChannelPaneLabels`, `ChannelViewPopup`, and a `Graphics.Blit` channel blit; `DbgEnableNormalId` is present.
- NamerDipSwitchTests pins 7 gates; NamerEditorWindowSmokeTests pins 7 toggle labels + 6 pane labels (method name unchanged).
- Live-editor full EditMode suite reports `FINISHED ... failed=0` in Temp/260918-k8k-editmode-results.txt (no Refresh, marker file not console logs).
- New NamerChannelView.shader (+ Unity-generated .meta) is force-added under Packages/ (git add -f).
</verification>

<success_criteria>
- The 7th "Normal" toggle neutralizes the decoded tangent-space normal to flat (0,0,1) in the shaded After view, defaults neutral, and is pinned by the shader-gate and label tests.
- Six channel panes (Base, Roughness, AO, Metallic, Emissive, Normal) render under the toggle row, decode through the shared NAMER_DECODE_SURFACE (no drift), show neutral boxes with no generated textures, and open/close a large popup on click / click-away.
- All window- and popup-owned RenderTextures and channel-view materials are released/disposed on destroy/close.
- The full EditMode suite passes in the live editor with a single failed=0 result.
</success_criteria>

<output>
Create `.planning/quick/260918-k8k-namer-normal-shaded-view-gate-texture-ch/260918-k8k-SUMMARY.md` when done
</output>
