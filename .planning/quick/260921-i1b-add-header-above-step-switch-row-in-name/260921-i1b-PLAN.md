---
phase: quick-260921-i1b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs
autonomous: false
requirements: [QUICK]

must_haves:
  truths:
    - "The step-switch checkbox row in the NAMER Processor window has a visible bold header directly above it"
    - "The header's help line states that VC + Residual, Roughness, and AO gate pipeline stages applied on the next Process run, while Metallic and Emissive gate the shader only (live, no re-Process needed)"
    - "The five toggles, their tooltips, their persisted settings, and their write-through behavior are unchanged"
  artifacts:
    - path: "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs"
      provides: "Title + help labels at the top of DrawStepSwitches"
      contains: "Stage & Shader Gates"
  key_links: []
---

<objective>
Add a visible header above the step-switch (DIP-01 dip-switch) row in the NAMER Processor editor window so users can tell what the five checkboxes are: pipeline stage gates vs. shader-only gates.

Purpose: The row is the first thing in the window's scroll view but has no title — only per-toggle tooltips. Users cannot tell what the row is without hovering. One bold title + one mini help line fixes this.

Output: Two `EditorGUILayout.LabelField` lines inserted at the top of `DrawStepSwitches` in `NamerEditorWindow.cs`. Nothing else changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Target file: `Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs`

<interfaces>
Current code at the insertion point (verified 2026-09-21, lines 845-856; anchor on the method
name and BeginDisabledGroup, not the line numbers):

```csharp
/// <summary>
/// DIP-01 hard stage dip-switch row rendered above the foldouts. VC + Residual reuses
/// the existing <see cref="NamerProcessorSettings.DecompositionEnabled"/> setting;
/// Roughness/AO hard-gate the matching pipeline stages independently of their strength
/// sliders; Metallic/Emissive gate the shader only (no pipeline stage exists). Each
/// toggle writes through to its persisted setting.
/// </summary>
private void DrawStepSwitches()
{
    EditorGUI.BeginDisabledGroup(_busy);
    EditorGUILayout.BeginHorizontal();

    bool newDecomposition = EditorGUILayout.ToggleLeft(
        new GUIContent("VC + Residual", ...
```

Style precedents in the SAME file (match these, do not invent new styling):
- Section headers: `EditorGUILayout.LabelField("Action", EditorStyles.boldLabel)` (line 1797),
  `EditorGUILayout.LabelField("Statistics", EditorStyles.boldLabel)` (line 1692)
- Help/sub-labels: `EditorStyles.miniLabel` (lines 1071, 1686, 1704)
- Window menu path: `[MenuItem("Tools/NAMER/Processor")]` (line 151)
</interfaces>

Project constraints that apply:
- The interactive Unity Editor typically holds the project lock — do NOT launch a headless
  batchmode Unity run for this change. Validation is the grep gate plus the visual checkpoint.
- Minimal-change philosophy: two inserted lines only. No refactors, no new fields, no settings
  changes, no layout restructuring, no tooltip edits.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert header title + help line at the top of DrawStepSwitches</name>
  <files>Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs</files>
  <action>
    In `DrawStepSwitches` (currently around line 852), insert two label statements as the FIRST
    statements of the method body — above `EditorGUI.BeginDisabledGroup(_busy);` and therefore
    above `EditorGUILayout.BeginHorizontal();`. Placing them outside the disabled group keeps
    the header readable while the window is busy processing.

    Line 1 — title, using the file's existing boldLabel section-header precedent:
    `EditorGUILayout.LabelField` with text `Stage & Shader Gates` and `EditorStyles.boldLabel`.

    Line 2 — plain-language help, using the file's existing miniLabel precedent:
    `EditorGUILayout.LabelField` with text exactly:
    `VC + Residual, Roughness, and AO gate pipeline stages — applied on the next Process run. Metallic and Emissive gate the shader only (live, no re-Process needed).`
    styled with `EditorStyles.miniLabel`.

    This copy matches the semantics already documented in the method's XML doc comment
    (lines 845-851): the first three toggles hard-gate pipeline stages consumed on the next
    Process run; Metallic/Emissive only toggle shader contributions via ApplyDebugGates (live).

    Do NOT touch anything else: no changes to the five ToggleLeft controls, their GUIContent
    tooltips, the disabled-group wrapping, fields, settings, or any other method. The two
    inserted lines are the entire diff.
  </action>
  <verify>
    <automated>
      awk '/private void DrawStepSwitches\(\)/,/EditorGUI\.BeginDisabledGroup/' "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs" | grep -c "Stage & Shader Gates"
      AND
      awk '/private void DrawStepSwitches\(\)/,/EditorGUI\.BeginDisabledGroup/' "Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs" | grep -c "no re-Process needed"
      AND
      git diff --name-only -- Packages/
      Expected: first two counts == 1 (title and help line both sit between the method
      signature and the disabled group, i.e. above the toggle row); the git diff list contains
      ONLY Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs.
    </automated>
  </verify>
  <done>
    DrawStepSwitches renders a boldLabel title "Stage & Shader Gates" and a miniLabel help line
    above the horizontal toggle row; git diff shows only the two inserted lines in
    NamerEditorWindow.cs; the five toggles and their write-through behavior are untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the header renders correctly in the Unity Editor</name>
  <action>
    Present the completed Task 1 change to the user for visual verification in the live Unity
    Editor. No code work happens in this task — automation is already complete and grep-verified;
    this checkpoint confirms the edit compiles and renders correctly. Follow the steps in
    how-to-verify, then wait for the resume signal.
  </action>
  <what-built>
    A two-line header (bold title + mini help line) above the step-switch checkbox row in the
    NAMER Processor window. Code change is complete and grep-verified; this checkpoint confirms
    it compiles and renders correctly in the editor.
  </what-built>
  <how-to-verify>
    1. Focus the Unity Editor (project: Namer-Unity) so it recompiles the package edit.
       Check the Console — any compile error here fails this checkpoint (the insertion is two
       LabelField calls, so an error would indicate a typo, not a design issue).
    2. Open the window via menu `Tools > NAMER > Processor`.
    3. Confirm at the very top of the scroll area (above the row of five checkboxes
       "VC + Residual / Roughness / AO / Metallic / Emissive"):
       - A bold "Stage & Shader Gates" title line.
       - A small help line reading "VC + Residual, Roughness, and AO gate pipeline stages —
         applied on the next Process run. Metallic and Emissive gate the shader only (live,
         no re-Process needed)."
       - The help line is not visually clipped or overlapping the checkbox row.
    4. Confirm the five checkboxes still render in one row and toggling one still works
       (e.g. toggle Metallic — the preview updates live without a Process run).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (clipping, wording, placement)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static, hardcoded editor-only label text; no user input, no assets, no runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QUICK-01 | Tampering | NamerEditorWindow.cs label strings | accept | Editor-only display copy, no data flow, no untrusted input crosses any boundary |
</threat_model>

<verification>
- Grep gate (Task 1): both label strings present between `DrawStepSwitches()` signature and `EditorGUI.BeginDisabledGroup`, and the Packages/ diff touches only NamerEditorWindow.cs.
- Visual checkpoint (Task 2): window opens, header renders above the row, toggles unchanged.
</verification>

<success_criteria>
- Header title "Stage & Shader Gates" (boldLabel) + explanation help line (miniLabel) render above the step-switch row.
- Total diff: exactly two inserted LabelField statements in one file.
- Checkpoint approved by the user.
</success_criteria>

<output>
Create `.planning/quick/260921-i1b-add-header-above-step-switch-row-in-name/260921-i1b-SUMMARY.md` when done
</output>
