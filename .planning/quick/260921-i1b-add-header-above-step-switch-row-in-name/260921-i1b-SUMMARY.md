---
phase: quick-260921-i1b
plan: 01
subsystem: editor-ui
tags: [unity, editor-window, imgui, namer, ux]

# Dependency graph
requires: []
provides:
  - Bold "Stage & Shader Gates" section header directly above the DIP-01 step-switch toggle row in the NAMER Processor window
  - MiniLabel help line distinguishing pipeline-stage gates (next Process run) from live shader-only gates
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - In-file section-header precedent (EditorGUILayout.LabelField + EditorStyles.boldLabel / miniLabel) reused verbatim — no new styling

key-files:
  created: []
  modified:
    - Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs

key-decisions:
  - "Header lines placed OUTSIDE EditorGUI.BeginDisabledGroup so the title stays readable while the window is busy processing"
  - "Help copy mirrors the existing DrawStepSwitches XML doc semantics: VC+Residual/Roughness/AO gate pipeline stages consumed on the next Process run; Metallic/Emissive gate shader contributions live via ApplyDebugGates"

metrics:
  duration: 4min
  completed: 2026-09-21

---

# Quick Task 260921-i1b: Stage & Shader Gates header above the step-switch row Summary

Two `EditorGUILayout.LabelField` lines (bold title + miniLabel help) inserted as the first statements of `DrawStepSwitches` in the NAMER Processor window, labeling the five dip-switch toggles as pipeline-stage vs. shader-only gates.

## What Was Done

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | Insert header title + help line at the top of DrawStepSwitches | 33840f8 | Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs |
| 2 | Verify the header renders correctly in the Unity Editor (checkpoint) | — | (no code) |

### Task 1 detail

- Line 1: `EditorGUILayout.LabelField("Stage & Shader Gates", EditorStyles.boldLabel);`
- Line 2: `EditorGUILayout.LabelField("VC + Residual, Roughness, and AO gate pipeline stages — applied on the next Process run. Metallic and Emissive gate the shader only (live, no re-Process needed).", EditorStyles.miniLabel);`
- Both lines sit between the method's opening brace and `EditorGUI.BeginDisabledGroup(_busy)` — outside the disabled group, so the header remains readable while the window is busy.
- The entire diff is exactly these two inserted lines (verified: `git diff --stat` = 2 insertions, 0 deletions, 1 file).

## Verification

- Plan grep gate PASSED: `awk` range from `private void DrawStepSwitches()` to `EditorGUI.BeginDisabledGroup` contains exactly 1 occurrence of "Stage & Shader Gates" and 1 of "no re-Process needed".
- Diff isolation PASSED: `git diff --name-only -- Packages/` listed only `NamerEditorWindow.cs`.
- Compile-risk sanity (no Unity launch per environment constraint): both inserted calls use the identical `EditorGUILayout.LabelField(string, style)` overload already present in the same file (boldLabel at lines 1694/1799; miniLabel at lines 1073/1688/1706).
- Toggles untouched: the five `ToggleLeft` controls, their `GUIContent` tooltips, write-through `MarkDirty()`/settings assignments, and the disabled-group wrapping are byte-identical to before.

## Checkpoint Disposition

Task 2 (`checkpoint:human-verify`, gate=blocking) was **auto-approved** — `workflow.auto_advance` is `true` and the gate is visual-only (not a package-legitimacy `blocking-human` gate). No Unity batch run was launched (live editor holds the project lock). The live editor will recompile the package edit on next focus; if anything looks off (clipping, wording, placement), the two-line fix is trivial to adjust.

## Deviations from Plan

None — plan executed exactly as written. (Checkpoint auto-approval is standard auto-mode behavior, not a deviation.)

## Self-Check: PASSED

- Commit 33840f8 exists on branch `gsd/phase-04.2-gouraud-projection-one-texture-with-roughness-transfer-resid` (`feat(quick-260921-i1b): add Stage & Shader Gates header above step-switch row`, 1 file / 2 insertions).
- Modified file exists: `Packages/com.graffitientertainment.namer/Editor/UI/NamerEditorWindow.cs` (labels present at lines 854-855).
- No file deletions in the commit; no untracked files generated by this task.
